# Mr. Cha CRM Active Session Brief

> **이 파일은 매 세션 자동 로드된다. 60줄 이하를 유지한다**(AGENTS.md 핸드오프 규칙).
> 과거 세션 로그는 여기 쌓지 말고 `ref/session-archive.md`로 보낸다 — 2026-07-21에 이 파일이
> 142k자까지 자라 세션 컨텍스트의 14%를 먹고 있었다. 지속되는 결정·계약은 `AGENTS.md`(불변 규칙),
> `ref/specs/*`(설계 근거), `ref/current-working-state.md`(장기 상태)가 각각 집이다.

Last updated: 2026-07-22

## 지금 상태

**리팩토링 배치 13 전량 종결·머지·검증 완료. 진행 중인 미완 작업 없음.**

최근 머지(main): `#308` 게이트 거울 · `#309` 넛지 클러스터 · `#310` 위생·문서 · `#311` 죽은 시각 하네스 폐기 · `#312` 임베딩 모델 이관 · `#313` 유사도 임계값 · `#314` handoff `p_timezone` 명시 전달.
main 통합 검증 green — typecheck 0 · lint 0 · unit **1062** · server **641** · build · knip 7/9 · 픽스처 잔재 0. CF prod head `1de01a2` Active.

## 직전 세션 요약 (2026-07-22 · 0722-fresh-start)

**리팩토링 배치 13**(미감사 = #303·#304·#305·#306·#307, 코드 41파일 +1,483/−249 — 직전 배치의 이행 PR 2건이 감사 대상). 관례 전량: 기준선 재실측 → **4앵글 병렬 감사**(A 서버·B 클라·C 외부 계약·D 크로스커팅) → **적대 검증 3명**(V1 설계 비교·V2 반박·V3 하급 전수 심사) → ground-truth 스팟 → 3PR. 판정 = **CONFIRMED 8·ADJUSTED 2·기록만 6·기각 3**. SSOT = `ref/plans/2026-07-22-crm-refactor-batch-13.md`.

**① 배치 13 3PR(#308·#309·#310)** — 상세 판정·기각 박제·변이 실관찰은 전부 `ref/plans/2026-07-22-crm-refactor-batch-13.md`에 있다. 요지만: **K1 게이트 거울**(#308) = 지원집합 게이트가 화면 금융사와 어긋나 정당한 기간·약정거리가 사라지던 fail-closed 4경로. **스펙 §4.4의 "동기화 지점 4개" 목록 자체가 틀렸음**을 실증해 스펙 이행안을 기각하고 "DOM이 진실·거울은 파생·재동기화 1지점"(dep 없는 `useLayoutEffect` + `sameStringMap` bail-out **한 쌍**)으로 전환 · **K2 넛지**(#309) = 오프너 6곳 중 2곳 누락으로 정상 조작에 다이얼로그 3중 렌더 + 롤백 잔존 + 죽은 버튼. ⚠️이행 함정 = 권고된 한 줄 교체를 **조기 반환 분기에 적용하면 회귀**(꼬리에만 적용, 그 회귀 변이도 RED로 걸리게 잠금) · **위생·문서**(#310) = 고아 dismissal 생성 경위 특정 후 원천 1줄(스캐너 확장은 오탐 구조라 기각) + 배치 12 세션 블록 **아카이브 복원**(브리프 교체 때 덮여 사라져 감사 대상의 근거가 repo에 없었다).

**② 죽은 시각 하네스 폐기(#311)** — `visual:crm`·`screenshot:crm`·spec 3종이 **로그인 게이트(2026-06-18) 이후 두 달간 실행 원천 불가**였는데 `AGENTS.md`가 계속 그 도구를 가리키고 있었다. 폐기 + 검증 예산 문구를 실제 하는 일로 정정, `playwright.config.ts`만 재도입 주석과 함께 존치.

**③ 임베딩 모델 이관 (#312·#313).** `gemini-embedding-001`(shutdown 예정) → **`gemini-embedding-2`**. 차원 3072 동일이라 DB 스키마 무변경이지만 **공간은 호환 불가**(같은 문장 코사인 **0.03** 실측).
**핵심 안전장치 = `contentHash`에 모델명 포함** — 안 넣으면 기존 161행이 전부 hash skip → 구 벡터가 남은 채 새 데이터만 새 모델로 들어가 **조용히 섞인다**(이제 모델 상수 교체 = 전 코퍼스 해시 변경 = 자동 전량 재임베딩, 실행 **161/161·0 skip**). **`taskType` 제거**(2는 조용히 무시 — DOC/QUERY/생략 코사인 1.0 실측, 001은 0.911로 구분) + 응답 개수 검증(요청≠응답 → throw). **임계값 0.75 → 0.60**: 구 값은 001 공간 실측치라 새 공간에서 **관련 질문 8종 중 5종이 NO_HITS**였다(버려진 게 정확한 근거 — 프로필·서류함·견적요청 청크). 새 실측 관련 0.524~0.826 / 무관 0.461~0.540 → 0.60에서 관련 7/8·오탐 0/4. 테스트 임계값 하드코딩 3곳도 상대값화(리터럴이면 상수 조정 시 의도가 조용히 뒤집힌다). **앱도 같은 날 이관 완료** → 두 팀 공간 일치. 앞으로 앱 임베딩(`public.*`)을 참조하는 기능을 만들면 **그때 양쪽 모델 일치부터 확인**할 것(차원이 같아 겉보기 정상·유사도만 무작위라 증상으로 못 잡는다).

**④ 앱 팀 Security Advisor → 계약 프리즈 + `p_timezone` 명시 전달(#314).** Advisor 경고 6건은 전부 의도된 상태(baseline = Warnings 4·Info 2), `update_human_handoff_settings`는 **계약 프리즈**(authenticated 세션 호출 확인 — 서버 secret key 경로 0건). 역제기한 `p_timezone` DEFAULT 의존은 파급이 컸다 — 그 값이 앱 운영시간 판정의 `AT TIME ZONE` 기준이라 **DEFAULT가 UTC면 저장 한 번에 판정이 9시간 밀린다**(에러 없이). → 명시 전달 + 회귀 테스트(변이 RED). 앱도 DEFAULT 프리즈·시그니처 검증기 완료 = **양쪽 방어**. 계약 `AGENTS.md` · 경위 `ref/2026-07-22-app-security-advisor-reply.md`.

## ▶ 다음 작업 (2026-07-22 확정 — 새 세션에서 이 순서로)

**1. 업무 AI 실기 확인 (짧음 · 먼저)** — #312·#313이 실제 답변에서 작동하는지. **유사도 숫자만 봤고 실 답변은 한 번도 안 봤다**(임계값을 0.75→0.60으로 **내렸으니** 근거가 더 통과한다 — 애매한 게 섞여 답변을 흐리는지는 실물로만 안다).
- 확인 3가지: ①관련 질문이 근거를 제대로 찾는가 ②**무관 고객 청크가 sources에 섞이는가**(0.60 하향의 유일한 위험) ③답변 톤이 이전과 같은가(pending 항목 5에서 이사님 "기존처럼 나오면 됨" 승인분).
- 로그인은 `AGENTS.md` "로컬 브라우저 스모크 로그인 우회"(magiclink) 절차. **스모크 데이터(업무 AI 대화)는 공유 master라 반드시 정리**.
- 문제 있으면 `SIMILARITY_THRESHOLD` 한 줄 조정(근거 = `src/routes/assistant.ts` 주석의 실측 분포). 컷 분포는 로그 `[assistant] 근거 임계값 필터 N→M (컷 최고 X)`로도 본다.

**2. 리팩토링 배치 14 감사** — 미감사 **7건**(`#308`·`#309`·`#310`·`#311`·`#312`·`#313`·`#314`)으로 관례 규모(5~6) 초과.
- 감사 가치가 평소보다 높은 이유: **`#312`~`#314`는 오늘 외부 일정(001 shutdown)·앱 팀 회신에 맞춰 4앵글 감사 없이 바로 나갔다.** 실 데이터 161행을 전량 갈아엎었고(#312) 검색 동작을 바꿨다(#313).
- 중점 후보: `contentHash` 변경이 embed-on-write 등 다른 경로에 남긴 영향 · 임계값 0.60의 표본(12종) 적정성 · #308 재동기화 effect의 렌더 비용 · #311 폐기 잔재.
- 관례 = 기준선 재실측 → 4앵글 병렬 감사 → 적대 검증 3명 → ground-truth 스팟 → 판정 → PR. 직전 SSOT = `ref/plans/2026-07-22-crm-refactor-batch-13.md`.

## 대기 (우리 액션 없음)

`ref/director-pending-confirmations.md` — 항목 14(재판단) · 16·17(사후 공유) · **18·19 신설**(dismiss 권한 · 폴백 비가역성 — 둘 다 판단 요청, 기각 시 코드 변경 0/소형).
**파트너 Phase B 완료 시** 산은·iM·농협 게이트 자동 점등 → 실기 1회(우리 몫 작업 0). 이때 **구매방식 전환 후 기간 상태**(배치 13 B#7 — MG는 오늘도 갈린다) 한 줄 확인.
실기 협조 2건(FCM 실기기·앱 #582 크로스)은 **애플 개발자 등록 후 재론** — 먼저 밀지 말 것.
`visual:crm` 하네스는 **폐기 종결**(#311) — 자동 스크린샷/픽셀 비교는 없다. 시각 확인은 눈으로 1회 + magiclink 절차.

## Boot

1. `AGENTS.md` → 이 파일 순으로 읽는다.
2. `git status --short --branch` · `git log --oneline --decorate --max-count=5`
3. 더 필요하면: 과거 세션 경위 = `ref/session-archive.md` / 장기 상태 = `ref/current-working-state.md` / 설계 근거 = `ref/specs/*`, `ref/plans/*`
4. 23개 원본 기획 문서는 전략·로드맵·아키텍처를 명시적으로 다룰 때만 읽는다.

## 세션 마무리 규칙

- 이 파일은 **교체**한다(누적 금지). 직전 세션 요약만 남기고 그 이전 것은 `ref/session-archive.md` 맨 위로 옮긴다.
- 행위 변경이 생기면 `ref/director-pending-confirmations.md`에 등재한다(PR 본문 🟡와 병행).
- 지속되는 계약·함정은 `AGENTS.md`에, 설계 근거는 `ref/specs/*`에 — 여기 쌓지 않는다.
