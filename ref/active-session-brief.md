# Mr. Cha CRM Active Session Brief

Last updated: 2026-06-30

Purpose: `영실아 이어가자` / `CRM 이어가자` 이후 현재 작업만 빠르게 복구하기 위한 압축 문서.

## Boot

1. Read this file first.
2. Run `git status --short --branch` and `git log --oneline --decorate --max-count=5`.
3. Read `ref/current-working-state.md` only if this is insufficient.
4. 인증 설계/플랜: `ref/specs/2026-06-18-crm-auth-design.md`, `ref/plans/2026-06-18-crm-auth-*.md`. CRM 도메인 스펙: `ref/specs/2026-06-17-crm-*`.

## 알려진 이슈 (후속, 리팩토링 후 — 2026-06-29 진단만)

> - **① 니즈 앱 카드 늦게 로딩**: ✅**완료(PR #125 머지 `6705730`)** — `fetchCustomerQuoteRequestsCached`(고객별 Map+TTL60s+dedupe)+행 hover 프리패치(앱 유입 `source==="앱 견적비교"`만). 라우트 1왕복화는 마진 작아 미실행(범위 밖).
> - **② 상세 수정이 전체보기 즉시 반영 안 됨**: ✅**완료(리팩토링 세션 `44323cd`)** — savePatch/task mutation 성공 시 `onCustomerListChanged`/reloadCustomers.
> - **③ 상세 구매조건 7필드 미저장**(2026-06-30 진단, 새 세션 예정): `useCustomerPurchase`에서 **구매방식(needMethod)·출고 희망 시기(needTiming)만 `savePatch`로 영속**, 나머지 **계약기간·초기비용·연간 주행거리·인도 방식·계약 포커스·고객 특이사항·심사 특이사항 7개는 DB 컬럼이 없어 UI 상태만**(새로고침 원복). crm.customers 니즈 컬럼=needModel/Trim/Colors/Method/Timing/Memo뿐. → 수정 방향(결정 필요): ①컬럼 추가(마이그) ②jsonb 묶음(quotes.guidance 패턴) ③견적 도메인 분리. **고객 단위 vs 견적 단위 데이터 모델 결정이 핵심 → 새 세션 brainstorming.**

## ▶ 다음 세션에서 재개 — 고객 상세 거대 컴포넌트 분해 (2026-06-30 일시중단, 통합 브랜치 `refactor/crm-detail-decomposition` 로컬·미push·미머지)

> `KimMinjunDetailContent` **5437→2501줄**(구매조건·니즈까지 추출). 영역별 **커스텀 훅 + 프레젠테이션 컴포넌트** 분해 + `kim`→범용 리네임. brainstorming→spec→plan→subagent-driven 풀사이클. spec `ref/specs/2026-06-29-crm-detail-decomposition-design.md`·plan `ref/plans/2026-06-29-crm-detail-decomposition.md`(실행 순서·공통 절차 A~G·영역 인벤토리·줄번호 앵커). **본작업은 컨텍스트 커서 새 세션에서 이어가기로 사용자 결정.**
> - **설계(A)**: 영역 state/effect/핸들러→`client/src/components/customer-detail/hooks/use*.ts`, JSX→`customer-detail/*.tsx`. **cross-cutting(`openEditor`·`setOpenEditor`·`toggleEditor`·`savePatch`·`markRecentUpdate`·`kimEditorMatches`)는 부모 보유+인자 주입**(상태/니즈/구매조건 공유). 영역이 `editorOpen`(부모 onEditorOpenChange OR용)·`overlayOpen`(detailOverlayOpen OR용=서류/견적) 반환. 통합 브랜치 순차 누적→마지막 묶음 PR.
> - **추출 완료(코드검증 typecheck0/lint0/build 통과)**: 스캐폴드(`types.ts`)·헤더(`CustomerDetailHeader`)·고객메모·해야할일·예정일정·서류함·**상태+워크플로우**(`useCustomerWorkflow`+`StatusWorkflow`+`StatusFieldEditors`+`status-meta`)·**구매조건**(`useCustomerPurchase`+`PurchaseConditions`+`purchase-meta`, `850ec09`)·**니즈**(`useCustomerNeeds`+`NeedsDashboard`+`needs-meta`, `fc89fbd`).
> - **구매조건·니즈 = 2026-06-30 subagent-driven 추출+spec/code quality 2단계 리뷰 통과**. 구매조건 deviation=`purchasePopoverFrame` 부모 유지(cross-cutting positioning, spec OK). 니즈 code quality Important 2건 수정 반영: `customer`인자→`detail.id`(형제훅 일관·`!cid`가드 제거)·prop `needs`→`needsHook`(별칭 제거). 둘 다 byte-level 동작보존 검증됨.
> - **견적함 목록(9a)**(`useQuoteList`+`QuoteList`+`quote-meta`, `ddbdc15`) + **견적 워크벤치(9b~9e 통합)**(`useQuoteWorkbench`+`QuoteWorkbench`+`quote-workbench-meta`, `3dc7a4c`) = 2026-06-30 추출. **부모 `KimMinjunDetailContent` 5437→303줄(−94%).** 워크벤치는 **사용자 결정으로 9b~9e 4분할 대신 단일 훅 통합**(내부 강결합=pricing panel이 pickers 감쌈·persist가 전 sub 읽음·prefill 체인 → 4분할 시 props 폭발). 둘 다 spec(byte-identical·난점3종·ref subtree·seam·overlay OR 검증)+code quality 2단계 리뷰 통과. 매핑 산출물 `scratchpad/task9-map.md`(세션 로컬).
> - **분해 중 발견·수정한 기존 버그 3건(브라우저 검증 OK)**: ①메모 스크롤 체이닝(`5ea6cf1` `.kim-customer-memo-body`에 overscroll-behavior:contain) ②popover 잘림 2건(`57c9866`: 메모 삭제 popover가 메모 1개일 때 위로 떠 헤더에 가림→`memos.length>1`일 때만 위로 / 니즈 편집 popover가 `.kim-needs-dashboard{overflow:hidden}`에 clip→`:has(.kim-edit-popover.needs)`로 열림 시 overflow:visible) ③상세 수정→전체보기 즉시 반영(`44323cd`: `savePatch`+할일 task mutation 성공 시 `onCustomerListChanged=reloadCustomers`. 목록 표시필드=직군/연락처/상담경로/니즈/상담메모[=customer_tasks 최신 미완료 task]만, 거주지/메모/일정은 목록 미표시라 제외).
> - **브라우저 검증(유슨생)**: 닫힌 4개(메모/할일/일정/서류)+버그 fix 3건 = **전부 OK**. ✅**상태+워크플로우 본체도 동기화 버그fix 검증 과정에서 직접 확인 완료(유슨생, 2026-06-30)**.
> - **✅ 구매조건+니즈 묶음 브라우저 검증 완료(2026-06-30 유슨생)**: floating popover 위치(트리 이동했으나 fixed라 동일 좌표)·인라인 상세구매조건폼·니즈 단일 need 카드 편집(저장·롤백)·김지안 앱카드 목록→"견적작성"→워크벤치→승격배지·딥링크 `?quoteRequest=` prefill = **전부 OK·회귀 0**. 구매조건 7필드 미저장은 기존 동작 확정(위 follow-up).
> - **✅ 전 영역 코드추출 완료(2026-06-30)**: 9개 영역(헤더/메모/할일/일정/서류/상태워크플로우/구매조건/니즈/견적함목록/견적워크벤치) 전부 훅+컴포넌트로 분리. ✅**견적함+워크벤치 종합 브라우저 검증 OK(유슨생, 2026-06-30)**: 목록·행액션·미리보기·차량→가격·옵션/색상·할인·수기카드·앱카드·PDF원본·작성완료 INSERT→UPDATE(중복0)·발송·딥링크·prefill 전부 정상 — **분해 회귀 0**. → 마무리(부모 리네임·잔여 kim 정리·묶음 PR).
> - **검증 중 발견·수정 ②(`92eead5`, pre-existing 버그)**: 견적 **수정** 작성완료(차량 변경 시) → 외장/내장 색상·옵션·price 리셋(리로딩하면 정상=DB OK). 진단=작성완료 optimistic `setQuotes`가 `quote.trimId`를 새 차량으로 덮음→`openQuoteActionTrimId`(live quotes)→VehiclePicker `initialTrimId` 변동→`[initialTrimId]` effect 재발화→`applyTrimToPricing` 2회 실행(editPrefill 소비됨→`setExteriorColor(null)` 등). 추출 byte-identical(원래 버그). 수정=`editEntryTrimIdRef`로 수정 진입 trimId 스냅샷 고정→`initialTrimId` 불변→spurious 재실행 제거(영향=수정모드만, 신규/딥링크 editingQuoteId=null이라 무관). typecheck0/lint0/build OK. **✅브라우저 재검증 OK(유슨생).**
> - **검증 중 발견·수정 ③(`ab0be10`, pre-existing 버그, Option A)**: 수기 비교카드 — ⓐ미저장 작성완료 **데이터손실**(`extractWorkbenchScenarios`가 saved 카드만 추출→**filled 카드 전체** 추출, **empty-wipe 가드 유지**[scenarios:[] 절대 안보냄]·silent-delete 차단) ⓑ금융사 변경 **미리보기 즉시반영**(`cardScenario`를 입력 변경 시 갱신) ⓒ**카드1 mock 빈값 중립화**(세 카드 동일 빈 기본값→미입력 작성완료 시 가짜 금융 미저장, #120 follow-up "round-1 mock" 해소). ⓓ**작성완료 후 견적함 목록 즉시반영**(optimistic `setQuotes`가 scenarios 누락→`ScenarioInput`→`CustomerDetailScenario` 변환 반영, 화면-only·영속무관). 검증리뷰 data-safe·typecheck0/lint0/test**259**. **✅브라우저 검증 OK(유슨생)**. follow-up=lender"미선택"+monthly0 degenerate 저장 시나리오 un-save 작성완료 시 drop(극단 edge).
> - **검증 중 발견·수정 ①(`f4c07aa`, 분해 무관 기존 버그)**: 진행상태/계약가능성 목록↔상세 동기화. systematic-debugging 진단=**목록 mock-only 미완성 마이그**(#54 때 DB write를 상세 `updateCustomerWorkflow`에만 연결, 목록 미연결). 수정 ①목록 stage/chance 변경도 `updateCustomerWorkflow` 경유=DB저장+단일소스(`reloadCustomers` clobber 해소, 리로딩 보존) ②계약가능성 공식 `resolveChance`(`lib/customer-table.ts`) 헬퍼로 4곳 통일 — **Option A=계약완료/출고완료→무조건 "확정", override 무시**(유슨생 결정) ③`selectChance`·`updateCustomerWorkflow` 하드닝(계약완료 비확정 차단·self-heal). TDD 6+test**259**, typecheck0/lint0/build OK, 검증리뷰 회귀0(실데이터 chanceLabel 통합 무변화 확인). **✅브라우저 검증 OK(유슨생)**. **follow-up**: 김민준 DB 모순행(chance="낮음"+statusGroup="계약완료") 1회 정규화(화면은 헬퍼가 "확정" 표시·하드닝이 신규 모순 차단이라 선택사항).
> - **검증 묶음 방식 확정(2026-06-30)**: 구매조건+니즈 묶음 1회 + 견적함 단독. ⚠️**단위테스트 없어 동작보존=typecheck/lint/build+수동 브라우저 검증 의존(고위험)**.
> - **Minor 후속(cleanup 묶음, 분해 완료 후 일괄)**: 구매조건 3건(raw setter `handlers` 래핑·`kimEditorMatches` 비대칭 주석·label→kind 맵 추출) / 니즈 2건(`KimNeedsState` export 불필요·`NeedsDashboard` `detail` 전체 prop→`appUserId`만) / 9a(계약해제 confirm JSX 중복·handlers raw setter 13개→명명) / **워크벤치 5건**(①`solutionWorkbenchPurchaseMethod` 초기값 `kimMinjunPurchaseFields`→주입 `purchaseFields`[런타임무영향=seam이 덮음·import제거] ②`openWorkbenchForQuoteRequest` seam그룹 이동 ③effect 섹션 주석 ④guidance onChange 핸들러 추출 ⑤`quoteList` 통째주입+`setQuotes` raw→명명핸들러 tighten). 전부 동작 무관·후속 PR.
> - **구매조건 7필드 저장 follow-up(기능, 분해와 별개)**: 계약기간·초기비용·연간주행·인도방식·계약포커스·고객/심사특이사항은 미저장(crm.customers 컬럼 없음=기존 의도된 동작, **분해 회귀 아님** — 추출 전후 `savePatch` byte-identical 확인). 저장하려면 customers 컬럼 추가 vs `quotes.guidance` jsonb 결정 필요한 별도 슬라이스. **2026-06-30 유슨생 검증: 기존 동작 유지·분해 계속 결정**(구매방식 needMethod·출고시기 needTiming만 저장).
> - **재개 방법**: `git switch refactor/crm-detail-decomposition` 확인→남은 영역은 plan 공통 절차 A~G + 직전 훅(`useCustomerMemos`/`useCustomerWorkflow`) 패턴 참조로 subagent dispatch. 부모 grep으로 위치 재확인(줄번호 drift). 각 영역 후 typecheck/lint/build 게이트 + 복잡영역 개별 브라우저 검증.

## 최근 main 직접 수정 (2026-06-29, 니즈/인박스 UI 폴리시·관리상태)

> - **PR #123(대기)** `fix/crm-detail-manage-status` `0c90947`: 상세 관리상태를 목록과 동일 규칙(`resolveKimManageStatus`: override→mock map→"")으로, 무조건 "정상" 폴백 제거(신규=공백/"—"). 향후 last_activity_at 자동계산 전환 시도 "신규=공백" 유지.
> - **main 직접(소규모 UI)**: 니즈 앱카드 — 문의사항/색상 값있을때만(sentinel 숨김)·카드목록 470px 스크롤+overscroll-contain·field display:block 위정렬(`224ed67`~`e351b21`). 앱 인박스 테이블 — 행 세로정렬 top 통일·칩높이=버튼·매칭셀 Safari flex-td 버그회피(inner div)(`6d02fe4`~`6cb4a02`). 사이드바 '앱 견적요청'에 새요청 빨간배지(`d1ef92a`).
> - **검증 OK(유슨생)**: 앱→견적요청 실유입 테스트 = 토스트+벨배지+사이드바배지+인박스 유입 전부 동작 확인. S1.5 RLS 실INSERT 검증 완료.

## 최신 작업 — 고객 니즈 영역 = 앱 견적요청 카드 목록 (PR #122 머지 `089205e`, 2026-06-29)

> 견적요청 파이프라인의 **고객-상세 통합** 슬라이스. 고객 상세 "고객의 니즈" 영역을, 앱 유입 고객(`app_user_id` 있음)이면 **그 고객의 앱 견적요청을 카드로 목록 렌더**(여러 관심 차종 한눈에), 수기 고객은 **기존 단일 need 카드 그대로**(회귀 없음)로 분기. 각 카드 "견적 작성"→워크벤치 prefill(S3 재사용)→견적함 승격, 승격된 요청엔 "견적 N건" 배지. 문의사항(`need_memo`)·관심 색상(`need_colors`)은 고객 단위로 카드 아래 유지. brainstorming→spec→plan→subagent(4 task+final 리뷰) 풀사이클.
> - **백엔드**: `listQuoteRequests`의 batch-read를 `buildAppQuoteRequestRows` 헬퍼로 **추출** → `listQuoteRequestsByUser(appUserId)`가 `eq(quoteRequests.userId, appUserId)` 필터만 더해 공유(인박스 회귀 가드 테스트). 라우트 `GET /api/customers/:id/quote-requests`(`getCustomerAppUserId`로 고객→app_user_id 조회: **고객 없음=null→404 / app_user 없음=[]→200 / 있으면 목록**, `run`의 null≠[] 구분 활용).
> - **프론트**: `getCustomer`는 이미 `appUserId` 반환 → lib `CustomerDetailData.appUserId` + `toCustomerDetail` 노출만. `fetchCustomerQuoteRequests(customerId)`(인박스 `toAppQuoteRequest` 어댑터 재사용). `CustomerDetailPage`: `openWorkbenchForQuoteRequest(reqId)` **추출**(URL `?quoteRequest=` 진입 + 니즈 카드 버튼 **공유** — 여러 카드 각각 동작=1회 가드 한계 회피) + `appRequests` 상태/로드effect(appUserId 있을 때만, cancel 가드)/`reloadAppRequests`(승격 후 배지 갱신) + 니즈 영역 분기 렌더 + CSS(`.kim-needs-request-*`).
> - **앱 분기 read-only**: 요청은 앱 소유(CRM 미수정). 앱 고객 `need_memo` 편집은 후속(기존 needs popover가 need_model까지 함께 수정해 카드 목록과 의미 어긋남→앱 분기선 비노출).
> - 검증 typecheck0·lint0·test:unit**253**·test:server**123**·build OK. DB 실측: 김민준(CU-2605-0020) app_user_id NULL=수기분기 / 김지안(CU-2606-0001) app_user_id+요청**4건**=앱분기 카드4. subagent 4 task별 spec+품질 리뷰 + 전체 통합 final "ready to merge"(Minor만). 보완: Task1 row↔select drift 주석·Task2 테스트4 join 강화(요청보유 app_user 선택)·Task4 customer.id narrow 가드.
> - ⚠️**머지 직후 김지안 "앱 견적요청 없음" 오표시는 코드 버그 아님 — 로컬 dev 백엔드 미재시작이 원인**: `dev:api`(`bun run src/local-dev.ts`)는 **watch 없음** → 프론트는 HMR로 새 앱 분기 떴지만 백엔드 새 라우트 부재 → 404→catch→[]. **`bun run dev` 재시작**하면 카드4 정상(로컬 코드 경로 4건 반환 실증). prod는 배포 롤아웃 후 자동 반영. **브라우저 검증(유슨생)**: 김지안 카드4·"견적 작성"→워크벤치·승격 후 배지 / 김민준 수기 회귀X / 인박스 `?quoteRequest=` 회귀X.
> - ⚠️**follow-up**: 처리 카드 접기/필터, 수기 고객 여러 차종 입력, 앱 분기 need_memo 편집, 관리상태 통일, `fetchCustomerQuoteRequests` 무캐시(인박스와 비대칭, 현재 규모 무관).
> - 스펙 `ref/specs/2026-06-29-crm-customer-needs-app-requests-design.md`·플랜 `ref/plans/2026-06-29-crm-customer-needs-app-requests.md`.

## 최신 작업 — 고객 상세 레이아웃 전체 고객 일반화 (PR #120 머지 `d6639a7`, 2026-06-28)

> 김민준(CU-2605-0020) 전용 시범 상세 레이아웃(`KimMinjunDetailContent`)을 **모든 고객**으로 일반화. S2로 생성된 신규 고객(김지안 등)도 같은 표준 상세를 쓰게 됨. **로컬 검증 완료·브라우저 검증만 남음**(유슨생, ⚠️머지 게이트). brainstorming→spec→plan→subagent(5 task+final 리뷰) 풀사이클.
> - **+선행 버그fix 별도 머지(PR #119 `1a100f2`)**: S2 신규 고객 생성 후 `App.customers` 목록 stale(새로고침 전 안 보임) → `reloadCustomers` 추출+생성 콜백 갱신. systematic-debugging 진단(DB·API 정상, 프론트 state 미갱신).
> - **Task 1**: `isKimMinjun` 게이트 제거 → 모든 고객 detail fetch + `KimMinjunDetailContent` 단일 경로. legacy 레이아웃 JSX(summary/action-rail)+죽은 helper(detailRows 등) 제거(-223줄). drawer 닫기=App Esc/backdrop(김민준 기존 동일, 회귀X). **Task 2**: 하드코딩 "김민준"/"CU-2605-0020" 9곳→`customer.name`/`customerId`. **Task 3**: 데이터 소스 없는 mock 빈값/0(`kimMinjunPurchaseFields`→""→렌더"미정"[기존 sentinel], `kimMaybachQuotePricingMock`→0[차량 선택 시 catalog]). 구매방식/출고시기는 detail 유지. **Task 4**: 빈 섹션 안내 4곳(메모/할일/일정/견적, 서류는 기존 `.kim-doc-empty`)+`.kim-list-empty`. **Task 5**: 미사용 legacy CSS 12클래스 제거(-138줄, `customer-detail-summary`는 KimMinjunDetailHeader 사용=보존).
> - **김민준 회귀**: mock 빈값화로 "60개월"/Maybach 243백만→"미정"/0(원래 mock, spec 합의). 구조 변화 없음·풀데이터 정상.
> - 검증 typecheck0·lint0·test:unit(회귀0)·build OK. 커밋 `3702005`(T1)·`55989cb`(T2)·`544cc01`(T3)·`40da1db`(T4)·`a78ff50`(T5). subagent 5 task별 spec+품질 리뷰+final 리뷰 통과.
> - ⚠️**브라우저 검증=머지 게이트**(유슨생): 김지안(빈, 크래시X·빈안내·오표시X)·김민준(풀, 회귀X)·중간 고객 3종.
> - ⚠️**follow-up(일반화로 드러남, 미수정)**: ①**비교카드 round-1 mock**(`kimManualQuoteConditionCards` 우리금융캐피탈/2,398,000/5.32% — 모든 고객 워크벤치 "견적 작성" 시 가짜 금융 숫자 노출, display만·미저장=#4c "비교카드 절반 mock" 한계) ②needs.method/memo 빈상태 bare ③나머지 legacy `detail-*` CSS family(detail-timeline/tabs/ai-section 등 orphan) ④dead props onBack/onFullScreen(전체화면 진입 UI 없음=김민준 기존 동일) ⑤**kim 리네임**(다음 슬라이스).
> - 스펙 `ref/specs/2026-06-28-crm-customer-detail-generalize-design.md`·플랜 `ref/plans/2026-06-28-crm-customer-detail-generalize.md`.

## 최신 작업 — 앱 견적요청 견적 승격 (PR #118 머지 `27eed2a`, 2026-06-28)

> 견적요청 파이프라인 **마지막 슬라이스 = 견적 승격**. 인박스 연결(app_user) 행 "견적 작성" → 그 고객 워크벤치를 **차량/구매방식/옵션 prefill**된 채 자동 오픈 → `crm.quotes` INSERT + `source_quote_request_id` 연결, 인박스엔 "견적 N건" 배지. **로컬 검증 완료·브라우저 검증만 남음**(유슨생, ⚠️김민준 한정). 마이그 0(컬럼 기존, loose id). brainstorming→spec→plan→subagent 풀사이클(6 task, 각 spec+품질 리뷰).
> - **백엔드**: `getQuoteRequestDetail(id)`+`GET /api/quote-requests/:id`(prefill용 trimId·payment·optionIds 단건) + `createQuote`에 `sourceQuoteRequestId` 관통(type→zod `z.uuid().nullable().optional()`→INSERT→클라 payload) + `listQuoteRequests` **4번째 병렬 갈래**(quotes 역참조→`promotedQuoteCount`). ⚠️`quote_request_options.trim_option_id`=catalog `trim_options.id` 동일 체계(실측 확인).
> - **프론트 lib**: 어댑터 `promotedQuoteCount` 노출 + `fetchQuoteRequestDetail`(payment 영문→한글, `PAYMENT_METHOD_LABEL` export). 인박스: app_user 행 "견적 작성" **Link**(`/customer-detail/:code?quoteRequest=id`)+"견적 N건" 배지.
> - **통합**(`CustomerDetailPage`): URL `?quoteRequest` 감지 effect(ref 중복방지+cancel/unmount 가드+`.finally` navigate replace로 param 제거) → `fetchQuoteRequestDetail` → **`editPrefill`(수정·가격)과 별개 경로** `quoteRequestPrefill`(차량 trimId·옵션ids만, **가격은 catalog 계산 보존**) + 구매방식(고객 primary 선깔기→매칭 시 override) + `sourceQuoteRequestId` state→신규 INSERT payload. source는 prefill 워크벤치에만(일반 `+`/수정 진입서 clear).
> - **서버 테스트=tx-롤백**(단건·source 저장·역참조 카운트). 검증 typecheck0·lint0·test:unit**250**(+어댑터/fetch 2)·test:server**116**(+5)·build OK. subagent 6 task별 spec+품질 리뷰 통과(옵션 join 검증·왕복 주석·상대단언·Link a11y·cancel guard·purchaseMethod 폴백 fix).
> - ⚠️**중대 한계**: 워크벤치 prefill이 `KimMinjunDetailContent`(CU-2605-0020 전용 시범) 안이라 **"견적 작성"은 김민준만 동작**. 타 고객은 legacy 레이아웃→클릭해도 silently 무동작(워크벤치 안 열림·param만 잔존). 파일럿 구조라 regression 아님. 일반화는 본체 표준화 트랙. **브라우저 검증은 CU-2605-0020로.** + 데이터 제약(매칭 0이라 app_user 행 없음→실 유입 후 검증).
> - 스펙 `ref/specs/2026-06-28-crm-quote-promotion-design.md`·플랜 `ref/plans/2026-06-28-crm-quote-promotion.md`.
> - **다음**: 견적요청 파이프라인(S1 인박스+S1.5 알림+S2 유입+승격) **완성**. 후속 후보=워크벤치 본체 일반화(전 고객), 실 유입 데이터 확보 후 브라우저 검증, 견적 계산엔진, 고객 앱(Flutter) 노출.

## 최신 작업 — 앱 견적요청 고객 유입 S2 (PR #117 머지 `44af2a2`, 2026-06-28)

> 견적요청 파이프라인 세 번째 슬라이스 **S2 = 고객 유입**(견적요청 도메인 **첫 쓰기**). 상담사 1클릭으로 앱 요청자를 CRM 고객에 **연결(link)/신규 생성(create)**. **로컬 검증 완료·브라우저 검증만 남음**(유슨생). 마이그 0. brainstorming→spec→plan→subagent 풀사이클.
> - **백엔드**(`db/queries/quote-requests.ts`+`routes/quote-requests.ts`): `nextCustomerCode`(CU-YYMM-####, `customer-quotes.ts:186` nextQuoteCode 복제) + `linkRequestToCustomer`(요청 user_id→고객 app_user_id set) + `createCustomerFromRequest`(profiles+요청 매핑 INSERT, **중복방지**=같은 user_id 기존 고객 있으면 반환). 라우트 `POST /:id/link`{customerId}·`POST /:id/create-customer`(transaction). create 매핑=name(full_name??이름미상)·phone·app_user_id·need_model/trim(catalog)·need_method(payment 한글)·source"앱 견적비교"·status_group"신규"/status"상담접수"·received_at(요청 created_at).
> - **프론트**: 어댑터 `AppQuoteRequest`에 `matchedCustomerId/Name/Code` 노출(S1 보류분) + lib `linkRequestToCustomer`/`createCustomerFromRequest`(sendJson POST→성공 시 인박스 캐시 force fresh+`invalidateCustomerDetail`) + `AppRequestsPage` 매칭 상태별 버튼(none→"신규 생성"·phone→"○○○에 연결"·app_user→"고객 보기" Link)+onToast. 뮤테이션당 refetch 1회(lib force+핸들러 false=캐시 재사용).
> - **서버 테스트=tx-롤백 패턴**(`throw ROLLBACK`+`.rejects.toThrow`)으로 실 DB 부작용 0(link set·CU코드·중복·404). ⚠️IDE "await has no effect"(TS80007)는 bun-types 거짓양성(검증 유효 실증).
> - ⚠️**데이터 제약**: 현재 phone 매칭 0(profiles 전화 NULL·요청자 3명 테스트계정)→"연결" 버튼 안 뜸, "생성"은 테스트계정만(name=제임스 등). 실 의미는 앱에서 전화 입력 회원이 견적요청해야. 로직만 완성.
> - 검증 typecheck0·lint0·test:unit**248**(+어댑터 matched 1)·test:server**111**(+4)·build OK. 커밋 `84aa68c`(백엔드)·`e63bbae`(프론트). subagent 스펙+품질 리뷰 통과(중복방지·tx-롤백 유효·double-refetch fix).
> - 스펙 `ref/specs/2026-06-28-crm-app-requests-promote-design.md`·플랜 `ref/plans/2026-06-28-crm-app-requests-promote.md`.
> - **다음 = S3 견적 승격(마지막 슬라이스)**: 요청 행 "견적 작성"→워크벤치 prefill(차량/구매방식/옵션)→`crm.quotes` INSERT+`source_quote_request_id`.

## 최신 작업 — 앱 견적요청 인박스 perf (PR #116 머지 `941a7f9`, 2026-06-27)

> S1 인박스 **배포환경 로딩 지연** systematic-debugging 진단·수정. 진단=쿼리 자체 16ms(빠름)인데 `listQuoteRequests`가 trims→options→customers **직렬 4왕복**, CF(Hyperdrive) 왕복당 RTT가 커서 왕복 수만큼 곱해짐(로컬은 RTT 작아 안 드러남)+프론트 캐시/프리패치 부재. **수정 2**: ①서버 `Promise.all` 병렬화(4왕복→2왕복, 결과 동일=test:server107 가드, CF ~2배 #104 동형) ②프론트 캐시(`lib/quote-requests.ts` `fetchAppQuoteRequestsCached(force)`, detailCache 동형 단일키·TTL60s·dedupe)+사이드메뉴 hover `prefetchAppQuoteRequests`. **실시간 INSERT(signal)·60s 폴백은 `force=true`로 캐시 우회(fresh)**, 첫 진입만 캐시 허용(`firstLoadRef`). 캐시 테스트 5. typecheck0·lint0·test:unit247·build OK. **고객 상세 리로딩 느림=정상(버그X)**: getCustomer 이미 병렬, 새로고침=캐시소멸+cold+CF baseline, 목록클릭 빠름=hover프리패치/캐시 hit. 성능 체감 prod 확인=유슨생.

## 최신 작업 — 앱 견적요청 실시간 알림 S1.5 (PR #115 머지 `2b6417d`, 2026-06-27)

> 견적요청 파이프라인 두 번째 슬라이스 **S1.5 = 실시간 알림**(S1 인박스에 능동 알림 추가). **로컬 검증 완료·브라우저(실 INSERT) 검증만 남음**(유슨생). 마이그 0. brainstorming→spec→plan→subagent 풀사이클.
> - **방식**: **Supabase Realtime** `postgres_changes`(INSERT, `public.quote_requests`). publication 이미 켜짐→DB작업0·polling아님. App 레벨 **단일 전역 구독**, INSERT는 **트리거로만**(payload 안 씀)→기존 `fetchAppQuoteRequests()` 재호출(S1 어댑터 재사용).
> - **표현**: ①토스트(차량명, "새 앱 견적요청: {차량}") ②Topbar 벨 뱃지 카운트 ③인박스 자동갱신. **현재 `/app-requests`면 토스트·카운트 생략**(signal++만). 인박스 진입 시 카운트 0(세션 휘발). 벨 popover 상단 "새 앱 견적요청 N건→보기"(전체/견적 탭만).
> - **컴포넌트**: `lib/quote-requests-realtime.ts`(신규 `subscribeNewQuoteRequests(onInsert)` 구독 wrapper+cleanup `removeChannel`) + `App.tsx`(전역 구독 effect deps `[auth.authed, showToast]`·`newAppRequestCount`/`appRequestSignal`·`locationRef`=**useLayoutEffect 동기화**[render-time ref 쓰기는 `react-hooks/refs` 룰 금지]·`markAppRequestsRead`) + `Topbar`(벨뱃지+popover 실항목) + `AppRequestsPage`(`{signal,onRead}` props·signal 재fetch·**60s 폴링 폴백**[Realtime 끊김 보험]·진입 시 리셋) + index.css.
> - **앱 패턴 참고**(검증됨): Flutter `admin_shell`(전역 구독+탭이동 시 리셋+현재탭이면 카운트제외)·`admin_list_auto_refresh`(Realtime+60s 폴백). 본 설계가 그대로 따름.
> - ⚠️**Realtime RLS**: 구독자(CRM 로그인 계정) `profiles.role` staff+ 여야 INSERT 수신(RLS SELECT). supabase-js v2 세션 JWT 자동. **브라우저 실 INSERT로 반드시 검증**(안 오면 setAuth/RLS, spec §"Realtime 인증/RLS").
> - 검증 typecheck0·lint0·test:unit**242**(+realtime wrapper 2)·test:server107·build OK. 커밋 `da8d779`(wrapper)·`d5018f3`(통합). subagent별 spec+quality 리뷰 통과(탭필터·"최근"·locationRef race[useLayoutEffect]·구독 누수0·stale closure0).
> - 스펙 `ref/specs/2026-06-27-crm-app-requests-realtime-design.md`·플랜 `ref/plans/2026-06-27-crm-app-requests-realtime.md`.
> - **다음**: S2 고객 유입(전화매칭 연결 + 신규 `crm.customers` 생성, 채번 `nextCustomerCode()` `CU-YYMM-####`) → S3 견적 승격.

## 최신 작업 — 앱 견적요청 인박스 S1 (PR #114 머지 `b8fa707`, 2026-06-27)

> 견적요청 파이프라인(앱 public→CRM, 방향 C 하이브리드)의 **첫 슬라이스 S1 = 읽기 전용 인박스**. brainstorming(별 세션)→spec→plan→subagent 실행 완료, **로컬 검증 끝·브라우저 검증만 남음**(인증 세션=유슨생).
> - **범위**: 앱 `public.quote_requests`(97건/3명)를 CRM이 **직접 read**(복사/sync 금지) → 별도 '앱 견적요청' 사이드메뉴 인박스. **마이그 0**.
> - **백엔드**(3계층): `src/db/public-app.ts`(public read 전용 drizzle 정의 3테이블, 메인 generate 격리=catalog.ts 패턴) + `db/queries/quote-requests.ts`(`listQuoteRequests`=public+catalog+crm 3스키마 batch read, N+1 회피, 매칭 app_user_id>phone) + `routes/quote-requests.ts`(`GET /api/quote-requests`) + `app.ts` 마운트. ⚠️drizzle은 `pgSchema("public")` 금지→`pgTable`이 public 정본(주석).
> - **프론트**: `lib/quote-requests.ts`(어댑터 `toAppQuoteRequest`+`fetchAppQuoteRequests`, enum 한글=**Flutter SSOT**: payment lease=운용리스/rent=장기렌트/installment=할부/cash=일시불·deposit=보증금/advance=선수금/prepayment=선납금·status open=진행중/closed=마감/completed=완료) + `pages/AppRequestsPage.tsx`(요청일·요청자·차량+차량가·구매방식·조건·옵션·상태·매칭) + Sidebar '앱 견적요청'(견적 관리 아이콘 공유, 별도 아이콘 후속).
> - **매칭키** `profiles.phone_number`↔`customers.phone`(양쪽 010+8 숫자, 정규화 불필요). ⚠️**현재 매칭 0건**: profiles 9명 전부 phone NULL·요청자 3명(제임스/김지안/강현준)=admin/manager 테스트계정 → 인박스는 전부 "신규(미연결)" 표시가 정상. 실 매칭은 S2/실앱가입 후.
> - **검증**: typecheck0·lint0·test:unit**240**(+어댑터6)·test:server**107**(+quote-requests3)·build OK. 커밋 3개(`b464a37` 백엔드·`d65ecab` 어댑터·`f8d5dcd` 페이지). subagent별 spec+quality 리뷰 통과(brittle 테스트 제거·or()불변식 주석·matchLabel 공백·에러시 "—"·trimPrice 노출 반영).
> - **다음**: **S1.5 실시간 알림**(고객 INSERT→CRM 알림. **Supabase Realtime** `postgres_changes` INSERT 구독 — `public.quote_requests`가 **이미 supabase_realtime publication 등록됨**→DB작업0·polling불필요. 표현=토스트+Topbar 벨 뱃지+인박스 자동갱신. RLS staff read OK·supabase 클라/Topbar 알림 UI 이미 존재. 별도 brainstorming→spec→plan) → S2 유입(전화매칭 1클릭 `app_user_id` set + 미매칭 신규 `crm.customers` 생성, **채번 `nextCustomerCode()` CU-YYMM-####**=기존 `nextQuoteCode` 복제. CU=종류/source=경로 분리) → S3 승격(요청→워크벤치 prefill→`crm.quotes` INSERT+`source_quote_request_id`).
> - 스펙 `ref/specs/2026-06-27-crm-app-quote-requests-inbox-design.md`·플랜 `ref/plans/2026-06-27-crm-app-quote-requests-inbox.md`.

## 최신 작업 — enum/lookup (파일럿 #107·chance #108·doctype #109·open-set #110·tech-enum #111 + **DB CHECK 전면 전환 #112** 머지, 2026-06-26)

> ✅ **lookup→DB CHECK 전면 전환 완료 (#112 머지 `a3c0110`, 2026-06-26)**: 유슨생 지적(관리 UI 안 만들면 lookup보다 정적 DB 제약이 무결성·단순성에서 유리) 채택 → 옵션③(lookup 폐기·DB 전환) 실행. `lookup_values`·`queries/lookups.ts`·`seed-lookups`·`seed:lookups` **폐기**, 어휘=`client/src/data` 코드 SSOT 단일. 검증=앱(zod+코드 순수함수 `lib/lookup-validate.ts`·`status-lookup.ts`, **DB왕복0**)+**DB CHECK 13컬럼**(어휘9: status_group·status·chance·source·customer_type·task.category·schedule.type·doc_type·purchase_method + 기술값4: quotes entry/app/decision/tax). 종속=앱 코드맵 `Map<status,Set<group>>` **다부모**(추후재컨택=3그룹 정확, lookup 복합키 과제 해소). 마이그 `0007`(DROP lookup_values+14 CHECK)+`0008`(quotes.status 자유텍스트 판명→제외=13). CHECK 값=`sql.raw` 리터럴(param이면 `$1` placeholder로 깨짐). garbage 4건(test/시드 잔재) 삭제+`seed-customers` '비교 견적' 재발원 수정. **보류**: `priority`(레거시 읽기전용)·`customer_type_detail`(자유)·`consultations.status`(빈)·`quotes.status`(자유텍스트). 검증 typecheck0·lint0·test:unit234·test:server104·build OK, master 적용·사전밖 UPDATE 거부 실측. ⚠️**순서 함정 발생**: DB(lookup DROP)를 코드 배포보다 먼저 적용해 머지~배포(~1분) 사이 prod 쓰기(PATCH chance/source/status·task/sched/doc) `lookup_values 없음` 500 가능 — #112 머지로 해소(무중단이면 코드 먼저 배포→DB 변경 순이었어야). 스펙 `ref/specs/2026-06-26-crm-lookup-to-db-check-design.md`·플랜 `ref/plans/2026-06-26-crm-lookup-to-db-check.md`.

- **enum/lookup 도메인 정리의 첫 슬라이스(인프라 + 진행상태 파일럿) 완료·main 머지·prod 배포 트리거**. crm 업무어휘를 단일 lookup 테이블로 옮기는 패턴을 가장 복잡한 진행상태(status_group 1차→status 2차 종속) 하나로 검증. brainstorming→spec→plan→executing-plans 풀사이클.
  - **DB**: `crm.lookup_values`(category/value/label/parent_value/sort_order/active, (category,value) unique) + 마이그 `0005`(crm only·additive, master 적용 완료). `value`=현행 text 그대로라 `customers.status_group/status` 컬럼·기존 데이터 **무변경**(데이터 마이그 0). lookup=유효성·열거 사전.
  - **검증**: 종속 판정 순수함수 `src/lib/status-lookup.ts checkStatusSelection`(DB 미접근; **단독 전송=유효성만, 둘 다=종속까지**로 기존 PATCH 경로 보존) + `src/db/queries/lookups.ts`(`listLookup`·`validateStatusSelection`; status 키 올 때만 lookup 1쿼리=perf 중립, 없으면 왕복 0). `routes/customers.ts` PATCH 연결(위반 400).
  - **시드**: `seed:lookups`(`scripts/seed-lookups.ts`, `customerStatusGroups` 상수 → 39행, 멱등 2회 동일). **⚠️종속 중복 한계**: 같은 2차 문자열이 여러 1차에 중복("추후재컨택"·"지속적부재"·"구매시기미도래") → (category,value) unique라 첫 그룹만 1행. 이번 검증 범위(유효성+둘 다 보낼 때 일치)엔 허용, 정밀 종속은 후속 `(category,value,parent_value)` 복합키로 승격.
  - 검증 typecheck0·lint0·test:server**78**(+status-lookup 8·customers 4)·test:unit234·build OK. **⚠️prod 브라우저 검증은 사용자(유슨생)가 전체 일괄 진행 예정**. **⚠️제품 결정 미확정**: "관리 UI 보류·lookup만" 방향이 이사님 의중과 맞는지 최종 확인 필요(spec 명시).
  - 스펙 `ref/specs/2026-06-26-crm-enum-lookup-status-pilot-design.md`·플랜 `ref/plans/2026-06-26-crm-enum-lookup-status-pilot.md`.
- **chance 슬라이스 완료 (🅰 가로 첫 도메인, PR #108 머지 `ba4db0c`)**: chance(계약가능성). 기존 lookup_values **재사용(마이그 0)** + category="chance" 5행 시드(총 44행) + 범용 `validateLookupValue(category,value)`(종속 없는 닫힌집합용·`src/db/queries/lookups.ts`, 향후 닫힌 도메인 재사용) + `customers` PATCH 검증(chance 키 올 때만 1쿼리, 없는값 400). **priority는 레거시**(PATCH 쓰기경로 없음·`customer-table.ts chanceLabel` 폴백 입력)라 비범위·문서화만. "확정"↔계약완료 종속은 워크플로우 규칙(updateCustomerWorkflow) 유지·검증 안 함. 검증 typecheck0·lint0·test:server**81**(+chance 3)·test:unit234·build OK. 스펙 `ref/specs/2026-06-26-crm-lookup-chance-design.md`·플랜 `ref/plans/2026-06-26-crm-lookup-chance.md`.
  - **doctype 정리 완료 (🅳, PR #109 머지 `bcfd98b`)**: 서류 title/doc_type 중복 정리(#69 후속). 죽은 title 코드참조 8곳 제거 + DROP(마이그 `0006`, crm only). doc_type 22종 `DOC_TYPE_OPTIONS` data SSOT 이동(`client/src/data/customers.ts`, classify 반환값과 일치). category="doc_type" 22행 시드(총 66행). 서류 POST/PATCH `validateLookupValue("doc_type")` 검증. **title DROP 안전성**: mismatch 1건(`W투어.jpg` stale title="기타서류" vs doc_type="사업자등록증" — 사용자 수정 후 title이 옛 자동분류값으로 굳음) 확인 후 안전 진행. 검증 typecheck0·lint0·test:server**83**(+doc_type 2)·test:unit234·build OK. 스펙 `ref/specs/2026-06-26-crm-doctype-cleanup-design.md`·플랜 `ref/plans/2026-06-26-crm-doctype-cleanup.md`.
  - **open-set 완료 (🅰 열린집합, PR #110 머지 `3a5ef1d`)**: source/tasks.category/schedules.type lookup화. 옵션 상수 data SSOT 이동(`SOURCE_*`·`TASK_CATEGORY_OPTIONS`·`SCHEDULE_TYPE_OPTIONS`, `kim-status-fields`는 re-export 이름유지). **source "기타" 자유입력 제거**(`parseKimSourceValue` string 정규화·닫힌화, 편집기 custom·저장 분기 제거). 시드 `source`13·`task_category`6·`schedule_type`8 = 27행(총 93행). customers PATCH(source)·task/schedule POST·PATCH(category/type) `validateLookupValue` 검증. **이사님 결정 확정: 관리 UI 안 만듦**(어휘변경=개발자 코드/시드)·열린집합 어휘=**현행 코드 목록**(source 13종=재구매/검색/기타 포함, 이사님 문서 10종과 다름)·team 별도. 검증 typecheck0·lint0·test:server**86**(+3)·test:unit234·build OK. 스펙 `ref/specs/2026-06-26-crm-lookup-open-set-design.md`·플랜 `ref/plans/2026-06-26-crm-lookup-open-set.md`.
  - **다음(enum/lookup 트랙)**: 🅰 **team 보류** — 이사님 '국산팀/수입팀'(차종 기준)인데 **현재 team은 조직/기능 팀**(인천본사·상담·견적·계약·출고, 상담→출고 워크플로우)이고 **advisor 배정과 강결합**(`kim-status-fields.ts kimAdvisorOptions`=팀별 담당자, `KimAdvisorStatusEditor`가 team 고르면 그 팀 담당자만, `parse/formatKimAdvisorValue`="담당자·팀"). 성격 불일치 → **기존 5팀 유지·국산/수입 보류·이사님 재확인 대기**(대체 vs 별개 축[새 차종팀 컬럼] vs 현행유지). DB 분포(20명): 상담7·출고4·인천본사4·견적3·계약2. 🅱세로(프론트 동적소비)=관리 UI 안 만들기 확정돼 **보류 확정**. 🅲기술값 enum=**완료(#111 머지 `941587f`: customerType 3종·purchaseMethod 6종 z.string→z.enum + PURCHASE_METHOD_OPTIONS SSOT + QT-2606-0003 "비교 견적" 잔재삭제. 핵심 entry/app/decision/tax는 이미 z.enum이었음. test:server88·test:unit234)**. 그 외 별도 트랙(견적 (B) 카드 후속·워크벤치 헤더 ②). **enum/lookup 백엔드 검증 트랙 완료**(team만 이사님 재확인 대기). **DB CHECK 전면 전환=#112 완료**(lookup 폐기·맨 위 ✅ 참고).

## Current Focus (2026-06-24) — #4 견적 **읽기(#4a)+쓰기(#4b)+생성 #4c(1·2·3a·3b)+#4d 원본 업로드 + 수정 워크벤치화(#92~#98, composer 제거) + 저장/발송 흐름 재설계(#99) 전부 main 머지**. row 클릭 버그 fix(#91). #3 서류 완료. **#99로 "작성완료=저장 / 발송=저장+발송" 확정 + 신규 작성완료 비교카드 입력 리셋 회귀fix까지 브라우저 검증 완료(신규 INSERT→UPDATE 중복방지·입력보존·리로드저장·외장컬러·발송 OK). 워크벤치 헤더 mock(Maybach/60개월) 실제값화·잔상fix(main 직접)·죽은 mock "기존 세부 견적 수기 입력" 제거(#100)·**추가 안내 사항 저장 (A) #101 완료**·**추가 안내 (B) 미리보기 카드 데이터화 #102 완료(브라우저 검증 OK)**·**차량 로딩 중 작성완료 데이터손실 가드 #103·차량 로딩 perf 번들+병렬화 #104(prod 실측 ~1.7s→~0.6s 3배)·**수정 진입 prefetch+스켈레톤 #105(prod 즉시 확인 OK)·작성완료 후 재진입 옛 옵션 prefill stale fix #106(prod 확인 OK)**·**#4c-2/3a/3b·#4d 전부 prod 브라우저 검증 완료(A 차량/색상/옵션 영속·B 다중시나리오/대표·C 원본 업로드 OK)**. 다음(이 #4 트랙)=**(B) 후속=핵심포인트/추천이유/서비스 카드 레이아웃 확장 or 고객 앱(Flutter) 노출** or 워크벤치 헤더 레이아웃(②, 아래). **enum/lookup은 #107로 착수 → 맨 위 '최신 작업' 섹션 참고.**

### 알려진 레이아웃 이슈 (별도 작업 — 정밀 픽셀 측정+브라우저 반복 필요, composer가 우선)

- **① composer 견적 수정 모달 왼쪽 잘림 (✅무효 — #98로 composer 완전 제거, 견적 수정은 워크벤치 일원화)**: ~~견적함 행 `···`→"견적 수정" → `quoteComposerMode==="edit"` 모달(`kim-quote-modal kim-quote-builder-modal`, 제목 "수정 후 고객 앱 재발송")이 **좌측으로 밀려 왼쪽 절반(차량선택/솔루션결과 카드) 뷰포트 밖으로 잘림**. backdrop은 `place-items:center`인데 비대칭 잘림(우측 X/수정후발송은 보임). **추정=고객 상세 drawer 안에서 열려 drawer 컨테이너 기준 중앙정렬이 어긋남(미확인). 측정 필요.** (워크벤치=별개 전체화면 시트, composer=중앙 모달.)~~
- **② 워크벤치 헤더 잘림 (진단 완료)**: `.kim-quote-workbench-head` 고정 `height:85px` + `.kim-quote-workbench-head-copy{position:absolute;right:890px}` 매직넘버 → 좁은 화면(모달=`inset:0 0 0 234px` 사이드바 제외)서 h2(20px,flex-wrap) 2줄 wrap→상단 글자 잘림+메타(p) 겹침. `.head-tools`도 absolute(`right:14;bottom:12`). **⚠️flex 전환 시도(head=flex space-between, copy flex:1, tools flex:0, h2 nowrap) 실패→되돌림**: tools(구매방식+작성방식 select 2 + 버튼 6)가 `flex:0 0 auto;nowrap`이라 안 줄어 copy를 0으로 짜부러뜨림→h2 글자 세로 박살(악화). **근본=헤더 정보 과밀+반응형 부재. 진짜 해결은 2줄 재배치 or 버튼 아이콘화, 실측 선행.** 실측 스니펫: 워크벤치 콘솔서 `innerWidth` + `.kim-quote-workbench-head-tools` width.

- **견적 워크벤치 저장/발송 흐름 재설계 (PR #99 머지 `3ce6ae9`)**: 워크벤치 "저장처럼 보이는 버튼 3개 중 DB 영속은 발송 하나뿐"이던 **UX 함정** 해소(systematic-debugging=코드 정상, UX 함정). `persistWorkbenchQuote({send})` 헬퍼로 `saveQuoteFromWorkbench`(발송)+`saveQuoteDetailDraft`(검증만) **통합**. **"작성완료"=DB 저장**(차량/옵션/색상/가격/시나리오 전부, 발송X, 워크벤치 유지) / **"수정 후 발송"·"작성 후 발송"=저장+`appStatus=sent`+재발송**(미리보기 게이트와 독립, 자체 검증). 신규 첫 INSERT 후 **`persistedQuoteIdRef`(useRef)로 이후 UPDATE 대상 추적**→중복 INSERT 방지. "조건 저장" 토스트 "담았습니다"로 명확화, 비교카드 "재입력" 죽은버튼 제거. **⚠️회귀fix(같은 PR): 신규 작성완료가 `setEditingQuoteId(id)`를 호출하면 비교카드 `key`(`${editingQuoteId??"new"}-…`)가 바뀌어 uncontrolled input이 `defaultValue`로 리마운트 리셋(보증금 3,000,000→카드 기본 "30")던 것을 → `editingQuoteId`(=key·prefill) 대신 `persistedQuoteIdRef`로 우회(key 불변). ref 리셋=신규 열기·수정 진입·발송 시.** 마이그레이션 없음. 검증 typecheck0·lint0·test:unit**224**·test:server**62**·build OK. **브라우저 검증(카카오 세션) 완료: 신규 INSERT→재작성완료 UPDATE(중복 없음)·입력 보존·리로드 저장·외장컬러·수정 후 발송 OK.** 스펙 `ref/specs/2026-06-24-crm-quote-workbench-save-flow-design.md`·플랜 `ref/plans/2026-06-24-crm-quote-workbench-save-flow.md`.
  - **추가 안내 사항 저장 (A) 완료 (PR #101 머지 `668da1c`)**: 워크벤치 **"추가 안내 사항"(출고시기/재고/예상출고/고객지역/핵심포인트/추천이유/서비스1~4)을 `quotes.guidance` jsonb에 저장**. controlled state(`guidance`, `DEFAULT_QUOTE_GUIDANCE` 첫옵션 기본·서비스 제안값·추천이유 빈값) wiring → `persistWorkbenchQuote` snapshot 추출, 수정 진입 `editPrefill`+`setGuidance` 복원, 신규 열기 DEFAULT 리셋. select 옵션 상수 `client/src/data/quote-guidance.ts`. 백엔드 create/update+zod `quoteGuidanceSchema`, 서버 라운드트립 테스트. 마이그레이션 `0004`(guidance jsonb, crm only·additive) master 적용. **⚠️회귀fix(같은 PR): ①controlled onChange가 setGuidance updater(비동기) 안에서 `e.currentTarget.value` 접근 → Safari서 currentTarget=null TypeError 크래시 → onChange서 값 먼저 변수 추출 ②`validateQuoteDetailDraft`가 추가 안내까지 필수검증해 빈 추천이유에 작성완료 막힘 → `.kim-app-guidance-grid` 검증 제외(선택 입력).** 검증 typecheck0·lint0·test:unit225·test:server63·build OK. **브라우저 검증 완료(크래시 해결·빈값 작성완료·저장/복원).** 스펙 `…quote-guidance-save-design.md`·플랜 `…quote-guidance-save.md`.
  - **추가 안내 (B) 미리보기 카드 데이터화 완료 (PR #102 머지 `2172b70`)**: 워크벤치 앱 견적카드 미리보기(상시 `kim-app-card-preview` + 확대 모달 `isQuoteAppCardPreviewOpen`)가 차량명(Maybach 하드코딩)·가격·구매방식·금융사까지 거의 전부 mock이던 것을 **워크벤치 실데이터로 연결**. ①순수 viewmodel `client/src/lib/kim-app-card.ts` `buildAppCardModel`(state 스냅샷→카드 라벨, TDD 4테스트) ②단일 프레젠테이션 컴포넌트 `client/src/components/KimAppCardPreview.tsx`(상시+모달 중복 마크업 ~137줄 일원화, `model` props만, DOM 미접근) ③`CustomerDetailPage` 배선: `pricingInputs`/`cardScenario` state 추가, `recomputePricing`서 inputs 보관(basePrice/discount reactive), **조건 저장 시점 effect로 대표 시나리오 추출**(uncontrolled DOM이라 `savedManualQuoteConditionIds` 변경 effect), `appCardModel` 매 렌더 조립(순수, useMemo 없음), 카드 두 곳 교체. 연결=차량명/연식(`trimDetail.modelYear`)/기본가/할인/구매방식/약정/금융사/월납입/보증금/선수금/잔존/주행/취득원가/외장·내장 컬러/출고시기(`guidance` 재고·예상출고·고객지역). **소스 없는 금리·총비용은 안내 텍스트(`—`/`계산 후 안내`)** — 가짜 숫자 제거. **시나리오=저장된 대표(round1) 기준**(미저장 시 추천조건 블록 "조건 저장 후 표시" placeholder). 마이그레이션 없음. 검증 typecheck0·lint0·test:unit**229**·build OK. **브라우저 검증 완료(확대 모달서 BMW 5 Series 520i 실표시·Maybach 소멸·색상·guidance·금리"—"/총비용"계산 후 안내" 확인).** 스펙 `…quote-preview-card-data-design.md`·플랜 `…quote-preview-card-data.md`.
    - **남은 (B) 후속 (선택, 별도 슬라이스)**: **핵심포인트/추천이유/서비스1~4는 카드에 자리 없어 미연결 → 카드 레이아웃 확장** 필요. 실제 고객 앱(Flutter, 별도 레포) 노출은 더 뒤. D-6/미확인견적은 발송 상태값이라 현행 mock 유지. 죽은 CSS `.kim-app-condition-grid`·신규 `.kim-app-detail-empty` 스타일은 CSS 정리 사이클서.
- **차량 로딩 중 작성완료 데이터 손실 가드 (PR #103 머지 `df9c816`)**: 견적 수정 진입 시 차량(`trimDetail`)은 `fetchTrimDetail`(async catalog) 완료 후 채워지는데, `validateQuoteDetailDraft`가 `quoteDetailFormRef` 내부 input/textarea **빈값만** 검사(가격 input은 mock defaultValue로 항상 통과)하고 **차량 선택을 검증 안 해**, 로딩 중 작성완료 시 brand/model/trim/trimId=null로 UPDATE→기존 차량 소실(데이터 손실). systematic-debugging=검증 부재가 근본(타이밍 아님). ①`persistWorkbenchQuote` 검증 직후 `!trimDetail||!workbenchVehicle`이면 저장·발송 차단+안내(수정"불러오는 중"/신규"차량 먼저 선택"), 모든 저장 경로가 이 함수 경유라 전 경로 커버 ②작성완료·수정후발송 버튼 차량 로딩 전 `is-disabled`(회색/흐림, `pointer-events` 살아 클릭 시 ①가드 토스트) ③`.complete.is-disabled` opacity. **브라우저 검증 완료(로딩돼야 작성완료 활성)**. 검증 typecheck0·lint0·build OK.
- **견적 워크벤치 차량 로딩 perf 번들+병렬화 (PR #104 머지 `583bbc4`)**: 수정 진입 차량 로딩이 느려 #103 가드로 작성완료가 대기되던 것 — **prod 토큰 실측: trimDetail ~0.65s 최대, 클라 4요청(+중복 trimDetail) 직렬 ~1.7s**(각 왕복이 Hyperdrive origin 연결=요청별 생성이라 무거움). **캐싱(C) 배제**(trimDetail 캐시키 다양·진입 빈도 낮음·mc-master `/api/catalog` 읽기+쓰기라 비일원화). 대신 **왕복 축소+병렬화**: ①`getTrimDetail` 5쿼리 직렬→`Promise.all` 4개+relations(options.ids 의존)만 뒤(`/trims/:id` 신규모드도 수혜) ②번들 `getWorkbenchVehicle(trimId)`+`GET /api/vehicles/workbench`(trimDetail+brands+models+trims 한요청·내부 2라운드 병렬, 기존 get* 재사용=읽기 SSOT 단일) ③`fetchWorkbenchVehicle`+VehiclePicker 수정모드 1요청화 ④`VehicleSelection.trimDetail?` 동봉→`applyTrimToPricing`이 `selection.trimDetail ?? await fetchTrimDetail`로 수정모드 중복 재fetch 제거(신규는 폴백). **신규 모드 불변**. **prod 재실측: 번들 1요청 ~0.6s(번들 총시간≈trimDetail 단독, brands/models/trims가 병렬로 숨겨짐) → 클라 ~1.7s→~0.6s 3배 확증**. 캐싱/binding/SSOT통합(읽기 vehicles.ts↔catalog-admin.ts 중복)/프론트캐시는 범위 외(stale 0). 검증 typecheck0·lint0·test:server**66**·test:unit**230**·build OK. **✅ prod 확인(#105 hover prefetch로 수정 진입 즉시·번들 동작)**. 스펙 `…vehicle-perf-design.md`·플랜 `…vehicle-perf.md`.
- **견적 워크벤치 수정 진입 prefetch + 스켈레톤 (PR #105 머지 `bc5aca9`)**: #104로 ~0.6s가 됐으나 그 동안 빈 화면 체감 — ~0.6s는 CF엣지↔Supabase origin 왕복 베이스라인이라 실제 시간을 더 못 깎으니 **"기다림"을 숨김**. ①`client/src/lib/vehicles-cache.ts`(신규) `trimId→WorkbenchVehicle` 단일키 프론트 캐시(TTL60s+inflight dedupe; mc-master `catalog-cache`와 닮았으나 `/api/vehicles` 격리 위해 별도=mc-master 비건드림) `prefetchWorkbenchVehicle`/`fetchWorkbenchVehicleCached` ②견적 행 `onMouseEnter`→`prefetchWorkbenchVehicle(quote.trimId)` 백그라운드 워밍 ③VehiclePicker 수정모드가 `fetchWorkbenchVehicleCached` 소비→hover로 미리 받으면 **캐시 hit·네트워크0·즉시** ④캐시 miss(hover 없이 바로 클릭)면 차량선택 값+옵션/컬러 카드 **스켈레톤(shimmer)**. **신규 모드 불변**(editLoading=`initialTrimId!=null && loading && !brand`라 false·옵션컬러 `editingQuoteId && !trimDetail` 가드라 항상 OptionPicker). 무효화 불필요(catalog는 견적 저장과 무관·TTL60s+새로고침 리셋으로 stale 방어). 서버/Hyperdrive 캐싱/쿼리통합/mc-master 범위 외. 검증 typecheck0·lint0·build OK·vehicles-cache 4테스트(hit/dedupe/prefetch/TTL)·VehiclePicker 3테스트. **prod 확인 OK(hover 후 수정 진입 즉시 뜸)**. 스펙 `…prefetch-design.md`·플랜 `…prefetch.md`.
- **#4d 견적 원본 업로드 (PR #90 머지 `a0628ab`)**: 견적함 행에 이미지/PDF 드롭 → **Storage 업로드 + `quotes.file_*` 영속**(현재 메모리 objectUrl만이라 새로고침 시 소실되던 것). **+후속 보강(같은 PR): 견적함 행에 `fileName` 있으면 📎 파일명 칩(`kim-quote-attach-chip`, meta-pricing 줄, 클릭→미리보기) — 행에 원본 영속 여부가 안 보이던 UX 빈틈 메움**. **브라우저 일부 확인(로컬 dev): 행 드롭→첨부(`···` "견적 원본 보기" 등장)·미리보기 모달 OK. ⏳새로고침 영속·다운로드·삭제·재드롭 교체는 #4c 일괄 최종 검증.** **서류 #3 인프라 그대로 재사용**(`storage.ts`·private 버킷·secret key·`document-validation`·signed URL, storage mock도 테스트에 기존). **결정(brainstorming)**: 범위=**영속만**(OCR 제외), 진입점=**견적함 행 드롭만**(MVP, 워크벤치 원본인식 영속은 후속), 미리보기=**원본 직접**(썸네일 안 만듦 → **마이그레이션 0**, `quotes.file_*` 4컬럼 기존). ①읽기 `kim-quote.ts` `CustomerDetailQuote.fileName/fileSize/fileMime` + `toKimQuoteItem` 매핑 ②서버 `customer-quotes.ts` `setQuoteFile`/`clearQuoteFile`/`getQuoteFilePath`(id AND customer_id 가드, 이전 file_path returning=교체 보상삭제) + `customers.ts` getCustomer **file_path 비노출**(타입 `Omit` + map `{filePath:_,...rest}` 런타임 제거) + `routes/customers.ts` 라우트 3(`POST`/`DELETE`/`GET …/quotes/:childId/original[/url]`, MIME415·20MB413·없는견적404·교체 objectId per upload) ③프론트 `customer-quotes.ts` `uploadQuoteOriginal`/`deleteQuoteOriginal`/`getQuoteOriginalUrl`(성공시 invalidate) ④`CustomerDetailPage` `attachQuoteFileToQuote` 낙관·업로드·롤백(`kim-` 가드) + `removeQuoteOriginal` + 미리보기 `activePreviewQuoteUrl=objectUrl??signed` effect + 미리보기 모달 다운로드/삭제 버튼. 검증 typecheck0·lint0·test:unit**219**·test:server**61**·build OK. **✅ prod 브라우저 검증 완료**(드롭→영속·미리보기·다운로드·교체·삭제 OK. +API 영속: QT-2606-0004 mg캐피탈.png signed URL 200/image-png/313KB). 스펙 `ref/specs/2026-06-23-crm-quotes-original-upload-design.md`·플랜 `ref/plans/2026-06-23-crm-quotes-original-upload.md`.
- **#4c-3b 시나리오 비교 표시 + 대표 전환 (PR #89 머지 `cec975a`)**: 견적함에서 `scenarios.length>=2`일 때 **펼침(아코디언) 비교 + 대표(primary) 전환**. ①서버 `QuoteHeaderPatch.primaryScenarioId`(headerSet과 분리 — `updateQuote`가 시나리오 목록 조회 후 **해당 quote 시나리오일 때만 set**, 타 quote/없는 id 무시, null=해제) + `quotePatchBody` zod(`z.uuid().nullable().optional()`) ②프론트 `kim-quote.ts` 헬퍼 export(`flattenPrimaryScenario`=대표 평탄화 4필드, `formatScenarioMoneyMode`=percent"30%"/amount"1,000만원"만원절삭/none"없음"/max"최대", `formatTerm`/`formatMonthly`) + `KimQuoteItem.primaryScenarioId` 매핑 + `QuoteWritePatch.primaryScenarioId` ③`CustomerDetailPage` `expandedQuoteId` state + "비교 N▾" 토글 + 펼침 시나리오 카드(★대표/대표로 버튼, 보증금·선수금·잔존·약정 표시) + `setPrimaryScenario`(낙관+`apiUpdateQuote`+롤백, 평탄화 동시 갱신, `kim-` 임시id 가드) ④`index.css` `.kim-quote-compare*`/`.kim-quote-scenario-*`. **마이그레이션 없음**(primary_scenario_id 컬럼 기존). 검증 typecheck0·lint0·test:unit**217**·test:server**59**·build OK. **✅ prod 브라우저 검증 완료**(비교 N 펼침·대표전환·유지 OK. +API: QT-0005 시나리오 2건·전 견적 primaryScenarioId 설정). 스펙 `…scenario-compare-design.md`·플랜 `ref/plans/2026-06-22-crm-quotes-scenario-compare.md`.
- **#4c-3a 다중 시나리오 저장 (PR #88 머지 `053cfec`)**: 워크벤치 하단 비교카드(3슬롯)의 **입력 가능 필드만**(금융사·보증금·선수금·잔존·약정거리·월납입 + 상단 구매방식) 추출 → "조건 저장"한 카드를 시나리오 N건(1~3)으로 저장 + 읽기 어댑터 scenarios 배열 보존. **createQuote `scenario`(단수)→`scenarios`(복수, max 3)**, 단수 폴백으로 composer/#4c-2 하위호환. primary=round1. 추출=#4c-2 DOM 패턴(식별자 `data-scenario-card`/`data-sc-field` 부여, controlled wiring 없음). **⚠️비교카드 절반(기간/자동차세/보조금/계산결과)은 입력 불가 mock**(버튼 onClick 없음·value 고정)이라 #4c-3a 미저장 — 입력화는 후속(계산결과는 계산엔진과). 마이그레이션 없음. 검증 typecheck0·lint0·test:unit209·test:server58·build OK. **✅ prod 검증 완료**(다중 저장 OK. +API: QT-0005 시나리오 2건). 스펙 `…multi-scenario-design.md`·플랜 `…multi-scenario.md`.

- **#4c-2 워크벤치 영속화 (PR #87 머지 `12e571b`)**: 견적함 헤더 **`+`("견적 작성")가 여는 워크벤치**(이미 동작하는 계산기 — `VehiclePicker` catalog 차량 선택→`computePricing` 실가격, OptionPicker·ColorPicker·할인)의 **저장만 mock이던 것**(`saveQuoteFromWorkbench`가 입력 무시·Maybach 고정·`apiCreateQuote` 미호출)을 **실 입력 추출→INSERT 영속**. 범위=**차량+가격+색상**(금융조건 비교카드/다중 시나리오는 #4c-3). **마이그레이션 없음**(quotes 가격/색상/옵션 컬럼·읽기쿼리 이미 완비). 변경: ①`createQuote`/`QuoteCreateBody`+POST zod 가격/색상/옵션 헤더 컬럼 확장(composer 하위호환) ②`saveQuoteFromWorkbench` 재작성(차량 selection·옵션 ids 새 state 보관 → `readPricingInputs`+pricing+색상 추출 → 낙관+apiCreateQuote+임시id 교체+롤백) ③읽기 `CustomerDetailQuote`/`KimQuoteItem`/`toKimQuoteItem` 가격·색상 확장 ④견적함 행 최종차량가+색상 칩 표시. **⚠️핵심 발견: `quotes.trim_id`·`exterior/interior_color_id`는 schema 주석("Phase B")과 달리 실제 catalog FK(ON DELETE SET NULL, `drizzle/0001`) — catalog 실존 id만 INSERT 가능**(워크벤치는 실 catalog에서 골라 안전; 서버 테스트는 id null·snapshot만 검증). 검증 typecheck0·lint0·test:unit208·test:server56·build OK. **✅ prod 브라우저 검증 완료**(차량/가격/색상/옵션 저장→유지 OK. +API: QT-0005 제네시스 G80(trimId 3702)·QT-0006 현대 캐스퍼(trimId 2773) 차량/색상/옵션/가격 영속). **⚠️부수발견·fix (PR #106 머지 `1cfcf58`)**: 작성완료 후 워크벤치 닫고 재진입 시 옛 옵션/색상/가격 prefill(=editPrefill이 `detail.quotes`에서 생성되는데 detail은 부모가 마운트 시 1회 fetch한 prop이라 stale; quotes state만 낙관갱신·invalidate는 다음 리로딩만 fresh) → **작성완료/발송 성공(`apiUpdate/CreateQuote.then`) 후 `onQuotesPersisted` 콜백으로 부모가 `detail` 재fetch(setDetail)**해 현재 마운트 detail.quotes를 fresh화. 검증 typecheck0·lint0·build OK·prod 재현 확인. 스펙 `ref/specs/2026-06-22-crm-quotes-workbench-persist-design.md`·플랜 `ref/plans/2026-06-22-crm-quotes-workbench-persist.md`.
- **#4c-1 견적 생성 composer (PR #86 머지 `5f8b934`)**: 견적함 composer "견적 작성" add 분기 → 새 견적 **INSERT**(quote 1 + 대표 시나리오 1). **`quote_code` 서버 생성**(`nextQuoteCode` YYMM+max+1 → `QT-2606-####`, 트랜잭션). **이 INSERT 인프라를 #4c-2가 확장 재사용.** 스펙 `ref/specs/2026-06-21-crm-quotes-create-design.md`. (composer는 별도 진입 — 일반 `+`는 워크벤치=#4c-2가 영속화함.)
- **#4b 견적 쓰기 (PR #85 머지 `9642e61`)**: 김민준 견적함 **기존 견적 mutate**(메타 수정·삭제·decision/app status 토글) useState→DB. 단일 `PATCH /:id/quotes/:childId`(헤더+선택적 대표 시나리오, 트랜잭션) + `DELETE`. 대표 시나리오=primary→scenario_no 최소 **서버 resolve**. **발송 시 `sent_at` 서버 스탬프·`bumpRevision` SQL 증분.** Kim 핸들러 4개 낙관+롤백. 브라우저 검증 OK(앱 발송→새로고침 유지 실증). 스펙 `…quotes-write-design.md`·플랜 `…quotes-write.md`.

- 인증+CF 배포 + 코드리뷰(#43~#45) + **고객 읽기(#46/#51)** + **mc-master UX/성능(#47~#52)** + **딥링크(#53)** + **고객 쓰기 #1 본체(#54)·#2 자식CRUD(#55)** + **전화번호 숫자만저장(#56)·010 prefix+상세 로딩 병렬(#57)** + **상세 결과캐시(1d73cc2)·로그인 로고(c213f14)** 전부 main 머지. **crm.mrcha.app 라이브**.
- 김민준(`CU-2605-0020`) 상세는 이제 읽기+쓰기(상태/니즈/구매방식·출고시기/메모/할일/일정)가 DB 연동되고, 진입 로딩도 캐시+병렬로 거의 즉시. 김민준 외 고객은 아직 구 일반 레이아웃(읽기만).
- **다음 작업 후보**: ①**enum/lookup 도메인 정리**(별도 설계 — 시작 전 이사님께 "업무 어휘를 코드/배포 없이 직접 수정(관리 UI)까지 원하는지" 확인 필요. 서류 `customer_documents`의 title/doc_type 분류 중복 컬럼 정리도 여기서). ②**#4 견적**(crm.quotes·quote_scenarios, 고객 FK 연결됨). 이후 #5 advisor 배정.

## 리팩토링 트랙 (2026-06-20, 별도 사이클) — 안전 작업 소진, 본체 분해만 남음

- **PR #61~#78 머지**(코드베이스 전체 분석 5에이전트 기반). 죽은코드·도메인상수·lib/http 통일·백엔드 run()·**Topbar usePopoverDismiss(#72)**·**CSS brand 토큰화(#73**, `#5836ff` 399곳→`var(--brand)`, 검증=정적 diff로 시각변화0)·**거대파일 분해 1~4차(#74~#77)**: `CustomerDetailPage` **6099→5706줄**, 도메인별 타입+상수+헬퍼를 `lib/kim-detail-utils`·`kim-schedule`·`kim-status-fields`·`kim-popover-frames`로 분리(+단위테스트 42).
- **renderRow 분해(#78 머지)**: `CustomerManagementPage.renderRow` **481→87줄** 조립함수로. ①순수 헬퍼/상수/타입→`lib/customer-table.ts`(신규, page·셀 공유→순환 import 방지) ②9개 셀(select/info/vehicle/stage/chance/nextAction/operation/finalUpdate/actions)→`CustomerManagementRow.tsx`(신규) 컴포넌트 추출(셀 props=의존 상태/핸들러/ref 1:1). page **1691→1012줄**. legacy stage 죽은분기 전용 `changeCustomerStatus`·`stageSignal` 제거(`openStageFor` 인프라는 무해 잔존). typecheck0·lint0·test:unit189·build OK. **후속 #79 머지**: 잔존하던 `openStageFor` 죽은 인프라(상태·`stagePopoverRef`·전용 외부클릭 effect·`setOpenStageFor(null)` 8곳) 통째 제거(-47줄, page **1012→965줄**, 동작보존 test189).
- **다음 = 본체 일반화 (단독 분해 ❌)**: 본체 `KimMinjunDetailContent`(김민준 전용 시범 ~4500줄)는 **단독 분해 말고 '전체 고객 표준 일반화'에 흡수**(김민준 전용인 채 쪼개면 일반화 때 또 건드려 이중작업). 일반화 ≈ **데이터화(mock→DB #4) + 영역 컴포넌트 분해 + kim 리네임 + 구 레이아웃 대체**. **선행**: ①이사님 '점진 일반화' 컨펌(당장 전체 전환 ❌, 데이터 없는 고객 휑함) ②데이터화+빈상태 처리(현재 김민준만 풀세트 시드). **방식**=영역별(데이터화→분해→일반화) 점진, 이미 DB된 영역(상태·니즈·메모·할일·일정)부터·견적은 #4와 함께·kim 리네임 마지막. **제품 결정 선행·별도 세션**.
- **보류**: 견적 도메인 분리는 #4(crm.quotes) 데이터화 이후(mock·`KimQuoteItem` 타입 변경 가능). CSS 후속(죽은CSS=동적클래스 위험·spacing=안티패턴). `kim` prefix 일반화 리네임은 김민준 시범→전체표준 확정 후 일괄.
- **관례**: 거대파일 분해는 브랜치 먼저(#75 main 직접커밋 사고). 동작보존=typecheck0(참조정합)+단위테스트(입출력캡처).

## 완료 (2026-06-20)

- **보안: auth 훅 search_path 고정 (직접 DDL, 마이그레이션 없음)**: Supabase Security Advisor "Function Search Path Mutable" 경고 대상 `public.custom_access_token_hook`(CRM 인증 #36의 JWT `user_role` claim 주입 훅, `profiles.role`→claim) 에 `ALTER FUNCTION public.custom_access_token_hook(jsonb) SET search_path = '';` 적용. 본문이 이미 `public.profiles`/`public.user_role`로 정규화돼 있어 **동작 불변**(호출 검증 OK: 없는 user→`user_role:null`, 실제 user→`user_role:"admin"`). **앱+CRM 공유 Auth 훅**(토큰 발급마다 호출)이라 적용 후 카카오 로그인 정상 확인 권장. drizzle 관리 밖(public)이라 마이그레이션 파일 없음 — 재현은 위 SQL. **남은 advisor 경고 2건**(`increment_faq_click_count`·`transition_estimate_status`, SECURITY DEFINER + authenticated EXECUTE)은 **앱 함수**라 앱팀 영역.
- **고객 쓰기 #3 — 서류 업로드 (#58 머지)**: 김민준 서류함을 메모리(objectURL)→Supabase **private 버킷 + secret key 백엔드 경유**로 영속화. 6커밋 — ①검증유틸 `src/lib/document-validation.ts`(MIME 화이트리스트 이미지/PDF(원래 오피스 포함→#68서 제거)·20MB·`safeFileName` **ASCII-safe 정규화** — 한글 등 non-ASCII는 Supabase Storage 키가 `Invalid key`로 거부하므로 제거(확장자 보존, 원본명은 DB file_name 보존), 실 업로드 왕복으로 검증, TDD). ②`src/lib/storage.ts`(secret key supabase, upload/remove/signedUrl, env=`SUPABASE_SECRET_KEY`, legacy service_role 아님). ③`db/queries/customer-documents.ts`(add/update/delete/path/reorder/nextSortOrder, `id AND customer_id` 가드) + `getCustomer` documents 정렬·`file_path` 비노출. ④`routes/customers.ts` 서류 라우트 5(`POST` multipart·`PATCH docType`·`PATCH reorder`·`DELETE`·`GET …/url`) + 서버테스트 4(storage `mock.module`). ⑤프론트 `lib/customer-documents.ts` 5종(성공 시 `invalidateCustomerDetail`). ⑥`CustomerDetailPage` Kim 서류함 핸들러 DB연결(낙관+롤백·임시 id `kim-` 가드·미리보기 signed URL[objectUrl 우선]·오피스 다운로드 링크). **캐비엇**: signed URL TTL 60s(PDF iframe range 재요청 끊김 시 상향 검토); reorder는 `customer_id` 가드라 타 고객 무해하나 200 멱등(미래 multi-advisor 재검토). **검증**: typecheck0·lint0·test:unit 125·test:server 46·build OK. **상태**: secret key(로컬+CF)·private 버킷 `customer-documents` 등록 완료. **다운로드=blob 방식**(supabase signed URL Content-Disposition이 한글을 이중인코딩해 깨져서 — `%25EC…` 실증 — 프론트가 blob+`a.download`로 원본명 보존; 다운로드 fetch는 Storage CORS 의존, 배포환경 확인). 경로=`{customerId}/{objectId}-{ASCII safeName}`. 브라우저 수동검증은 배포환경에서. **후속 #59**(머지): 미리보기 잘림(이미지 폭맞춤+세로 스크롤)·분류 select 발견성(분류 변경은 원래 native `<select>`로 동작했으나 텍스트로 위장돼 못 알아봄 → 드롭다운 화살표 상시+hover 강조) CSS 수정. 자동분류는 파일명 기반이라 단서 없으면 "기타서류"(내용 OCR은 후속); 우상단 ⬇️=목록PDF(개별 다운로드는 미리보기 모달). **후속 #60**(머지, 미리보기 UX 종합): 모달=drawer 영역 중앙(좌측 목록 안 덮음)·헤더에 다운로드 버튼(blob 원본)·로딩 `onLoad` 한꺼번에·이미지 미리보기=**Supabase 이미지변환 썸네일**(w1000·q70, 1.4MB→~318KB; **다운로드는 원본** url/downloadUrl 분리)·스크롤 전파 차단(`body.kim-doc-preview-open`+drawer `overflow:hidden` + backdrop `overscroll-behavior:contain`)·모달 **고정크기 660×900**(로딩↔이미지 점프 제거, 이미지 `object-fit:contain`). **우측 여백은 원본 이미지(스캔본) 자체 여백**이라 그대로. PDF/오피스는 변환 불가라 원본. 스펙 `ref/specs/2026-06-20-crm-customer-documents-design.md`(후속 반영 섹션 포함)·플랜 `ref/plans/2026-06-20-crm-customer-documents.md`. **후속 #68**(머지): ⓐ오피스 허용 제거 — 이미지/PDF만(이사님 결정. `ALLOWED_MIME`=`application/pdf`+`image/*`만, `addDocumentFiles` 필터·`input accept`·서버 415테스트 정정). ⓑ서류함 우상단 ⬇️ = 이미지/PDF를 **하나의 PDF로 병합 다운로드**(금융사 일괄제출) — 신규 `client/src/lib/document-merge.ts`(pdf-lib): 표시순서대로 PDF=`copyPages`·이미지=A4 contain+**canvas JPEG 재인코딩**(progressive/webp/heic 등 pdf-lib 미지원 포맷도 안전)·실패항목 skip 집계·`김민준-서류.pdf`. **pdf-lib는 병합 시점에만 동적 import**(초기번들 1293→872KB, 별도청크 421KB). 기존 목록텍스트PDF(`exportDocumentBundleAsPdf`/`downloadTextAsPdf`) 대체. **후속 #69**(머지): 서류 **분류 변경이 재진입 시 옛값으로 되돌아오던 버그** 수정 — 분류 진실원본=`doc_type`(PATCH가 갱신)인데 재진입 초기화가 화면 제목을 레거시 `title` 컬럼에서 읽어 미반영(바뀐 값은 서브라인 status 자리에 노출)이었음. 초기화 매핑을 `title:d.docType??d.title`·`status:"분류완료"`로 통일(기존 깨진 데이터도 `doc_type` 우선이라 자동복원, 마이그레이션 불필요). **남은 정리**: DB `title`/`doc_type` 두 컬럼이 분류 중복 → title 컬럼 제거·status 출처(자동/수동) 영속은 **enum/lookup 사이클**에서. **후속 #84**(머지 `65a6215`): 미리보기 **Safari 회귀** 수정 — #60이 도입한 `render/image` 변환이 Accept 협상으로 **WebP**를 내보내 Safari가 미리보기를 못 그리던 것(Chrome OK, `format:"origin"`은 무효 실증)을, **업로드 시 브라우저 canvas로 JPEG 썸네일 생성·저장**으로 대체. **`crm.customer_documents.thumb_path` 컬럼 신설 = 마이그레이션 `drizzle/0003` master 적용 완료**(nullable·additive·하위호환). ⚠️**팀 공유 master DB 스키마 변경**(다른 세션도 동일 DB). thumb_path는 **서류 행당 컬럼 + 업로드마다 `objectId` 생성**이라 한 고객의 **다중 이미지도 각자 썸네일**(경로 `{cid}/{objectId}-thumb.jpg`); 옛 문서·비이미지는 **원본 폴백**, 다운로드는 항상 원본. 신규 `client/src/lib/image-thumbnail.ts`(`createImageBitmap`+canvas, 디코딩 실패→thumb 없이 진행), `uploadDocument`가 이미지면 thumb 동봉. +이미지 미리보기 **여백 제거**(PDF처럼 `width:100%`)·**다운로드 아이콘 즉시 표시**·**업로드 직후 미리보기 안 뜨던 기존 버그** 수정(`App.showToast` `useCallback` 미적용→`onToast`를 deps에 둔 미리보기 effect가 앱 리렌더마다 churn→로딩 플래그 stranded; 로딩여부를 "로드된 URL==현재 URL" 파생값으로 수정). 검증 typecheck0·lint0·test:unit198·test:server49·build OK.
- **전화번호 정규화 (#56·#57)**: DB는 **숫자만 11자리**(`01095880812`) 저장(기존 20행 `regexp_replace`로 정규화·시드/쓰기도 숫자만). 표시는 `lib.formatPhone`로 `010-9588-0812`. 입력/수정은 **010 고정 prefix + 뒤 8자리(4-4)만**(`KimPhoneStatusInput`, `saveStatusField` phone이 `010`+8 저장). 이전 프리뷰(replace) 입력의 **백스페이스 전체삭제 버그**도 일반 편집으로 수정.
- **상세 로딩 perf (#57·1d73cc2·4042617)**: ①`getCustomer` 자식 5쿼리 **순차(6왕복)→`Promise.all` 병렬(2왕복)**. ②행 hover 프리패치 + ③**결과 캐시(TTL 60s)+쓰기 시 무효화** — 재진입 즉시(왕복 0), 쓰기(updateCustomer·자식 CRUD) 성공 시 `invalidateCustomerDetail(id)`로 stale 방지. ④로딩 중 "헤더 먼저" progressive 제거→중립 스켈레톤(준비되면 한 번에). 체감 거의 즉시.
- **로그인 로고 (c213f14)**: 로그인 화면 `Mr. Cha CRM` 위 차선생 로고(앱 로그인과 동일 `mrcha-logo-color.svg`, 80px 라운드).
- **고객 쓰기 #2 — 자식 CRUD (#55 머지)**: 메모/할일/일정 추가·수정·삭제·완료토글 DB 저장. 4커밋 — ①`customer_schedules.done` 컬럼(drizzle 0002, 일정 완료용). ②백 `customer-children.ts`(query 9: add/update/delete × memo/task/schedule, where=`id AND customer_id`) + `routes/customers.ts` 중첩 라우트 9개(`POST /:id/{memos,tasks,schedules}`·`PATCH·DELETE /:id/…/:childId`) + 서버테스트 3(라운드트립·404). ③프론트 `lib/customer-children.ts` 9개 + `CustomerDetailSchedule.done`. ④Kim 핸들러 12개 wiring(낙관+롤백, **추가 시 임시 id→서버 uuid 교체**, 토글=PATCH done, 일정 완료 시드 `detail.schedules.filter(done)`). **캐비엇**: 추가 직후 POST 해소 전(임시 id) 그 항목 조작은 `id.startsWith("kim-")` 가드로 API 생략(드문 race). **검증**: typecheck0·lint0·test:unit 125·test:server 39·build OK. 브라우저 유지확인 수동.
- **고객 쓰기 #1 — 본체 필드 (#54 머지)**: 고객 본체 컬럼 인라인 수정을 `PATCH /api/customers/:id`로 저장(낙관+롤백). 4커밋 — ①백 `updateCustomer` query + `PATCH /:id`(`customerWriteSchema` 14컬럼 partial, export) + 서버테스트 5(schema·404·400·비파괴200). ②프론트 `lib.updateCustomer`+`CustomerWritePatch` + chance 읽기(`CustomerRow.chance`·`Customer.chance`·`toCustomer`). ③`App.updateCustomerWorkflow`가 statusGroup/status/chance PATCH(목록·상세 공통, 계약완료→확정 동기화, 롤백) + `fetchCustomers`가 chance로 `chanceOverrides` 시드. ④Kim 핸들러 `savePatch` 헬퍼로 연락처/직군/거주지/상담경로/니즈/구매방식/출고시기 PATCH+롤백. **캐비엇**: 비컬럼 구매조건(계약기간 등)은 편집되나 미저장(새로고침 원복, 견적 도메인); manageStatus·advisor 미저장(컬럼없음/다음). **검증**: typecheck0·lint0·test:unit 125·test:server 36·build OK. 브라우저 유지확인은 수동(인증 세션).
- **고객 상세 라우팅 딥링크 (#53 머지)**: 선택 고객을 React 상태(`selectedCustomerNo`/`customerDetailPanelOpen`)가 아니라 **URL이 single source of truth**로. 2커밋 — ①`lib/customer-route.ts` `customerCodeFromLocation(pathname,search)` 순수함수(+테스트 7): `/customer-detail/:code`→path code, `/customers?customer=`→쿼리값. ②`App.tsx` — 상태 2개 제거+버그폴백(`?? customers[0]`) 제거, `customersLoaded` 추가, `selectedCode`/`selectedCustomer`/`isDrawerOpen` URL 파생, 라우트 `/customer-detail/:code`(+`/customer-detail`→리다이렉트, 로딩/없음 처리), 핸들러 navigate화(드로어 열림=`?customer=` push, 재오픈=replace), ESC/백드롭=navigate. `CustomerDetailPage`/`CustomerManagementPage` 무변경(값 출처만). 보너스: 브라우저 뒤로가기 자연 동작. **검증**: typecheck 0·lint 0·test:unit 124·build OK. e2e는 인증 세션 필요라 새로고침/공유 **수동 확인**.
- **고객 상세 읽기 연결 (#51 머지)**: 김민준 상세 drawer를 실 DB로. 4커밋 — ①`lib/customers.ts` `CustomerDetailData`/`toCustomerDetail`(순수)/`fetchCustomerDetail` + `Customer.id`(uuid, 상세 fetch 키) + 단위테스트(toCustomerDetail/formatActivity, 미사용 `fetchCustomer` 제거). ②시드 `scripts/seed-customers.ts`에 김민준 풀세트 전용 블록(컬럼 update + 자식 delete→insert로 멱등 — tasks 4/memos 3/schedules 1/documents 2, needTrim·needColors·needTiming·needMemo·residence). ③`CustomerDetailPage.tsx` — `KimMinjunDetailContent`에 `detail` prop 추가, in-scope `useState` 초기값을 const 6개 제거 후 `detail` 파생으로 교체(statusValues/needs/purchaseFields[구매방식·출고시기]/schedules/checkItems+completed/customerMemos/documents), `KimMinjunDetailHeader` 텍스트 prop화. ④외곽 `CustomerDetailPage`가 `customer.id`로 자체 fetch(로딩/에러 게이팅, `key={customer.id}` 마운트), `.kim-detail-loading` CSS. **메모 createdAt은 `formatActivity`("26/05/14 13:18")** — 정렬은 HH:mm만 보는 `kimTimeLabelMinutes`와 호환. **검증**: typecheck 0·lint 0·test:unit 109·test:server 31·build OK·시드 멱등·`getCustomer` 데이터 형태 일치. **e2e**: `tools/customer-detail-screenshot.spec.ts` 헤더 assertion 정정했으나, #46 이후 목록/상세가 API(JWKS 인증)라 **헤드리스 e2e는 로그인 세션 필요 → 브라우저 시각 확인은 수동**(로그인 후 `bun run dev` 또는 배포본).
- **mc-master UX/성능 (#47~#50)**: ①고객상세 전체화면 헤더 중복 제거(#47, customer-detail은 CustomerDetailPage 자체 헤더 있어 App.tsx 공통헤더 숨김). ②브랜드/모델 전환 잔상 제거(#48 — brandId/modelId 변경 시 **렌더 중 캐시 동기 리셋**, React 'adjusting state when a prop changes' 패턴. useEffect 비동기 setState라 페인트 후 갱신→이전선택 한 프레임 잔상이었음. catalog-cache에 동기 getter getCachedTrims/TrimColors/OptionSummary). ③재진입 속도+트림뷰 브랜드 복원(#49 — brands도 catalog-cache 캐시+`useState` lazy init, `modelId→brandId` 역인덱스로 사이드바·isDomestic 정합. 재진입 시 brands만 미캐시라 블로킹이었음). ④앱 로드 시 카탈로그 프리워밍(#50 — App.tsx 마운트 시 `prefetchCatalog`로 brands+첫모델 미리). **한계**: 트림 URL 직접진입/새로고침은 역인덱스 miss→첫브랜드 폴백(brandId URL 미반영, 후속). 첫 로드 0ms는 불가(네트워크 baseline ~0.5s).
- **CRM 고객 읽기 DB 연결 (#46)**: CRM 도메인 첫 DB 연결. catalog와 동일 3계층 — `db/queries/customers.ts`(`listCustomers`+상담메모용 customer_tasks lateral join·`getCustomer` 자식 묶음) → `routes/customers.ts`(`GET /api/customers`·`/:id`, app.ts 마운트) → `lib/customers.ts`(`fetchCustomers`·DB→`Customer` adapter, `no`는 customerCode 파생, advisor "미배정" 폴백). `App.tsx`가 목업→API 로드(로딩/에러·selectedCustomer null 가드). 시드 `scripts/seed-customers.ts`(멱등, 목업 20명)·`seed:customers`. **범위 외(다음)**: 쓰기(상태/메모/니즈 PATCH)·advisor 이름(profiles)·정산·Topbar 전역검색·견적. 스펙 `ref/specs/2026-06-19-crm-customers-read-design.md`, 플랜 `ref/plans/2026-06-19-crm-customers-read.md`.
- **청소+안정성 (#43)**: unused export 제거(`getCachedTrims`·`wonText`, `modelHasCodes`는 export만) · AuthProvider `getSession().catch`+`getRoleClaim` try/catch(무한로딩 방지, claim 실패는 권한없음 폴백) · Topbar 아바타 실패상태를 boolean→실패URL 저장(재로그인 시 자동 재시도).
- **MCMasterPage 훅 분리 (#44)**: 550→434줄. `mc-master/useMcMasterCatalog(modelId)`(로딩/캐시/reload*) + `useMcMasterSelection`(선택/드래그). 동작보존(test:unit 99). 두 영역 걸친 핸들러(onDrop/bulkDelete/doMove)는 컴포넌트 유지.
- **catalog 라우트 분리 (#45)**: 238→18줄. `routes/catalog/{shared,models,trims,options}.ts`. sub-app 마운트 아님 — `register*(catalog)` 함수가 절대경로 그대로 등록(경로 100% 보존, 23라우트·프론트17 매칭 확인). shared=공통 스키마/`dbErrorMessage`/`run`. trim 본문은 `trimBody` 하나로 create=`.extend({modelId})`/patch=`.partial()`.

## 완료 (이전 세션, 전부 main 머지)

- **인증 통합 (#36)**: 백엔드 JWKS 게이트(top-level `user_role`, customer/무토큰 차단) + 프론트 카카오 로그인/AuthProvider/RequireAuth/`apiFetch` 토큰주입/role 고정(mock 역할탭 제거). `src/auth/*`·`src/middleware/auth.ts`·`src/app.ts`(createApp) · `client/src/lib/{supabase,api,auth}.ts`·`client/src/auth/*`.
- **CF 배포 정상화**: #37 SPA 라우팅(`functions/[[path]].ts`가 `/api`만 Hono, 나머지 정적+`_redirects`) · #38 prod VITE env(`.env.production` 커밋 + `wrangler.jsonc` vars `SUPABASE_URL`) · #39 onError 진단 · #40 연결 복원력.
- **DATABASE_URL → transaction pooler(6543)** (CF production+preview secret). CF Workers 동시 연결 한계 해결.
- **mc-master 연결 안정화 (#40)**: `apiFetch` GET 5xx 재시도(backoff+jitter, 쓰기 제외) + `MCMasterPage` loadError 성공시 리셋. 첫 로드 비결정적 500 복구(검증 6/6 배너0).
- **Cloudflare Hyperdrive 도입 (#41 #42)** — 동시 연결 **근본책 완료**. db를 요청 컨텍스트화(`createDbClient`/`getDefaultDb`+`dbMiddleware`가 `c.var.db` 주입, query는 `executor` 일원화). **핵심 교훈 2가지**: ①Workers는 요청 간 DB 소켓 재사용 불가 → Hyperdrive 경로는 **요청마다 생성 + `waitUntil(client.end())`**(메모이즈 ❌, 재사용 시 'Worker hung'). fallback(로컬/테스트)만 싱글톤. ②Hyperdrive origin은 **direct connection**(`db.<ref>.supabase.co:5432`, user `postgres`)이어야 함 — session pooler는 `pool_size 15` EMAXCONNSESSION로 막힘. binding `HYPERDRIVE`(id `a09fec47…`), 캐싱 disabled, `origin_connection_limit 20`. 검증: prod 25동시×6=150/150 200, hang0 500 0. 스펙 `ref/specs/2026-06-18-crm-hyperdrive-design.md`.
- **Topbar 프로필 실제화 (d2ae492)** — 우상단 아바타=카카오 `avatar_url`(AuthProvider가 세션 user_metadata에서 노출, http→https 정규화, 로드실패→기본아이콘), 이름=`full_name`(없으면 email). 권한은 기존 `roleTab`(user_role claim) 그대로. cjLogo 목업 제거. `client/src/auth/AuthProvider.tsx`(name·avatarUrl 추가)·`App.tsx`·`components/Topbar.tsx`.
- **mc-master 성능: 프런트 캐시+프리패치 (b6d7c52 384c997)** — prod 클릭 랙(요청별 연결+Hyperdrive 왕복, 로컬은 웜 싱글톤이라 빠름) 흡수. `client/src/pages/mc-master/catalog-cache.ts`(제네릭 `makeCache`: 모델·트림·색상·옵션요약, 30s 신선도 + inflight dedupe + 모델 이미지 워밍). 브랜드/모델 hover→프리패치, 재방문 즉시(SWR), 편집 후 `{force:true}` 갱신, 썸네일 `loading=lazy`. **Hyperdrive read 캐싱은 보류**(Caveats).

## Next

- ~~읽기 #51~~ · ~~딥링크 #53~~ · ~~쓰기 #1 #54~~ · ~~쓰기 #2 #55~~ · ~~전화번호 #56·#57~~ · ~~상세 로딩 perf~~ ✅ main 머지 · ~~서류 #3 #58~~ ✅ main 머지(위 "완료" 참고).
- **enum/lookup 도메인 정리 (별도 설계 사이클, 고객 쓰기와 시너지)** — crm 스키마는 현재 전부 text. 업무 어휘(진행상태 1/2차·유입경로·서류종류·할일분류 등)는 **lookup 테이블**(이사님이 배포 없이 수정), 기술 내부값(customer_type·*_mode·quote app/decision/entry_mode 등)은 **enum**. 종속(status_group→status)은 lookup이 자연. 쓰기 PATCH 검증이 도메인 물려받음.
- ~~견적 읽기 #4a~~ · ~~견적 쓰기 #4b~~ · ~~견적 생성 #4c-1 composer~~ ✅ (위 Current Focus). **#4c는 3슬라이스로 분해**(2026-06-21 합의): #4c-1 composer 추가 ✅ / **#4c-2 워크벤치 저장**(`saveQuoteFromWorkbench` Maybach mock→실 입력 pricing/options/colors/discounts/vehicle 매핑, **"워크벤치를 진짜 계산기로" 제품 결정 선행**) / **#4c-3 다중 시나리오 1~3 비교 + 대표 지정 UI**(현재 트리거 없음, #4b PATCH의 scenario를 N건+primary로 확장). 이후 **#4d 원본 업로드**(서류 Storage 재사용·file_* 영속).
- (선택) Topbar 전역검색 DB 연결, 정산 도메인, 목록 페이지네이션 서버사이드(현재 클라, 20명은 충분).
- (후속, 선택) **prepare:true 전환** — direct origin(5432)은 prepared statement 지원. 현재 두 경로 모두 `prepare:false`(6543 fallback 호환). plan 캐싱 perf 원하면 Hyperdrive 경로만 true.

## ⚠️ Caveats

- CF Pages env: VITE_* 공개값은 `.env.production`(커밋)으로 빌드 주입, 비밀(DATABASE_URL)은 CF secret. 환경변수는 `wrangler.jsonc`로 관리(대시보드는 secret만 가능).
- **DATABASE_URL secret 변경 후 재배포 필수** — 기존 배포는 옛 값. (단 Hyperdrive **origin** 변경은 `wrangler hyperdrive update <id>`로 server-side라 재배포 불요. binding id 불변.)
- **서류 업로드(#3) 규약**: 백엔드는 **신규 secret key**(`SUPABASE_SECRET_KEY`, `sb_secret_…` — publishable 체계의 짝, **legacy service_role 아님**)로 Storage admin. 키는 **백엔드 전용**(프론트 노출 금지, 프론트엔 signed URL만). private 버킷 `customer-documents`. secret key는 CF secret(prod·preview) 변경 후 재배포 필요. 새 서류 쓰기 경로도 `invalidateCustomerDetail` 필수(상세 캐시 불변식). signed URL TTL 60s(PDF iframe range 끊김 시 상향 검토).
- `src/db/client.ts`는 fallback 시 `process.env.DATABASE_URL` 읽음 — CF는 nodejs_compat로 process.env 채움. CF prod 정상 경로는 `c.env.HYPERDRIVE.connectionString`(요청별 생성).
- postgres-js `max:1`(tx 롤백 깸)·`fetch_types:false`(타입파싱 깸)는 **금지**(검증됨).
- **Hyperdrive 코드 불변식**: Hyperdrive 경로는 요청별 `createDbClient`+응답 후 `client.end()`. db를 모듈/요청 간 메모이즈하면 'Worker hung' 재발. 메모이즈는 fallback(getDefaultDb)만.
- CF prod 배포는 엣지 롤아웃에 ~1분 — 머지 직후 e2e는 옛/새 isolate 혼재로 일부 실패할 수 있음. `/api/health`의 `hyperdrive:true` 일관 확인 후 재검증.
- `DATABASE_URL`(fallback, 6543 transaction pooler)은 Hyperdrive origin(direct 5432)과 **별개**. 둘 다 유지.
- role 변경은 토큰 갱신(~1h)/재로그인까지 반영 안 됨(claim 방식).
- **mc-master는 프런트 캐시(`catalog-cache.ts`) 경유** — query를 직접 호출 말고 `fetchModelsCached`/`fetchTrimsCached`/`fetchTrimColorsCached`/`fetchOptionSummaryCached` 사용. 편집 후 목록 갱신은 `{force:true}`(reload* 함수) 필수 — 안 그러면 30s 동안 옛 데이터.
- **Hyperdrive read 캐싱 보류 결정**: `/api/catalog/*`는 읽기+쓰기 공용이라 켜면 편집 직후 stale + 프런트 force-refetch 무력화. 캐싱은 읽기 전용 `/api/vehicles/*`(고객 견적)에만, 필요 시 별도 cached 바인딩+PR.
- **고객 상세 캐시 불변식**: `lib/customers.ts` `detailCache`(프론트, TTL 60s)는 쓰기 성공 시 반드시 `invalidateCustomerDetail(customerId)`로 버려야 함. **새 쓰기 경로(서류#3·견적#4·advisor#5 등)를 추가하면 그 lib 함수에도 invalidate 호출 필수** — 안 그러면 재진입 시 stale.
- **전화번호 저장 규약**: DB=**숫자만 11자리**(`010`+8). 표시는 `lib.formatPhone`, 입력은 010 고정 prefix+뒤 8자리(`KimPhoneStatusInput`). 새 phone 입출력은 이 규약 따를 것.
- **커밋 메시지에 skip-ci 마커 금지**: feature 브랜치 커밋(spec/plan/brief 포함)에 그 토큰을 쓰면 squash 본문에 합쳐져 **CF 배포가 스킵**됨(2026-06-19 #51·#53서 2회 사고). 그 토큰은 머지와 무관한 main 직접 docs 커밋에만. 보정=마커 없는 빈 커밋 트리거 or CF 대시보드 수동 빌드.

## Verification (2026-06-19)

- (2026-06-20 서류 #3 + 후속 #68·#69) `typecheck` 0 · `lint` 0 · `test:unit` **140** · `test:server` **47**(`bun test --env-file=.env.local`; #68서 오피스 415 +1) · `build` OK(pdf-lib 동적 import 코드스플릿 확인). **브라우저 검증 완료(배포본)**: 업로드·미리보기·다운로드·**이미지/PDF 병합 다운로드(`김민준-서류.pdf`)·분류 변경 재진입 유지** 전부 정상. secret key·버킷 등록 완료.
- 전화번호: prod 20행 전부 숫자 11자리 확인. 상세 로딩: 재진입 캐시 hit 즉시·쓰기 후 무효화 확인(사용자 체감 "빨라짐").
- (이전) `test:unit` 99 · `test:server` 28 기준 항목은 아래 유지.
- crm.mrcha.app: 카카오 로그인 화면 · SPA 라우팅(`/`,`/quotes` 200) · `/api` 게이트(401) · **mc-master 첫 로드 6/6 정상**.
- **Hyperdrive(#41 #42)**: prod `/api/catalog/models` **25동시×6=150/150 200**(hang 0, 500 0), mc-master 첫 로드 6엔드포인트 동시 전부 200. `/api/health` → `hyperdrive:true`.
- **Topbar·mc-master 성능(d2ae492 b6d7c52 384c997)**: `typecheck`/`lint`/`build`/`test:unit` 99 통과. 체감은 배포 후 확인.

## Collaboration

- (Codex 세션) User=이사님, assistant=영실. (Claude Code 세션) 호칭은 CLAUDE.md 팀 구성(송실장/유슨생).
- 판단 질문(`어때`/`괜찮을까`/`너 생각은?`): 추천·트레이드오프 먼저 + `적용할까요?`. 실행어(`응`/`해줘`/`진행해`): 즉시 실행.
- 팀 공유 결정·맥락은 git(`ref/*.md`, `AGENTS.md`)에. 로컬 메모리는 공유 안 됨.
