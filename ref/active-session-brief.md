# Mr. Cha CRM Active Session Brief

> **자동 로드 · 60줄 이하 유지**(AGENTS.md). 과거 로그 = `ref/session-archive.md` · 지속 결정 = `AGENTS.md` · 설계 = `ref/specs/*` · 장기 상태 = `ref/current-working-state.md`.

Last updated: 2026-08-04

## 지금 상태

main 전량 green(CI 8단계). unit **1371** · pure **281**. 회원탈퇴 CRM 몫·파기 감사 모두 종결.
출고/정산 축은 **이사님 회신이 전부 도착**해 미결정 0 — 계약·화면 모두 구현 완료(잔여 = 목록 컬럼 1건).

## ▶ 다음 (집에서 이어서)

- **정산 축 잔여 = 목록 컬럼 1건**(`#446`·`#447`로 계약·화면은 끝났다). 정산 탭의 비용/마진/상태를
  보이려면 **목록 응답에 정산을 실어야 하는데 "목록 미포함" 원칙과 충돌**한다(exposure tripwire가
  잠근다) → **admin 전용 목록 조회 설계가 선행**. 실 데이터 1행이라 급하지 않다.
- ✅ **정산 화면 육안 확인 완료**(08-05) — `#448` 팝오버 3연쇄 수정 포함. ⚠️ **자기 스크롤에
  자기가 닫히던 버그** = `usePopoverViewportClose`에 selfRef(가변 팝오버 신설 시 반드시 넘길 것).
- **잔여 2건(둘 다 머지는 끝났다 — 실행·육안만 남음)**: ⓐ**김지운(`jiandolce@gmail.com`) 탈퇴
  테스트 미실행**(견적 20·**앱카드 19**라 카드 회수 경로 검증 가치가 크다) ⓑ`#445` 배너 레이아웃
  **육안 미확인** — 대기 큐가 0건이라 배너가 안 뜬다. **ⓐ를 하면 ⓑ가 딸려 온다**(탈퇴 접수 =
  큐 1건 → 배너 노출).
- ✅ **이월(07-31) 실기 4건 전량 종결**(08-05 브라우저): `#410` 레이아웃(페이지 스크롤 0·타이틀
  부동) · **팀장 폼 할인 부재**+admin 유지 · `#404` 팀장 개방(진입점·내 요청·저장 **"승인 요청"**) ·
  **교차 세션 실시간**(A 무터치로 3→6→7 — 마침 이주선 팀장 실업무 요청이 겹쳐 **타 사용자
  broadcast까지 실증**됐다). ⚠️ `#404` **미관 항목**(칩 3색·중복 적재·구분선)만 미확인.

## 직전 세션 (08-04 · 유슨생)

- **`#442` 업무 AI 파기 로직 감사** → 상 0·중 1·하 3·행위 변경 0. 파기 로직 결함 **0건**.
  고친 중 1건 = 경계 테스트가 "하루 늦게 지우는" 변이를 통과시켰다(픽스처를 30일 ±1시간으로 좁힘 —
  **그 간격을 다시 벌리지 말 것**). 판정 SSOT = `ref/plans/2026-08-04-crm-purge-logic-audit.md`.
- **🔴 김민준(`CU-2605-0020`) 삭제됨** — 회원탈퇴 실경로 테스트. 전량 파기·복구 불가. **유일한
  풀세트 시드**였다(견적 4·서류 4·메모 3 + Storage 6객체). 전 단계 실측 정상. **시범 고객·시드
  재지정이 선행**(AGENTS.md 반영).
- **`#446`·`#447` 정산 비용·단계**(이사님 확정 — 계약 + 화면): 비용 5종(썬팅·블랙박스·탁송·
  페이백·직접입력) **jsonb 배열**(마이그 **0052**) · **페이백도 비용으로 차감**(양수 저장) ·
  단계 3종 · **마진은 파생**(미저장, 계산 불가 시 "—"). ⚠️ **권한 충돌 해소** — 정산은 admin
  단독인데 "정산요청"만 담당자 행위라 `POST .../settlement/request` **한 경로만** 열고
  **미정산→정산요청 전이 하나로** 좁혔다(금액은 응답에도 없음). 담당자는 정산을 못 읽어
  **버튼 활성 여부를 알 수 없으므로 항상 눌리고 서버가 409로 판단**한다.
- **`#443`·`#444` 의존성** 패치 6 + 마이너 8. 잔여 = **major 2건 보류**(TS 7 = eslint peer `<6.1.0` → lint 사망 · `@types/node` = bun이라 실익 0).
- **`#445` 탈퇴 배너** — 원인은 배너가 아니라 부모(2열 grid 칸 침범). rail 밖 전체 폭으로 이동.
- **이사님 확정 박제**: 히어로 대수 표기 = **바 목록 유지**(호버 툴팁 제안은 현물 보고 닫힘 —
  다시 올라오면 "이미 보고 결정") · 금융리스/일시불 = 실적 없음(구현 변경 0).

## 대기

**이사님** = `ref/director-pending-confirmations.md` **16건**(항목 14·16~30) + Supabase
**"EXCEEDING USAGE LIMITS" 배지**(08-01) 플랜 + **D+5 최악 6일차** SLA 해석.
💡 회신 때 **🟡 하드 삭제 확장(`#440`)을 항목 28**(AI 기록에 번호 잔존)**과 묶을 것** — 같은 축·미등재.

## Boot

`AGENTS.md` → 이 파일 → `git status -sb` · `git log --oneline -5`. 상세: 정산 = spec §6a · 보존 계약 = AGENTS.md.

## 세션 마무리 규칙

**교체**(누적 금지 · 이전 요약은 `ref/session-archive.md` 맨 위로). 행위 변경은
`ref/director-pending-confirmations.md` 등재(**유슨생 현장 승인은 등재 없이 박제** · 이사님 확정
설계를 뒤집는 건만 등재 · 신설 시 그 파일 롤업 2곳도). 상세 규칙 = AGENTS.md "Handoff Documents".
