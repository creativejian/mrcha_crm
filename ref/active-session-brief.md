# Mr. Cha CRM Active Session Brief

> **자동 로드 · 60줄 이하 유지**(AGENTS.md). 과거 로그 = `ref/session-archive.md` · 지속 결정 = `AGENTS.md` · 설계 = `ref/specs/*` · 장기 상태 = `ref/current-working-state.md`.

Last updated: 2026-08-04

## 지금 상태

main 전량 green(CI 8단계). unit **1360** · pure **281**. 회원탈퇴 CRM 몫·파기 감사 모두 종결.
출고/정산 축은 **이사님 회신이 전부 도착**해 미결정 0 — 데이터 계약까지 구현했고 **화면만 남았다**.

## ▶ 다음 (집에서 이어서)

- 🔴 **정산 2단계 = 화면**(`#446`의 후속). ①정산 섹션 비용 항목 입력(추가·삭제·**직접입력 라벨**)
  ②출고 정보 팝오버에 **"정산 요청" 버튼**(담당자용 — `POST /:id/settlement/request`, 미정산일 때만
  활성) ③정산 탭 목록의 비용/마진/상태 컬럼 실데이터화. 계약·산식·권한은 전부 확정됐다
  (spec §6a) — **화면만 얹으면 된다**. ⚠️ 실 DB `customer_deliveries` **1행·확정일 0건**이라
  화면 확인은 Ladle이나 수기 입력이 필요하다.
- **미머지 확인 2건**: ⓐ`#445` 배너 레이아웃은 **육안 미확인**(대기 큐 0건이라 안 뜬다 — 김지운
  삭제하면 뜬다) ⓑ**김지운(`jiandolce@gmail.com`) 탈퇴 테스트 미실행**(견적 20·**앱카드 19**라
  카드 회수 경로 검증 가치가 큼).
- **이월(07-31)**: 실기 4건 — `#410` 레이아웃 · 팀장 폼 할인 · 교차 세션 실시간 · `#404` 체크리스트.

## 직전 세션 (08-04 · 유슨생)

- **`#442` 업무 AI 파기 로직 감사** → 상 0·중 1·하 3·행위 변경 0. 파기 로직 결함 **0건**.
  고친 중 1건 = 경계 테스트가 "하루 늦게 지우는" 변이를 통과시켰다(픽스처를 30일 ±1시간으로 좁힘 —
  **그 간격을 다시 벌리지 말 것**). 판정 SSOT = `ref/plans/2026-08-04-crm-purge-logic-audit.md`.
- **🔴 김민준(`CU-2605-0020`) 삭제됨** — 회원탈퇴 실경로 테스트(앱 접수 → 확정 실행). 전량 파기·
  복구 불가. **유일한 풀세트 시드**였다(견적 4·서류 4·메모 3 + Storage 6객체). 전 단계 실측 정상
  (감사행 익명·임베딩 0·업무 AI 244행 유지). **시범 고객·시드 재지정이 선행**(AGENTS.md 반영).
- **`#446` 정산 비용·단계 계약**(이사님 확정): 비용 5종(썬팅·블랙박스·탁송·페이백·직접입력)
  **jsonb 배열**(마이그 **0052**) · **페이백도 비용으로 차감**(양수 저장) · 단계 3종.
  ⚠️ **권한 충돌 해소** — 정산은 admin 단독인데 "정산요청"만 담당자 행위라, `POST .../settlement/request`
  **한 경로만** 열고 **미정산→정산요청 전이 하나로** 좁혔다(금액은 응답에도 없음).
- **`#443`·`#444` 의존성** 패치 6 + 마이너 8. 남은 outdated = **major 2건 보류**(TS 7 = eslint peer
  `<6.1.0` → lint 사망 · `@types/node` = bun이라 실익 0). 릴리스 노트 대조 = `#444` 코멘트.
- **`#445` 탈퇴 배너 레이아웃** — 원인은 배너가 아니라 부모(2열 grid의 칸을 뺏고 있었다). rail 밖
  전체 폭으로 이동.
- **이사님 확정 박제**: 히어로 대수 표기 = **바 목록 유지**(호버 툴팁 제안은 현물 보고 닫힘 —
  다시 올라오면 "이미 보고 결정") · 금융리스/일시불 = 실적 없음(구현 변경 0).

## 대기

**이사님** = `ref/director-pending-confirmations.md` **16건**(항목 14·16~30) + Supabase
**"EXCEEDING USAGE LIMITS" 배지**(08-01) 플랜 + **D+5 최악 6일차** SLA 해석.
💡 회신 때 **🟡 하드 삭제 확장(`#440`)을 항목 28**(AI 기록에 번호 잔존)**과 묶을 것** — 같은 축·미등재.

## Boot

`AGENTS.md` → 이 파일 → `git status -sb` · `git log --oneline -5`. 상세: 정산 = spec §6a + `#446` 본문 ·
업무 AI 보존 계약 = AGENTS.md.

## 세션 마무리 규칙

**교체**(누적 금지 · 이전 요약은 `ref/session-archive.md` 맨 위로). 행위 변경은
`ref/director-pending-confirmations.md` 등재(**유슨생 현장 승인은 등재 없이 박제** · 이사님 확정
설계를 뒤집는 건만 등재 · 신설 시 그 파일 롤업 2곳도). 상세 규칙 = AGENTS.md "Handoff Documents".
