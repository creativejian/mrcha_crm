# Mr. Cha CRM Active Session Brief

> **자동 로드 · 60줄 이하 유지**(AGENTS.md). 과거 로그 = `ref/session-archive.md` · 지속 결정 = `AGENTS.md` · 설계 = `ref/specs/*` · 장기 상태 = `ref/current-working-state.md`.

Last updated: 2026-08-05

## 지금 상태

main 전량 green(CI 8단계). unit **1395** · pure **290**. 회원탈퇴 CRM 몫·파기 감사 모두 종결.
**정산 축은 워크플로·목록 모두 종결**(`#449` 단계 편집 · `#450` 목록 컬럼) — 남은 건 이사님 확인 2건뿐.

## ▶ 다음

- 🔴 **바로 착수 = `CustomerManagementRow` 팝오버 분리**(951줄 · 유슨생 지시 2026-08-05, 새 세션에서).
  `#450`에 흡수시키려 했으나 그 PR이 서버 계약 변경까지 겹쳐 17파일 471줄이 돼 **빼고 냈다**
  (동작 변경과 파일 이동이 한 diff에 섞이면 리뷰가 불가) → **단독 PR이 유일한 경로**.
  뽑을 단위 = **`DeliveryInfoPopover` 통째로**(출고 8필드+정산+비용행+요청버튼 ~230줄). 정산만
  떼면 안 된다 — 출고와 **한 저장 경로**를 공유한다(입금일·실입금액·비용이 같은 PUT에 실린다).
  ⚠️ `usePopoverViewportClose`의 **selfRef 인자 유지 필수** — 빠뜨리면 팝오버가 자기 스크롤에
  자기를 닫는다(`#448` 실버그). **목표는 동작 변경 0** — 순수 이동이라 unit 1395가 그대로 통과해야
  한다. 근거·트리거 이력 = 로컬 메모리 `crm-row-popover-split-trigger`.
- **잔여 2건(머지는 끝났다 — 실행·육안만)**: ⓐ**김지운(`jiandolce@gmail.com`) 탈퇴 테스트 미실행**
  (견적 20·**앱카드 19** — 카드 회수 경로 검증) ⓑ`#445` 배너 **육안 미확인**(대기 큐 0건이라 안
  뜬다). **ⓐ를 하면 ⓑ가 딸려 온다**(탈퇴 접수 = 큐 1건 → 배너 노출).
- **🔴 시범 고객·풀세트 시드 재지정**(상세 드로어 작업의 선행 — 김민준 삭제로 공백). 어느 고객으로
  옮길지는 **이사님 결정 영역**이라 임의로 정하지 않는다(AGENTS.md).

## 직전 세션 (08-05 · 유슨생)

- **`#450` 정산 목록 컬럼 실데이터화** — 이 탭은 **도입 이래 실 데이터에서 늘 빈 목록**이었다
  (필터가 목업 전용 필드를 봐 전 행 탈락). ⚠️ **"admin 전용 목록 조회 설계 선행"은 오판**이었다 —
  tripwire 주석의 "필드 단위 마스킹 선례 없음"이 **거짓**이고 `src/lib/dealer-visibility.ts`(07-27)가
  이미 같은 패턴이라 **그 미러링**으로 끝났다(틀린 주석 2곳 정정). 층 = 필터:쿼리 WHERE /
  가시성:lib. tripwire는 **쿼리 → 라우트 레벨로 이전**(쿼리가 이제 정산을 항상 뽑는다).
  곁가지 = 목업 `delivery` 누락 · 비용 빈 배열 0 단정 → "—" · **달력 2번 클릭 버그**(플래그만
  내리고 실제로 안 닫음 — 기존 테스트가 호출 횟수만 봐 통과하며 살아 있었다) ·
  **날짜 표시 SSOT** `formatDateDisplay`(표시=슬래시 / 저장=하이픈).
- **`#449` 정산 단계 편집 UI** — 담당자 "정산요청"을 admin이 받는 화면(팝오버 정산 섹션 최상단
  select 3종 · Safari `bindSelect` 실기 확인). ⚠️ **status는 조회 이후 바뀐 경우에만 body에 싣는다**
  — 항상 보내면 팝오버를 열어 둔 사이 올라온 요청을 저장 한 번으로 "미정산"으로 되돌린다(patch가
  부분인 이유). 그물 2종 모두 변이 주입으로 확인.
- **🔴 김민준(`CU-2605-0020`) 삭제됨**(탈퇴 실경로 테스트·복구 불가) — 유일한 풀세트 시드였다.
- 08-04 오후 세션(`#442`·`#443`·`#444`·`#446`·`#447`·`#448`·이월 실기 4건)은 archive 맨 위로.

## 대기

**이사님** = `ref/director-pending-confirmations.md` **18건**(항목 14·16~32 — **31·32 신설**:
정산 목록 대상 기준=계약 확정일 / 열 이름 "실입금액". 같은 화면이라 **묶어서** 여쭐 것) + Supabase
**"EXCEEDING USAGE LIMITS" 배지**(08-01) 플랜 + **D+5 최악 6일차** SLA 해석.
💡 회신 때 **🟡 하드 삭제 확장(`#440`)을 항목 28**(AI 기록에 번호 잔존)**과 묶을 것** — 같은 축·미등재.

## Boot

`AGENTS.md` → 이 파일 → `git status -sb` · `git log --oneline -5`. 상세: 정산 = spec §6a · 보존 계약 = AGENTS.md.

## 세션 마무리 규칙

**교체**(누적 금지 · 이전 요약은 `ref/session-archive.md` 맨 위로). 행위 변경은
`ref/director-pending-confirmations.md` 등재(**유슨생 현장 승인은 등재 없이 박제** · 이사님 확정
설계를 뒤집는 건만 등재 · 신설 시 그 파일 롤업 2곳도). 상세 규칙 = AGENTS.md "Handoff Documents".
