# Mr. Cha CRM Active Session Brief

> **자동 로드 · 60줄 이하 유지**(AGENTS.md). 과거 로그 = `ref/session-archive.md` · 지속 결정 = `AGENTS.md` · 설계 = `ref/specs/*` · 장기 상태 = `ref/current-working-state.md`.

Last updated: 2026-07-31 (오전)

## 지금 상태

**main 전량 green · 브랜치 0.** MC 마스터 변경 승인 **3부작(`#399`·`#402`·`#404`) + 실기 후속
7건(`#405`~`#411`) 전부 머지·배포.** unit **1267**. 실기(유슨생)가 진행되며 후속이 즉시
반영된 하루 — 아래 요약이 그 목록.

## 직전 세션 요약 (07-31 오전 · 유슨생 — PR 3 실기 + 후속 6건)

- **`#405`**: 착지 플래시가 sticky 트림명 셀을 반투명화해 뒤 열이 비치던 문제(불투명 등가색
  키프레임) + 승인/반려/취소를 고객 목록 칩(`.badge`) 버튼으로.
- **`#406`**: **프리필** — 재제출은 교체(spec §6.1)인데 폼이 원값에서 시작해 나눠 저장하면 앞
  변경분이 소실 → 내 pending payload를 폼에 겹침(+안내 문구, 옵션 에디터 포함) · 헤더 (N)
  모듈 캐시(재마운트 즉시 표시 — F5는 물리 한계) · 헤더 도구 묶음 우측 고정(`va-head-tools`) ·
  **교차 세션 실시간**(`crm-catalog-change-queue` broadcast — typing 채널 매니저 미러, 승인↔
  적재/취소가 리로딩 없이 상호 반영. 신호 유실 그물 = focus·폴링 유지).
- **`#407`**: **내 요청 자동 소멸**(삭제 버튼 기각 — 유슨생 방향 확정): pending 상시 · rejected는
  같은 대상 재요청 시 즉시 숨김+7일 창 · approved 24시간 · canceled 숨김. 순수
  `filterMyRequestVisible`(클라 표시 필터 — 행은 감사 기록으로 유지, `decidedAt` 타입 추가).
- **`#408`**: **팀장 제안에서 할인 3필드 제외**(spec §3.1 정정 박제) — 확정 할인은 딜러 제안→
  관리자 채택 체계 소유. 서버 strip(fail-closed) + 팀장 폼 숨김. 잡힌 실결함: 폼 오픈 시점 구
  할인값이 승인 replay로 채택값을 되돌리는 사고·채택발 드리프트 409 간섭.
- **`#409`**: 팝오버 내부 스크롤 경계 체이닝 차단(`overscroll-behavior: contain` — 딜러 팝오버 포함).
- **`#410`**: **mc-master 뷰포트 맞춤** — 표 스크롤러 근사 상수(210px)와 실제 크롬 차이만큼
  페이지가 넘쳐 표 끝에서 타이틀이 밀리던 문제. `.main:has(.va-card)` flex-fit으로 페이지 스크롤
  제거(타이틀·헤더·브랜드 열 고정, 근사 폐기). ⚠️ **CSS 레이아웃 변경 — 실기 미확인.**
- **`#411`**: 반려 사유 입력 즉시 포커스(autoFocus — 클릭 전엔 입력칸인지 안 읽혔다) + placeholder
  글자를 확인/취소 칩과 같은 11px로(높이·radius도 칩 정합).
- 설계 문답 박제: 반려 사유 **필수 유지**(비동기 피드백 유일 채널 — 귀찮아지면 프리셋 칩으로
  완화) · 두 매니저 충돌 = 배지 예방선 → 저장 409(다른 kind는 공존, 의도) · 프리필 에코 보강은
  과하다고 판단해 철회(sub-second 창, 실기에서 밟히면 재론).

## ▶ 다음 — 잔여 실기 확인 (유슨생)

① **`#410` 레이아웃**(admin·팀장·딜러 전폭 각각 — 타이틀 부동·브랜드 열 자체 스크롤·낮은 창
높이·착지 hl) ② 팀장 폼 할인 섹션 부재 + admin 폼 유지 ③ 교차 세션 실시간(브라우저 2개 —
승인↔취소 즉시 반영) ④ PR `#404` 본문 체크리스트 잔여(칩 3색·중복 적재 모습·팀장 구분선 미관
등). 매니저 계정 = 상담사테스트(role=manager, magiclink 절차 = dealer-magiclink-smoke 메모리).

## 후속 후보 (비긴급)

spec 부록 B(딜러 제안 배지 합산·staff 개방 재론·대기열 알림) · `mine=1` 50건 창(오래된 pending
밀림 — 서버 몫) · 반려 사유 프리셋 칩 · 팝오버 재클릭 안 닫힘·`/mc-master` 라우트 무가드(선재).

## 대기

**이사님** = 기존 항목 그대로(07-29 브리프 → 아카이브).

## Boot

`AGENTS.md` → 이 파일 → `git status --short --branch` · `git log --oneline -5`. 승인 워크플로
상세 = `ref/specs/2026-07-30-crm-catalog-change-approval-design.md`(§3.1 정정 포함) · plans pr1~3.

## 세션 마무리 규칙

이 파일은 **교체**(누적 금지, 이전 요약은 `ref/session-archive.md` 맨 위로). 행위 변경은
`ref/director-pending-confirmations.md` 등재(**유슨생 현장 승인 시 등재 없이 박제** · 이사님 확정 설계를
뒤집는 건은 등재 · **신설 시 그 파일 롤업 2곳도 함께 갱신**).
