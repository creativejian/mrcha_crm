# Mr. Cha CRM Active Session Brief

Last updated: 2026-07-15 (**세션 0715-rest-refactoring(유슨생): ①배치 5 보류 3건 해소(#247) ②출고 관리 헤더 잘림 픽스(#248) ③고객관리 5 mode 콘솔 레이아웃 통일(#249) — 전부 squash 머지·prod Active.**
**#249 `8b7683d` 고객관리 5 mode(consulting·contract·delivery·settlement·hold) 콘솔 레이아웃 통일** — 전체 보기(all)만 콘솔 문법이던 것을 전 mode로. brainstorm(Visual Companion 목업)→spec(`ref/specs/2026-07-15-crm-console-layout-unify-design.md`)→plan(`ref/plans/2026-07-15-crm-console-layout-unify.md`)→인라인 TDD 실행. **핵심 SSOT**: `isConsole=mode==="all"` 레이아웃 게이트를 `isAllMode` 필터 세트 선택자로 축소 — 레이아웃 클래스(page/card/rail/toolbar/headbar/table/pagination) 전 mode 무조건 콘솔, mode 차이는 필터 항목 한 곳(all=계약가능성·관리상태 / 나머지=뷰 select 3개)에만. 컨트롤 rail JSX 두 벌→한 벌 수렴, 구식 마크업(검색 input·select 3벌·TOTAL·뷰 네이티브) 삭제, **신규 CSS 0벌**(customer-console-* 재사용). **결정**: 1줄 배치(목업 A)·카운트 "전체 N명"·죽은 뷰 select 3개 유지(renderConsoleFilter `includeAllOption` 일반화로 pill 흡수, 동작은 mock)·클래스 이름 유지. **부수 해소**: 담당자 변경 팝오버가 비콘솔에서 깨지던 실버그(`.advisor-change-confirm`이 `.customer-console-headbar` 하위 스코프, #216 콘솔 전제) 자동 해소. **실기 중 발견·동반 픽스**: contract mode 헤더 담당↔상담메모 순서 뒤바뀜(heads/tableColumns가 renderRow priority→advisor 순과 반대, 프로토타입 이래 기존 버그·통일 무관) → consulting/hold와 정합 swap + 순서 회귀 테스트. 검증: typecheck 0·lint 0·unit 653+순서테스트·build·knip 회귀 0. 유슨생 실기(계약 관리 헤더 확인·전 mode) 후 머지.
**#248 `5cdfa45` 출고 관리(delivery) 헤더 우측 잘림** — renderRow fallthrough가 헤더에 없는 priority 셀을 그려 table-layout:fixed에서 actions가 colgroup 밖으로 밀림(프로토타입 이래, delivery 필터에 실 고객 생겨 발현). `tableColumns.includes("action")` 조건화로 해소, 전 mode 헤더=바디 정합 테스트.
**#247 `685acc8` 배치 5 보류 3건 전량 해소 — 배치 5(#238~#247) 보류 0 종결:**
①**3-C** `resolveUpdateBadge` 반환에 **`displayInfo` 제3 필드 합성**(`info ?? 유효 수동 시 manualUpdateInfo`) — 보류 근거였던 충돌은 info 필드에 합칠 때만 성립(info = 응답 SLA `firstResponseDisplay` 입력·#240 계약 테스트), 제3 필드라 둘 다 불변·셀의 `customer` 이중 읽기만 해소(TDD 5, RED 실관찰) ②**4-A** `.console-table` SSOT 자급자족 = **`:where(.console-table)` 스켈레톤(특이성 0,0,1)** — 전역 th,td와 동일 값 명시, 지금은 뒤 import인 전역이 동률에서 이겨 계산값 불변·전역 소멸 시에만 이어받음. **⚠️함정 실측: (0,1,1) 흡수는 전역을 이기던 셀 클래스 6종(.select/.operation/.text-block/.chance/.final-update/.actions-cell — 전부 0,1,0)을 역전시켜 회귀 — 금지 주석 박제** ③**4-B** 무력 `table-scroll` 클래스 콘솔 래퍼 2곳 제거(같은 특이성·후순위 overflow:hidden이 항상 이김 — 계산값 불변). 사실 정정: hidden은 #238이 아니라 콘솔 원설계부터. 증명 = 실 DOM 구조 하네스(조상 체인+셀 오버라이드+비콘솔 대조군) main↔브랜치 빌드 CSS computed 대조 **25요소×348속성 diff 0** + **변이 검증**(전역 3규칙 제거 → 콘솔 골격 유지·대조군 상실 = 스켈레톤 live). **기각 박제 2건(재제안 금지, plan 표)**: 4-A 원안 전역 th,td 스코프화(bare 테이블 9곳 = 전부 프로토타입 페이지, 검증 비용 > [하] 실익) · 4-B 옵션 2 overflow-x:auto 스크롤 복원(콘솔 원설계·리포트 0 — "좁은 뷰포트 지원" 제품 판단 선행, 요구 시 별도 슬라이스). plan SSOT = `ref/plans/2026-07-14-crm-refactor-batch-5.md`. 검증: typecheck 0·lint 0·unit 636(+5)·build·knip main 동일. 그 외 대기 — 이사님 사후 공유(pending 항목 9)/FCM 실기기 e2e/역할 매트릭스 2단계/#582 크로스 실기.**)

이전: 2026-07-14 저녁 (**세션 0714-quote-jeffAPI(유슨생): 계산엔진(솔루션 조회) 완결 — PR #241 squash 머지 `632da0e`.**
**견적 워크벤치 계산기 버튼 = 파트너(제프 financial-dolim-solution) 계산 API 실연동**(자체 엔진 아님 — 원래 "이사님 브레인스토밍 선행"이었으나 유슨생 주도 확정, **사후 공유 = `ref/director-pending-confirmations.md` 항목 9**). 스펙(계약 SSOT+개정 1·2) `ref/specs/2026-07-14-crm-solution-quote-integration-design.md` · 플랜 `ref/plans/2026-07-14-crm-solution-quote-integration.md`. 구성: ①클라 순수 계층 `solution-quote.ts`(어휘 SSOT 8사·매퍼 %상한·방어 파서)+`lease-rate.ts`(**앱 리스계산기 RATE 솔버 원본 이식 — 500벡터 bit-identical**)+`solution-ranking.ts`(제프 sortQuotes 미러) ②서버 릴레이 `POST /api/solution/calculate`(zod SSOT 파생+컴파일 파리티·X-API-Key 조건부·X-Request-ID·8s·바디스톨 504/502·401/403 AUTH_FAILED 503) ③마이그 0031 실 DB 적용(시나리오 스냅샷 4컬럼: lender/워크북 버전/시각/raw) ④워크벤치 — 계산기 3분기(**미선택→금융사 랭킹 모달**(전사 병렬 조회·정렬 4종·뱃지 동률 복수·차액·로고 8종 이식·미취급 조용히 제외·에러성 전사 실패는 사유 표면화)/지원사→바로/미지원사→경고)·**결과 4필드(반납/인수/출고 전/금리) 읽기 전용 파생**(반납=월납입×기간+선수금/인수=+잔가/출고 전=보증금+선수금+기타비용/**금리=실질 IRR — 표면금리 아님**)·스냅샷 생명주기(저장 spread/재진입 max 잔가 raw 재시드/리셋+isConnected 잔상 가드).
**⚠️ 운영 전제**: ①✅ **external 전환까지 완결(2026-07-14 밤)** — 제프 external calculate 배포(`0bc7dda`) 후 CRM이 `https://mc.mrcha.app/api/external/quotes/calculate` + `PARTNER_QUOTE_API_KEY`(앱 공유 키)로 전환. CF Pages Production secrets 2종 등록·재배포(`e812de3`)·prod 200 실검증. 스펙 §10 전량 종결(자동차세 = 운용리스 별도·렌트 내장·토글 표시용 확정). Preview 미등록 — 의도 ②env 이름 = 앱 Supabase Edge와 통일(`PARTNER_QUOTE_API_*` — 키는 앱과 같은 공유 키, external 전환 시에만 필수) ③`.env.local` URL은 **내부 calculate**(external calculate는 404 미존재 — cheapest만 있음. cheapest URL을 넣으면 랭킹 모달 전사 실패 — 실사고로 fail-loud 표면화 픽스됨) ④제프 전달 목록 = 스펙 §10(external calculate 신설·자동차세 의미·키 공유 고지·X-Request-ID `crm-<uuid>` — 응답 확장은 개정으로 불필요) ⑤월납입 채움 = 표시 라운딩(운용리스 100원 올림, 원값은 solution_raw).
검증: typecheck 0·lint 0·unit 607·server 538·build·knip 신규 0 + 격리 스택 스모크 2회(파트너 prod 실계산 — **월납입 파리티 정확 일치**·랭킹 6사·행 클릭 채움·파생 정합·재진입 보존·잔재 0). 실행 = subagent-driven(태스크별 2단계 리뷰+최종 통합 리뷰). 그 외 대기 — FCM 실기기 e2e/역할 매트릭스 2단계/#582 크로스 실기(이사님 협조).**)

이전: 2026-07-14 (**세션 0714(유슨생): 3PR — 콘솔 테이블 SSOT(#238)·앱 채팅 딥링크(#239)·수동 관리 상태 신규 표시(#240, B2 기각 번복).**
①**#238 `c7f4f37` 앱 견적요청 테이블을 고객관리 콘솔 문법으로 통일** — 공용 `.console-table(-scroll)` SSOT 신설(controls.css). 원인 = `.app-requests-page` 자체 패딩이 `.main` 22px 프레임과 이중 + 래퍼 서피스 부재 + 스코프 없는 전역 `th` 룰(회색 바닥) 상속. 서피스·톤 계층만 공유 — 고객 테이블 고유 인터랙션(hover·detail-open)과 견적요청 고유 규칙(컬럼 폭·vertical-align)은 각자 잔류(행 hover는 클릭 의미론이라 의도적 비공유). 격리 스택 1440px 계산값 기계 대조 전부 동일·콘솔 회귀 0.
②**#239 `56362c8` 고객 상세 앱 채팅 버튼 실동작** — 상담경로 옆 버튼(구 목업 토스트)을 `/chat?user=<appUserId>` 딥링크로(콘솔→상세 #135의 역방향). ChatPage는 `?user` 파람을 `active` 파생 폴백으로 편입(URL이 선택의 소스 — 드로어 `?customer` 미러, `set-state-in-effect` 없이). 게이트 = 앱 source **+ appUserId 연결**(미연결 6명은 버튼 숨김 — 행위 변경). **미발견 = 빈 창+토스트**(첫 세션 폴백이 "잘못 이동"으로 읽히던 실기 피드백 반영). ⚠️알려진 한계: 현재 연결 고객 2명(김지안·제임스) 전부 **admin 롤 테스트 계정** 연결이라 콘솔 큐(customer 롤만, #135)가 못 봐 "세션 없음" — 실 고객 연결부터 자연 동작(해피 패스는 customer 롤 강현준 딥링크로 실측).
③**#240 `58fa9db` 유효 수동 관리 상태를 신규·상담접수에도 표시·리포트 포함** — 유슨생 실사용 버그 리포트(신규 김지안에 지정해도 목록 미반영). **배치 4 B2 기각 번복**: 기각 근거 "클라도 공백"은 배지 셀만 본 불완전 전제(필터·드로어는 수동 인정 → 필터에 걸리는데 배지 공백 모순 실존 + 수동 지정 자체가 상담사 액션). 셀 게이트 `displayInfo && updateStatus` + `manualUpdateInfo` 팝오버 폴백("관리 상태 수동 지정" — deriveFinalUpdateInfo에 안 합침: 응답 SLA "대기 중" 유지 의도). 서버 stale_customers 신규 제외를 파생 경로만으로(잠금 테스트 의도 플립 RED→GREEN). plan 기각 표 각주 + **확인 대기 항목 8 ② 병기**(이사님 사후 확인 묶음). 실기: 지정→배지 즉시·F5 영속·필터 정합·psql 원복.
검증(최종 main): typecheck 0·lint 0·unit 558·server 522·build. 그 외 대기 — 계산엔진(이사님 선행)/FCM 실기기 e2e/역할 매트릭스 2단계/#582 크로스 실기(준비 완료 — 이사님 협조 대기).**)

이전: 2026-07-13 밤 (**세션 0713-fresh-start(유슨생): 리팩토링 배치 4 전량 완료 — 6PR 머지(#229~#234).** 범위 #211~#228(106파일) 5앵글 병렬 감사 → 후보 ~30건 → 적대 검증(CONFIRMED 21·ADJUSTED 7·REFUTED 2) → 이행. **감사·판정·이행 SSOT = `ref/plans/2026-07-13-crm-refactor-batch-4.md`(다음 배치는 이것부터).** 핵심: ①**#229 수동 관리 상태 클라 이중 소스 폐기** — manageStatusOverrides(삭제 경로 0·App 불멸·실패 롤백 누락)를 제거하고 row(manageStatus/manageStatusAt/lastActivityAt) 낙관 갱신 단일화(`lib/customer-workflow.ts` TDD 10). 격리 스택 스모크: 수동 지정→동일 스탬프 t→**실활동 직후 배지 즉시 파생 복귀**(구 코드는 F5까지 잔존)→F5 일관→UI 삭제 원복·잔재 0 ②**#231 app_user_id partial unique index(마이그 0030 실 DB 적용)** + link 가드 SSOT(`db/queries/app-user-link.ts`) + 🔴**drizzle 0.44+ 래핑으로 dbErrorMessage 매핑 전체가 원문 노출로 퇴화해 있던 잠복 결함 수정**(collectErrorText cause 체인 — 신규 라우트 에러 매핑은 이 함정 주의) ③**#230 tripwire 사각 해소**(승격 픽스처 실채번+미등록 이름 3곳·consultations report-only 스캔·감사 잔재 scan/clean 대칭 — 스모크 원복에서 감사 스캔 실전 첫 작동) ④#232 CSS 팝오버 3벌 공용화+dead 6건(계산값 전수 증명, `tools/verify-css-key-maps.sh`) ⑤#233 위생(Storage 배열 1왕복·등록 폼 Enter 제출·인박스 사유 표면화·게이트 주석 3벌 정정·AGENTS.md 13테이블) ⑥#234 deleteSelected stale 클로저 race→서버 리로드 통일 ⑦후속: 의존성 in-range 10개 bump(#236 — TS7·@types/node 26 홀드 유지, lucide 1.24 `-icon` 클래스 부재 재확인) · 앱 견적요청 테이블 폴리시(#237 — 닫힌 어휘 컬럼 고정 폭 nowrap + 승격 견적 수를 "견적 작성" 우상단 빨간 카운트 배지로, 1440px 실측). **항목 8은 같은 날 ①로 이행 완료(#235 `fc3b882` — 유효 수동 비'정상'은 일수 게이트 무관 리포트 포함, 유슨생 선승인·이사님 사후 확인만 대기. 신규·상담접수 제외 미러 유지, 테스트 4종 RED→GREEN, server 522)**. 기각 2건(신규·상담접수 모순 — 클라도 공백 미러 / label 로컬 tz — atIso 우선·로컬이 표준)은 plan에 재제안 금지 박제. 최종 main: typecheck 0·lint 0·unit 554·server 518·build·잔재 0. 그 외 대기 — 계산엔진(이사님 선행)/FCM 실기기 e2e/역할 매트릭스 2단계/#582 크로스 실기.**)

이전: 2026-07-13 (**세션 0713-header-ssot(유슨생): 6PR 머지 + 🔴이사님 확인 대기 전량 종결(미해결 0·구현 대기 0).** 추가분 = **수동 관리 상태 영속화+스누즈(#228, 이사님 7-①)**: 마이그 0029(`manage_status`·`manage_status_at`, 실 DB 적용)·유효 규칙 `manage_status_at >= staffActivityAt`(서버 `manualManageStatusActive`↔클라 `effectiveManageStatus` 파리티 잠금)·**동일 now 스탬프 계약**(PATCH가 updated_at을 bump하므로 같은 now가 아니면 저장 즉시 만료)·워크플로우 카드 관리 상태 팝오버 재개방(구 "자동 반영됩니다" 토스트 차단 해제 — 수동 입력 트리거 전무였음 실측)·AI 도구(stale/delivery) 유효 수동 반영(정상=제외·"○○(수동 지정)")·수동 재문의도 manage_status(recontacted는 쓰기 0·데이터 0 빈 컬럼 실측 — 미래 앱 자동 감지 몫). 브라우저 스모크(팝오버→지연 저장→동일 스탬프 t→리로드 배지 유지→byte-exact 원복).** ①헤더 SSOT 정리(#223 — breadcrumb 문자열 통합·dead 모드/메타 제거·전체 보기 서브타이틀 실문구화) ②allDraft 리네임 소진(#224 — 모드 `all` 복귀·draft 식별자/CSS 클래스 console 계열, **빌드 CSS 역리네임 byte-diff 기계 증명**) ③**이사님 Discord 일괄 답변 6건 수신 → 전량 이행**: link 충돌 안내 UX(#225 — LinkConflictError conflict 동봉·인박스 인라인 안내, 역방향은 사유 토스트) · **콘솔 톤 전 페이지 확산(#226 — 헤더 20px·흰 바닥 기본화, 고객 관리 전용 변형 소멸, 격리 스택 12화면 스크린샷 전수·회귀 0)** · AI 리포트 재문의 우선(#227 — stale_customers 라벨 대체·delivery_risk 병기, 변이 검증 실관찰). 나머지 3건(리포트 일수 통일·배지 견적 활동·AI 힌트 톤)은 현행 승인 종결. **`ref/director-pending-confirmations.md` = 미해결 0·구현 대기 0.** ⚠️발견 2: 공통 헤더 관리자 지표 스트립(2026년 5월·전체 출고 86대)은 하드코딩 목업(Topbar.tsx:354, 출고/정산 데이터화 이후 실제화 가능) · `manageStatusOverride`는 비영속 클라 메모리(컬럼 없음 — 리로드 시 클라도 소실, 영속화 여부는 열린 제품 결정). 그 외 대기 — 계산엔진(이사님 브레인스토밍 선행)/FCM 실기기 e2e/역할 매트릭스 2단계/#582 크로스 실기.**)

이전: 2026-07-11 (**세션 0711-operation-time(유슨생) 후반: ①#582 3단 전부 완료(CRM #218·앱 #646 — 아래 항목) ②사이드바 버그 2건 픽스(`218deab` main 직접 — 하단 철학 박스 flex 찌그러짐·펼침 헤더 active 오인) ③dealer 쓰기 전면 차단(#220 squash `ec2583f` — 아래 항목).**)

이전: 2026-07-11 (**세션 0711(유슨생): 일괄 담당자 변경 실동작 — 서버 변경 0·클라 오케스트레이션(#216 머지). 이로써 고객 목록 헤드바 액션 3개(담당자 변경·삭제 #212·등록 #215) 전량 실동작.**)

이전: 2026-07-10 (**세션 0710-2(유슨생): 고객 수기 등록 신설 — `POST /api/customers` + 헤드바 `[+ 고객 등록]` 폼 팝오버 실동작(**#215 squash `4f0fff0` 머지·prod Active 확인**).**)

이전: 2026-07-10 (**세션 0710: ①앱 팀 `profiles` 권한 축소 대응(교차검증·INSERT 구멍 제보→앱 수정 확인)·`profiles` 쓰기 금지 tripwire(#211) ②CRM 전용 publishable 키 교체·prod 반영 ③**고객 하드 삭제 신설(#212·#213)** — admin 전용·앱 카드 409 가드·감사 기록·확인창 대상 이름 ④테스트 픽스처 잔재 tripwire(#214).)

이전: 2026-07-09 (**세션 0709-next-step: ①카드 UI 상태 CardUiState 통합 완료·머지(PR #198 squash b6088ad) — 계산엔진 선행 정지작업 소진 ②🔴운영 알림 오염 사고 해소(PR #199 squash 3766de2) — test:server가 운영 디스코드/FCM 42건 발송하던 것을 `withNotifyGuard` 트랜잭션으로 차단.**)

이전: 2026-07-08 (**세션 0708-FCM-notification-02: 배정 알림 슬라이스 2 완료·머지(PR #194 squash 2141c34) — 실시간 상담 수신 On/Off 영속(`crm.staff_settings`+`GET/PATCH /api/me/live-consulting`)+배정 드롭다운 Off 상담사 제외(`GET /api/staff` liveReceiving·`fetchStaffOptions`→백엔드 통합). CRM 내부용 확정(앱 미소비)·브라우저 스모크 통과.** 이전 0708: 슬라이스 1(PR #193)·앱이 서버 발송 인프라 이미 소유 발견(트리거+send-push Edge)·구 A안/CF Pages 전제 폐기. **세션 0708-book-consultation: 앱 상담신청 → CRM 통합 완료(브랜치 `feat/crm-consultation-integration`·PR 대기 — 견적요청 스택 통합·CRM 전용 삭제/인라인 확인, 마이그 0025·0026, 브라우저 종단 스모크). 다음 = FCM 실기기 e2e(앱 몫 완료, device_tokens만) / 계산엔진(이사님 선행) / 상담신청 인박스·임베딩(보류)**)

Purpose: `CRM 이어가자`, `CRM 시작하자`, `영실아 이어가자` 이후 현재 CRM 작업만 빠르게 복구하기 위한 압축 문서다. 완료된 세부 로그는 git/PR과 `ref/specs|plans`를 기준으로 확인한다.

## Boot

1. Read CRM `AGENTS.md`.
2. Read this file.
3. Run `git status --short --branch` and `git log --oneline --decorate --max-count=5`.
4. Read `ref/current-working-state.md` only if this is insufficient.
5. Do not read planning source files unless the task touches strategy, roadmap, AI policy, architecture, or quote engine decisions.

## Current Focus

- **✅ AI 힌트 실데이터화(2026-07-12 세션 0712-AI-hint, PR #221 squash 머지 `2e7a342`)**: 고객 목록 AI 힌트(hover 보라 말풍선)를 목업 하드코딩 20문장 → **실 데이터 기반 Gemini 생성값**(`crm.customers.ai_summary`)으로 교체 + 실변경 시 자동 재생성. spec `ref/specs/2026-07-12-crm-ai-hint-datafication-design.md` · plan `ref/plans/2026-07-12-crm-ai-hint-datafication.md`(**맨 위 ✅ 진행 상태 = 태스크·커밋·리뷰 상세 SSOT**). 8태스크 subagent-driven(태스크별 spec/품질 2단계 리뷰).
  - **구성**: 마이그 0028(`ai_summary_source_hash` — 입력 재료 hash skip, **실 DB 적용됨**) · 순수 계층(`src/lib/ai-hint.ts` — 재료 조립·프롬프트(목업 역설계 few-shot)·sanitize) · 로더/라이터(`src/db/queries/ai-hint-sources.ts`) · 훅(`src/lib/ai-hint-on-write.ts` — embed-on-write 미러, 게이트 3규칙 **독립 env `AI_HINT_ON_WRITE`**, `test:server` 프리픽스 추가) · **라우트 배선 16곳**(고객 POST/PATCH·메모/할일/견적 CUD·승격 4·상담 dismiss — 리뷰가 찾은 재료 경로) · 클라(목업 테이블 폐기·`parseAiHintParts` 굵게 렌더·**값 없으면 버튼 숨김**=앱 고객 빈 말풍선 해소·검색/레거시 셀 평문화) · 백필(`src/scripts/backfill-ai-hints.ts`).
  - **백필 실 실행 완료**: 22/22 generated — 1차 실측 톤 드리프트(명령형 종결 5건·굵게 3곳)를 프롬프트 강화로 교정 후 재생성(hash 클리어 → 전건 평서 압축형 47~77자). 멱등 증명(재실행 전건 unchanged). **일정·서류는 재료 의도적 제외**(재료↔트리거 정합 원칙 — 편입 시 훅 콜사이트 동반).
  - 검증: typecheck/lint 0 · server **501** · unit **532** · build · knip main과 동일 + **격리 스택 브라우저 스모크 A~F 통과**(생성 문장 교체·빈 말풍선 해소·버튼 숨김·검색 매칭·실 Gemini 재생성 e2e — 메모 추가/삭제 후 hash 수렴 원문장 byte-exact 복원 실증, 원복·잔재 0).
  - **🟡 이사님 사후 공유**: 목업 20문장(이사님 5/19 설계) → AI 생성 교체 — `ref/director-pending-confirmations.md` **항목 5**. follow-up(PR 본문): 김지안 굵게 3곳 수용(재료 키워드 3개·near-deterministic) · 앱 직접 쓰기(채팅·신규 견적요청)는 훅 없음=백필 보정(embed와 동일 한계) · 길이 초과(>90자) 관측 로그 기반 프롬프트 재조정.

- **✅ dealer 쓰기 전면 차단(2026-07-11, PR #220 squash `ec2583f`)**: 쓰기 라우트 29개 중 role 게이트 2곳뿐 — dealer가 API 직접 호출로 메모/할일/일정/견적/서류 삭제 등 수행 가능했다(UI 숨김은 UX 보조). **전역 `dealerWriteGate`**(`src/middleware/role-gate.ts`) = dealer+쓰기 메서드 403 fail-closed, 체인 규약 auth(401)→gate(403)→db(`app.ts` `protect()` 헬퍼). 구 인라인 dealer 게이트(고객 등록)는 전역으로 이관. dealer 읽기 불변.
  - **🔑 allowlist 구조(이사님·유슨생 확정 요구)**: 미래 딜러의 정당한 쓰기 = **MC 마스터 트림 할인 입력(자사/제휴/타사)** — 라우트 신설 시 `DEALER_WRITE_ALLOWLIST`에 `{method, path: /^…$/}` 한 줄 등록으로 개방(앵커 정규식 강제, 테스트 잠금).
  - **2단계(역할×라우트 매트릭스 — admin/staff 세분: 예. 견적 삭제를 누가?)는 범위 밖** — 실무 정책 결정이라 **이사님 확인 대기 목록에 추가**. 검증: server 474(+7)·unit 523·4종 + **변이 검증 실관찰**(게이트 제거 시 dealer 테스트 3종 정확 실패).
  - **⚠️ 교훈 — 403 전제 픽스처도 게이트 회귀 시 행을 만든다**: 변이 실행 중 dealer POST가 실 고객("게이트검증") 생성 → 잔재 tripwire(#214)가 즉시 검출 → 이름 registry(`TEST_CUSTOMER_NAMES`) 등록+`--clean` 정리. 게이트 테스트 픽스처 이름도 registry 선등록이 원칙.

- **✅ 실시간 상담 운영 설정 콘솔(2026-07-11 세션 0711-operation-time, PR #218 squash `40d8786` 머지·prod 반영 실측)**: 앱 이슈 dl-auto/mr-cha-app#582의 CRM 몫(2단계). 앱의 로컬 운영시간 하드코딩+임시 AUTO/ON/OFF를 Supabase SSOT+CRM 콘솔로 이전하는 3자 작업. spec `ref/specs/2026-07-11-crm-handoff-operation-settings-design.md`(계약 SSOT) · plan `ref/plans/2026-07-11-crm-handoff-operation-settings.md`.
  - **3단 전부 완료**: ①Supabase(앱 팀) ✅ 같은 날 배포(`public.human_handoff_settings` singleton+감사+RPC 3종+Realtime, 마이그 `20260711170000`) — CRM이 시드·REVOKE·RLS·토요일 판정까지 실측 검증 ②CRM 2단계 ✅ 이 PR ③앱 3단계 ✅ **배포 완료(2026-07-11 14:37 KST, 앱 PR #646)** — 판정 전부 서버 RPC 경유·로컬 override 제거. **🔴 이 시점부터 CRM 콘솔의 강제 OFF/스케줄 변경 = 실 고객 즉시 적용(운영 주의).** **⚠️ 단 수일간 force_off 실효성 100% 아님(앱 팀 고지 2026-07-11)**: Flutter 웹 서비스워커 캐시 특성상 구 번들 클라이언트는 재방문 사이클 전까지 **로컬 판정**을 계속 쓴다 — force_off를 걸어도 구 번들 사용자는 운영시간 내 pending 전이 가능. 자연 소멸(보통 수일). 부수: 구 번들이 만든 깨진 문구 행(`**"응"**`·system_kind 없음)이 간헐 생성될 수 있음 — 앱 #647(15:07 문구 교정 배포)이 새 번들에 반영됐고, 트리클은 앱 팀이 멱등 치환 + 다음 주 초 최종 sweep 마이그레이션으로 종결 예정. 16:13 트리클 행은 이사님 테스트 계정(98b58a1d 김지안 customer) 실측 — 실 고객 영향 0. 잔여 = 크로스 실기(CRM force_off ↔ 앱 채팅 force_message 표시) 1회 — **트리클 소멸 후가 정확**(구 번들 탭이면 로컬 문구가 나와 오판 위험), 운영시간 외 시간대가 적기.
  - **구성**: `/handoff-operation` 설정 페이지(admin 3중 fail-closed — 메뉴 `isAdminRole` 그룹·라우트 finance 패턴·RPC 42501→403) = 현재 판정 카드·모드 3상태·요일 7행 운영시간·안내 문구 2종·변경 사유(필수)·감사 이력(변경자 이름 staff 디렉토리 해석). 진입 = 프로필 팝오버 **"차선생 앱 설정 > 상담 운영"(이사님 결정 위치)**. + ChatPage 탭 행 **운영 상태 배지**(Realtime+60s 재판정, admin 클릭 시 설정 페이지). **서버(src/) 변경 0** — 쓰기는 `update_human_handoff_settings` RPC 단일 경로(브라우저 supabase-js, admin 검사+UPDATE+감사 INSERT 서버 원자).
  - **⚠️ 축 구분**: 팝오버 기존 On/Off(#194 `crm.staff_settings.live_receiving`)는 **상담사 개인 수신**, 이 설정은 **전사 운영 모드** — 다른 것. 물리 분리로 혼동 차단(기존 토글 불변).
  - **⚠️ 스모크 주의(다음 세션)**: 앱 3단계 배포 **후**에는 force_off 저장이 실 고객 앱을 즉시 차단한다 — 모드를 만지는 스모크는 앱 전환 여부 확인 후 신속 원복(설정 스탬프 `updated_by`/`updated_at` psql 복원 + 스모크 감사 행 DELETE까지, 이번 세션 절차가 선례. 감사 테이블은 직접 INSERT 불가지만 postgres 롤 DELETE 가능).
  - 검증: typecheck 0·lint 0·unit **523**(+14 TDD)·build + 격리 스택 브라우저 스모크(시드 표시→force_off 저장→psql·감사 대조→**psql UPDATE 4초 내 배지 Realtime 자동 갱신 실증**→원복·잔재 0 byte-exact). 유슨생 실기 지적 1건 반영: 설정 무변경 시 저장 버튼만 죽어 있어 고장처럼 읽힘 → "변경된 설정이 없습니다" 안내 표시(`8dfe483`).
  - **머지 후 폴리시 3건(유슨생 실기, 전부 main·prod)**: ①상태 카드 미리보기 마크다운 원문 노출 → 공용 MarkdownMessage + `withAppLineBreaks`(앱 MarkdownBody `softLineBreak:true` 실측 미러 — 단일 개행만 hard break 승격, 유닛 16종) + textarea 서식 힌트(`5661956`) ②**2열 레이아웃**(#219 squash `390bcde`) — 좌 편집 폼/우 현재 상태·이력(고객 상세 drawer "좌 작업/우 참조" 문법), 1280px 이하 1열(폼 먼저) ③폭 상한 제거+우측 clamp(380px, 28vw, 640px) 유동화. **부산물 — 앱 문구 마크다운 깨짐 발견·제보**: `handoff_consultation_messages.dart:5,10`·`chat_provider.dart:1133`의 `**"응"**이라고`가 CommonMark 강조 불성립(닫는 `**` 앞 문장부호+뒤 한글) → 앱·CRM 양쪽 원문 노출. 수정은 앱 몫(`"**응**"` 형태), 과거 행 백필 불요(일회성 안내).
  - **follow-up(PR 본문)**: 공통 헤더 프로토타입 버튼 2개가 이 페이지에도 노출(전 페이지 공통 — 범위 밖) · 비admin 배지 실기(staff 실계정 필요) · 판정 카드 안내 문구는 마크다운 원문 표시(의도) · 공휴일/임시 휴무·timezone 편집은 이슈 "추후 확장".

- **✅ 고객 수기 등록 신설(2026-07-10 세션 0710-2, 브랜치 `feat/crm-customer-create` PR 대기)**: 헤드바 `[+ 고객 등록]` 목업 → 실동작. spec `ref/specs/2026-07-10-crm-customer-create-design.md` · plan `ref/plans/2026-07-10-crm-customer-create.md`(8태스크 subagent-driven, 태스크별 spec/quality 2단계 리뷰 + 최종 통합 리뷰).
  - **결정(유슨생)**: ①최소 폼(이름 필수·연락처·유입 경로) → 저장 즉시 **새 고객 상세 드로어 자동 오픈**(드로어는 URL single source라 `navigate(?customer=code)` + 리로드 도착만으로 열림 — 새 상태 0) ②**등록자 본인 자동 배정**(advisorId+advisorName+assignedAt 동반, `getStaffName` 실패 시 미배정 fail-open — staff scope가 advisor_id 매칭이라 미배정이면 등록자 AI 조회에서 빠짐) ③연락처 중복 **소프트 경고**(등록 차단 아님 — 가족 공유 번호 예외, 클라 목록 비교라 서버 왕복 0) ④dealer 403 fail-closed ⑤**source는 수동 어휘만**(`SOURCE_MANUAL_OPTIONS` — 자동 어휘를 수기 등록이 쓰면 앱 유입 통계 오염) ⑥승격 쿼리와 코드 비공유(입력 해석이 전부 달라 과추상화 기각).
  - **잔재 tripwire 확장 동반**: 이 라우트 테스트는 **실채번**(CU-YYMM-####) 고객을 만들어 접두사 registry가 못 잡는다 → `TEST_CUSTOMER_NAMES = ["수기등록테스트"]`(이름 registry) + `customerResidueWhere()`(scan·--clean 공유, 괄호 가드·빈 배열 가드). **새 실채번 픽스처는 이름을 이 registry에 등록**할 것.
  - 검증: typecheck 0·lint 0·unit **503**(+5)·server **467**(+7? 실측 460→467)·build + **격리 스택 브라우저 스모크**(등록→CU-2607-0001 실채번→드로어 자동 오픈→psql 시드·자동배정 대조→중복 경고(박서연 실번호)→빈 이름 인라인 에러→새로고침 영속→UI 삭제 원복→감사 행 정리→잔재 0).
  - **follow-up(PR 본문)**: 이름 픽스처 드리프트 가드 부재(코드 접두사 계약 스캔과 비대칭) · 실패 토스트 1800ms(리로드 실패 안내치고 짧음) · 등록/삭제 팝오버 동시 오픈 가능(기존 패턴 확장) · 등록 직후 드로어 오픈 전 짧은 공백(스모크에선 미체감).

- **✅ 일괄 담당자 변경 실동작(2026-07-11 세션 0711, 브랜치 `feat/crm-bulk-advisor-change` PR 대기)**: 헤드바 마지막 목업 해소 — **고객 목록 헤드바 액션 3개 전량 실동작**(담당자 변경·삭제 #212·등록 #215). spec `ref/specs/2026-07-11-crm-bulk-advisor-change-design.md` · plan `ref/plans/2026-07-11-crm-bulk-advisor-change.md`(4태스크 subagent-driven + 2단계 리뷰 + 최종 통합 리뷰).
  - **결정(유슨생)**: ①**서버 변경 0** — 선택 고객마다 개별 `PATCH` 순차(`customer-bulk-delete.ts` 미러, 건별 독립·실패 목록). 배정 규칙(실변경 시만 assignedAt 스탬프·advisorId 동봉·self 알림 skip·동일 담당자 no-op)은 서버 PATCH 기존재분이 건별 성립 ②**알림 N개 수용**(고객당 1건 = 개별 배정 N번과 동일 의미론 — 묶음 알림은 실사용 관찰 후) ③버튼 노출 관리자/팀장(`showAdvisorColumn` 정합, 서버 추가 게이트 없음) ④완료 후 서버 리로드 + **성공한 건만 선택 해제**(실패 행 유지 — 재시도, deleteSelected 대칭).
  - **리뷰 반영 3건**: 닫기 경로(성공·취소·토글) select 공통 리셋(끈적한 초안 → 오배정 경로 차단) · 실패 행 선택 유지 · **리로드 실패를 advisorNotice로 맥락화**(#215 관례 — prop 타입 `() => void | Promise<boolean>` 확장, 전역 배너만으론 "변경 실패"로 오인).
  - `formatDeleteTargetNames`→**`formatBulkTargetNames`** 공용화(삭제·배정 확인창 공유). lib 유닛이 페이로드를 `{advisorName, advisorId}` 정확 2필드로 잠금(team 미포함 — 몰래 추가하면 테스트가 깨짐).
  - 검증: typecheck 0·lint 0·unit **509**(+4)·server 467·build·잔재 0 + **격리 스택 브라우저 스모크**(김지안·제임스 2명 선택→유재민 일괄 변경→목록 셀 "배정 유재민" 갱신+선택 해제→psql advisor_id/assigned_at 대조(순차 56ms 간격)→**psql 세 컬럼 원값 복원**(#202 선례 — 재배정도 실변경이라 UI 원복 불가)).
  - **follow-up(PR 본문)**: 빈 배열 테스트 미러 갭 · 팝오버 3종 비배타 오픈 · 전체선택 해제→재선택 시 팝오버 재등장(형제 공유 특성) · 묶음 알림.
  - **⚠️ "담당자" UI 3종 현황(전부 다른 것)**: 필터(헤드바 select·읽기) / **일괄 변경(헤드바 버튼 — 이 세션에서 실동작화)** / 개별 배정(상세 팝오버 #177) — 이제 셋 다 동작.

- **✅ 테스트 픽스처 잔재 tripwire(2026-07-10, PR #214 squash `4e43398`)**: `DATABASE_URL`이 공유 master라 테스트가 진짜 고객·견적 행을 만든다. 정상 종료하면 `afterAll`이 지우지만 **실행이 끊기면 남는다** — `CU-EMBRT-…/배선테스트`가 07-09에 남아 이사님 고객 목록에 유령으로 떴고 **사람 눈이 발견**했다(어떤 테스트도 못 잡았다).
  - **2겹**: ①`src/test-utils/fixture-codes.test.ts` — 실 DB 테스트(`getDefaultDb` 참조)의 `CU-`/`QT-`/`PUSH-` 코드 리터럴이 registry(`fixture-codes.ts`, 접두사 12+3종)에 있는지. **드리프트하면 ②가 그 행을 못 본다** ②`src/test-utils/fixture-residue.test.ts` — 매 `test:server`마다 실 DB 잔재(픽스처 고객·견적 + 고아 임베딩 + **고아 앱 카드**) 조회. `bun test`는 파일 순차 실행 + `afterAll`이 다음 파일 전에 끝나므로 **run 시작 시점에 이미 있던 잔재는 순서 무관하게 잡힌다**(우리가 당한 실패 모드가 정확히 그것).
  - 정리 = `bun run check:residue -- --clean` — **crm 스키마만**. `public.advisor_quotes` 고아 카드는 **보고만** 한다(앱 소유 — 고객 삭제 슬라이스의 소유권 경계와 동일).
  - **가드가 스스로 잡아낸 것**: ⓐ`QT-EMBSRC-`는 **변수(`QUOTE_CODE`)로 조립**돼 `quoteCode:` 앞뒤 방식으로는 안 잡혔다 → 값의 모양으로 뽑아 발견. **가드 없었으면 이 접두사 잔재는 영구 미검출** ⓑ`PUSH-TEST-`는 `CU-` 규칙 이탈(`CU-%TEST%` 패턴이었으면 놓침) ⓒ순수 유닛의 인메모리 객체 오검출 → `getDefaultDb` 참조를 판별자로 ⓓ실채번 단언 `/^CU-\d{4}-\d{4}$/`를 코드로 오인 → 접두사 뒤 영숫자 1자 이상 요구 ⓔ**주석 속 코드를 위반으로 잡았다**(규칙을 설명하는 문장이 위반이 되면 아무도 안 쓴다 — `profiles-write-guard`가 같은 이유로 `stripComments`를 쓰는데 빼먹었다).
  - 검증: typecheck0·lint0·knip clean·server **459**(+12)·unit 498·build. **변이 3종 실관찰**(미등록 접두사 추가/registry 제거/변수 조립 접두사 제거 → 각각 정확히 검출) + **`--clean` 실증**(잔재 심기→exit 1→정리→0건, 실고객 22명 불변). 규칙 SSOT = `AGENTS.md`.

- **✅ 고객 하드 삭제 신설(2026-07-10, PR #212 squash `ac0d049`, prod 반영 확인)**: 이사님 실기 지적 "삭제해도 리로딩하면 되살아난다" — **버그가 아니라 미구현**이었다(`deleteSelected`가 프론트 배열만 필터, `DELETE /api/customers/:id` 라우트 부재). spec `ref/specs/2026-07-10-crm-customer-delete-design.md`.
  - **결정(이사님)**: ①**하드 삭제**(소프트 삭제 기각 — `불발` 상태·`보류/이탈` 화면이 이미 "감추기" 담당. 축이 둘이면 드리프트) ②**앱 카드 있으면 409 거부**(회수 ❌) ③앱 계정 연결 고객도 삭제 가능(`profiles` 불가침) ④감사 기록 ⑤**admin 전용 403 fail-closed** ⑥다건 = 건별 독립 트랜잭션 + 실패 목록.
  - **🔑 원칙 정정**: "앱 카드 불멸"이 아니다 — 견적함에서 발송 견적을 지우면 **지금도** 앱 카드가 사라진다(`deleteQuote`→`deleteAdvisorQuoteByCrmQuoteId`, #159 결정 7). 진짜 원칙은 **"고객 삭제가 앱 카드를 조용히 연쇄 삭제하지 않는다"**. 견적 삭제 = 의도한 행동의 직접 결과(유지) / 고객 삭제 = 모르는 부작용(차단). 지우려면 **견적함에서 견적 삭제(카드 회수) → 고객 삭제** 2단계. 편의 경로(한 번에 회수) **기각** — 되돌릴 수 없는 조작에 편의 경로를 미리 뚫지 않는다.
  - **소유권 경계 = "스키마"가 아니라 "저작자"**: `public`이 `crm.customers`를 참조하는 FK **0개**(실측) → 앱 데이터는 우리 코드가 결정할 때만 건드려진다. **CRM이 쓴 것만 CRM이 거둔다** — `profiles`·`quote_requests` 행·`consultations`·`chat_sessions`는 한 줄도 안 건드림.
  - **순서 2개가 load-bearing**: ⓐ**가드는 트랜잭션 안**(밖이면 통과~삭제 사이 다른 세션 발송이 유령 카드 생성) ⓑ**Storage는 커밋 후**(롤백 불가 — 커밋 전에 지우면 트랜잭션 실패 시 DB 행은 살고 파일만 증발). 견적은 일괄 DELETE가 아니라 `deleteQuote()` 견적당 호출(해체 SSOT, 불변식 의존 회피).
  - **임베딩은 손댈 것 0** — `crm.embeddings.customer_id` 전 행 non-null(실측) → CASCADE만으로 업무 AI가 그 고객을 완전히 잊는다(테스트로 잠금).
  - **함께 고친 기존 결함 2**: ①**견적 삭제가 Storage를 안 지웠다**(서류 삭제는 지움 — 비대칭). `crm.quotes.file_path` 고아 누적 중이었음(실측 1건). 회귀 2종+변이 검증 ②**견적 삭제 확인 문구를 잠그는 테스트 0건**(되돌릴 수 없는 조작의 유일한 안전장치인데 무방비) → `quote-meta.test.ts` 신설 + 문구 보강(이사님 승인): "고객 앱 견적함에서도 사라지며, **되돌릴 수 없습니다.** 다시 보내려면 새 견적을 발송해야 합니다."
  - 마이그 **0027** `crm.customer_deletions`(감사 — 스냅샷 없음: 복원은 인박스 재승격, 개인정보 파기 시 스냅샷이 파기 대상). `src/db/queries/customer-delete.ts`(순환 import 회피 경계). 클라 `deleteCustomersBulk`(순수, 유닛 6종)+확인창+admin만 렌더+**성공한 건만** 목록 제거.
  - 검증: typecheck0·lint0·knip clean·server **447**(+9)·unit **493**(+15)·build + **격리 스택 스모크 2종**(유령 행 `배선테스트`(테스트 픽스처 잔재) 삭제→200→리로딩 후에도 소멸 / 앱 카드 보유 고객→409+화면 사유+행 생존). 공유 master 알림 0·고아 카드 0·잔재 0. **prod 반영 실측**(번들에 새 문구, 무토큰 DELETE 401).
  - **follow-up**: `담당자 변경`·`고객 등록` 버튼 여전히 목업 · **테스트 픽스처 잔재 tripwire**(`CU-EMBRT-`·`CU-ROUTE-`·`CU-SEND-`·`CU-RSEND-`) 별도 슬라이스 · `requireRole` 게이트 다른 파괴적 라우트로 확산.

- **✅ 앱 `public.profiles` 권한 축소 대응 완료(2026-07-10)**: 앱이 휴대폰 인증 우회(authenticated가 `phone_verified_at` 직접 UPDATE) 차단 → `REVOKE UPDATE ON public.profiles FROM anon, authenticated`. **CRM 영향 0**(서버는 `postgres` 롤, 브라우저는 SELECT만).
  - **우리가 되돌려준 것 3**: ①앱이 지목한 스모크 대상 2곳은 **서버 drizzle(postgres 롤)**이라 REVOKE 무관 — 실제 노출면은 `client/src/lib/chat.ts:143` 실시간 상담 콘솔 **하나** ②**역할 승격 방어가 트리거 한 겹**: `custom_access_token_hook`이 `profiles.role`→JWT `user_role`, 그게 **CRM의 유일한 인증 게이트**(`auth/verify.ts`). `prevent_profile_role_change`의 탈출구 GUC에 역할 검사 부재 → 앱이 `current_user='postgres'` 조건 추가(+SECURITY INVOKER 전환)로 수정 ③`consultations.ts:105`는 폼 우선 — 무비판 신뢰 경로는 `quote-requests.ts:351-352` 하나.
  - **🔴 우리가 발견한 실구멍(앱 수정 완료)**: 트리거가 **BEFORE UPDATE 전용**인데 `authenticated`가 `INSERT(role)` 보유 + INSERT 정책에 role 제약 없음 → 프로필 행이 없는 유저의 다음 로그인에서 조작 클라이언트가 `role:'admin'` 삽입 가능(앱 `ensureProfileExists`가 그 상태를 상정·6곳 호출). **롤백 트랜잭션으로 실증**(`role_after_insert = admin`). 앱이 `WITH CHECK (auth.uid()=id AND role='customer')` 적용 → **재검증 완료**: 공격 3종(UPDATE+GUC / upsert / 순수 INSERT) 전부 차단, 정상 경로 4종(복구 INSERT·기본값·아바타 UPDATE·admin의 role 변경) 보존.
  - **CRM 몫**: `profiles` 쓰기 금지 tripwire(**PR #211** `8446e31`) — `src/db/profiles-write-guard.test.ts`가 drizzle·supabase-js·원시 SQL 3경로 스캔. "위반 0건"만 단언하면 정규식이 고장나도 통과하므로 **탐지기 자체도 테스트**(잡을 것 3·흘려보낼 것 4·글롭이 실제로 훑었는지). 실 저장소에 위반을 심어 검출 실관찰. `AGENTS.md`에 계약 2건 박제(profiles read 전용 · **채팅 system 문구가 앱과 `startsWith` 결합** — `handoff_system_messages.dart:20-21`, 한 글자만 바꿔도 앱 상태 복원이 조용히 깨짐).
  - **브라우저 스모크 통과**(magiclink admin): `chat_sessions` 11행·`profiles` embed 조인 11행·큐 7건·REST 4xx/5xx 0·supabase 쓰기 0. **첫 스크립트는 셀렉터가 틀려 `1 passed`인데 아무것도 검증 안 했다** — PostgREST 응답 본문을 직접 뜯는 방식으로 교체(embed가 죽으면 고객명이 `"고객"`으로 조용히 폴백).
  - **✅ CRM 전용 publishable 키 교체(`22bf260`+`4154f38`, prod 반영)**: 앱과 공유하던 기본 키 → `crm_project` 전용 키. `.env.production` 하나가 CF Pages **Production·Preview 둘 다** 지배(빌드 명령 하나·mode override 없음). **⚠️ 사고**: 키 교체 커밋 본문에 *"[마커]를 붙이면 배포가 스킵된다"*고 **경고하면서 그 토큰을 글자로 적어** CF가 스킵 → prod이 8분간 옛 키 서빙. 빈 커밋으로 복구. **GitHub check-runs/deployments는 비어 있다** — 배포 확인은 `bunx wrangler pages deployment list --project-name mrcha-crm`(스킵은 `Status: Idle`).
  - **앱 잔여**: `profiles.username` DROP(사전 공유 예정) → 우리는 `src/db/public-app.ts:79` 한 줄 제거. 채팅 문구→메타 필드 신호 전환은 나중 합의.

- **✅ 이사님 확인 대기 전량 종결(2026-07-13) — `ref/director-pending-confirmations.md`**: Discord 일괄 답변 6건 수신 → 결정 3건 즉시 이행(#225 link 안내 UX·#226 콘솔 확산·#227 AI 재문의 우선), 3건 현행 승인. **미해결 0·구현 대기 0.** 새 배치에서 행위 변경이 생기면 그 파일에 다시 누적한다(프로세스 유지).
- **✅ 0709 리팩토링 배치 3 — 정합성 파트 완료·머지(2026-07-09, PR #203 `0a8a813` · #204 `b857501` · #205 `a145b90`)**: 배치 2(#179~#184) 이후 미감사 구간 **#185~#202**(66파일 +2743/-730)를 **5앵글 병렬 감사**(업무AI·알림/푸시·서버쿼리/라우트·클라·크로스커팅) → 후보 27건 → **6개 적대 검증관이 전건 반박 시도**(CONFIRMED/ADJUSTED/REFUTED). 유슨생 지시로 **상+중 정합성 5건만 착수**, 나머지는 plan에 박제. **감사·검증·기각 SSOT = `ref/plans/2026-07-09-crm-refactor-batch-3.md`**(다음 배치 착수 시 이것부터 읽는다).
  - **🔴 #203 = 지금 오답이던 실버그**: `customer_consultations`·`customer_quotes`가 `orderBy(createdAt)` **ASC**라 행 상한 30 절단이 **최신을 버렸다**. 실측 제임스(`faf23ef2`) 상담 44건 중 **1월대 30건만 남고 최신 14건(~5/28) 유실**. 절단 문구도 `외 14명 — 상위 30명만 표시`(행이 상담인데 "명"). → `desc(createdAt)` + `capReportLines(lines, unit)` 단위 인자화("명"/"건"), 호출부 4곳 배선. **⚠️ 아래 #196 항목의 "제임스 상담 내역 정확 나열" 검증 기록은 사실이 아니었다(정정 표기함).**
  - **#204 = 무활동 일수 계산법 드리프트**: 같은 소스(`staffActivityAt`)·같은 임계(`STALE_THRESHOLDS`)인데 서버 `floor(경과/24h)` ↔ 클라 **브라우저 로컬** 달력일. 활동 7일 전 13:00·지금 12:00이면 서버 6일(리포트 누락) ↔ 클라 7일(배지 "확인필요") = 화면 내 모순(#180이 자식 집합 드리프트로 없앤 것과 같은 부류, 다른 메커니즘). 파리티 테스트가 **버킷 경계만** 잠그고 계산법은 안 잠갔다. → `kst-date.ts`에 `kstDayIndex`/`kstDayDiff` 신설, `daysSince(at, now)`가 소비, 클라 `dayStart`도 KST 고정, **파리티를 계산법 동치까지 확장**(`TZ=America/New_York`로 RED 실증). **UTC↔KST 완전 일치는 원리적으로 가능**(한국은 고정 UTC+9·DST 없음 — `daysSince`는 UTC 달력이 아니라 tz-무관 경과시간이었던 게 문제).
  - **#205 = 승격 경로 정합성 3건**: ⓐ**상담 승격이 앱 견적요청 임베딩을 미스케줄**(잠복) — `quote_request` 청크는 `app_user_id` 연결이 있어야 적재되는데(`embed-sources.ts:216-232` 미연결이면 null), 견적요청 라우트는 link·create 양쪽에서 스케줄하고 상담 라우트는 link 0건·create는 `customer_profile`만. 승격 후엔 인박스가 승격 액션을 더는 안 보여줘(`matchType`→`app_user`) **어느 경로도 훅을 다시 안 부른다** → 백필 전까지 코퍼스 영구 공백. 실측 교집합 유저 1명. 상담 인박스(Task 8) 보류라 현재 도달 불가지만 **착수 즉시 발현**. → **`src/lib/promotion-embeds.ts` SSOT 신설, 라우트 4곳 공유**(청크 텍스트 불변 = 백필 소급 불필요). *(감사원 1명이 "문서화된 의도"로 오판 — `routes/consultations.ts:24-27` 주석은 `customer_profile` 재임베딩과 상담신청 문의 임베딩만 다루고 `quote_request`엔 침묵. 적대 검증이 판정.)* ⓑ**link 역방향 덮어쓰기**(지금 도달 가능) — 409 가드는 "들어오는 userId가 다른 고객에 붙었나"(정방향)만 보고, 대상 고객의 기존 `app_user_id`는 조건 없이 교체한다. `crm.customers.app_user_id`에 **UNIQUE 제약 0건**(실측)이라 DB도 안 막고, 전화 매칭 후보는 이미 연결된 고객도 노출(`matchType="phone"`) → 견적요청 인박스 "연결" 한 번에 원래 앱 계정이 고아. → **양쪽 역방향 가드**(같은 계정 재연결은 멱등). **기존 quote-requests 테스트 2개가 실제로 그 덮어쓰기를 수행 중이었다**(`limit(1)`이 뽑는 실 master 첫 고객 `848713e0`이 이미 연결 상태 — tx 롤백이라 무해). ⓒ`customer-quotes.send.test.ts:125-128`의 `advisor_quotes` viewed_at UPDATE가 **guard 밖**(같은 파일 `:334`는 guard). 현재 위험 0이나 막고 있는 건 **앱 소유 트리거**의 2분기(`sent_at` 미변경 early return — psql 실측)뿐 → 앱이 열람 알림 추가 시 #199 재현. `withNotifyGuard`로 방어 이관.
  - **🟡 이사님 확인 2건**: ①#204 — AI `stale_customers`·`delivery_risk`에 뜨는 고객 집합이 경계에서 바뀐다(목록 배지와 **일치하는** 방향) ②#205ⓑ — link 역방향 충돌이 **fail-closed 409**. "기존 연결 고객으로 유도" UX 대안 판단.
  - **함께 발견(범위 밖, plan 기록)**: 클라 배지의 `재문의`·`manageStatusOverride` 오버라이드를 AI 도구는 모른다 → 30일+ 방치인 재문의 고객이 목록 "재문의" / AI "장기방치".
  - **검증**: main 통합 **typecheck 0 · lint 0 · test:server 408 · test:unit 478 · build**. 각 PR TDD(RED 실관찰 후 GREEN). 테스트 잔재 0행(psql).
- **✅ 0709 배치 3 잔여 2건 완료·머지(2026-07-09, PR #209 `50b5444` · #210 `c31d112`) — 배치 3 전량 소진**: 통합 검증 typecheck 0 · lint 0 · knip clean · test:server **431** · test:unit 478 · build. `net._http_response` 증가분 0.
  - **#209 PR G `guardedDb` — 틀린 근거로 포기했던 커버리지 복원**: `notify-gate.ts`의 "`app.request()`는 dbMiddleware가 **별도 커넥션**이라 SET LOCAL이 안 닿는다"는 인과 서술이 **사실이 아니었다**. 테스트엔 `c.env.HYPERDRIVE`가 없어 `!connStr` 브랜치가 타고 **라우트도 테스트도 같은 `getDefaultDb()` 싱글톤**을 쓴다. 진짜 봉쇄는 라우트가 자기 `c.var.db.transaction()`을 여는데 **SET LOCAL 주입 seam이 없다**는 것. → `guardedDb(db)`(Proxy로 `transaction()`만 가로채 첫 문장에 GUC. 메서드는 원본에 bind — drizzle이 `this.session` 읽음, private 필드 없음 실측) + `setTestDb(db)`(`middleware/db.ts`, **`!connStr` + `NODE_ENV==='test'` 이중 게이트** — ⚠️**로컬 dev도 `!connStr`이라 NODE_ENV 게이트 없으면 로컬 실알림이 조용히 죽는다**, 회귀 테스트로 잠금. prod `HYPERDRIVE` 경로는 seam을 아예 안 읽음). `routes/customers.send.test.ts` 4종(발송→advisor_quotes 커밋 / 요청 completed / DELETE 회수+open 복원 / 앱 미연결 스킵). **안전 근거는 합성** — ①guardedDb가 GUC를 켠다(대조군: 맨 db는 안 켜짐) ②setTestDb가 라우트 db를 바꾼다(identity 프로브). **실측**: 트리거 활성 + 가드 존재 + advisor_quotes INSERT 발생, **그럼에도 `net._http_response` 증가분 0**. **한계**: `db.transaction()` 경로만 커버 — autocommit 단발 쿼리는 여전히 `withNotifyGuard` 필요.
  - **#210 PR H 라우터 병렬화 — 측정 없이 주장하지 않았다**: 라우터는 `question`+`history`만 쓰는데(hits 비의존) `Promise.all([history, embed→search, staffName])` 완주 후 순차 실행되고 있었다. 같은 슬롯으로 이동. **왕복 수·비용은 그대로**(줄지 않음), 벽시계만 `max(임베딩+검색, 히스토리+라우팅)`로 겹친다. **실측(로컬 실 Gemini·실 master)**: 라우터가 일관되게 507~536ms → 자유 질문당 **-507/-513/-536ms**. **골든을 먼저 커밋**(기존 테스트는 최종값만 봐 호출 순서·부정 가드를 안 잠갔다): tool 지정 시 라우터 미호출 / 라우터는 history를 받는다 / call→`runAssistantTool`은 그 뒤 / 임베딩 실패 500·도구 미실행. **변이 검증 실관찰**(history를 `[]`로 → ②가, toolKey 가드 풀면 → ①이 정확히 실패). 겹침 자체도 구 코드 RED 실관찰 후 GREEN. **의도한 행위 변화 1건**: 검색이 먼저 실패하면 이미 출발한 라우팅 응답이 버려진다(실패 경로 라우팅 1회 추가) — `routeAssistantTool`이 자체 catch로 null만 반환해 **500 계약은 불변**.
  - **▶ 다음 미확정**: ①견적 계산엔진(이사님 브레인스토밍 선행 — 선행 정지작업 전부 소진) ②FCM 실기기 e2e ③목록/상세 화면(비 AI) 역할 scope. 배치 3 잔여 저우선 항목(`createQuoteCode "2606"` cosmetic · knip 잔여 unused export 5/type 7 · dismiss 존재검증 현행 유지 · 게이트 3규칙 2벌 **추출 비권장**)은 plan에 근거와 함께 박제.

- **✅ 0709 배치 3 나머지 완료·머지(2026-07-09, PR #206 `7e245c1` · #207 `9bd6f29` · #208 `0463268`)**: 감사에서 나온 dead code·툴체인·소형 정비를 전부 소진했다. **통합 검증 typecheck 0 · lint 0 · knip clean · test:server 412 · test:unit 478 · build.**
  - **#206 dead code + 툴체인**: dead export 3(`stageSignal`·`statusGroupByStatus`—**App.tsx:82이 글자 그대로 동일한 로직을 로컬 재정의**해 쓰고 있었다→lib import로 통합·`listConsultationIdsByUser`—주석이 "임베딩 훅용"이라 오도, 상담 RAG는 #196에서 폐기) · `.advisor-change-pill` 잔재(CSS + `.closest()` no-op 셀렉터) · **`knip.json` 정리 → Unused files 0 · Configuration hints 0**(오탐 9건이 진짜 dead를 가리고 있었다. `drizzle.config.catalog.ts`는 `"*.config.ts"` 글롭이 매치 못 함) · `.env.example`에 `SUPABASE_URL`(없으면 인증 500)·`GEMINI_API_KEY`(**`CRM_BASE_URL`은 죽은 키 아님** — `playwright.config.ts:21` 사용) · `embeddings.test.ts` 랜덤 서픽스.
  - **#207 dead CSS 23클래스**(2545→2515 규칙, −3.5KB): **감사 표에 없던 함정 2개 발견** — ①`@keyframes stage-status-popover-in`은 **live**(이름만 stage-status-*, live 클래스 `.stage-two-step-popover`가 사용) ②`.stage-status-popover, .chance-status-popover` 블록의 `position:absolute`는 아래 블록이 재선언 안 함 → **블록 삭제 금지, 조각만 제거**. **기계 검증 `tools/verify-dead-css.sh` 신설**(재사용): 단순 byte-diff 불가(결합 셀렉터에서 조각을 빼면 **minifier가 같은 셀렉터 두 블록을 접는다** — 계산값은 동일) → ①제거 대상 0회 ②live 15종 등장 불변 + `:has()` 4종 보존 ③접힌 블록은 **계산값** 비교(`z-index:90`이 남아도 같은 블록 뒤 `z-index:160`이 이긴다 — "존재"가 아니라 "마지막에 이기는 값") ④keyframes 정의·사용 유지. 시각 회귀 0.
  - **#208 서버 소형 정비**: `sent:0` 로깅 분리(앱 `send-push`가 토큰 0개면 `{sent:0}` **200** — "아무도 못 받음"과 "N대 전달"이 tail에서 구분 안 됐다. 이 구역 두 사고의 한 겹 아래) · `metaById` 가드(라우팅 도구 경로 hits>0에서 결과 버리는 원격 왕복) · 도구 미러 `nameFilter{Params,Conds,Label}` 뼈대 공유 · `removeOrphanObject` 헬퍼(같은 Storage 삭제 실패를 5곳 무로그 + 3곳 상이 문구 → 단일 grep 토큰).
  - **▶ PR G·PR H 둘 다 완료·머지**(#209·#210 — 위 별도 항목).
  - **남은 것(저우선, plan에 근거)**: `createQuoteCode "2606"`(cosmetic, DB 유입 불가) · knip 잔여 unused export 5 + type 7(파일 내부 사용/배럴/타입 전용) · dismiss 존재검증(현행 유지 합리) · 게이트 3규칙 2벌(**추출 비권장** — 명시성이 실이익).
  - **🚫 적대 검증이 기각(재제안 금지 — plan에 반박 근거)**: `QuoteWorkbench:416-417` onChange+onInput 이중 바인딩 제거(델리게이션 컨테이너라 텍스트만 좁히기 불가 + uncontrolled 금융사 select의 미리보기 갱신 **유일 경로** + Safari sacred rule 위험, 이득은 측정 안 된 sub-ms) · draft 필터 `setCurrentPage(1)` 누락(**이미 호출됨** — 공용 렌더러 `CustomerManagementPage.tsx:720`) · 역할→한글 라벨 맵 파리티(`ROLE_CLAIM_TO_TAB`은 값 타입 `RoleTab`인 **UI 탭 식별자**, `CRM_ROLE_LABELS`는 AI 표시 라벨 — 개념이 달라 공유하면 오히려 잘못) · 라우터 프롬프트에 `customer_consultations` 유도 추가(유도는 **선언 description**에 이미 존재) · `CRM_BASE_URL` 죽은 키 삭제(`playwright.config.ts:21`이 사용) · `.env.example`에 `VITE_*` 추가(`.env.production`이 담당).

- **✅ send-push 인증 헤더 2단계 완료 + 🔴배정 알림 prod 무발송 실버그 발견·수정(2026-07-09, PR #201 f111f83 + PR #202 209b724)**: **CF tail 실측으로 `TypeError: Illegal invocation` 발견** — `pushNotifyDeps.fetchImpl(...)`는 **메서드 호출**이라 `this=pushNotifyDeps`가 되고, Workers의 global `fetch`는 `this`가 globalThis/undefined가 아니면 던진다. `sendAssignmentPush`의 try/catch(best-effort 계약)가 그 예외를 **조용히 삼켰다** → **#193 머지(07-08) 이후 prod 배정 알림이 한 번도 나가지 않았다**(send-push 인증과 무관한 별건). **로컬 bun의 fetch는 this를 안 따져 유닛·로컬 dev로 재현 불가** = prod 전용 실패. 실기기 e2e를 한 번도 안 해 두 달 가까이 미발견(오전 알림 오염과 대칭 — 이번엔 조용히 **안** 쏘고 있었다. 둘 다 "실패가 조용해서 늦게 발견"). **수정** = 호출 전 지역 변수로 뽑아 plain call(`const fetchImpl = pushNotifyDeps.fetchImpl; await fetchImpl(...)` — ESM strict이라 this=undefined). prod에서 멀쩡하던 `gemini-post.ts:14`(`opts.fetchImpl ?? fetch`)가 이미 안전한 형태였다. **`deps.fetchImpl(` 메서드 호출은 src/ 전체에서 push-notify.ts 한 곳뿐**(전수 확인). 회귀 테스트 = fetchImpl을 non-arrow function으로 두고 `this===undefined` 단언(초기값 실패 방향이라 미호출도 잡힘). **prod 실증(19:29 KST)**: `[push] 배정 알림 → user=89ca32c9… "담당 고객으로 배정되었습니다"` — 앱이 `SEND_PUSH_ENFORCE=on`인데 401 없이 성공 = **`X-Push-Secret` 헤더가 실려 인증 통과**(2단계 완료 동시 증명). 대상은 토큰 없는 상담사라 `{sent:0}`·실 푸시 0. 검증 배정은 `assigned_at`까지 psql로 원복. **⚠️ 교훈: Workers에서 `obj.fetchImpl(...)` 금지 — 반드시 지역 변수 plain call.**: 앱의 `send-push` Edge Function이 `verify_jwt=false`+내부 인증 0이라 URL·user_id만 알면 임의 푸시 주입 가능. 앱이 `X-Push-Secret` 공유 시크릿 검사를 도입하는데, **CRM이 그 함수를 직접 부르는 유일한 지점**이 `src/lib/push-notify.ts:31`(소비처 = `routes/customers.ts:158` 고객 담당자 배정 PATCH)이라 헤더를 붙여야 한다. (견적 발송 알림은 `on_advisor_quote_sent` 트리거 경유 = **CRM 코드 0줄**, 손댈 것 없음. CRM 배정 알림 ≠ 앱 `notify_chat_session_assigned` — 고객 담당자 배정 vs 채팅 세션 배정, **중복 아님**.)
  **무중단 3단 배포 — 순서 엄수**: ①앱 = soft 검증 배포(헤더 없으면 warn 후 통과) + 앱 트리거 3개 헤더 부착 → **CRM 영향 0** ②**CRM = 아래 작업** → 앱이 "헤더 없는 호출 0건" 확인 ③앱 = 401 강제 전환 + Sentry warning. **②완료 확인 없이는 ③으로 안 넘어간다**(앱 팀 확약).
  **CRM 작업 순서**:
  1. ~~조건부 헤더~~ **✅ 완료(PR #201 squash f111f83)**: `push-notify.ts`가 `env.SEND_PUSH_SECRET ?? process.env.SEND_PUSH_SECRET`을 읽어 **조건부 헤더** `X-Push-Secret`(fail-open — 미설정 시 헤더 생략 + `console.warn`. 배포 순서가 어긋나도 배정 알림이 안 끊긴다. CRM은 발신 측이라 fail-closed의 보안 이득 0, 반대로 알림이 조용히 사라지는 게 더 나쁘다 — 앱 팀 동의).
  2. ~~401 구분 로깅~~ **✅ 완료**: `res.status === 401` → `[push] AUTH_FAILED(401)` 토큰(tail grep용). 다른 실패(네트워크·5xx)와 분리. 3단계 전환 후 시크릿 누락이 조용히 묻히면 이번 알림 오염과 같은 부류(실패해도 조용)가 된다. 앱은 Sentry warning, CRM은 이 로그 — 이중 감시.
  3. ~~테스트~~ **✅ 완료**: `push-notify.test.ts` 4종(헤더 동봉 / 미설정 시 생략+호출 진행=fail-open 계약 잠금 / 401 구분 / 401 외 기존 문구 유지). server 393 pass.
  4. ~~`.env.example`~~ **✅ 완료**(`SEND_PUSH_SECRET=` 자리, 값 없음 + 로컬 dev 실푸시 주의).
  5. **⏳ 대기(시크릿 수령 후 — 유슨생/이사님 계정 작업)**: CF Pages **Production** secret 등록(Workers & Pages → mrcha-crm → Settings → Environment variables → Production, **Encrypt 필수**. `GEMINI_API_KEY` 선례. **Preview 환경엔 넣지 말 것** — PR 프리뷰가 실 푸시를 쏜다). secret 추가는 기존 배포에 자동 반영 안 됨 → `배포 관리 → Retry deployment` 또는 새 커밋 push. `.env.local` 추가는 선택(넣으면 로컬 배정도 실 푸시).
  6. **⏳ 대기**: prod 배포 후 담당자 배정 1건 → CF tail에 `AUTH_FAILED` 없음 확인 → **앱 팀에 "2단계 완료" 통보** → 앱 3단계.
     **🔴 `device_tokens`는 0행이 아니다(2026-07-09 실측 2행 — 앱 팀 정정)**: `ed7ae30f` 김지안(admin·`dolcejian@kakao.com`, 07-09 13:14 등록)·`873bd465` 유재민(admin·`zmes@kakao.com`, 07-08 20:27). **둘 다 이사님 계정의 웹 브라우저.** 이 둘에게 배정하면 **실제 FCM 푸시가 이사님 브라우저로 날아간다**(오전 알림 오염과 같은 모양). 검증은 **토큰 없는 상담사에게 배정** — 그러면 send-push가 토큰을 못 찾아 `{sent:0}` 200을 반환하고 인증 통과만 확인된다. **토큰은 웹 로그인 때마다 새로 생기므로 미리 조회한 결과를 믿지 말고 배정 직전에 재확인**:
     ```sql
     select user_id from public.device_tokens;  -- 이 목록에 없는 상담사를 고른다
     ```
     현재 안전 대상 = `9b1b3bc3` 자메스관리자(admin, 스모크 계정)·`89ca32c9` 디엘오토솔루션의 혁명적인 개(manager). **주의: 배정자 자신에게는 알림이 skip된다**(`advisorId ≠ c.var.user.id` 조건) — 자메스관리자로 로그인했다면 다른 사람에게 배정해야 발송 경로를 탄다.
  **🔒 시크릿 값은 커밋·PR·이슈·메신저·브리프 어디에도 남기지 않는다.** `.env.local`(gitignore `.gitignore:10`)과 CF Pages secret에만. **주의**: `.env.local`에 넣으면 **로컬 dev 배정도 실 푸시가 나간다** — `PUSH_NOTIFY` 게이트는 `NODE_ENV=test`만 기본 off라 로컬 dev(on)는 실호출한다(현행도 동일). 로컬에서 배정 만질 땐 `PUSH_NOTIFY=off`.
  (앱 몫 별건: `notify-admin`·`check-vonage-balance`도 `verify_jwt=false`지만 트리거·cron 전용이라 앱 팀이 CRM 협조 없이 잠근다.)
- **🔴 운영 알림 오염 사고 → 해소(2026-07-09, PR #199 squash 3766de2 + PR #200)**: **로컬 `bun run test:server` 1회가 운영 디스코드 알림·FCM 푸시 42건을 실제로 발송**하고 있었다(앱 팀 제보). 원인 = `DATABASE_URL`이 공유 master라 `public.consultations`(`on_consultation_created`)·`public.advisor_quotes`(`on_advisor_quote_sent`)에 픽스처 INSERT하면 `net.http_post`→Edge Function이 나간다(테스트가 행을 지워도 트리거는 이미 발화). **`PUSH_NOTIFY=off`는 무력** — CRM 앱 코드(`push-notify.ts`) 게이트일 뿐 Postgres 트리거는 프로세스 env를 못 본다. CI 아님(workflow 없음) = 로컬 수동 실행마다 재발. **해법** = 앱 팀 트리거 가드(`20260709103000_skip_notify_guard.sql`: `current_setting('app.skip_notify',true)='on' AND session_user='postgres'` → RETURN NEW) + CRM은 **`withNotifyGuard(db, (tx) => …)`**(`src/test-utils/notify-gate.ts` = `db.transaction` + `set_config('app.skip_notify','on',true)`)로 픽스처를 감싼다. INSERT/UPDATE는 그대로, 알림만 스킵.
  **함정 6**: ①값은 소문자 `'on'` 엄격 비교 ②SET LOCAL은 **트랜잭션 안에서만** — `db.insert()` 단독은 자동커밋이라 안 걸림 ③`updateQuote(…,ex)`처럼 Executor 받는 프로덕션 함수는 `tx`를 넘기면 내부 `upsertAdvisorQuote`까지 같은 트랜잭션 ④**`application_name` 방식 불가** — Supavisor pooler가 덮어씀(6543·5432·startup options 전부 실측) ⑤`session_user`는 pooler·SECURITY DEFINER 경유에도 `postgres` 유지(가드 조건 충족 실측) ⑥**세션 레벨 `SET`(is_local 없이) 금지** — transaction pooler가 백엔드를 재사용해 GUC가 남으면 다른 `postgres` 커넥션(dev 서버·백필)의 **진짜 알림이 조용히 묻힌다**(앱 팀도 자기 제안을 철회). `notify_advisor_quote`는 `AFTER INSERT OR UPDATE`지만 첫 분기 `IF TG_OP='UPDATE' AND NEW.sent_at IS NOT DISTINCT FROM OLD.sent_at THEN RETURN NEW` 때문에 **`viewed_at` 스탬프로는 알림이 안 나간다**(INSERT + `sent_at` 실변경 UPDATE만).
  **라우트 경로(PR #200)**: `app.request()`는 dbMiddleware가 별도 커넥션이라 SET LOCAL이 안 닿는다 → GUC로 풀지 않고 **알림 테이블을 아예 안 건드리게** 짠다. 발송 훅은 고객에 `app_user_id`가 있을 때만 돌므로(`customer-quotes.ts:214` `if (!appUserId) return`) `seedLocalCustomer()`(app_user_id 없음)를 쓰면 훅 자체가 안 탄다. 게이트·`NOTIFY_TRIGGER_TESTS`·`test:server:notify` **전부 삭제**(스위치가 남아 있으면 누군가 켠다 — 실제로 한 번 켰다).
  **⚠️ 의도적으로 포기한 커버리지**: 라우트 → `updateQuote` → `syncAdvisorQuoteOnSend`(`customer-quotes.ts:210`) **통합 경로 전체는 이제 어떤 테스트도 타지 않는다**. 라우트가 `customerId`를 잘못 넘기거나 트랜잭션 경계가 어긋나는 회귀는 안 잡힌다. 각 조각(`valid_until` 스탬프=`updateQuote`, `advisor_quotes` upsert=`send.test.ts`)은 검증되지만 **"이미 커버된다"고 믿고 그 경로를 건드리면 안 된다**. `app.request()`가 별도 커넥션을 여는 한 안전히 테스트할 방법이 없어 감수한 것.
  검증: test:server **389 pass/0 skip/0 fail** + `net._http_response` 증가분 **0**(원래 42) 실측. `pg_net`은 트랜잭션 안전(롤백 시 알림 취소 — 실측, 향후 롤백 패턴 여지). chat_messages/chat_sessions 쓰는 CRM 테스트는 0건. 규칙 SSOT = `AGENTS.md`. (앱 몫 잔여: `notify-admin`이 `verify_jwt=false`라 외부에서 가짜 알림 주입 가능 — 앱 팀이 pg_net 헤더 키로 차단 예정, CRM 영향 없음.)
- **✅ 카드 UI 상태 CardUiState 통합 완료·머지(2026-07-09, PR #198 squash b6088ad)**: 견적 워크벤치 비교카드(`견적 작성 1/2/3`)의 UI 상태를 속성별 `Record` 8벌(`manualTermMonths`·`manualDepositModes`·`manualDownPaymentModes`·`manualResidualModes`·`manualMileageModes`·`manualMileageValues`·`manualCarTaxIncluded`·`manualSubsidyApplicable`) → **카드별 `CardUiState` 객체 1벌**(`cardUi: Record<cardId, CardUiState>`). **행위 무변경 순수 리팩토링**(계산엔진 선행 정지작업 — 브리프 잔여 보류 항목 소진). 동기 = `Record` 키 누락이 읽는 쪽 폴백(`?? 60`/`?? "basic"`/`?? false`)에 조용히 흡수돼 컴파일러가 못 잡던 것(#163 저장 payload 오염의 구조적 원인). 결과: `clearCardUiState()` 8줄→`setCardUi({})` 1줄 · `openEditQuote` 복원 8줄→`cardUiMapFromScenarios` 1줄 · 미리보기 effect deps 11→4 · 기본값 `DEFAULT_CARD_UI` SSOT(통합 전 폴백과 동일 값 테스트로 잠금) · `MILEAGE_BASIC_VALUE` 상수(리터럴 4벌 해소). **이중 소스 해소** — `depositMode`/`downPaymentMode`/`residualMode`가 `Record`와 `ManualCard` 양쪽에 있던 것(`?? card.depositMode` 폴백)을 `ManualCard`에서 제거, `CardUiState`가 단일 소스(금액 표시값 `depositValue` 등은 uncontrolled defaultValue라 카드 잔류). **기존 회귀 테스트 승계** — `useQuoteWorkbench.residue.test.tsx`(#163 잔상 그물)를 새 구조로 이전해 행위 무변경 증명으로 재사용(승격 케이스는 `cardUi` 통째 비교로 assertion 강화). **범위 밖(의도)**: 금액 입력칸 uncontrolled DOM↔state 이중성(#168 discount_lines 스냅샷 설계) · `discountLines`/`acquisitionTaxMode`/`primaryDiscountUnit`(카드별 아님 — `clearCardUiState`가 계속 함께 청소). plan `ref/plans/2026-07-09-crm-card-ui-state.md`. 검증 typecheck0·lint0·unit477(신규 13)·server389·build + **격리 스택 브라우저 스모크 5종**(신규 저장→psql 대조 전필드 일치 · 수정 재진입 복원+카드2/3 잔상0 · 약정거리 basic 복귀 리셋 · 초기화 잔상0 · 승격 시드 카드1만 `기간36·선수금%`). 스모크 견적 UI 삭제 경로 원복(psql 직접 삭제 금지 — 임베딩 고아), baseline 복귀·고아 0 실측.
- **✅ 고객 지역 거주지 단일 소스(2026-07-07, PR #191 squash e8251ea)**: 견적 워크벤치 "고객 지역"(`guidance.customerRegion`) **입력 select 제거** → 거주지가 단일 소스. `regionFromResidence`가 거주지 전체(구/시까지, "·"→공백 `인천광역시 남동구`) 반환, **신규·수정 진입 양쪽** 거주지 파생(저장본 무시 — 옛 "확인 필요" 최신 거주지로 갱신). `guidance.customerRegion` 필드·payload 계약 불변(앱 파리티 무변경)·저장 guidance 불변(백필 소급 불필요). 안 쓰이게 된 시·도 옵션목록/regionOptions import 정리. 발송본은 advisor_quotes 박제(불변), 미발송 재열람 시 최신 반영. 검증 typecheck0·lint0·unit458·build.
- **✅ 업무 AI 견적 조회 정확성(2026-07-07, PR #192 squash 6b36a74)**: 실기 — "김지안 견적 몇개"에 **존재하지 않는 QT-2607-0001 쏘렌토 환각**(실제 QT-0005 BMW 발송완료·QT-0007 쏘렌토 작성중). 원인 2겹: ①`search_customers`가 견적 미조회 ②게이트가 "근거0건에서만 라우팅"이라 견적질문이 견적/프로필 청크 잡혀 RAG로 새고 top-k 나열론 개수 환각. 수정: **`customer_quotes` 도구**(name 부분일치, `crm.quotes` 직접 — 코드·차종·발송상태, brand/model/trim_name 컬럼 상주라 catalog 조인 불요·scope 균일)+**라우팅 우선 게이트**(`!tool&&hits===0`→`!tool`: 근거 있어도 라우터 call이면 도구, 서술형 none/null이면 RAG 유지, none 안내문구는 hits0일때만)+**근거 이름 중복 제거**(`stripChunkCustomerPrefix` — "김지안·고객 김지안"→"김지안·", 임베딩 불변·재임베딩 불필요). 오라우팅 방지=라우터 프롬프트+**양방향 골든 2**(서술형+근거→none→RAG/집계+근거→call→도구). 검증 typecheck0·lint0·server355·실Gemini 라우팅/오라우팅 e2e(견적·오늘할일→call, 근황·요약·"어떤사람"→none). 클라 변경0. **업무 AI 도구 8종**(+customer_quotes). 추가 도구 후보는 이사님 holding.
- **✅ 배정 알림 슬라이스 2 완료·머지(2026-07-08, PR #194 squash 2141c34)**: 실시간 상담 수신 On/Off 영속 + 배정 드롭다운 Off 제외. **저장소=`crm.staff_settings` 신설**(staff_user_id uuid PK·live_receiving bool default true·마이그 0024, **CRM 내부용 확정** — 앱 미소비라 `public.profiles` 컬럼 배제·앱 마이그 협의 불요). **백엔드** `GET/PATCH /api/me/live-consulting`(본인 수신 upsert, PATCH=sendJson 관례·수동 JSON 파싱으로 malformed body 400 보장)+`GET /api/staff`에 `liveReceiving` 비파괴 추가(staff_settings LEFT JOIN coalesce true, **고객 담당자 배정은 무시**). **배정 경로 통합** — supabase-js 직결 `fetchStaffOptions` 제거→백엔드 `GET /api/staff`(`fetchStaffDirectory`) 재사용(중복 제거), `ChatSessionHeader`는 후보만 `liveReceiving` 필터·**`assignedName`은 전체 목록 해석**(이미 배정된 Off 상담사 이름 유지). **Topbar** 마운트 GET 로드(딜러 제외·실패 시 기본 true)+저장 PATCH(낙관·조용한 롤백=onToast 부재·`touchedRef` in-flight 덮어쓰기 가드). 배정 알림 훅(슬라이스1) 무수정(Off면 배정 자체 안 됨→알림 자연 억제). spec `ref/specs/2026-07-08-crm-live-consulting-receiving-design.md`·plan `ref/plans/2026-07-08-crm-live-consulting-receiving.md`. 검증 typecheck0·lint0·unit459·server367(실 master GET/PATCH 왕복·staff liveReceiving coalesce 실증)·build + 브라우저 스모크(magiclink admin: Off 저장→psql live_receiving=f→새로고침 Off 복원→baseline 원복). **캐시 stale 허용**(fetchStaffDirectory 세션 캐시 — 배정 콘솔 완전 실시간 필터는 follow-up). **미래 앱이 이 상태 읽어야 하면 public 이관 재설계**(현재 CRM 내부용 확정).
- **✅ 배정 알림 FCM 슬라이스 1 완료·머지(2026-07-08, PR #193 squash 108e670)**: **핵심 발견 — 앱이 서버 측 FCM 발송 인프라를 이미 prod 배포·소유**: `public.device_tokens`(**⚠️ 0행 아님 — 2026-07-09 실측 2행, 둘 다 이사님 계정 웹 브라우저. 배정 검증 시 이 목록에 없는 상담사를 골라야 실 푸시가 안 나간다. 로그인 때마다 늘어나므로 배정 직전 재조회 필수**) + 트리거 `on_advisor_quote_sent`(advisor_quotes INSERT/재발송 UPDATE)·`on_staff_chat_message` → `net.http_post` → `send-push` Edge Function(device_tokens read→FCM HTTP v1→만료 토큰 정리, 시크릿=**Supabase Edge `FCM_SERVICE_ACCOUNT`**). 앱 딥링크 `sanitizePushLink` 허용=`/chat`·`/my_quotes/detail/`·`/my_quotes/advisor/`(고객 화면만). → **브리프 구 A안(CRM이 FCM v1 직접 발송)·CF Pages 시크릿 전제 폐기**(하면 중복 푸시). "B안(Edge+웹훅) 과설계 기각"도 틀림 — 앱이 정확히 B안 채택. **견적 발송→고객 = CRM 코드 0 자동**(CRM advisor_quotes upsert #159 → `on_advisor_quote_sent` 트리거 → send-push → 고객 "견적이 도착했습니다"+딥링크). **PR #193 CRM 몫 = 고객 담당자 배정**(crm.customers.advisor_id 백엔드 PATCH → `holdWork`→`sendAssignmentPush`(send-push 호출, **CRM은 FCM 직접 발송 안 함**), 딥링크 없는 알림="CRM에서 확인"). 발송 조건: advisorName 실변경 + advisorId NOT NULL(대상 user_id) + advisorId≠배정자(`c.var.user.id`) — **배정 기능은 self 허용, 알림만 skip**(저장 로직 불변). `src/lib/push-notify.ts`(sendAssignmentPush + `assignmentPushEnabled` 게이트=embed-on-write 대칭 NODE_ENV=test 기본 off, `test:server`에 `PUSH_NOTIFY=off` 프리픽스 2중 방어) + `getCustomerAdvisorName`에 name 추가. 검증 typecheck0·lint0·server362·build. spec `ref/specs/2026-07-08-crm-assignment-push-notification-design.md`·plan `ref/plans/2026-07-08-crm-assignment-push-notification.md`(부록 A=앱 트리거 계약). **앱 몫 = ✅ 완료·적용 확인(2026-07-08 psql/secrets)**: ①`chat_sessions` 배정 트리거 master 적용(`notify_chat_session_assigned` SECURITY DEFINER + 트리거 `on_chat_session_assign` enabled, 앱 마이그 `20260708100000` — 부록 A 계약 일치·트리거명/title 문구만 미세차·EXECUTE는 postgres만=`20260708110000` REVOKE authenticated/service_role 강화) ②`FCM_SERVICE_ACCOUNT` 시크릿 설정됨(2026-07-07, `supabase secrets list` 확인 — 파이프라인 유일 미확인 지점이었으나 해소). **남은 것 = 실기기 e2e만**(상담사 앱 실기기 로그인 후 배정→앱 푸시 수신. **웹 토큰 2행은 이사님 브라우저라 e2e 대상이 아니다** — 실기기 토큰이 따로 필요). **슬라이스 2 = ✅ 완료·머지(PR #194 — 위 별도 항목).** follow-up(PR #193 본문): tag 필드 비대칭(§5.4 각주↔구현 3필드), 동명이인 상담사 재배정 시 알림 유실(기존 assignedAt advisorName 비교 로직).
- **✅ 앱 상담신청 → CRM 통합 완료(2026-07-08, 유슨생 세션 0708-book-consultation, 브랜치 `feat/crm-consultation-integration`·PR 대기)**: **서버**=`consultations` read-only 미러(drizzle export `consultationRequests` — `crm.consultations` 이름충돌 회피)+`src/db/queries/consultations.ts`(list·listByUser·listIdsByUser·createCustomerFromConsultation·linkConsultationToCustomer, **폼 우선 phone**·**userId nullable guard**·app_user_id **dedupe(source 최초 유입 유지)**)+`/api/consultations`(GET·link·create-customer·**DELETE**)+customers `/:id/consultations`. source 어휘 `앱 상담신청`+**CHECK 마이그 0025**. **UI(고객 상세)**=상담신청 문의 카드를 견적요청 카드와 **같은 `kim-needs-request-scroll` 스택에 통합**(별도 리스트 아님 — 유슨생 지시), 읽기전용(carModel 값있을때만·문의사항 notes)+앰버 톤·MessageSquareText 아이콘 구분. **CRM 전용 삭제**(유슨생 지시)=`crm.consultation_dismissals`(**마이그 0026**)+`DELETE /api/consultations/:id`(dismiss insert만 — **`public.consultations` 절대 불변**)+목록서 dismissed 제외+카드 **인라인 확인**(X→삭제/취소, 오클릭 방지). 검증 typecheck0·lint0·unit464·server387·build + **브라우저 종단 스모크**(제임스 CU-2606-0002 45건: 스택 통합·삭제후 public 45 불변·새로고침 영속·확인 흐름, 공유 master 원복). spec/plan Open Questions 실측 확정 갱신됨(OpenQ1 폼 우선). follow-up: 삭제 복원 UI 없음(dismissal은 DB에 남아 복구 가능).
- **✅ 업무 AI 상담 Q&A = `customer_consultations` 도구 완료·머지(2026-07-08, PR #196 squash 5050246)**: "○○ 상담 문의 뭐였어" 응답. **⚠️중요 학습 — RAG 임베딩 접근 폐기, 도구로 피벗**: 처음 Task 7을 consultation_request 임베딩 청크로 구현(브랜치 완성+백필 44)했으나 **실 /ask e2e에서 어시스턴트가 tool-first(#171~#192)라 고객명 상담 질문이 RAG에 도달 못 하고 tool로 라우팅→상담 도구 부재로 "상담 안 했다" 오답** 발견. 견적 환각을 customer_quotes로 고친 것과 동일 구조 → **`customer_consultations` 도구**(customer_quotes 미러: public.consultations를 app_user_id로 CRM 고객 연결·dismissed `notExists` 제외·이름+scope 필터, read-only)로 대체. 라우터는 ASSISTANT_TOOL_DECLARATIONS 파생이라 자동 배선. **임베딩 접근 완전 원복**(master에서 consultation_request 임베딩 44 삭제·embeddings check 0026 복원·drizzle 저널 0027 제거 — 마이그 순번은 0025·0026까지만 유효, **0027은 존재하지 않음**). 검증 typecheck0·lint0·unit464·server389 + 실 Gemini /ask e2e(dismissed 제외 확인). **🔴 정정(2026-07-09, PR #203)**: 당시 e2e를 "제임스 상담 내역 **정확 나열**"로 기록했으나 **사실이 아니었다** — 도구가 `orderBy(createdAt)` ASC라 44건 중 오래된 30건만 나오고 최신 14건이 잘려 있었다(절단 문구도 "외 14명" 오표기). 나열된 행이 실재하는 상담이라 e2e 눈으로는 정답처럼 보였다. **교훈 2개**: ①고객명 factual 질문은 도구가 정답, RAG는 서술형만 도달(원래 교훈) ②**"정확 나열"류 검증은 총건수를 DB와 대조하지 않으면 절단·정렬 결함을 못 잡는다** — 상한 있는 리포트는 반드시 `count(*)` 대조.
- **⏸ 보류(나중, 유슨생 지시 2026-07-08): 상담-우선 유저 승격 인박스(Task 8)**: 상담만 하고 견적요청 없는 유저(김지안 37건·Jian Kim 1건 — CRM 미연결)를 고객으로 승격하는 UI. 백엔드는 상존(`create-customer`/`link` 라우트·`GET /api/consultations` pending·`listConsultations`), **트리거 화면만 부재**. **배치 확정 — 새 항목 아님**: 사이드바 **상담사 배정 > "상담 신청 DB"**(`advisor-assignment-db`)가 이미 그 자리(이름 그대로). **현재 스텁** — `Sidebar.tsx`가 `setSelectedDraftMenu`로 **메뉴 하이라이트만** 바꾸고 App.tsx에 라우트/렌더 없음(공사중 대시보드류 placeholder, 형제 "실시간 상담 요청"=`advisor-assignment-live`도 스텁). 구현 = 이 스텁을 **실제 navigate + 인박스 페이지 렌더**로 배선(앱 견적요청 인박스 `AppRequestsPage`/`/app-requests` 미러 참고). **착수 전 확정 필요 2건**: ①"상담 신청 DB" 범위 = 앱 상담신청(`public.consultations`)만 vs 더 넓게(앱 견적요청 통합/수기 상담 포함) ②행 단위 = per-consultation(83건 noisy·DB이름엔 맞음) vs per-user 그룹(3명·승격 실용, 앞서 권장). 실가치는 앱 로그인화 후(현재 승격 대상 과도기 2명). — (착수 근거·선행조건 원문은 아래 유지) 설계 노트 **`ref/specs/2026-07-07-crm-consultation-request-integration-design.md`** + **구현 계획 `ref/plans/2026-07-07-crm-consultation-request-integration.md`(8태스크, Task1~5 완전코드·Task6~8+OpenQ5 앱계약 의존)**. 핵심 = **앱 유저 1=CRM 고객 1(`app_user_id`), 견적요청+상담신청이 한 워크벤치**. 상담신청(`public.consultations` — 견적요청과 달리 **전화번호 O**, 폼 입력)은 현재 CRM 미연결 — 견적요청 패턴(`createCustomerFromRequest`·`app_user_id` 통합·`phone→연락처` :344 상주) 재사용해 붙인다. 상담신청=**읽기전용 문의 카드(건별, 김민준 니즈카드 UI 재사용, 수정 안함**=앱 원본 진실 보존, 상담사 정리는 need_memo 등 별도), `source`=최초 유입 고정(`앱 견적요청`/**`앱 상담신청` 신규**, 이후 활동은 카드 누적), 채팅 아이콘 나중(실시간 채팅). **선행 3조건 전부 충족 확인(2026-07-08 앱폴더/DB 실측)**: ⓐ번호 강제=앱 배포(`20260708023000_add_required_authentication`)+상담신청은 폼 phone 자체 확보(book_consultation `required`·CHECK `^010[0-9]{8}$`)라 소급 불필요, ⓑ로그인화=`ai-analyst/tools/book_consultation.ts` Edge가 **항상 `user_id`(인증 유저)** 삽입(consultations **83행 전부 user_id·phone 보유**), ⓒDDL 계약=실측 컬럼(`id·user_id·customer_name·phone_number`(NOT NULL)`·car_model·notes·status·created_at`)이 plan 가정과 **완전 일치**. **OpenQ1(이름/번호 소스)=폼 우선 확정**(book_consultation이 폼값 저장·profiles는 11명 중 **9명 phone 공란** — plan 서술 'profiles 우선'은 오류, 코드 라인244 `req.phoneNumber||profile`은 이미 폼 우선이라 정합 → **서술만 정정 필요**). OpenQ2 car_model=값 있을때만 노출 방어, OpenQ3 status=read-only 시작(단 consultations RLS에 'Staff can update consultation status' 정책 상존—추후 전이 가능), OpenQ4 중복=app_user_id dedupe·source 최초고정, OpenQ5 인박스=UX 구현시. **번호없는 테스트 고객(profiles 9명 카카오)은 통합 무관** — 상담신청은 폼 phone 보유, 오히려 통합이 빈 CRM 연락처 채우는 경로(Task5 보강). **착수 = plan/spec Open Questions를 이 실측으로 확정 갱신(OpenQ1 서술→폼 우선) 후 Task1~8 subagent-driven.** 상담신청 생성 흐름=앱 AI 채팅 a2ui 폼(chat_forms.dart consultationForm: customer_name+phone_number)→book_consultation Edge insert.
- **✅ 0706 리팩토링 배치 2 완결(2026-07-06 심야 2차, 유슨생 세션 0706-AI-refactoring)**: #168~#178 머지 범위를 0705 방식으로 점검 — **4앵글 감사(코퍼스 파이프라인·라우트/도구/scope·클라·크로스커팅) → 후보 27건 전건 적대 검증(CONFIRMED 19·ADJUSTED 8) → 6PR 전부 squash 머지**. 계획/기각/보류 SSOT = `ref/plans/2026-07-06-crm-refactor-batch.md`. **A(#179)** 정합성 3건: 승격 create-customer의 customer_profile 훅 누락(상담경로/구매방식 근거 갭) + staff 디렉토리 ADVISOR_ROLES 분리(dealer 노출 시 fail-closed 모순)·orderBy 정렬 + link 중복 연결 409 가드(`ConflictError`→run() 409, app_user_id 중복 고객 유일 경로 차단)+로더/백필 고객 해석 결정화. **B(#180)** 활동 파생 seam: #171 lastActivityAt↔#154 staffActivityAt 자식 집합 드리프트(quotes↔documents — 목록 배지·AI 리포트 화면 내 모순) → `db/queries/activity.ts` SSOT(합집합 5자식)+STALE_THRESHOLDS 클라 파리티 테스트+quotes(customer_id,created_at) 인덱스 마이그 0022(적용). **C(#181)** 서버 소형 9건: DELETE 임베딩 정리 4벌→cleanupEmbeddingOnDelete·KST 헬퍼 `lib/kst-date.ts` 중립 이동(kstTodayDate 복제 제거)·searchEmbeddings CustomerScope 직결(string[] dead 분기 제거)·StreamAskArgs.hits dead 제거·TOOL_DECLARATIONS Record화·도구 2쿼리 병렬화·stale/delivery 리포트 상한 30+'외 N명'·백필 옵션 inArray 필터·assistant.test.ts ragFakes/askJson 위생. **D(#182)** 코퍼스 로더↔백필 미러 공유: 프로젝션 상수 5종+quoteChunkTextOf/quoteRequestChunkTextOf+ORPHAN_PREDICATES(고아 술어를 빌더 규칙 곁 병치) — **백필 실 실행 161청크 전건 hash skip(행위 무변경 기계 증명)**. **E(#183)** 어휘 SSOT: 요청 라벨 2맵 `client/src/data/quote-request-labels.ts` 물리 공유(서버 복제본 삭제)·`APP_QUOTE_REQUEST_SOURCE` 상수(SOURCE_AUTOMATIC_OPTIONS[0] 참조)·appStatus "viewed" 축소(zod 400 회귀 테스트+CHECK 마이그 0023 적용·viewed 0행 실측). **F(#184)** 클라: discountLineWon 산술 1벌화(역산↔정산 load-bearing 불변)·advisorOptionsByTeam 목업 폐기(ADVISOR_TEAMS·parseAdvisorTeam)·할인 세그먼트 겹침 CSS(auto 트랙 2px 붕괴+페인트 순서 — col3 max-content, **격리 스택 실기: 히트 4/4·겹침 0·넘침 0 실측**). **최종 main 통합 검증 green: typecheck 0·lint 0·server 343·unit 457·build.**
  **이사님 확인 2건**: ①B 활동 파생 합집합 — 목록 관리 상태 배지가 견적 활동을 인정(덜 stale 방향으로만 변화, PR #180 본문) ②A-3 link 충돌은 409 거절로 fail-closed — "기존 연결 고객으로 유도" UX 대안 판단(PR #179 본문).
  **잔여 보류**: 백로그 B 트리거(미충족)만 남음. (카드 Record→CardUiState = **✅ 해소, PR #198** — 위 별도 항목.) 07-07 해소 2건: ①dead CSS 동적접두 10종(#189 — 7종 dead 삭제 기계 증명·option-picker-dot--{3,4,5}는 `group % 6` 생성 live 확정) ②**앱카드 라벨 헬퍼 물리 1벌화(#190, 유슨생 승인 — 이사님 사후 확인)**: `client/src/lib/app-card-labels.ts` 신설(부작용 0 계약)+서버→클라 순수 모듈 import 경계 규칙 AGENTS.md 신설. 파리티 테스트+백필 161 전건 hash skip으로 행위 무변경 증명. stampLabelOf(타임존 상이)·guidanceOf·discountRowLabel(의도 이탈)은 공유 제외.
- **✅ 업무 AI 사용자 인지(2026-07-07, PR #186 squash bb5041f)**: 유슨생 실기 지적 2건 — "난 누구야?"(정보 부재)·"내가 계약한 고객?"(admin scope=all이라 '내가' 소실, 전체 계약완료 나열 오답). **`current_user` 도구**(이름·역할 한글 CRM_ROLE_LABELS·담당 고객 수, scope 무관) + **search_customers `mine` 필터**(질문 의도 — 권한 scope와 별개 개념, staff는 scope 강제 불변·admin/manager에서 1인칭 의미 성립) + `runAssistantTool(key,params,scope,user,ex)`(AuthedUser 전달) + **withCurrentUserContext**(/ask 양 경로 프롬프트에 "현재 로그인 사용자는 이름(역할)" — getStaffName 병렬 조회) + TOOL_SYSTEM_PROMPT "빠짐없이 나열"(7건 조회 5명 나열 실측 누락 대응). 검증 4종+build(server 349·unit 457) + 실 Gemini e2e 3종(자메스관리자 정답·mine 0건 정답·7명 전부 나열, 대화 원복).
- **✅ 업무 AI 범위 밖 질문 안내 문구(2026-07-07, PR #185 squash 0f57311)**: 유슨생 실기 지적("오늘 날씨?"에 NO_HITS 문구 — 고장처럼 읽힘) 해소. `routeAssistantTool` 판정 3갈래(`RoutedToolDecision`) — call(도구 실행)/none(모델의 "해당 없음" 명시 판단 → `OUT_OF_SCOPE_ANSWER` 안내 문구)/null(실패 — 범위 밖 단정 없이 기존 NO_HITS 폴백 유지가 계약). /ask 양 경로 emptyAnswer 분기+관측 로그. 검증 4종+build(server 346·unit 457) + 실 Gemini e2e(날씨→안내 문구·"앱 유입 고객"→도구 라우팅 8명 골든 불변, 대화 원복). 이 e2e가 배치 2 실기 follow-up ①(업무 AI 종단)도 겸함 — 잔여 실기 follow-up: ②목록 배지 스팟(B 행위 변경) ③link 409 클라 에러 표면.
- **▶ 다음 미확정 — 후보**: ①견적 계산엔진(이사님 브레인스토밍 선행 — **선행 정지작업 CardUiState는 #198로 완료**, 카드 조건이 계산 입력이라 이제 계산 로직만 얹으면 된다) ②목록/상세 화면(비 AI) 역할 scope(#176은 AI 조회만). (**guidance 지역 옵션 확장=해소** — 거주지 입력 `regionOptions`가 전국 17개 시·도(전북·전남·제주 포함) 상주 + `customerRegion`은 #191로 거주지 파생(`regionFromResidence`가 raw 그대로 반환)이라 옵션 셋 개념 소멸.) (FCM 배정 알림: 슬라이스 1 #193·슬라이스 2 #194·앱 몫(트리거+시크릿, 2026-07-08 확인) 완료 — 실기기 e2e만 남음.)
  **FCM 선행 조건(구 2026-07-07 판단)은 폐기·해소** — 앱이 이미 Firebase 완비+서버 발송 인프라(트리거+send-push Edge) prod 배포 완료(위 ✅ 참조). 구 A안(CRM 직접 FCM v1 발송)·CF Pages 시크릿·"이사님/앱 선행 ⓐ~ⓔ" 전제 전부 무효. 실제 시크릿=Supabase Edge `FCM_SERVICE_ACCOUNT`.
  **공통 함정(업무 AI 코퍼스 작업 시)**: EMBED_ON_WRITE 게이트 3규칙(직접 `bun test <파일>` 금지 — 반드시 `bun run test:server`), 견적/메모 psql 직접 삭제 금지(임베딩 고아), 실 DB 픽스처 랜덤 서픽스, 빌더 변경 = hash 불일치 → 백필 재실행 소급(라벨 SSOT가 `client/src/data/quote-request-labels.ts`로 이동 — 이 값 변경도 청크 content 변경).
- **✅ 코퍼스 확장 ④ 앱 견적요청 완료·머지(2026-07-06 심야, PR #178 squash 913b302)**: "앱으로 뭘 요청했었지?" 해소(클라 변경 0). **`quote_request` 청크**(요청당 1행 — 조건 밀도가 견적과 동급이라 aggregate 아님) = 요청일(KST)·차량(카탈로그 라벨)·방식 한글·기간·보증금 병기(유형+%+금액)·옵션·차량가. **status 미포함**(앱 write 전이를 CRM 훅이 못 쫓아 스테일 박제 — 견적 열람 미포함과 동일 논리). 고객 연결 = **app_user_id 직접 연결만**(phone 매칭은 표시용 휴리스틱). **승격 훅** = link/create-customer 성공 시 그 유저 요청 전부 스케줄(연결이 생겨야 적재 가능한 정확한 시점, hash skip이 기적재 흡수) — **기연결 유저의 신규 앱 요청은 백필이 유일한 보정 경로**(주기 보정은 CF Cron 검토 보류). `lib/quote-request-labels.ts` 신설(PAYMENT/DEPOSIT 한글 서버 SSOT — queries NEED_METHOD_LABEL 복제 해소). CHECK 마이그 0021(적용) + **백필 실 실행 99건 소급**(코퍼스 62→161청크). 검증 4종+build(server 342·unit 454) + 승격 배선 테스트(스모크 요청→create-customer→임베딩 실측→cascade 원복) + 실 Gemini e2e(김지안 요청 4건 목록 정답·제임스 G 63 정확 인용). follow-up: 테스트 유저 다요청이 TOP_K 점유(실운영 완화 관찰), consultations 자동 수신 도입 시 훅 추가.
- **✅ 배정 UI 디렉토리화 완료·머지(2026-07-06 심야, PR #177 squash e9fec19 + 브라우저 실기)**: `ADVISOR_NAMES` 하드코딩 목업 폐기(사용처 0·상수 제거) — 상세 편집기+목록 순환/필터 2곳 전부 **`GET /api/staff` 디렉토리 기반**. `client/src/lib/staff.ts`(세션 캐시+inflight dedupe+`staffNameOf` 동기 해석+`useStaffDirectory`). 편집기 담당자 select = **uncontrolled defaultValue(Safari 함정 무관), 디렉토리 도착 시 key 리마운트 재시드**, 초기 선택 advisorId 매칭 우선·이름 폴백, 팀별 담당자 필터 목업 폐기(팀 select는 표시 잔존). `saveAdvisorField` = `{advisorName, advisorId, team}` 동봉(해석 실패 시 저장 차단)+낙관 advisorId 상태·롤백. 클라 타입 advisorId 배선(서버 getTableColumns라 이미 반환 — 타입만). 검증 4종+build(unit 454·server 337) + 실기(필터 실 직원 4명·강현준 배정→psql advisor_id 실측·재편집 id 매칭·profile 청크 재임베딩 실측·원복). follow-up(PR 본문): ~~목록 순환 배정 버튼~~(**이미 제거됨 — 2026-07-10 실측, 코드에 없다**), "선택 고객 배정 변경" **일괄 버튼은 여전히 목업**(onClick 없음), 팀 select 어휘·존폐.
- **✅ 역할 scope 실제화 완료·머지(2026-07-06 심야, PR #176 squash c5cd176)**: 이사님 요구 이행 — **admin/manager=전체("all"), staff={advisorId:sub}, dealer 등 그 외 role도 fail-closed**(resolveCustomerScope 본문 교체, B1 seam). searchEmbeddings `{advisorId}` 변형(customers.advisor_id 서브쿼리 — 전 코퍼스가 customer_id 기반이라 고객 scope 하나로 걸러짐) + `runAssistantTool(key,params,scope,ex)` 도구 6종 균일 필터(버튼·라우팅 양 경로). **배정 정합 규칙**: PATCH advisorId(uuid) 수용, advisorName만 변경 시 id 자동 해제(스테일 id로 남의 고객이 타 상담사 scope에 잡히는 사고 방지). `GET /api/staff` 디렉토리 신설. **백필 1건**(김지안 — profiles의 customer 역할 동명이인은 role 필터로 배제, 이름 매칭 실제 깨지는 케이스 실존). 검증 4종+build(server 337·신규 12) + 실 Gemini e2e(staff 본인 담당 정답/남의 고객 "조회 결과 없음" — 누출 0·존재 비노출/버튼 도구 본인 행만/admin 전체 불변). follow-up: 목록·상세 화면(비 AI) scope 별도, staff 실계정 생기면 크로스 실기.
- **✅ 코퍼스 확장 ③ 견적 청크 보강 완료·머지(2026-07-06 밤, PR #175 squash 23574df)**: buildQuoteChunkText에 **할인 구성**(`할인 6,700,000원 (재구매 할인 200,000원)` — percent 지원·0원 행 병기·jsonb 방어 파싱)+**비교안**(대표 제외 시나리오 목록) 추가, 콜사이트 2곳 배선(필수 필드로 컴파일러가 누락 방지). **백필 소급 실행**(6 재임베딩·56 skip — 할인/비교안 있는 견적만 변경 실측). e2e: 앵커형 "제임스 520i 할인 조건" → 총액+재구매 할인 정답, G80 비교안 정답. **알려진 한계(실측)**: 앵커 없는 라벨 집계 질문("재구매 할인 들어간 견적 뭐 있어?")은 미스 — 할인 든/안 든 견적 유사도 무변별(0.72~0.745, 서비스 보일러플레이트 희석), 임계값 인하는 #169와 충돌. follow-up(PR 본문): 도구 할인 필터/청크 다이어트/질의 재작성.
- **✅ 코퍼스 확장 ② 서류함 완료·머지(2026-07-06 밤, PR #174 squash f548b34)**: "서류 뭐 들어왔어?" 해소(클라 변경 0). **`customer_documents` aggregate 청크**(고객당 1행, source_id=customer_id, 라벨 "서류함") = 분류·파일명·업로드일(KST) 목록(내용 OCR 아님) — 목록 질문이라 근거 1청크에 전체 목록(서류당 얇은 청크는 TOP_K 점유·매칭 약함, profile 선례). **목록 순서 = 업로드일(created_at,id) 고정**(sortOrder면 reorder마다 재임베딩 — reorder 라우트 훅 없음 의도). **DELETE = 동기 삭제 아닌 재임베딩**(aggregate — 마지막 삭제는 빈 텍스트로 행 제거). 훅 3곳(업로드/분류/삭제) + CHECK 마이그 0020(적용) + 백필 고객별 그룹 collect/고아(서류 0건). 검증 4종+build(server 327) + 백필 실 실행(신규 1 = 김민준 4건 소급) + 실 Gemini e2e(업로드 2건→1114ms→목록 정답 인용→원복 완전).
- **✅ 코퍼스 확장 ① 일정 완료·머지(2026-07-06 밤, PR #173 squash 243ff38)**: "다음 일정 뭐지?" 해소(클라 변경 0). **schedule 청크**(일정당 1행, source_id=schedule_id) = `날짜(요일) 시간 · 타입 · 메모 · 완료(done시)` — **날짜 절대값 명시·과거 일정 전체 포함·상대 라벨(예정/지남) 금지**(다음 날 스테일). **`withTodayContext`** — /ask 양 경로 시스템 프롬프트에 오늘 날짜(KST·요일) 주입(오늘을 모르면 "다음" 판단 불가 — 이 슬라이스의 숨은 전제, KST 환산은 yymmKstOf와 동일 사유). **e2e 실측 결함 수정**: 지난 일정만 잡힌 "다음 일정" 질문에 모델이 NO_HITS 고정 문구로 도망(#172 TOOL_SYSTEM_PROMPT와 같은 부류) → withTodayContext에 "미래 일정 없으면 없다고 답하고 지난 일정 언급" 지시 → "예정된 일정은 없습니다" 교정 실측. 배선 = 일정 CRUD 훅(done 토글 포함, DELETE 동기 정리) + CHECK 마이그 0019(적용·실측) + 백필(실 실행: 61청크 중 신규 1 소급·60 hash skip·고아 0). 검증 4종+build green(server 323·unit 450 골든 불변) + 실 Gemini e2e(일정 POST→846ms 임베딩→"2026-07-10(금) 15:00" 정답·schedule 근거 인용→원복 완전). follow-up(PR 본문): 지난 일정 언급은 소프트 지시(실사용 관찰), withTodayContext 도구 경로 중복 무해 관찰.
- 그 외 후보(미확정): ①역할 scope 실제화(팀 개념 없음 — 아래 참조. 도구 쿼리 담당자 필터 접점) ②견적 계산엔진(이사님 브레인스토밍 선행) ③FCM 푸시(이사님 우선순위 대기).
- **✅ 업무 AI 자유 질문 도구 라우팅 완료·머지(2026-07-06 저녁, PR #172 squash 5837537 + 실기 확인 + 이사님 컨펌 반영)**: "앱을 통해서 들어온 고객은 누구야" 같은 자유 문장 집계 질문 해소(클라 변경 0). **설계 = RAG 우선·근거 0건 폴백**: 근거 잡히는 질문은 기존 경로 그대로(추가 호출 0·오라우팅 구조적 불가 = 골든 가드 테스트), 근거 0건(기존 NO_HITS 지점)에서만 `assistant-tool-router.ts`가 functionDeclarations 동봉 1차 논스트림 호출(mode AUTO·화이트리스트 게이트·실패 null→NO_HITS 폴백 — 500으로 안 샘). 신규 도구 `search_customers`(이름/진행 상태/구매방식/상담경로 **부분 일치**, zod 파라미터 좁힘). `runAssistantTool(key, params, ex)`. **e2e 발견 결함 수정: TOOL_SYSTEM_PROMPT 분리** — RAG 프롬프트의 NO_HITS 지시가 도구 답변에 실리면 리포트 8건이 있어도 모델이 고정 문구를 뱉음(실측) → 도구 경로(버튼 포함)는 전용 프롬프트, 테스트 잠금. **이사님 컨펌(07-06 저녁) 3건 반영**: ①quote_ready 의도대로 ②계약완료 단계 = "출고 준비 및 정산 준비" 개념(출고/정산 화면 CRM 구현 후 데이터 기반 쿼리 교체) ③chance = 상담사 수동 입력값(자동 분기 아님 — 주석·도구 설명 명시). 검증: 4종+build green(unit 450·server 314) + 실 Gemini e2e(앱 8명/유튜브 2명 정확·잡담 NO_HITS 유지·골든 불변) + 브라우저 실기. follow-up: 집계형인데 우연히 근거 ≥1건이면 RAG 조각 답변(실사용 관찰), 도구 담당자 필터는 역할 scope에서.
- **✅ 업무 AI 빠른 질문 5종 복구 완료·머지(2026-07-06 오후, PR #171 squash 4f015f3 + 실기 확인)**: 빠른 질문(이사님 제작)은 집계·조건형이라 RAG 원리적 불가 — #169 후 전부 NO_HITS로 죽어 있던 것을 **도구 호출 B안 PR1(버튼 결정론)**로 복구. **화이트리스트 도구 5종**(read-only·자유 SQL 금지): 오늘 처리할 일(미완료 할일 급함/오늘+오늘 일정 KST)/계약 가능성 순위(확정>높음>중간>보류>낮음 상위 20)/응답 지연(7·15·30일 버킷, 신규·상담접수 제외)/견적 보낼 고객[잠정]/출고·정산 리스크[잠정]. `/ask` `tool` 키(zod enum) → 임베딩 검색 생략·리포트 결과를 근거 1청크로 생성(**0건도 "조회 결과 없음"으로 실어** NO_HITS 오답 방지), sources "리포트 — N건 조회" 표기, NO_HITS 게이트 hits→promptChunks. 클라 = 빠른 질문 **텍스트 정확 일치 시만 tool 동봉**(문구 수정 = 기존 RAG), 클라 매핑↔서버 enum 파리티 테스트(서버 모듈 테스트 전용 import — 번들 미유입). Gemini 호출 1회 유지(버튼이 의도 확정 — 라우팅 호출 불필요). 검증: 4종+build green(unit 450·server 305, 기존 RAG 테스트 19종 불변=골든 가드) + 실 API e2e 5종 + 브라우저 실기 3종(응답 지연 16명·순위·리스크 6명). **PR2(자유 질문 모델 라우팅 — functionDeclarations)는 이사님 피드백 후 별도 슬라이스.** follow-up(PR 본문): 역할 scope 시 도구에 담당자 필터("내가"의 실제 의미), lastActivityAt 파생 2벌(listCustomers와) seam 공유.
- **✅ 업무 AI 고객 프로필 청크 완료·머지(2026-07-06 오후, PR #170 squash 11a7bd2 + 실기 확인)**: 코퍼스에 프로필·구조화 니즈가 통째로 빠져 "거주지/구매시기/직군" 기본 질문을 못 답하던 것 해소. **`customer_profile` 청크**(고객당 1행, source_id=customer_id) = 거주지·직군(유형·상세)·상담경로·담당자 + 구조화 니즈 11종(관심 차종=모델·트림/구매방식/구매시기/컬러/비교 차종/계약기간/초기비용/주행거리/출고 방식/계약 중점). **의도적 제외**: phone(PII)·진행 상태(메타 병기 중복·재임베딩 잦음)·서술형 니즈 3필드(별도 청크). 빈 값·"확인 필요" 센티널 항목째 생략, 전부 비면 삭제 경로. 배선 = `buildCustomerProfileChunkText`(assistant-corpus) + loadCorpusSource 분기 + 고객 PATCH 훅(`CUSTOMER_PROFILE_EMBED_KEYS` — 구성 필드 온 PATCH만, hash skip이 no-op 흡수) + CHECK 마이그 0018(적용·실측) + 백필(실 실행: 프로필 22행·기존 38 hash skip). 검증 4종+build(server 296) + e2e(PATCH→재임베딩 실측·/ask 프로필 질문 인용·원복) + 실기(김지안 구매시기·오세린 경로 — 근거 관련만·정답). follow-up(PR 본문): needCompare 쓰기 스키마 부재, 전부 비워진 프로필의 백필 고아 정리 한계(정상 경로는 훅이 처리).
- **✅ 업무 AI 근거 유사도 임계값 완료·머지(2026-07-06 오후, PR #169 squash e73fa77 + 유슨생 실기 확인)**: `/ask` `TOP_K=8` 고정·임계값 없음(B1 follow-up)이라 무관 청크 8개가 근거 목록(작은 글씨)+Gemini 프롬프트 양쪽에 실리던 것 해소(#162 견적 청크로 체감). **실측 선행**(질문 8종×코퍼스 37청크, /ask 동일 경로 프로브): 진짜 관련 0.765~0.835 · 무관 노이즈 ≤0.7573 · 무관 질문 최고 0.61~0.72 → 갭(0.757↔0.765) 하단 마진으로 **`SIMILARITY_THRESHOLD=0.75`(재현율 우선, `≥` 유지)**. 검색 직후 단일 지점 필터(JSON·스트림 공통), **전부 미달 = 기존 hits 0건 경로 재사용**(NO_HITS_ANSWER, Gemini 생성 미호출), 최소 보장 폴백은 의도적 미채택(무관 질문에 억지 근거 잔존 부작용). **관측 로그** `[assistant] 근거 임계값 필터 8→2 (컷 최고 0.7483)` — prod tail로 컷 분포 보고 재조정(컷 최고점이 임계값 근접 빈발 = 신호). 클라 변경 0(searchEmbeddings가 이미 similarity 반환). 검증: 4종+build green(server 291·신규 3: 경계값 유지/JSON·스트림 전부 미달) + 실 API e2e(관련 8→2·잡담 8→0→고정 답변, 스모크 대화 4행 원복) + 실기(제임스 견적 질문 → 근거 3건 전부 제임스). follow-up(PR 본문): 코퍼스 성장 후 로그 기반 재조정(0.75~0.76), 근거 접힘 토글(UI), 대명사 후속 질의 약함(질의 재작성 — B1 기존 한계). **+운영 교훈(07-06)**: 견적/메모를 **psql로 직접 삭제 금지** — 서버 삭제 훅(임베딩 동기 정리)을 우회해 고아 임베딩이 남는다(스모크 견적 QT-2607-0007 잔재 고아 1행 발견·정리 완료. 삭제는 UI/API 경로로).
- **✅ 할인 구성 내역(discount_lines) 슬라이스 완료·머지(2026-07-06 아침, PR #168 squash 923c7b5)**: 이사님 결정(07-05 통화) "CRM은 모든 할인 항목 표시, 고객 앱은 총액만" 이행. ⓐ**영속화** — 서버 zod `discountLines:[{label,amount,unit}]`(**전체 교체, null=클리어**, unit 어휘 zod 게이트=클라가 jsonb 형태를 신뢰하는 근거)→headerSet/createQuote, 클라 persist는 **행 금액을 DOM에서 스냅샷**(`readDiscountLineSnapshots` — 행 금액의 진실 원본은 uncontrolled input, state amount는 초기 표시값), **수정 진입 복원** = 배치 A의 `setDiscountLines([])` 청소를 `restoreDiscountLines`(quote-workbench-meta.ts 순수 헬퍼, TDD 6케이스)로 교체 — **기본 할인 = finalDiscount(총액) − Σ추가 행 환산액** 역산(percent 기준 = base+option·syncDiscountTotalFromRows 동일 산술·음수 0 클램프·행 id 매번 재발급=uncontrolled 리마운트 보장), `applyTrimToPricing`은 `prefill.pricing.primaryDiscount` 주입(총액이면 이중 계상) ⓑ**발송 조립기 라벨 제거** — `app-card-payload.ts` discount_lines 읽기 삭제, `discountRowLabel` 항상 "최대 할인 적용"(인계문 계약표 한 줄 갱신, 앱 코드 변경 0) ⓒ**CRM 미리보기 병기 유지** — `app-card.ts` 의도 주석 + **파리티 테스트는 discountRowLabel을 명시 이탈 처리(스킵 아닌 양쪽 값 각각 잠금)**. 검증: 4종+build green(unit 448·server 288) + 격리 스택 브라우저 스모크(신규 저장→psql discount_lines 2행 실측→수정 재진입 행/기본 할인 6,500,000 분리 산술 복원→미리보기 "최대 할인 적용 (재구매 할인·기타)" 병기 실증→스모크 견적 삭제 원복). **기발송 견적 소급 불가**(발송본 고정 — 신규/수정 재발송분부터). follow-up(PR 본문): 할인 %/금액 세그먼트 버튼 중앙이 money input에 겹침(실클릭 가능·히트 영역 절반 — CSS 폴리시 후보), 업무 AI 견적 청크 할인 라벨 미포함, 0원 행도 병기(현행 유지).
- **✅ 0705 리팩토링 배치 완결(2026-07-05 심야, 유슨생 세션) — #154~#162 감사(4앵글) → 5PR 전부 squash 머지**: 계획/보류 목록 SSOT = `ref/plans/2026-07-05-crm-refactor-batch.md`. **A(#163 f9218a9)** 워크벤치 카드 UI 잔상 픽스(모드 Record·할인·취득세가 persist payload에 실려 **저장까지 오염** — red 5종 선행. 수정 진입 `acquisitionTaxMode` 미복원 실버그 포함) + `client/src/lib/select-bind.ts`(bindSelect/useActionSelect — Safari 병행 바인딩 미적용 **9곳** 픽스: 워크벤치 약정거리·서류 문서종류·목록 필터 3·mc-master 패널 6, 기존 4벌 이관 — 이후 controlled select는 이 헬퍼 필수) + 할인 항목명 state 배선. **B(#164 927c0b8)** `resolveGeminiTargetFromRequest` SSOT(백로그 트리거② 이행, assistant+embed-on-write 소비)·`holdStreamLifetime`→`holdWork` 체인 통일(#143류 주석 규약의 코드화)·채번 `lib/business-code.ts`(KST 고정 — **CF UTC 월경계 전월 채번 실버그 수정**)·advisor upsert 공통 1벌화+회수 RETURNING·`pickPrimaryScenario` 4번째 패턴 치환. **C(#165 6f16f32)** 자식 4테이블 `(customer_id,created_at)` 인덱스+`last_activity_at` drop(마이그 0017 **적용·psql 실측 완료**). **D(#166 00a9940)** appStatus "viewed" dead 정리(실데이터 0 확인 — 열람은 viewedAt SSOT, `quoteAppSendLabel` 중복 1벌화)+라벨 SSOT(`downPaymentRowLabelOf`·`dedupedModelTrim` 전 화면 확산 — 견적함 행 트림 표기 변경 있음)+`resolveUpdateBadge`(버킷 합성 3곳 통합)+percent blur "0"·guidance 동시존재 테스트·`initialQuotePricingResult` 리네임. **E(#167 f6f57b0)** dead CSS **kim-* 80종/2,282줄 제거**(workbench.css −41%, 번들 −11.8%) — 빌드 셀렉터 diff 기계 증명(제거분 외 byte-동일), 동적접두 보류 10종은 PR 본문. D는 A 머지 후 충돌 1건(import) 리베이스 해소. **최종 main 통합 검증 green: typecheck 0·lint 0·unit 441·server 287·build**. **실기 follow-up 3건 전부 통과**(07-05 심야, 유슨생: Safari select·상세 화면 스모크·트림 dedupe 표기). discount_lines는 이사님 결정 완료 → 위 "다음 작업" 항목. 잔여 보류 = 클라↔서버 라벨 헬퍼 물리 공유(팀 합의)·카드 9 Record→CardUiState(M~L)·viewed 서버 zod/CHECK 축소·`source:"앱 견적요청"` 리터럴 상수화.
- **✅ 업무 AI 견적 코퍼스 확장 + 증분 임베딩 완료·머지(2026-07-05 밤, PR #162 squash 188a2de)**: "오늘 발송한 견적을 업무 AI가 답하나?" 2겹 해소. spec `ref/specs/2026-07-05-crm-quote-corpus-embed-on-write-design.md`(확정 결정 6) · plan `ref/plans/2026-07-05-crm-quote-corpus-embed-on-write.md`(9태스크 TDD, subagent-driven + 태스크별 spec/quality 2단계 리뷰 + 최종 통합 리뷰 Ready to merge). 구성: ①**견적 청크**(견적당 1행, 헤더+대표 시나리오+guidance, `buildQuoteChunkText` — app-card-payload 라벨 헬퍼 export 재사용·복제 0, **열람 미포함**=앱이 advisor_quotes 직접 write라 CRM 훅 없음·스테일 박제 방지) + CHECK 마이그 0016(`quote` 소스타입) ②**증분 임베딩 훅** `src/lib/embed-on-write.ts`(deps 주입형) — 쓰기 라우트 8콜사이트(메모/할일/니즈 3필드/견적, 견적은 **트랜잭션 커밋 후** 스케줄) → 응답 비차단(`holdWork` dbHold **체인** — 니즈 3필드 동시 PATCH 다건 안전, holdStreamLifetime과 구분) → fresh read→**content_hash skip**(B1 follow-up 흡수)→임베딩 upsert, 원본 소실/빈 텍스트=행 삭제, DELETE 3곳은 동기 정리 ③**백필 = 보정 도구화**(견적 collect+hash skip+고아 정리 7분기) ④`pickPrimaryScenario` SSOT(발송 조립기·로더·백필 3곳 복제 해소). **함정(재발 방지)**: ⓐ**게이트 3규칙** — 명시 off 항상/`bun test`(NODE_ENV=test 자동)는 기본 off·명시 on만/그 외 키 존재 시 on. npm 스크립트(`test:server`의 `EMBED_ON_WRITE=off` 프리픽스) 우회 직접 `bun test <파일>` 실행이 실 Gemini 호출+master 오염 14행 낸 **실사고** 대응(이중 방어) ⓑ실 DB 테스트 픽스처는 랜덤 서픽스(고정 코드는 afterAll 실패 시 unique 위반 연쇄 실패) ⓒ`assistant_messages.staff_user_id`는 uuid — makeTestAuth 기본 sub로 /ask 영속 불가(스모크는 UUID sub 주입). 검증: 4종+build green(unit 421·server 278) + **백필 실 실행**(37청크 중 8 임베딩=견적 7 소급+누락 메모 1, 29 hash skip, 고아 0) + **실 Gemini e2e 스모크**(메모 저장→749ms 임베딩→/ask가 스모크 고객 근거 인용(sources 1위)→견적 임베딩 715ms→삭제 동기 제거, 원복 byte-exact). follow-up(PR 본문 예정): 고객명 변경 시 기존 임베딩 스테일(백필이 보정), updateQuote target 4번째 유사 패턴, embeddings.test.ts 고정 픽스처, 라벨 헬퍼 변경 시 재임베딩 트리거, 유사도 임계값(B1 기존). **다음 후보 = ①역할 scope 실제화 ②계산엔진(이사님 브레인스토밍 선행) ③FCM 푸시 ④지역 옵션 확장**.
- **✅ 견적 앱 발송 파이프라인 완료·머지(2026-07-05, PR #159 squash d50a6c7 + 실계정 크로스 테스트 통과)**: 발송 시 앱카드 **라벨 완성본**을 `public.advisor_quotes`에 upsert(재발송=crm_quote_id conflict 교체+viewed_at NULL 리셋), 원 요청 `completed` 전이, 견적 삭제 시 카드 회수, 앱 열람(viewed_at)→CRM 견적함 "고객 열람/미열람" 배지 read-through. 이사님 승인 3건(public 신설·completed 전이·bids 폐기 — DROP은 미룸). spec `ref/specs/2026-07-05-crm-quote-app-send-design.md` · plan `ref/plans/2026-07-05-crm-quote-app-send.md`(편차 노트) · **payload 계약 SSOT = `ref/2026-07-05-app-advisor-quotes-handoff.md` 2절**(앱 세션과 동기 완료 — discountLabel 맨숫자·quoteCodeLabel No. 접두 없음은 앱 렌더 정적 부착). 구성: 쿼리 모듈(`src/db/queries/advisor-quotes.ts`) + **서버 조립기 `src/lib/app-card-payload.ts`**(buildAppCardModel 재현 — statusLabel/ddayLabel 제외=앱이 viewed_at/valid_until 컬럼에서 계산, payloadVersion:1, KST stamp 고정, 파생가 computePricing 재계산) + **파리티 가드 `client/src/lib/app-card-payload-parity.test.ts`**(필드 집합 클라 파생 잠금 — red 시 서버 조립기·인계문·Flutter 3곳 전파) + 발송 훅(updateQuote **맨 끝** sent 분기: appUserId 게이트→fresh read→catalog modelYear 조인→upsert→completed. deleteQuote 회수+DELETE 라우트 트랜잭션화) + read-through(getCustomer 병합, 클라 타입 변경 0) + 열람 배지(`viewedBadgeOf` — **detail.appUserId 게이트**). **함정(재발 방지)**: ①crm loose id↔public 엄격 FK — 원 요청 삭제된 견적 발송은 존재 확인 후 quote_request_id null 강등(FK 23503 차단 실증) ②`q.sentAt`은 Date, advisor는 string — toISOString 경계 ③기존 customers.test.ts 발송 테스트가 훅 도입으로 고아 카드를 남기던 누수 수정(finally 회수). 검증: 4종+build green(unit 421·server 246, 태스크별 2단계 리뷰+최종 통합 리뷰) + 격리 스택 스모크(발송→payload 실측→열람 전환→삭제 회수, 원복) + **실계정 크로스**(zmes 320i: CRM 발송→앱 "견적 도착" 배지→앱 열람→CRM "고객 열람"). **+Realtime 경량 알림 완료(2026-07-05 직후, 앱 세션)**: advisor_quotes publication 추가 마이그레이션 + 앱 provider 구독(autoDispose·user_id 서버 필터 의도적 생략=DELETE old row 한계) — **재발송 실시간 반영 실기 검증**(앱 상세 열어둔 채 CRM "수정 후 발송" → 리로딩 없이 카드 즉시 교체). 인계문은 취득원가 9필드 맨숫자 정정 포함 **정적 부착 예외 3종**(discountLabel·quoteCodeLabel·취득원가)으로 완결. 남은 알림=FCM 푸시(앱 미실행 시 — 이사님 우선순위 대기). follow-up=PR 본문(purchaseMethod 미리보기 충실도, viewed 구 분기 dead 정리, 삭제 시 요청 재open 비대칭 이사님 확인 등). **다음 후보 = 역할 scope 실제화 / 계산엔진(이사님 브레인스토밍 선행) / FCM 푸시 / 지역 옵션 확장**.
- **✅ 앱 견적요청 승격 갭 슬라이스 완료·머지(2026-07-04 밤, PR #158 squash a9c5f00 + 실기 폴리시 2건: 승격 버튼 가로 행·nowrap)**: 유슨생 실기 지적 4건 해소 — ①승격 prefill 완결(기간·보증금/선수금/선납금·비율/금액 → 카드1 시드, `client/src/lib/quote-request-seed.ts` 순수 함수 TDD. **비율>0=% 모드·금액 무시(CRM 최종가 재계산 정합)**, 일시불 코드 가드) ②니즈/인박스 카드 보증금 % 병기(`deposit_ratio`는 **drizzle 컬럼 정의 자체가 누락**돼 있었음 — public-app.ts에 추가, mode:"number" 이탈 근거 주석) ③견적함 "앱 요청" 배지(`source_quote_request_id` 클라 배선) ④니즈 카드 승격 견적 있으면 "견적 보기"(기본, 서버 `promotedQuoteIds` 최신순)+"추가 작성"(보조) ⑤워크벤치 가격 잔상 `resetWorkbenchPricing()` 4경로. **이사님 도메인 규칙 표(스펙 영속, 발송 파이프라인 재사용)**: 리스/렌트=보증금·선수금(%→금액 파생 공존), 할부=선납금(금액만 — 선수금 행+라벨 전환 `downPaymentRowLabel`), 일시불=없음. **의도 확정**: 시드=입력 보조 — isFilled(금융사 or 월납입>0) 전까지 저장·미리보기 미반영. 스모크=격리 스택(API 8799+vite 5174 임시 config — 사용자 dev 불가침, 원복·삭제 완료). follow-up=PR 본문(할부 승격 실기·라벨 규칙 2곳 인라인 등).
- **✅ 앱카드 4섹션 리디자인 완료·머지(2026-07-04 밤, PR #157 squash e1ed299)**: 디자인 4장 대조 보정(녹색 헤더바+흰 본문 구조·솔리드 블루/오렌지 헤더·모달 720px·차명 dedupe·잔존 %선행(섹션3)·서비스 라벨/값 행·CTA 검정+상세견적 pill) + 유슨생 실기 피드백 3건(아코디언 기본 접힘·"상세 견적"=일괄 펼침 토글 — 앱 동작 미러, `+ 추가` 버튼 하단 여백, **고객 지역 기본값을 거주지 파생으로**(`regionFromResidence` — 미입력="확인 필요" 임의 지역 확정 표기 방지·옵션 밖="기타", 구 기본값 "서울" 오염 4건은 psql 백필로 정정: 김민준 3건→인천·김지안 1건→확인 필요)). guidance 지역은 작성완료 시점 **스냅샷**(거주지 변경해도 기존 견적 카드 불변 — 발송본 고정 의도). follow-up 추가: 지역 옵션 셋 확장(전북/전남 등 — **해소: regionOptions 전국 17개 시·도 상주·#191 거주지 파생**). spec `ref/specs/2026-07-04-crm-app-card-redesign-design.md` · plan `ref/plans/2026-07-04-crm-app-card-redesign.md`(9태스크 TDD, subagent-driven + 태스크별 spec/quality 2단계 리뷰). ①AppCardPreview 4섹션 리라이트(신규 `app-card-*` CSS — kim-* 신규 0, wrapper `.kim-app-card-preview`만 유지=워크벤치 grid 결합) + buildAppCardModel v2(TDD 13 — percent 병기 환산 기준=finalVehiclePrice·총비용 반납 우선·발송 전/후 상태 전환, D-day/발송시각은 견적함과 SSOT 위임 validLabelFromUntil·formatActivity) ②시나리오 확장 필드 7종(금리/반납·인수 총비용/출고전납입/자동차세/보조금) zod→insertScenarios→클라 타입→워크벤치 수기 입력·토글 실배선→prefill 왕복 ③갭ⓐ 발송 시 `valid_until=sent_at+7일` 서버 스탬프(재발송=리셋 의도) ⓑkeyPoint→keyPoints 배열(+동적 입력, 구행은 `normalizeQuoteGuidance` read 하위호환 — **서버 zod는 keyPoints만 수용**) ⓒ서비스 `+` 동적 칸(저장 직전 sanitize) ⓓNo.=quote_code. **함정(재발 방지)**: 금리 입력은 `data-discount-unit="percent"`로 jeff money 콤마 포맷 우회(소수 보존)+`parseInterestRate` 100% 상한(콤마 오입력 "5,32"→532% 무음 차단), 프리필 폴백은 `|| "0"`(`?? "0"`은 EditScenario 빈 문자열 통과→validateQuoteDetailDraft가 수정 저장 전면 차단하는 회귀 — 실제 발생·수정), 전역 `button:disabled` 물빠짐(opacity 0.48)은 항상-disabled 장식 CTA에 오버라이드 필요. 검증 4종+build green(unit 394·server 221) + 브라우저 스모크(신규 견적 왕복·미리보기 4섹션 라이브·발송 D-7 배지+psql `valid_until-sent_at=7d` 실측·프리필 왕복(금리 소수·토글·keyPoints/서비스)·스모크 견적 삭제 원복, 캡처=로컬 `screenshots/app-card-modal-smoke-*.png`). follow-up=PR 본문(구 kim-app dead CSS·Safari datalist 실기·모델명 중복 표기 dedupe 등). **다음 = 유슨생 디자인 4장 첨부 → 픽셀 대조·색 보정 커밋 → #157 머지 → 견적 앱 발송 파이프라인(payload=이 카드 데이터)**.
- **✅ prod SSE 전면 불능(524) 3연속 해소(2026-07-03 밤, PR #145·#146·#147 머지·prod 실측 완료)** — #144 머지 후 prod 실측에서 업무 AI 스트리밍이 전면 524(무한 로딩 닷)+유령 빈 placeholder로 발견(리전 차단 때문에 #142~#143의 prod 스트리밍이 실측된 적이 없었음). 근본 원인 3겹: ①**`functions/[[path]].ts`가 `app.fetch`에 ExecutionContext 미전달** → `c.executionCtx` throw → dbMiddleware 폴백 `await end`가 dbHold(스트림 수명)를 인라인 대기 → **Response 반환이 스트림 종료까지 막히는 데드락**(로컬은 HYPERDRIVE 분기 없어 원천 미재현, #142 abort 가드도 prod에서 작동한 적 없었음). fix=context 전달+폴백 비차단(#145, 회귀 테스트 3종) ②중단 시 업스트림 Gemini fetch AbortSignal 배선(#146) ③**CF Pages는 클라 disconnect가 어느 채널로도 안 온다**(sse.onAbort·raw signal 미발화, pending read/write는 reject 없이 동결 → finalize가 waitUntil 유예 ~30s 넘겨 취소) → **SSE 코멘트 하트비트(`: hb` 5s 주기, 쓰기 3s 타임아웃=사망 판정) + raceDead(gen 읽기·클라 쓰기 전부 사망 promise와 race)**로 어디에 얼어도 릴레이 탈출→finalize 완주(#147, 스트림 라이프사이클 진단 로그 3종). **prod 실측**: 완주 첫바이트 2.8s·done 영속, 조기 중단 부분+"(중단됨)" 저장(tail로 `heartbeat-timeout` 감지 실증), 유령 0. 계층 격리는 일회용 진단 Worker(에코 SSE·프록시/직결 fetch 타이밍)로 수행 후 삭제. **신규 스트리밍 라우트 전제 3종 세트: dbHold(#143, =holdStreamLifetime #151) + Pages 엔트리 ExecutionContext 전달 + 하트비트/raceDead(=createSseLiveness `src/lib/sse-liveness.ts` #153).**
- **✅ 업무 AI SSE 스트리밍 + 새 턴 앵커 스크롤 완료·머지(2026-07-03, PR #142)** — 앱 미러. 상세는 아래 "업무 AI SSE 스트리밍" 섹션.
- **✅ SSE P0 hotfix 머지(PR #143)**: prod 스모크에서 발견 — dbMiddleware의 `waitUntil(client.end())`가 스트림 **완료가 아니라 Response 반환 직후** 실행돼 finalize 마감 쿼리가 죽은 연결로 전부 실패(정상 완료 done 미송출+유령 빈 placeholder, 로컬 bun 싱글톤 db라 미발현). fix = **dbHold 패턴**(`c.set("dbHold", 스트림수명promise)` → 미들웨어 `endAfterHold`가 해소 후 end) + 클라 stop 재조회 재시도·빈 assistant 행 숨김 + STOP_SUFFIX 파리티 + askAssistant 데드코드 제거. **스트리밍 라우트 신설 시 dbHold 필수.**
- **✅ Gemini 리전 차단 해소(2026-07-03, PR #144 머지)** — 원인은 CF Workers **HKG 콜로 라우팅**(cf-ray 실측 KIX 200·HKG 500·NRT 200 — 홍콩은 유료 티어여도 Gemini 차단 지역). A안 구현: **Supabase Edge 투명 릴레이 `supabase/functions/crm-gemini-proxy/`**(staff JWT 게이트=`../crm-analyst/auth.ts` 재사용 + 호스트 교체만, 경로 allowlist 3메서드·쿼리 `alt=sse`만·업스트림 예외 502) + CRM **`GeminiTarget` 스위치**(`src/lib/gemini-target.ts` — `GEMINI_PROXY_URL` env 설정 시 프록시 경유: Authorization 포워딩+**`x-region: ap-northeast-2` 핀**, 미설정=직결(로컬 dev·백필)). 키는 양 모드 `x-goog-api-key` 헤더(`?key=` 쿼리 제거 — 게이트웨이 로그 노출 방지). Edge 배포 + CF Pages Production `GEMINI_PROXY_URL` 설정 완료, **롤백 = env 제거**. 실측: 릴레이 401/404/200·SSE 패스스루·응답헤더 `x-sb-edge-region: ap-northeast-2`(핀 실증)·로컬 백엔드 e2e(논스트리밍+스트리밍) 완주. **핵심 함정(재발 방지)**: ①x-region 핀 없으면 Edge가 호출자 최근접 실행이라 HKG 재현 ②Authorization을 Google로 전달 금지(OAuth 오인 401) ③릴레이는 바디 버퍼링 금지(`upstream.body` 그대로) ④`../crm-analyst/auth.ts`는 **배포 시점 번들 스냅샷** — auth.ts 수정 시 crm-analyst만 아니라 **crm-gemini-proxy도 재배포**. follow-up(PR 본문): 리전 핀 폴백 없음(Edge 서울 장애 시 env 제거로 직결 복귀), crm-analyst `?key=`→헤더 통일, Gemini 호출자 늘면 env→target 헬퍼 중앙화. **prod 실측 완료**(HKG 콜로 200 — 리전 해소 실증, embed+generate 프록시 경유. 이 실측에서 발견된 스트리밍 524는 프록시와 무관한 별건 — 위 #145~#147 참조).
- **CSS 구조 변경(2026-07-03, #141 머지): `client/src/index.css` 17,187줄 단일 파일을 `client/src/styles/` 도메인별 24파일로 순서 보존 기계 분할.** index.css는 tailwind 임포트+`@import` 목록만. **`@import` 순서 = 캐스케이드 순서, 재배치 금지**(뒤 파일이 앞 파일을 의도적으로 덮는 오버라이드 다수 — index.css 상단 경고 주석 참조). 새 CSS 룰은 해당 도메인 파일에 추가(고객 상세는 shell/needs/workspace/workbench/cards/preview/tasks/work 8분할). 분할 전후 빌드 산출 CSS byte-identical 검증 완료(시각 회귀 0). git blame은 분할 시점 이전은 구 index.css로 추적.
- **최신 진행(2026-07-02 밤): AI 슬라이스 전체 코드리뷰 + 후속 3 PR 완료·머지 + 통합 브라우저 스모크 통과.** 8앵글 리뷰(정합성3·재사용·단순화·효율·설계깊이·컨벤션, 후보 35→검증 30 생존)로 #129~#135 범위 점검. ①**#136 정합성 6건**: 서류 AI분류 경합 3건(분류 중 수동분류 클로버·삭제 후 유령 업로드·업로드 중 변경 드리프트 — `manualDocTypesRef`/`removedTempIdsRef`), regex 폴백을 'AI분류'로 오표기(→`{docType,source}` 반환+'자동인식' 배지), 담당자 assigned_at 무조건 재스탬프(→실변경 시만, 해제 시 null), 업무 AI IME Enter 가드, ChatComposer 실패 복원이 신규 입력 덮어쓰기. ②**#137 가드·정리·효율**: 서류22종 Edge↔프론트·CRM_ROLES 서버↔Edge **패리티 테스트**(드리프트 tripwire), `.md-body` CSS 공용화(2벌 미러→베이스+`--md-*` 변수, strong 드리프트 해소), `/ask` dead 필드 제거(응답=`{messages}`만·클라 `AssistantAskResult`), embedTexts **100개 배치 청킹**, `/ask` 히스토리∥임베딩 병렬, 병합 다운로드 병렬화, fileToBase64→FileReader, `EMBEDDING_DIM` 상수화, backfill collect() 헬퍼, 서류함 카드 min-height 자립. ③**#138 업무 AI 패널 구조 추출**: `useAssistantThread`(스레드 상태기계, Topbar 소유)+`AiAssistantPanel` — 히스토리 로드 실패 영구 고착(→error 상태+재시도), 늦은 초기 fetch가 새 대화 덮어쓰는 race(→id merge+복합정렬), 에러 turn 역전/동일문구 소실(→afterMessageId 자리고정+tempId), children[2] 스크롤 결합(→data-eid 앵커). Topbar -150줄. **스모크**(agent-browser, admin magiclink 세션): 업무 AI 실 Gemini RAG 왕복·마크다운·리로드 복원, 서류 업로드→'자동인식' 배지 실증→병합 6건→정리, 배정 재저장 assigned_at 불변 실증, 채팅 콘솔 큐/스레드/md-body 실측. 미실증=경합 타이밍·IME 실기(유닛으로 커버).
- 리뷰 잔여(의도적 보류): scope seam SQL predicate·`advisor_id` 병기 → **crm.staff 파운데이션 슬라이스에서**, ChatThread↔패널 스크롤앵커·커서 페이지네이션 공용 훅 → **통합 보류 확정**(SSE 슬라이스에서 스크롤 문법이 갈라져 — 최하단 추적 vs 새 턴 앵커 — 공용화 실익 감소, 필요 재부상 시 별도 리팩토링), pending 배지 초기 잔량(#135 follow-up 유지), 대명사 질의 retrieval 약함(B1 기존 한계 — 유사도 임계값 follow-up과 함께).
- 이전 진행: `crm-analyst` 서류 자동분류 **완료**(Edge Function 배포 + PR #129 머지). 후속으로 담당자 배정 DB 영속 버그 수정 머지(#130 — `crm.customers.advisor_name` 추가, `saveAdvisorField`→`savePatch` 연결, 목록 `advisor` 하드코딩 제거). 상세는 아래 요약.
- 고객 상세 거대 컴포넌트 분해: **완료로 종결**. 9영역 추출이 이미 main 반영(`CustomerDetailPage.tsx` 5437→303줄), stale 브랜치 `refactor/crm-detail-decomposition` 삭제됨. 잔여는 `kim`→범용 리네임(순수 이름 정리·기능 무변경·저우선)뿐.
- 슬라이스B 업무 AI 채팅 **완료·머지**(PR #132, 2026-07-02): pgvector RAG(`crm.embeddings` 3072)로 Topbar 업무 AI 실동작. 상세는 아래 요약.
- 슬라이스C1 업무 AI **대화 영속+멀티턴+앱 UI 완료·머지**(PR #133) + **히스토리 페이지네이션·UI 폴리시 완료·머지**(PR #134, 2026-07-02): `crm.assistant_messages` 영속, 최근10턴 멀티턴, react-markdown, 위로 스크롤 이전대화 로드(커서), AI답변 박스제거·overscroll·확대패널높이 등 브라우저 실측 폴리시. 상세는 아래 요약.
- **실시간 상담 콘솔 v1 완료·머지**(PR #135, 2026-07-02 — 워크트리/브랜치 정리 완료): 앱 고객 채팅(public.chat_sessions/chat_messages)을 CRM ChatPage 실동작 콘솔로. 상세는 아래 요약.
- **UX 후속 완료·머지**(PR #139, 2026-07-03): ①실시간 상담 `.chat-messages` overscroll-behavior:contain(리스트 끝 휠이 배경 문서 스크롤로 전파되던 쉬프트 차단, CDP 조준 wheel 실측) ②업무 AI 히스토리 **프리페치**(Topbar 마운트 시·딜러 제외 — "느림" 원인은 백엔드(11ms) 아니라 팝오버 연 뒤에야 fetch+loading 무표시) + loading 중 DoubleBounceDots.
- **✅ AI 슬라이스 리팩토링 A+C 완료(2026-07-03 밤, PR #151·#152 머지 + crm-analyst 재배포 + prod 스모크 통과)**: 백로그 즉시 항목 전부 실행. **#151(A+B 1~5)** = ①DISPLAY_LIMIT↔AI_HISTORY_PAGE 파리티 테스트(클라 상수를 의존성 없는 `client/src/lib/assistant-history.ts`로 분리 — useAssistantThread 직접 import는 supabase env 부작용 체인이라 불가) ②classifyGeminiError bun↔Deno 파리티 픽스처 8종 ③NO_HITS_ANSWER SSOT를 assistant-prompt.ts로 이동·보간(프롬프트 인용구 마침표 1자 추가가 유일한 행위 변경) ④**holdStreamLifetime(c)** — dbHold+waitUntil 원자 등록 헬퍼(middleware/db.ts, #143 불변조건을 구조 규칙으로) + tryWaitUntil이 executionCtx try/catch 2벌 흡수, 파라미터는 구조적 타입(hono Context가 Variables invariant) ⑤sleepUnlessAborted → `src/lib/sleep.ts` 순수 이동+유닛 3케이스. **#152(C 7~10)** = ⑦**geminiPost**(`src/lib/gemini-post.ts`) — generate/stream/embed 3벌 재시도 골격 통합(로그 포맷 `[assistant] Gemini {label} …` 보존) ⑧generateAnswer(Stream) trailing 인자→opts 객체({history,fetchImpl,signal}, `undefined,` 필러 제거) ⑨crm-analyst `?key=`→x-goog-api-key 헤더+Deno 테스트(**재배포 완료**) ⑩insertTurn 헬퍼(now/now+1ms 커서 규약 SSOT). 검증: 4종+build+deno 3종 전부 green(server 209·unit 361·deno 14). **prod 스모크(API 직접)**: 완주 첫바이트 2.79s·done 영속, 조기 절단 시 부분 390자+" (중단됨)" 저장(하트비트 사망 판정 경로 실증)·유령 placeholder 0. 스모크 대화 6행 master에서 삭제. **+브라우저 실기(유슨생) 3종 통과**: ①서류 업로드 'AI분류' 배지(재배포 crm-analyst의 헤더 키 실호출 검증 — 유일한 미실증 지점이었음) ②업무 AI 타자기·중지(클라 stop 경로 #148~#150) ③이전 대화 위로 스크롤 로드(AI_HISTORY_PAGE 이동 접점). **백로그 6 sse-liveness 추출 완료(PR #153 머지, 2026-07-04)**: streamAsk의 생존 감시 기계장치(clientDead/markDead·raceDead·하트비트 IIFE·hbStop)를 `src/lib/sse-liveness.ts` `createSseLiveness({writeRaw, heartbeatMs, writeTimeoutMs, onDead})`로 순수 이동 — 채널 배선(sse.onAbort+raw signal)·도메인(업스트림 중단·finalize)은 라우트 잔류, tail 로그 문자열 보존. TDD 유닛 8종(하트비트 3분기: 정상 정지/write-timeout/write-throw + raceDead/markDead — prod tail로만 검증되던 경로 유닛화). 검증 4종+build green(server 217·unit 361) + 로컬 SSE 스모크(PORT=8799 별도 인스턴스: 완주 done 영속 1.76s·2s 절단 부분 439자+"(중단됨)" 저장·유령 0, 스모크 4행 master 삭제). **잔여 = B 트리거 3건뿐**(supersede 시 드레인 컨트롤러, 2번째 백엔드 Gemini 호출자 시 resolveGeminiTargetFromRequest, 4번째 메서드 시 프록시 allowlist). 상세 = `ref/ai-refactor-backlog-2026-07-03.md`(완료 표시됨).
- 다음 후보: ①**역할 기반 scope 실제화(보류 — 나중에)**: **팀 개념 없음 확정(유슨생 확인, 2026-07-03) — "팀장"은 팀 관리가 아니라 권한 레벨만 상이.** 따라서 crm.staff/팀 테이블·팀 모델링 불필요, scope는 "admin/팀장=전체, 상담사=본인 담당(advisor 매칭)" 역할 분기로 축소(advisor_id uuid 병기 + `resolveCustomerScope` seam 본문 교체 + 보류된 seam predicate). staff 계정 0이라 실사용 압력 없음 → 데이터화 이후로 ②**✅데이터화 마감+kim 리네임 완결(2026-07-04, PR #154·#156 머지)**: 이사님 점진 일반화 컨펌(유슨생 전달) 후 실행. **#154** = 관리 상태 실데이터화(서버 `GREATEST(customers.updated_at, 자식4 max(created_at))` 파생 + `lib/manage-status.ts` bucketing·달력일 기준·`atIso`) + 타임라인 consultations 하이브리드 병합(0행=무변화) + 장식 mock 2곳 제거 + **🐛drizzle 상관 서브쿼리 `${table.col}` 보간이 bare 컬럼으로 렌더링돼 섀도잉되는 버그 발견 — 기존 `latestTask`(목록 상담 메모)가 prod에서 항상 null이던 실버그 수정**(외부참조는 `crm.customers.id` 완전정규화 필수, 코드 주석 고정). **#156** = kim 심볼 118종+파일 13개 리네임(기능 무변경 기계 증명, CSS 클래스·데이터 문자열 불가침, ⚠️BSD sed `\b` 무음 실패 — perl 사용). spec: `ref/specs/2026-07-04-crm-detail-datafication-design.md`(follow-up: 자식 customer_id 인덱스·자식 updated_at·last_activity_at drop·목록 useMemo 시간 staleness·dead CSS 2블록). 검증: 4종+build green(unit 372·server 219)·브라우저 스모크(관리 상태 psql 대조 포함) 통과. **후속 결정(2026-07-04, 이사님 컨펌)**: 상담 타임라인은 수동 입력 없는 **자동 갱신 구조 확정** — 상담 기록 쓰기 CRUD 후보 폐기, consultations는 자동 기록 수신처(채팅 AI 상담 요약 등)로 유지. → **다음 슬라이스 확정 방향 = ①앱카드 4섹션 리디자인(선행) → ②견적 앱 발송 파이프라인(2026-07-04 조사 완료, 유슨생 지시)**. **①(2026-07-04 추가, 이사님 디자인 확정)**: 앱카드보기를 4섹션 상세 카드로 확장(헤더 D-day·월납입금/금리/잔존가치/총비용/할인/보증금/주행거리/핵심포인트 ▸ 출고정보+취득원가 구성 ▸ 추천 견적 조건(대표 시나리오 전체) ▸ 추천이유+서비스 목록+No.) — 디자인 스크린샷 4장 보유(유슨생). **데이터 검증 완료: 필드 거의 전부 기존재** — quotes(가격/할인 discount_lines/취득원가 구성/컬러/stock_status/`guidance` jsonb{deliveryComment,stockNotice,expectedDelivery,customerRegion,keyPoint,recommendReason,services[]}) + quote_scenarios(금융사/기간/보증금/선수금/잔존가치/약정거리/자동차세/보조금/금리/반납·인수 총비용/월납입금/출고전납입=dueAtDelivery) + primary_scenario_id. 실데이터(QT-2606-0007 guidance 채워짐) 확인. **소형 갭 4**: ⓐvalid_until 컬럼만 있고 워크벤치 입력 없음(D-day 정책) ⓑkeyPoint 단일 문자열 vs 카드 bullet 3개 ⓒservices 워크벤치 4칸 고정 vs 카드 6항목(배열이라 데이터는 수용) ⓓ카드 No. 포맷(quote_code vs 별도 채번). 구현 = `buildAppCardModel`(lib/app-card.ts) 확장 + `AppCardPreview` 리라이트 + 워크벤치 소형 입력 보강, CRM 단독 작업(금리 등 데이터는 있는데 현 프리뷰 조립이 NO_SOURCE 처리 — 리디자인 때 배선). ②발송 payload = 이 확장 카드 데이터 기반. 실태: CRM "앱 발송"=`crm.quotes.app_status/sent_at` 내부 스탬프뿐, **앱이 읽을 경로 전무**(앱은 public만 읽고 crm 스키마 참조 0, 견적요청 상세엔 "딜러 입찰 대기" 스텁만, 견적 도착 알림/Realtime 없음 — 앱 실측: quote_detail_screen.dart:202 스텁). **이사님 결정: 딜러 입찰 모델 폐기**(public.bids 0행 폐기 대상, ai_estimates=AI 분석 기능이라 무관). 목표 흐름 = 앱 quote_requests(인바운드 #114~#122 완성, `crm.quotes.source_quote_request_id` 연결·워크벤치 프리필 시 채워짐 실증) → 상담사 견적 → 발송 시 **public 수신 테이블(신설) write** → 앱 견적요청 상세 스텁 자리+견적함 표시. 설계 결정 필요(브레인스토밍): ①수신 테이블 스키마(user_id RLS+quote_request_id nullable — 요청 무관 제안 견적 허용 여부) ②payload 스냅샷(jsonb vs 정규컬럼, 발송본 고정+revision) ③앱 표시 위치·알림 ④viewed_at 회수 경로 ⑤quote_requests.status 전이(completed?) ⑥**public 스키마 DDL은 앱 관할(불가침 원칙) — 테이블 신설은 이사님/앱 마이그레이션 경로 협의 필수** ⑦bids·스텁 정리(Flutter 작업, 크로스 레포). 그 외 대기: 견적 계산엔진(나중으로 미룸 — 이사님 브레인스토밍 선행), 역할 scope 실제화(①).
- 완료된 고객/견적/서류/니즈/상세 저장 관련 세부 이력은 main git/PR 기록과 관련 specs/plans를 기준으로 본다.

## 실시간 상담 콘솔 v1 (완료·머지 — PR #135)

- **spec** `ref/specs/2026-07-02-crm-realtime-chat-design.md` · **plan** `ref/plans/2026-07-02-crm-realtime-chat.md`(11 Task, TDD, subagent-driven — plan 맨 끝 "구현 편차 노트"가 코드 블록보다 우선).
- **아키텍처**: 프론트 supabase-js 직결(staff JWT+RLS+Realtime) — 앱 admin 섹션(Flutter) 웹 포팅. **백엔드·DDL 변경 0**(staff RLS 정책·Realtime publication 기존재 실측). payload·system 문구·전이 순서 = 앱 코드 byte-level 미러(테스트로 고정).
- **범위**: 큐(mode 3탭+전체)·히스토리(user_id 기준+커서 더보기)·Realtime 수신·배정/채팅 시작(neq 경합 가드)/AI 반환·상담원 전송(낙관+롤백)·첨부 표시·고객 상세 이동(appUserId 매칭)·Topbar/사이드바 알림·**타이핑 인디케이터**(broadcast `typing:<uid>` 앱 상호운용)·AI 메시지 마크다운·아바타·카카오식 말풍선(라벨 밖 위+고객명).
- **핵심 함정(재발 방지)**: ①supabase-js 같은 topic 재사용 → 공존 구독 충돌(채널 suffix 고유화; typing만 예외=매니저+250ms 유예 teardown) ②**Dart↔JS broadcast 봉투 차이**(Dart는 payload에 type/event 합쳐 평면 전송 — 수신은 양형태 수용, 송신은 평면 sender_type 동봉) ③PostgREST `.or()` 타임스탬프 quoting ④wal2json 직렬화(스페이스·+00) — toEpoch/parseChatTimestamp로 흡수(JSC/Safari NaN 방지).
- **검증**: typecheck0·lint0·unit 314·build + 이사님(앱 고객)↔유슨생(CRM) 실기 크로스 스모크(메시지 왕복·타이핑 양방향·마크다운·아바타·스크롤 앵커) 통과.
- **follow-up**(PR 본문): 자동선택 상태 draft pin·human+타상담원 회수 액션·대기시간 라벨 interval 갱신·pending 실잔량 배지·AI 상담 요약·상담원 첨부 업로드·종료 상태(앱 협의)·팀 scope.

## crm-analyst 서류 자동분류 (완료 — 배포 + #129 머지)

- **spec** `ref/specs/2026-07-01-crm-analyst-document-classify-design.md` · **plan** `ref/plans/2026-07-01-crm-analyst-doc-classify.md`(8 Task, TDD).
- **결정**: ①함수 소스=CRM 레포 신설(`supabase/functions/crm-analyst/`, git 응집도) ②자동확정+사후수정(현 '파일 캐비닛' 흐름 유지) ③regex=폴백 체인(vision→unknown/에러→regex→기타서류) ④모델 `gemini-3.1-flash-lite`(앱 동일) ⑤호출흐름 A(프론트 직접 invoke). Edge=vision+unknown만, regex 폴백은 프론트 lib(책임 분리).
- **✅ Task 1~7 완료(Edge Function 백엔드 + 프론트 통합)**: 스캐폴드+deno config+eslint ignore · `doc-types.ts`(22종/프롬프트/responseSchema, 프론트 `DOC_TYPE_OPTIONS`와 byte-identical) · `auth.ts`(staff JWT 게이트, `src/auth/verify.ts` 재현) · `gemini.ts`(vision 호출+에러분류+재시도) · `index.ts`(Hono 조립 CORS+인증+분류, **미들웨어 `*`로 인증 우회 방지**, CORS allowHeaders에 x-client-info·apikey) · 프론트 `document-classify.ts`(AI 분류+regex 폴백, base64 32KB 청크) · `useCustomerDocuments` 교체+파일별 병렬화(optimistic "분류 중…"→"AI분류", tempId에 index 포함). 각 Task spec+quality 2단계 리뷰 + 전체 최종 리뷰(opus) 통과. 검증 typecheck0·lint0·test:unit**272**·deno test**13** 전부 green.
- **✅ Task 8 배포 완료**: master(`wmkbmlespgzkeekliwio`)에 `supabase functions deploy crm-analyst`로 배포(공유 master라 함수명 지정해 앱 함수 불가침). GEMINI_API_KEY는 프로젝트 전역 secret 재사용. staff 브라우저 검증 OK. (미배포/GEMINI 미설정이어도 프론트 regex 폴백이라 업로드는 계속 동작.)
- **follow-up**: ①`nextSortOrder` race(`src/db/queries/customer-documents.ts` `max(sort_order)+1` 비트랜잭션 — 병렬 업로드 시 sort_order 중복, **표시순서만·데이터 무결성 OK**, 백엔드 트랜잭션 fast-follow) ②재무제표 당해/전기 프롬프트 시간앵커(당해=최근연도 명시 or Date 주입 — 배포 후 실측 튜닝) ③`deno.lock`(root+`supabase/functions/`) untracked — 커밋 vs .gitignore 결정(재현성은 import_map 버전핀 hono@4.6.14/jose@6.2.3로 이미 확보).
- **다음 슬라이스(후속)**: 슬라이스B 완료(#132, 아래 요약).

## 업무 AI SSE 스트리밍 + 새 턴 앵커 (완료·머지 — PR #142)

- **spec** `ref/specs/2026-07-03-crm-work-ai-sse-streaming-design.md` · **plan** `ref/plans/2026-07-03-crm-work-ai-sse-streaming.md`(12 Task TDD, subagent-driven + 태스크별 spec/quality 2단계 리뷰 + 최종 통합 리뷰).
- **백엔드**: `/ask` body `stream:true` 분기(미지정=기존 JSON 경로 불변) — user+빈 placeholder **선저장** → `generateAnswerStream`(Gemini alt=sse) 청크를 SSE `text` 릴레이 → `finalizeStreamedAnswer`(정상=원문·중지=`" (중단됨)"`·에러=`" (연결 오류로 중단됨)"`·0자=placeholder 삭제+error 이벤트) → `done`={messages: 영속본 2건}. update/delete 쿼리는 **staffUserId 이중 키**.
- **프론트**: `createSseParser`(증분)+`askAssistantStream`(fetch stream, **done 즉시 resolve**·finally reader.cancel)+`useAssistantThread` 드레인 타자기(**38ms 틱, 스텝 2/4/7/11 — 앱 수치 테스트 고정**)+Send↔Stop 토글(stop=abort→500ms 후 재조회 동기화)+새 턴 앵커(질문 상단 20px, 마지막 턴 assistant에 min-height 예약=`computeTurnMinHeight`, **ResizeObserver**로 확대/축소·리사이즈 재계산).
- **핵심 함정(재발 방지)**: ①스트림 조기 종료 시 `reader.cancel()` 필수(releaseLock만으론 업스트림 Gemini/HTTP 잔존 — 백/프론트 대칭) ②CF `waitUntil`은 **릴레이 루프 전에 guard 프로미스 선등록**(abort 직후 아이솔레이트 회수 창에서 마감 저장 유실→유령 빈 말풍선) ③done 이벤트 수신 즉시 반환(물리 close 대기 중 abort가 완료본 버림) ④`return await streamAsk(...)` — await 없으면 선저장 실패가 try/catch를 우회 ⑤확대/축소 재계산은 expanded effect가 아니라 ResizeObserver(CSS 전환 완료 전 측정 잔존 — 스모크 실측으로 발견) ⑥SSE 클라 소비는 `TextDecoder {stream:true}` 필수(멀티바이트 경계).
- **검증**: unit 357·server 176·typecheck/lint 0·build + 브라우저 스모크 실측(타자기 성장 곡선 0→239자, 질문 gap 22~23px+smooth 감속, 중지 부분저장 429자+"(중단됨)" 리로드 보존, 유령 빈 말풍선 0, 재오픈 최하단). 스모크 대화는 master에서 삭제 완료.
- **follow-up**(PR 본문): 클라 `askAssistant` 데드코드 정리, "(중단됨)" 클라 하드코딩↔서버 STOP_SUFFIX 상수 동기화, 생성 중 재질문 supersede(현재 중지 후 재질문 2단계), 부분 마크다운 렌더 성능 prod 실측, 세션 최초 턴 min-height 53px 드리프트 1회 관찰(재현 실패 — prod 재발 시 후속).

## 슬라이스 B1 업무 AI 채팅 (완료 — #132 머지)

- **spec** `ref/specs/2026-07-02-crm-work-ai-chat-design.md` · **plan** `ref/plans/2026-07-02-crm-work-ai-chat.md`(12 Task, TDD, subagent-driven).
- pgvector RAG 수직 슬라이스: `crm.embeddings`(vector 3072 + halfvec HNSW, customer FK cascade; 마이그 0012·0013) ← 백필 `src/scripts/backfill-embeddings.ts`(코퍼스=메모/할일/상담이력/니즈3필드). `POST /api/assistant/ask`(staff JWT, `src/routes/assistant.ts`, `assistantDeps` 주입) = `resolveCustomerScope`→질문임베딩→pgvector top-k(halfvec cosine)→Gemini `gemini-3.1-flash-lite` 생성→`{answer,sources}`. 프론트 `client/src/lib/assistant.ts`+Topbar 팝오버 실연결(단일샷·평문). 임베딩 모델 `gemini-embedding-001`(3072).
- 검증: 백엔드 144·프론트 274·typecheck0·lint0·build·백필 29/29·RAG 실 Gemini end-to-end·브라우저 스모크 통과.
- **scope=admin(v1)**: `resolveCustomerScope`(`src/lib/assistant-scope.ts`)가 `"all"` 반환하는 seam. manager=팀/staff=본인은 **후속 crm.staff/팀 파운데이션 슬라이스가 이 함수 본문만 교체**(호출부 불변, 리스트/상세 scope에도 재사용). 팀 개념 데이터 전무(profiles=team 없음·불가침, staff 계정 0)라 파운데이션 선행 필요.
- **프로덕션**: `GEMINI_API_KEY` CF Pages 시크릿(Production) 설정 완료. 임베딩 데이터는 공유 master라 이미 적재(백필 실행됨). `c.env.GEMINI_API_KEY ?? process.env`로 읽음.
- **follow-up(별도)**: ①~~채팅 히스토리 영속~~·~~멀티턴~~ = **C1(#133)로 완료** ②content_hash 재임베딩 skip·백필 배치청킹·유사도 임계값(현재 top-k 항상 반환) ③견적 코퍼스 확장 ④SSE 스트리밍(후속).

## 슬라이스 C1 업무 AI 대화 영속+멀티턴+앱 UI (완료 — #133 머지)

- **spec** `ref/specs/2026-07-02-crm-work-ai-chat-persist-design.md` · **plan** `ref/plans/2026-07-02-crm-work-ai-chat-persist.md`(10 Task, TDD, subagent-driven).
- **영속**: `crm.assistant_messages`(staff별 평면 스트림, `staff_user_id`=JWT sub loose id, role user/assistant, content, sources jsonb, created_at; 마이그 0014, 인덱스 (staff_user_id,created_at)). `src/db/queries/assistant-messages.ts`(insert 원자·list 최근N 역순).
- **엔드포인트**(`src/routes/assistant.ts`): `POST /ask`가 최근10턴 멀티턴 컨텍스트로 Gemini 호출 + 성공 시 user/assistant **원자 저장**(실패 시 미저장), `GET /messages`=본인 최근30. `generateAnswer`에 history(assistant→model) 인자. **B1 평문 프롬프트 지시 제거**(마크다운 허용). `assistantDeps`에 쿼리 2개 추가.
- **프론트**: `askAssistant`(messages 반환)+`fetchAssistantMessages`, `MarkdownMessage`(react-markdown@10+remark-gfm, XSS 안전), `DoubleBounceDots`(앱 로딩 포팅·shimmer 아님). Topbar 진입 시 히스토리 로드·마크다운 렌더·항상 최하단 스크롤.
- **브라우저 스모크 후 UI fix 3건**(#133 내): 마크다운 인라인(레거시 `strong{display:block}` 해제+`list-style:disc`/`li::marker`), 유저버블 폭 점프(낙관적 turn `<div>`→`<Fragment>`로 grid 직접 자식화), 리로드 하단 스크롤.
- 검증: 백엔드 149·프론트 277·typecheck0·lint0·build·실 Gemini 멀티턴 스모크·브라우저 스모크·최종 리뷰(opus) 통과. `GEMINI_API_KEY` CF Prod 설정 완료.
- **후속 폴리시 완료 (PR #134, 2026-07-02)**: ①히스토리 **커서 페이지네이션**(백엔드 `listRecentMessages` before 복합커서 `(created_at,id)` + `GET /messages?before&beforeId`; 프론트 상단 스크롤 시 이전대화 로드→**상단 노출**(children[2].offsetTop, 40px 밖이라 연쇄로딩 방지), `AI_HISTORY_PAGE=30`) ②**AI답변 박스 제거·full-width**(user 버블만 유지) ③채팅 본문 `overscroll-behavior:contain`(배경 스크롤 전파 차단) ④로딩 인디케이터 **최소 400ms + top-center 오버레이**(레이아웃 무영향) ⑤확대 패널 body `calc(100vh-224px)`(뷰포트 안·하단 여백 ~20px — 넘치면 문서 스크롤↑→배경 쉬프트). 브라우저 실측 반복 후 확정.
- **남은 follow-up**: ~~SSE 스트리밍+타자기+송신/중지 토글~~(#142 완료), 히스토리 pruning. `aiTurns.answer` dead 필드는 #142 리라이트로 자연 제거됨.

## 고객 상세 분해 요약

- 목표: `KimMinjunDetailContent`를 영역별 hook + presentation component로 분해하고 `kim` 전용명을 범용화한다.
- 완료된 추출 영역: 헤더, 메모, 할일, 일정, 서류, 상태/워크플로우, 구매조건, 니즈, 견적함 목록, 견적 워크벤치.
- 부모는 cross-cutting 상태와 핸들러만 보유한다: `openEditor`, `setOpenEditor`, `toggleEditor`, `savePatch`, `markRecentUpdate`, `kimEditorMatches` 등.
- 견적 워크벤치는 9b~9e를 단일 훅으로 통합하는 방향으로 확정했다. 내부 결합이 강해 4분할은 props 폭발 위험이 컸다.
- 브라우저 검증: 구매조건/니즈, 견적함/워크벤치, 앱카드, PDF원본, 작성완료 INSERT/UPDATE, 발송, 딥링크/prefill까지 검증 완료로 기록되어 있다.
- 남은 일: 없음. `kim` 리네임도 **완료**(2026-07-04, PR #156 — TS 심볼·파일명 전량, `kim-*` CSS 클래스는 시각 회귀 0 원칙으로 의도적 보류). `data/prototype.ts`·`QuotesPage`의 "김민준" 목업 문자열은 합의대로 불변.

## 최근 완료 요약

- 니즈 앱 카드 늦은 로딩: 고객별 cache+TTL+dedupe와 hover prefetch로 완료.
- 상세 수정 즉시 반영: savePatch/task mutation 성공 시 목록 reload 경로 보강 완료.
- 상세 구매조건 7필드 저장: `crm.customers` 컬럼과 CHECK/상수 SSOT, 낙관/롤백 저장 경로 완료.
- 고객 니즈 영역 앱 견적요청 카드 목록: 앱 유입 고객은 여러 견적요청 카드, 수기 고객은 기존 단일 need 카드 유지.
- 고객 상세 전체 고객 일반화, 서류 업로드/미리보기/분류, 견적 읽기/쓰기/생성/원본 업로드, 워크벤치 저장/발송 흐름은 main에 머지된 완료 항목으로 본다.
- 의존성 minor/patch 일괄 bump(#127, @types/node major는 24 라인 유지).

## Files / References

- CSS: `client/src/index.css`(@import 목록만) + `client/src/styles/*.css`(도메인별 24파일, **임포트 순서=캐스케이드 — 재배치 금지**)
- Main UI: `client/src/pages/CustomerDetailPage.tsx`
- Customer detail components/hooks: `client/src/components/customer-detail/`
- CRM AI: `supabase/functions/crm-analyst/`(배포됨) + 프론트 `client/src/lib/document-classify.ts` + Gemini 릴레이 `supabase/functions/crm-gemini-proxy/`(+`src/lib/gemini-target.ts`, spec/plan: `ref/specs|plans/2026-07-03-crm-gemini-edge-proxy*`)
- crm-analyst references: `ref/specs/2026-07-01-crm-analyst-document-classify-design.md`, `ref/plans/2026-07-01-crm-analyst-doc-classify.md`
- Auth/spec references: `ref/specs/2026-06-18-crm-auth-design.md`, `ref/plans/2026-06-18-crm-auth-*.md`
- Decomposition references: `ref/specs/2026-06-29-crm-detail-decomposition-design.md`, `ref/plans/2026-06-29-crm-detail-decomposition.md`
- Purchase conditions references: `ref/specs/2026-06-30-crm-purchase-conditions-persist-design.md`, `ref/plans/2026-06-30-crm-purchase-conditions-persist.md`

## Verification / Next

- DOM or TypeScript changes: run `bun run typecheck`.
- Broader CRM changes: run `bun run typecheck`, `bun run lint`, `bun run test:unit`, and `bun run build` when feasible.
- Edge Function(Deno) changes: `deno test/lint/check --config supabase/functions/deno.json ...`.
- Large visual layout changes: use browser/screenshot verification after stabilization, not after every spacing tweak.
- Backend(Hono) 테스트는 `bun run test:server`(bun:test, `--env-file=.env.local`로 실 master DB), 프론트는 `bun run test:unit`(vitest).
- Current likely next step: `crm-analyst`·배정·상세분해·**업무 AI B1(#132)~SSE 스트리밍(#142) 전체**·**실시간 상담 콘솔(#135)** 모두 완료·머지됨. 다음 큰 슬라이스 = **데이터화/일반화**(이사님 점진 일반화 컨펌 선행). 역할 기반 scope 실제화는 그 뒤(팀 개념 없음 — 위 다음 후보 참조). 신규 brainstorming→spec→plan.

## Collaboration

- User is `이사님`; assistant is `영실`.
- Judgment questions: give recommendation first, ask `적용할까요?`.
- Execution words like `응`, `해줘`, `적용해`, `진행하자`: implement directly.
