# Mr. Cha CRM Active Session Brief

> **이 파일은 매 세션 자동 로드된다. 60줄 이하를 유지한다**(AGENTS.md 핸드오프 규칙).
> 과거 세션 로그는 여기 쌓지 말고 `ref/session-archive.md`로 보낸다 — 2026-07-21에 이 파일이
> 142k자까지 자라 세션 컨텍스트의 14%를 먹고 있었다. 지속되는 결정·계약은 `AGENTS.md`(불변 규칙),
> `ref/specs/*`(설계 근거), `ref/current-working-state.md`(장기 상태)가 각각 집이다.

Last updated: 2026-07-22

## 지금 상태

**리팩토링 배치 14 전량 종결 + Gemini 생성 모델 상향까지 완료. 진행 중인 미완 작업 없음.**

최근 머지(main `c6c1ce3`): `#315` 도구 오라우팅 차단+폴백 · `#316` 임베딩 해시 네임스페이스 분리 · `ef45ebb` 업무 AI 문서·그물 · `#317` 워크벤치 재동기화·팝오버 · `#318` 계약 프리즈·위생 · **`#319` Gemini 모델 상향**(+Edge 재배포·AI 힌트 22건 재생성 완료).
main 통합 검증 green — typecheck 0 · lint 0 · unit **1063** · server **650** · build · knip 7/9 **+ hints 0** · 픽스처 잔재 0.

## 직전 세션 요약 (2026-07-22 · 0722-AI-test)

**① 업무 AI 실기 확인(브리프 예정 작업).** magiclink 스모크로 `#312`·`#313`을 실물 확인 — 관련 질문 근거 정확·잡담 8→0(컷 최고 0.4575)·톤 정상. **여기서 별개 결함을 발견**: 라우팅 우선 게이트가 RAG 근거를 가려 "정보가 없습니다"로 답하는 사례 2건(마이바흐·박서연). → **`#315`**로 2겹 수정. ⚠️**핵심 교훈 = Gemini function calling에서 인자 날조는 시스템 프롬프트로 안 막힌다**(실기 재현) — 모델이 실제로 보는 **도구/파라미터 description**을 조여야 멈췄고, "관심 차종" 같은 **부정 예시를 description에 직접 박은 것**이 결정적이었다.

**② 배치 14 감사**(미감사 8건 = `#308`~`#315`). 관례 전량 — 기준선 재실측 → 4앵글 병렬 → **적대 검증 3렌즈 87표** → ground-truth 스팟 11건(실 Gemini·실 코퍼스·변이 주입) → 4PR. 판정 = **CONFIRMED 3 · ADJUSTED 15 · 기록만 5 · 기각 3**(+제안 기각 9종). SSOT = `ref/plans/2026-07-22-crm-refactor-batch-14.md`(268줄 — 상세·기각 근거·감사 한계 전부 거기).

**③ 4PR 이행.** 요지만 — **K1**(`#316`): `#312`가 `contentHash`에 모델명 salt를 섞으며 **임베딩과 무관한 AI 힌트 캐시 키까지 무효화**(22/22 구 스킴 실측). 발화 전에 잡아 순수 해시로 분리 → **재매치 22/22 실증 = churn 0 회피**. 임베딩 코퍼스 불변식 가드도 신설 · **K2**(`ef45ebb`): sources 타 고객 혼입의 원인이 임계값이 아니라 **`TOP_K` 구조**임을 확정(간격 0.017 = 절대 컷 불가), `#313`이 드롭한 근거 카테고리 복원, 프롬프트 하드닝 문자열 잠금(지워도 GREEN이었다) · **K3·K4**(`#317`): 재동기화 effect bail-out을 업데이터 밖으로(커밋당 호스트 2회 실행 해소), `#308`이 남긴 변이 기록이 **양방향으로 틀렸음**을 재실측해 정정(RED = 3·4·6·7), 팝오버 버튼 집합 잠금 · **K5·D6·D8**(`#318`): handoff RPC 페이로드 **형상 전량 단언**(프리즈인데 `p_timezone` 하나만 잠겨 있었다), `#311`이 남긴 죽은 글롭 제거(`format:check`가 `--write`로도 green 불가였다), **`AGENTS.md`에 임베딩 계약 신설**(모델명이 매 세션 교체되는 brief에만 있었다).

**④ Gemini 생성 모델 상향(`#319`).** `gemini-3.1-flash-lite` → **용도별 2분할**: 업무 AI 답변·**도구 라우팅**·AI 힌트 = `gemini-3.5-flash-lite` / **서류 vision 분류만 `gemini-3.6-flash`**(오분류 = 사람 재확인 시간인데 업로드당 1회·출력 20토큰이라 월 수천 원 차). ⚠️**임베딩과 달리 생성은 저장물이 없어 앱과 일치시킬 이유가 없다**(계약 아님). **`GEN_MODEL` 변경 = 라우팅 판단 변경**이고 자동 테스트는 라우터를 페이크 주입해 못 잡으므로(K2-d) **실기 골든 4종 필수** — 이번에 4/4 통과·톤 육안 확인. 서류는 **기존 서류함 3건 재분류 3/3 일치**(읽기만·쓰기 0). Edge는 CF와 별개라 `supabase functions deploy crm-analyst` 수동 배포(**슬러그 명시** — 생략 시 로컬 전체가 나간다. 앱 함수는 소스가 없어 구조적으로 안전, 타임스탬프로 실측 확인). AI 힌트 22건은 hash 클리어+백필로 통일 — **톤 형식 22/22 유지**(볼드+한 줄), 평균 58→51자. 구 문구 백업 보유. 계약 = `AGENTS.md`, 메모리 = `crm-gemini-model-layout`.

**⑤ 실수 3건(정직 기록).** ⓐsources 노이즈를 "0.60 하향의 위험"으로 **원인 오귀속**(실제는 `TOP_K`) ⓑpending 20에 "예외적 엣지"라 적었으나 실측 **20/22 고객 상시 0행** ⓒ**PR2를 브랜치 없이 main 직접 푸시**(유슨생 판단으로 존치) + 감사 워크플로우에 격리 지시를 안 넣어 병렬 에이전트가 **공유 워킹트리에서 서로의 변이를 밟음**(5건 — 자가검증 없었으면 K1 판정이 정반대). **다음 배치부터 변이 실험은 격리 사본에서만, 프로브에 자가검증 필수.**

## ▶ 다음 작업 (미확정 — 후보)

- **이사님 회신 대기가 최우선 후보**: `ref/director-pending-confirmations.md` **항목 20**(#315가 제시한 완화안이 실측상 성립 불가로 드러나 철회·교체 완료 — 이사님은 정정본을 보시게 된다) · **21·22·23 신설**(sources 타 고객 표시 · `none` 경로 문구 · 타임존 라벨). 21·22는 업무 AI 표시 UX라 **묶어서 결정받는 게 효율적**이다.
- **배치 15 감사**: 미감사 **5건**(`#316`·`ef45ebb`·`#317`·`#318`·`#319`)으로 관례 규모(5~6) 도달 — 지금 해도 되고 몇 건 더 쌓아도 된다. ⚠️**감사 워크플로우에 격리 지시를 넣을 것**(⑤ 참조 — 지난번 공유 워킹트리 변이 충돌 5건).
- **별건**(감사가 남긴 것): NO_HITS 문구와 sources가 동시 렌더되는 것(표시 정책과 함께) · 배치 13 별건 3종(`resetQuoteWorkbench` DOM 미청소 등) 계속 유효.
- **업무 AI 실기는 재확인 불필요** — 배치 14 감사가 사실상 수행했다(근거 회수·오염·톤 3가지 다 답이 나왔다).

## 대기 (우리 액션 없음)

`ref/director-pending-confirmations.md` — 항목 14(재판단) · 16·17 · 18·19 · **20**(정정 완료) · **21·22·23**(신설) · **24**(모델 상향·힌트 재생성 사후 공유).
**파트너 Phase B 완료 시** 산은·iM·농협 게이트 자동 점등 → 실기 1회. 이때 **구매방식 전환 후 기간 상태** 한 줄 확인.
실기 협조 2건(FCM 실기기·앱 #582 크로스)은 **애플 개발자 등록 후 재론** — 먼저 밀지 말 것.
자동 스크린샷/픽셀 비교 하네스는 **폐기 종결**(#311) — 시각 확인은 눈으로 1회 + magiclink 절차.

## Boot

1. `AGENTS.md` → 이 파일 순으로 읽는다.
2. `git status --short --branch` · `git log --oneline --decorate --max-count=5`
3. 더 필요하면: 과거 세션 경위 = `ref/session-archive.md` / 장기 상태 = `ref/current-working-state.md` / 설계 근거 = `ref/specs/*`, `ref/plans/*`
4. 23개 원본 기획 문서는 전략·로드맵·아키텍처를 명시적으로 다룰 때만 읽는다.

## 세션 마무리 규칙

- 이 파일은 **교체**한다(누적 금지). 직전 세션 요약만 남기고 그 이전 것은 `ref/session-archive.md` 맨 위로 옮긴다.
- 행위 변경이 생기면 `ref/director-pending-confirmations.md`에 등재한다(PR 본문 🟡와 병행).
- 지속되는 계약·함정은 `AGENTS.md`에, 설계 근거는 `ref/specs/*`에 — 여기 쌓지 않는다.
