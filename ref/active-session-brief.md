# Mr. Cha CRM Active Session Brief

> **자동 로드 · 60줄 이하 유지**(AGENTS.md). 과거 로그 = `ref/session-archive.md` · 지속 결정 = `AGENTS.md` · 설계 = `ref/specs/*` · 장기 상태 = `ref/current-working-state.md`.

Last updated: 2026-08-04

## 지금 상태

main 전량 green(CI 8단계). unit **1371** · pure **281**. 회원탈퇴 CRM 몫·파기 감사 모두 종결.
출고/정산 축은 **이사님 회신이 전부 도착**해 미결정 0 — 계약·화면 모두 구현 완료(잔여 = 목록 컬럼 1건).

## ▶ 다음 (집에서 이어서)

- 🔴 **바로 착수 = 정산상태 편집 UI**(기능 공백 — 워크플로가 끊겨 있다). 담당자 "정산 요청"은
  있는데 **admin이 정산완료로 넘길 화면이 없다**. 서버는 준비 끝(`PUT /settlement`가 `status`를
  받고 zod enum 3종). 할 일 = **정산 섹션**(admin 전용 · `CustomerManagementRow`의
  `DeliveryInfoPopover`)에 ①**상태 select 3종** ②저장 body에 `status` 싣기(지금은 입금일·실입금액·
  비용만 보낸다) ③현재 상태 표시(담당자가 올린 "정산요청"이 admin에게 안 보인다).
  ⚠️ controlled select라 **`bindSelect` 필수**(Safari). **팝오버 분리는 이번에 하지 않는다** —
  다음 정산 작업(목록 컬럼)의 큰 diff에 흡수(로컬 메모리 `crm-row-popover-split-trigger`).
- **정산 목록 컬럼**(그 다음): 비용/마진/상태를 목록에 보이려면 **목록 응답에 정산을 실어야 하는데
  "목록 미포함" 원칙과 충돌**한다(exposure tripwire가 잠금) → **admin 전용 목록 조회 설계가 선행**.
- **잔여 2건(머지는 끝났다 — 실행·육안만)**: ⓐ**김지운(`jiandolce@gmail.com`) 탈퇴 테스트 미실행**
  (견적 20·**앱카드 19** — 카드 회수 경로 검증) ⓑ`#445` 배너 **육안 미확인**(대기 큐 0건이라 안
  뜬다). **ⓐ를 하면 ⓑ가 딸려 온다**(탈퇴 접수 = 큐 1건 → 배너 노출).

## 직전 세션 (08-04~05 · 유슨생)

- **08-05**: `#448` 팝오버 3연쇄 수정(max-height · **자기 스크롤에 자기가 닫히던 버그** =
  `usePopoverViewportClose` **selfRef** · 금액 콤마·"원") · **이월 실기 4건 전량 종결**(브라우저 —
  교차 세션 실시간은 이주선 팀장 실업무 요청이 겹쳐 타 사용자 broadcast까지 실증) · 정산 화면 육안 확인.

- **`#442` 파기 로직 감사** → 상 0·중 1·하 3·행위 변경 0(결함 0건). 고친 중 1건 = 경계 테스트가
  "하루 늦게 지우는" 변이를 통과시켰다(픽스처 30일 ±1시간 — **다시 벌리지 말 것**).
  판정 SSOT = `ref/plans/2026-08-04-crm-purge-logic-audit.md`.
- **🔴 김민준(`CU-2605-0020`) 삭제됨** — 탈퇴 실경로 테스트(전량 파기·복구 불가). **유일한 풀세트
  시드**였다 → **시범 고객·시드 재지정이 선행**(AGENTS.md 반영).
- **`#446`·`#447` 정산 비용·단계**(이사님 확정): 비용 5종 **jsonb 배열**(마이그 **0052**) ·
  **페이백도 차감**(양수 저장) · 단계 3종 · **마진은 파생**(미저장, 불가 시 "—"). ⚠️ **권한 충돌
  해소** — 정산은 admin 단독인데 "정산요청"만 담당자 행위라 `POST .../settlement/request` **한
  경로만** 열고 **미정산→정산요청 전이 하나로** 좁혔다. 담당자는 정산을 못 읽어 **버튼이 항상
  눌리고 서버가 409로 판단**한다.
- **`#443`·`#444` 의존성** 패치 6 + 마이너 8. 잔여 = **major 2건 보류**(TS 7 = eslint peer `<6.1.0` → lint 사망 · `@types/node` = bun이라 실익 0). **`#445`** 탈퇴 배너 = 부모 2열 grid 칸 침범이 원인 → rail 밖 전체 폭.
- **이사님 확정**: 히어로 대수 = **바 목록 유지**(호버 제안은 현물 보고 닫힘) · 금융리스/일시불 = 실적 없음.

## 대기

**이사님** = `ref/director-pending-confirmations.md` **16건**(항목 14·16~30) + Supabase
**"EXCEEDING USAGE LIMITS" 배지**(08-01) 플랜 + **D+5 최악 6일차** SLA 해석.
💡 회신 때 **🟡 하드 삭제 확장(`#440`)을 항목 28**(AI 기록에 번호 잔존)**과 묶을 것** — 같은 축·미등재.

## Boot

`AGENTS.md` → 이 파일 → `git status -sb` · `git log --oneline -5`. 상세: 정산 = spec §6a · 보존 계약 = AGENTS.md.

## 세션 마무리 규칙

**교체**(누적 금지 · 이전 요약은 `ref/session-archive.md` 맨 위로). 행위 변경은
`ref/director-pending-confirmations.md` 등재(**유슨생 현장 승인은 등재 없이 박제** · 이사님 확정
설계를 뒤집는 건만 등재 · 신설 시 그 파일 롤업 2곳도). 상세 규칙 = AGENTS.md "Handoff Documents".
