# Mr. Cha CRM Active Session Brief

> **이 파일은 매 세션 자동 로드된다. 60줄 이하를 유지한다**(AGENTS.md 핸드오프 규칙).
> 과거 세션 로그는 여기 쌓지 말고 `ref/session-archive.md`로 보낸다 — 2026-07-21에 이 파일이
> 142k자까지 자라 세션 컨텍스트의 14%를 먹고 있었다. 지속되는 결정·계약은 `AGENTS.md`(불변 규칙),
> `ref/specs/*`(설계 근거), `ref/current-working-state.md`(장기 상태)가 각각 집이다.

Last updated: 2026-07-22

## 지금 상태

**리팩토링 배치 13 전량 종결·머지·검증 완료. 진행 중인 미완 작업 없음.**

최근 머지(main): `#308` 게이트 거울 · `#309` 넛지 클러스터 · `#310` 위생·문서.
main 통합 검증 green — typecheck 0 · lint 0 · unit **1061** · server **640** · build · knip 7/9 · 픽스처 잔재 0.

## 직전 세션 요약 (2026-07-22 · 0722-fresh-start)

**리팩토링 배치 13**(미감사 = #303·#304·#305·#306·#307, 코드 41파일 +1,483/−249 — 직전 배치의 이행 PR 2건이 감사 대상). 관례 전량: 기준선 재실측 → **4앵글 병렬 감사**(A 서버·B 클라·C 외부 계약·D 크로스커팅) → **적대 검증 3명**(V1 설계 비교·V2 반박·V3 하급 전수 심사) → ground-truth 스팟 → 3PR. 판정 = **CONFIRMED 8·ADJUSTED 2·기록만 6·기각 3**. SSOT = `ref/plans/2026-07-22-crm-refactor-batch-13.md`.

**① K1 게이트 거울(#308·중)** — 3앵글이 독립 발견. `lenderByCard`가 화면 금융사와 어긋나 **정당한 기간·약정거리가 사라지는 fail-closed** 4경로(수정 진입 잔상·랭킹 모달·구매방식 전환의 무이벤트 value 상실·`clearCardUiState`가 DOM은 두고 거울만 비움). **스펙 §4.4의 "동기화 지점 4개" 목록 자체가 틀렸다**(2지점 누락 + clear 지시는 어긋남 생성)는 게 V1 실증 → 스펙 이행안 기각하고 **"DOM이 진실·거울은 파생·재동기화 1지점"**(dep 없는 `useLayoutEffect` + `sameStringMap` bail-out **한 쌍**)으로 전환, +14/−2줄로 4경로 전부 닫힘. 회귀 그물 7종(실 컴포넌트 렌더 — 배선층은 `renderHook`으로 안 잡힘), 변이 3종 RED 실관찰(bail-out 무력화 = `Maximum update depth exceeded`).

**② K2 넛지(#309)** — 배치 12 구조 클리어의 미완분. 오프너 6곳 중 **2곳** 누락으로 **정상 조작에 다이얼로그 3중 동시 렌더**. + 마킹 롤백 시 넛지 잔존(contracting 0인데 계약완료 전이) + 니즈 카드 "견적 보기" 죽은 버튼(선재 #156·#158, 배치 12가 명시 이월). **⚠️ 이행 함정**: 권고된 "5줄→`closeQuoteActionPopover()`" 한 줄 교체를 **조기 반환 분기에 적용하면 안내가 아예 안 뜨는 회귀** — 꼬리에만 적용(전용 테스트 + 그 회귀 변이 M6도 RED로 걸림). K2-b는 **클릭-선행 레이스를 클라만으로 못 닫음**을 주석 박제(창을 좁힐 뿐).

**③ 위생·문서(#310)** — 고아 dismissal **생성 경위 특정**(게이트 변이 검증 중 실 INSERT) → 원천 1줄 + 실 DB 정리(스캐너 확장은 오탐 구조라 기각) · dismiss URL 회귀 잠금(쌍둥이 비대칭) · `AGENTS.md` import 목록 등재(모듈이 "등재됨"이라 자기증명하던 상태) · 배치 12 세션 블록 **아카이브 복원**(브리프 교체 때 강등 없이 덮여 사라져, 배치 13 감사 대상의 근거가 repo에 없었다).

## 다음 후보

- **🔴 `visual:crm` 하네스 결정** — 스냅샷 베이스라인(2026-05-17) < 카카오 로그인 게이트(2026-06-18)라 **약 2개월간 실행 원천 불가**(로그인 화면에서 멈춤). `AGENTS.md` 검증 예산이 죽은 도구를 가리킨다. (a) 되살리기(magiclink storageState + 재촬영) vs (b) 폐기.
- **파트너 Phase B 완료 시** 산은·iM·농협 게이트 자동 점등 → 실기 1회(우리 몫 작업 없음). 이때 **구매방식 전환 후 기간 상태**(B#7 — MG는 오늘도 갈림) 한 줄 확인.
- 배치 14는 미감사 축적 후(현재 #308·#309·#310).

## 대기 (우리 액션 없음)

`ref/director-pending-confirmations.md` — 항목 14(재판단) · 16·17(사후 공유) · **18·19 신설**(dismiss 권한 · 폴백 비가역성 — 둘 다 판단 요청, 기각 시 코드 변경 0/소형).
실기 협조 2건(FCM 실기기·앱 #582 크로스)은 **애플 개발자 등록 후 재론** — 먼저 밀지 말 것.

## Boot

1. `AGENTS.md` → 이 파일 순으로 읽는다.
2. `git status --short --branch` · `git log --oneline --decorate --max-count=5`
3. 더 필요하면: 과거 세션 경위 = `ref/session-archive.md` / 장기 상태 = `ref/current-working-state.md` / 설계 근거 = `ref/specs/*`, `ref/plans/*`
4. 23개 원본 기획 문서는 전략·로드맵·아키텍처를 명시적으로 다룰 때만 읽는다.

## 세션 마무리 규칙

- 이 파일은 **교체**한다(누적 금지). 직전 세션 요약만 남기고 그 이전 것은 `ref/session-archive.md` 맨 위로 옮긴다.
- 행위 변경이 생기면 `ref/director-pending-confirmations.md`에 등재한다(PR 본문 🟡와 병행).
- 지속되는 계약·함정은 `AGENTS.md`에, 설계 근거는 `ref/specs/*`에 — 여기 쌓지 않는다.
