# 리팩토링 배치 14 — 감사·판정·이행 SSOT (2026-07-22)

**범위**: 배치 13 이후 미감사 8건 = `#308`(게이트 거울)·`#309`(넛지 클러스터)·`#310`(위생·문서)·`#311`(죽은 시각 하네스 폐기)·`#312`(임베딩 모델 이관)·`#313`(유사도 임계값 0.60)·`#314`(운영 설정 `p_timezone`)·`#315`(도구 오라우팅 차단 + 폴백). 관례 규모(5~6) 초과.
**감사 가치가 평소보다 높았던 이유**: `#312`~`#315`는 외부 일정(001 shutdown)·앱 팀 회신·실기 관찰에 맞춰 **4앵글 감사 없이 바로 나갔다.** 실 데이터 161행을 전량 갈아엎었고(#312) 검색 동작을 바꿨다(#313·#315).
**방법**: 관례 — 기준선 재실측 → **4앵글 병렬 감사**(A 서버·B 클라·C 외부 계약·D 크로스커팅) → **적대 검증 3렌즈**(V1 설계대조·V2 반박·V3 하급 전수 심사) → 오케스트레이터 **ground-truth 스팟 11건**(실 Gemini·실 코퍼스·변이 주입) → 판정 → PR.

**기준선(부분 재실측)**: typecheck 0 · lint 0 · unit **1062**(스팟 B4가 전량 실행) · knip 7/9 · 픽스처 잔재 0 · `crm.embeddings` 161행 전량 현행 모델 해시 일치. ⚠️ `test:server` **641 전량은 재실행하지 않았다**(앵글별 개별 파일만 — 한계 절 참조).

**신성 규칙 점검**: `db:push`/마이그레이션 0 · `public.profiles` 쓰기 tripwire 8 pass · Workers fetch plain call 준수(`gemini-post.ts:14`) · 알림 트리거 4테이블 무접촉(감사 중 **조회조차 회피**) · 픽스처 registry clean(16 pass·잔재 0) · handoff RPC 계약 프리즈 유지(서버 호출 0건 + 원격 `pg_get_function_arguments()` 실측 일치) · Safari select clean(이번 배치 신규 `<select>` 0) · 서버→클라 import 경계 신규 위반 0 · Edge 패리티 clean(`supabase/` 무접촉). **정정 1건**: knip "7/9 무드리프트"는 `--no-config-hints`가 가린 상태였다(D6).

**판정 총계 — CONFIRMED 3 · ADJUSTED 15 · 기록만 5 · 기각 3**(+ 재제안 금지 제안 9종 박제).

---

## 클러스터 K1: 임베딩 해시 축 — `contentHash`가 두 도메인을 잘못 묶었다 (`#312`)

### K1-a 교차 도메인 오결합 — **하 ADJUSTED (앵글 A·C·D 3중 교차 · 스팟 PROVEN)**

**정정된 주장**: `#312`가 `contentHash`에 `EMBEDDING_MODEL`을 섞으면서(`src/lib/assistant-corpus.ts:51`), 임베딩과 무관한 **세 번째 소비자** `ai-hint-on-write.ts:33`(`crm.customers.ai_summary_source_hash`)의 캐시 키까지 무효화했다. 읽기 전용 프로브 3회(A·C·스팟) 전부 동일 실측 — `{ total: 22, matchNew: 0, matchOld: 22, other: 0 }`. 즉 **22명 전원이 구 스킴 해시를 들고 있고**, 재료 바이트는 100% 동일하다(`other: 0`이 이를 자기증명). `ai-hint-on-write.ts:34`의 `if (src.sourceHash === hash) return "unchanged"`가 전 고객에서 성립하지 않는다.

원 주장에서 **깎아낸 3가지**(세 검증자 일치):
- **"다음 쓰기마다"는 과장 — 고객당 1회성·자가치유.** 재생성 직후 `setCustomerAiHint`가 신 스킴 해시를 저장하므로 이후 `unchanged`가 복구된다. 피해 상한 = flash-lite **22콜 1회** + 힌트 문구 22건 재작성.
- **"이사님 톤 승인분 파괴"는 프레이밍 오류.** 승인 대상(pending 117)은 `AI_HINT_SYSTEM_PROMPT`가 만드는 **톤 정책**이고 프롬프트·`GEN_MODEL`·재료·temperature 0.2 전부 불변이라 톤은 보존된다. 정확히는 "가시 문구의 1회 무음 churn".
- **"결합 방향이 반대(프롬프트·GEN_MODEL 변경은 무효화 못 함)"는 `#312` 소관이 아니다** — 2026-07-12부터의 선재 속성이며 "프롬프트+hash 클리어+백필" 수동 절차가 이미 문서화돼 있다.

**⏱ 타이밍이 이득을 좌우한다**: 실측 시점 22/22가 아직 구 스킴 = **재생성이 아직 한 건도 발화하지 않았다.** ai-hint 해시를 **순수 `sha256(material)`로 되돌리는 형태**로 분리하면 22행이 즉시 재매치돼 churn이 **0으로 회피**된다. 늦을수록 그 이득이 사라지고, 프롬프트 버전 상수를 새로 섞으면 오히려 지적한 피해를 스스로 1회 재현한다.

**이행**: `embeddingContentHash(content)`(모델명 포함, 임베딩 2경로 전용) / `contentHash` 또는 `aiHintSourceHash(material)`(순수) 분리 + `assistant-corpus.ts:46` 주석의 "재임베딩 skip 판단용" 서술을 **실제 소비자 목록**으로 정정. 🟡 행위 변경(아래 참조).

### K1-b `#312`가 "핵심 안전장치"라 선언한 것에 회귀 테스트 0건 — **하 CONFIRMED (2/3 CONFIRMED)**

`assistant-corpus.ts:52`를 `update(content)`로 되돌리는 변이가 **전 스위트 GREEN**(스팟 3회 독립 재현: 56 pass·50 pass·0 RED). 원인은 구조적이다 — 세 소비 테스트가 전부 기대값을 **프로덕션 `contentHash` 자신으로** 계산하는 자기참조이고(`embed-on-write.test.ts:40`·`ai-hint-on-write.test.ts:36,41`), 저장소 전체 테스트에 `createHash`/`sha256` 리터럴이 **0건**이다. `crm.embeddings`에 모델 컬럼도 없어(`schema.ts:321-335`) 해시 접두가 **단일 방어선**이다.
**정밀 정정**: impl만 되돌리면 eslint `no-unused-vars`가 1건 잡지만, 자연스러운 정리(import 동반 삭제)는 lint·typecheck·test 전부 통과한다. 즉 lint는 가드가 아니라 완전 GREEN으로 안내하는 과속방지턱이다.
**이행**: 케이스 1종 — ①스킴 잠금 `expect(contentHash("x")).toBe(sha256(\`${EMBEDDING_MODEL}\n\` + "x"))` ②의도 잠금 `expect(...).not.toBe(sha256("x"))`(구분자 리팩터에 면역, salt 제거에만 RED). K1-a 적용 시 `embeddingContentHash`에 건다.

### K1-c 구·신 모델 혼입 탐지 자동 수단 0 — **하 ADJUSTED**

**정정된 주장**: 결함은 "백필 all-or-nothing"이 아니라 **"저장된 모든 벡터가 현재 모델 산출물"이라는 불변식을 확인할 수단이 없다**는 것이다. 세 검증자가 원 서술 3곳을 반증했다 — ①백필은 수동 스크립트라 실패가 **터미널에 크래시로 요란하다**(무증상 아님) ②`embed-on-write.ts:40-41`이 신 스킴 해시로 비교하므로 스테일 행은 **그 소스의 다음 편집으로도 자가치유**된다(영구 아님) ③레포에 `.github` 자체가 없어 "CI가 잡는다"는 성립하지 않는다.
**그럼에도 위험 순서가 이번에 실제로 실행됐다**(스팟 실측): `#312` 커밋 10:55:27 KST vs 백필 행 `updated_at` 10:56:05~11 KST = **백필이 prod 배포에 38초 선행**. 창 안 쓰기가 0건이라 실피해는 없었다.
**이행**: 판별식 1쿼리를 `check:residue`(또는 `fixture-residue` 옆 소형 테스트)에 추가 — `content_hash <> encode(digest(EMBEDDING_MODEL||chr(10)||content,'sha256'),'hex')` 행 수 > 0이면 실패. 실행 검증 완료(현재 161/161 일치·오탐 0·1초 미만). ⚠️ `embeddings.test.ts`의 리터럴 해시 픽스처(`"h1"`·`"hash-v1"`)가 run 중단 시 잔재로 남으면 오진하므로 실패 메시지에 두 원인을 병기할 것.
**기각**: 배치 단위 upsert(아래 박제).

---

## 클러스터 K2: 업무 AI 검색·라우팅 (`#313`·`#315`)

### K2-a 임계값 0.60에서 sources가 타 고객으로 채워진다 — **하 ADJUSTED (A1=C2 · 스팟 PROVEN)**

**정정된 주장**: "8건 중 6건이 타 고객"은 실측으로 정확히 재현됐다(스팟 3회, 소수 셋째 자리까지 일치 — 0.6282~0.6661). 그러나 **원인은 `SIMILARITY_THRESHOLD`가 아니라 `TOP_K = 8` 고정 + 고객당 청크 수**다: 코퍼스 161행 중 제임스 99·김민준 15·김지안 9이고 **나머지 19명은 정확히 2청크**다. 박서연 본인 청크 2개가 1·2위를 차지했으므로 검색은 **회수를 100% 성공**했고 나머지 6슬롯은 임계값과 무관하게 구조적으로 채워진다(본인 2위 0.6831 vs 타 고객 최상위 0.6661 = 간격 **0.017** — 어떤 절대 컷으로도 분리 불가).
- **다고객 근거는 설계 의도다**: 스펙 §74·§75·§77이 scope 필터만 두고 고객 필터를 두지 않으며, `buildContextBlock`이 블록마다 `[n] (고객명 · 상태)`를 라벨링하고 SYSTEM_PROMPT가 "관련 고객과 근거를 함께 제시"를 지시한다("마이바흐 관심 고객" 류 횡단 질문의 전제).
- **생성 오염은 반증됐다**: 스팟이 실 Gemini 생성을 3회 돌려 전부 대상 고객 근거만 인용했고(`[근거: 1, 2]`), 오귀속 유도 질문("박서연 연체 이력 있어?" — 김지안 심사메모가 top-1 0.8046)에도 **"관련 CRM 데이터를 찾지 못했습니다"**로 거부했다.
- **`#313`이 만든 회귀도 아니다**: 구 0.75는 삭제된 주석이 명시하듯 tag-along 밴드(≤0.757) **하단**에 놓인 재현율 우선 값이었고, 같은 질문에서 0.75는 8건 전부 컷 = NO_HITS(기능 파손)였다.

**남는 실질 = 문서 회귀 1건**: `#313`이 재보정하며 구 주석의 **'관련 질문에 딸려온 무관 청크' 카테고리를 통째로 드롭**했다(`git show 4a98d57` 삭제분에 실재). 그런데 새 주석(`assistant.ts:47-48`)은 "모델을 바꿀 때는 반드시 **같은 방법으로** 재실측할 것"이라 지시한다 → **틀린 측정법이 제도화된다.**
**이행**: `assistant.ts:39-43` 근거표에 그 카테고리를 실측값(0.628~0.672)과 함께 복원하되, **원인을 임계값이 아니라 TOP_K + 고객당 청크 수로 서술**하고 "단일 절대 임계값으로 분리 불가"를 함께 박는다. sources 표시 컷은 신규 UX 결정이므로 pending 경유(아래).

### K2-b 도구 0건 → RAG 폴백이 정당한 "0건" 답을 삼킨다 — **하 ADJUSTED (전반 확정 · 후반 기각)**

**전반부 CONFIRMED(스팟 e2e 재현)**: `"박서연 상담신청 뭐 했어?"` → 라우터가 `customer_consultations{name:박서연}`을 **정확히** 고르고 도구가 **정확히** 0행을 냈는데(`app_user_id NULL` → innerJoin 구조적 0행), `assistant.ts:169` 폴백이 그 정답을 버리고 프로필 청크의 "상담경로 앱 AI상담"을 **상담신청 내역인 것처럼** 서술했다. 변이 A/B로 인과 확정 — 폴백을 제거하면 같은 질문·같은 데이터에서 `"박서연 고객님의 상담신청 내역은 조회 결과가 없습니다"`가 나온다.
**후반부 기각(스팟 DISPROVEN)**: "가드 테스트가 발생 불가한 전제를 잠근다"는 사실이 아니다. `(call + 도구 0건 + hits 0)`은 실 코퍼스·실 라우터로 재현된다 — `"박서연 견적 몇 개야?"` top1 **0.586**(hits 0, 견적 실제 0건), `"장우진 견적"`·`"홍길동 상담신청"` 동일. 집계형 어투("몇 개야?")는 산문 청크와 유사도가 낮아 0.60을 못 넘는다. 남는 건 그 테스트가 **고른 질문 문자열**이 실 코퍼스와 어긋난다는 라벨 문제뿐.
**빈도 실측(원 발견보다 넓다)**: `crm.customers` 22명 중 app 연결 **2명뿐** → `customer_consultations`는 **20/22 고객에게 상시 0행**이다. "예외적 엣지"가 아니라 정상 다수다. true-zero 질문 프로브 7건 중 5건이 폴백을 탔다.
**이행(코드 행위 변경 없음)**: ①`assistant.test.ts:584`의 질문 라벨을 실제로 hits=0이 나는 조합으로 교체 ②`assistant.ts:169` 주석에 "진짜 0건도 함께 삼킨다(도구 결과만으로는 오라우팅과 구분 불가)" 한 줄 ③**pending 항목 20 정정**(아래 — 이게 이 발견의 최대 산출물).

### K2-c `none + hits=0 → OUT_OF_SCOPE` 게이트가 정당한 CRM 질문을 삼킨다 — **하 ADJUSTED (스팟 PROVEN)**

**정정된 주장**: `#315`가 라우터 `none`의 의미를 "**지원 필터 밖 조건의 정당한 CRM 질문**"까지 넓혔는데, `assistant.ts:173`의 게이트와 그 문구의 SSOT 주석(`assistant-prompt.ts:9-11` — 대상이 "잡담·일반 지식"이고 "진짜 CRM 질문일 수 있으면 범위 밖 단정 금지"가 명시 원칙)을 함께 갱신하지 않았다.
스팟 실측: `"마이바흐 관심 고객이 누구야?"` top1 **0.6146**(마진 0.0146, 주장 그대로) — 실제 붕괴점은 주석이 언급한 0.65가 아니라 **0.62**다. 임계값을 0.65로 되돌리는 변이를 주입하니 `OUT_OF_SCOPE_ANSWER`("CRM 업무 질문에 답하는 어시스턴트입니다…")가 **글자 그대로** 재현됐고, 라우터는 두 실행 모두 `{"kind":"none"}`을 반환해 게이트 전제도 확인됐다.
**인과 귀속 정정**: 이 결합은 `#315`가 만들지 않았다 — 서술형→`none` 지시는 `#172`(5837537), `none+hits=0`→범위 밖 게이트는 `#185`(0f57311), hits=0을 유의미하게 만든 건 `#192`다. `#315`는 부류를 넓혔을 뿐이다.
**회귀 그물 0**: 임계값 상수를 어떤 값으로 바꿔도 RED가 되는 테스트가 없다(테스트 유사도가 전부 상대값 또는 0.9 스텁). `none` 경로 테스트도 전부 "오늘 날씨?" 잡담뿐.
**이행**: `assistant.ts:50` 주석에 "`#315`의 미지원 필터 구제가 이 값 위에서 성립(마이바흐 실측 0.6146·붕괴점 0.62)" + `assistant-prompt.ts:9-11` SSOT 주석의 `none` 부류 서술 정정 + **회귀 테스트 1건**(정당한 CRM 질문 + none + hits 0). 문구 정책 변경(OUT_OF_SCOPE → NO_HITS)은 행위 변경이므로 pending 경유.

### K2-d `#315`가 실기로 반증한 주장이 세 번째 지점에 남았다 — **하 ADJUSTED**

`#315`는 `assistant-tools.ts:38-41`·`assistant-tool-router.ts:6-7`을 정정했지만 `assistant.ts:157-159`("오라우팅 방지는 라우터 프롬프트+골든 테스트가 잠근다")는 손대지 않았다. **정정 1**: 이 주석은 정정된 둘과 **같은 주장이 아니다** — `:155`가 라우팅 우선 게이트 전제를 오히려 정확히 서술하고 있어 나머지 둘이 여기서 드리프트한 기준점이었다. 잔여 오류는 꼬리 반 문장 두 개(①"서술형은 none/null" — `"박서연이 원하는 조건"` → `call` 관찰로 반증 ②"골든 테스트가 잠근다" — 지목된 5건이 전부 `routeAssistantTool` 페이크 주입 배선 테스트라 모델 판단은 원리적으로 못 잠근다). **정정 2**: 같은 어휘 지점은 3곳이 아니라 4곳이다(`assistant.test.ts:532-534`의 "오라우팅 방지 골든" 명명).
**부수 발견(V3, 이쪽이 실질 위험이 더 크다)**: `#315`가 추가한 프롬프트 하드닝 문구("다섯 가지 필터로만"·"추측해 채우지 마라"·"지어내지 마세요")를 잠그는 테스트가 **0개**다 — 지우면 전 스위트 GREEN. 실기로 확인된 유일한 1차 억제가 무방비다.
**이행**: `assistant.ts:157-159` 정정 + `assistant.test.ts:532-534`에 "(배선 한정)" 한 마디 + **프롬프트 문자열 포함 단언 1건**.

---

## 클러스터 K3: 게이트 거울 후속 (`#308`)

### K3-a 재동기화 effect의 setState가 무조건 발화한다 — **하 ADJUSTED (스팟 PROVEN)**

**정정된 주장**: bail-out이 업데이터 **안**에 있어 setState가 매 커밋 호출되고, React는 "렌더 1패스 후 bail out" 경로를 타 **훅 호스트(CustomerDetailContent) 함수 본문이 커밋당 2회 실행**된다. 스팟이 실 훅+실 컴포넌트로 재현: 워크벤치 **닫힘**에서도 `bump 1회 delta = 2`, 실 조작(비교카드 월납입금 입력)에서도 입력당 2회. `useLayoutEffect` 자체가 `#308` 신규 도입이므로 이 2배는 이번 배치 산물이다.
**축소된 부분**: ①**부모발 리렌더는 2배가 아니다**(eager bail-out 성립, 실측 `host=1`) — 트리거는 호스트 자신의 state 변경에 한정 ②bailed 패스는 **트리를 하강하지 않는다**(자식 렌더 카운트 원본·변이 동일) ③jsdom 벽시계 개선폭 **~9%**(렌더 수는 정확히 절반) ④예시로 든 "메모 입력"은 uncontrolled textarea라 부정확 — 비교카드 입력이 정확한 예시.
**처방 교체(중요)**: 원 제안(ref 선비교 후 조기 반환)은 **채택하지 않는다** — V2 프로브 실측상 외부가 상태만 비우면(`setLenderByCard({})` 패턴) ref판은 `{}`에 고착돼 `#308`이 세운 "거울은 DOM 파생·자가치유" 불변식을 판다. **채택안 = 클로저 state 선비교**: `if (sameStringMap(lenderByCard, next)) return; setLenderByCard(next);` — effect는 커밋 후 실행이라 클로저 값이 곧 현재 state이므로 bail-out 강도가 현행과 동일하면서 전 경로 최적(무관 커밋 `host 1`, 금융사 변경 `host 1/child 1`). `set-state-in-effect` disable 주석은 **유지**한다(그 형태는 룰이 여전히 발화 — 원 주장의 "disable도 사라진다"는 ref 안에서만 참). 회귀 그물 `QuoteWorkbench.gate.test.tsx` 7종이 그대로 green이어야 한다.
**부수 정정**: `:1274` 주석의 "(같은 값이면 setState가 리렌더를 안 낸다)"는 사실과 다르다 — 커밋은 안 나지만 렌더 1패스는 돈다. 코드를 안 고치더라도 이 문구는 정정 대상이다.

### K3-b `#308`이 남긴 변이 기록이 실측과 어긋난다 — **하 ADJUSTED (스팟 PROVEN)**

`QuoteWorkbench.gate.test.tsx:22`의 "재동기화 effect를 주석 처리하면 **3·4·5·6만 RED**"는 **양방향으로 틀렸다** — 실측 RED는 **3·4·6·7**이고 5(초기화)는 green이다. 그리고 PR 본문·`batch-13.md:88`의 "제거한 두 setter 각각에 전용 그물 1개씩 — 한 줄만 되살아나도 잡힌다"는 **거짓**이다(clear 복원·copy 복원 각각 **유닛 전체 1062 green**). 원인은 명확하다 — dep 없는 effect가 paint 전에 DOM에서 재파생하므로 두 setter의 부활은 **관측 불가능**하다.
**정정 2건**: ①오염 지점은 2곳이 아니라 **3곳**(테스트 헤더 + PR 본문 + **plan `batch-13.md:88`**) ②plan의 중간 진술("케이스 5는 M2를 못 잡고 M1만 잡는다", "케이스 7이 M2를 잡는다")은 **정확하다** — 거짓인 것은 결론 문장 하나뿐이므로 외과적으로 정정한다 ③원 발견의 "dep 축소 시 초기화 회귀가 무검출로 열린다"는 반증됨(변이 D → 케이스 4가 RED로 차단).
**이행**: 헤더를 실측대로 정정 + plan:88의 "전용 그물" 절 삭제 + 두 setter 자리에 "재동기화가 덮으므로 되살려도 테스트는 green — 넣지 말 것" 근거만 남긴다.

### K3-c 주석의 라인 참조가 같은 커밋 안에서 이미 스테일 — **note CONFIRMED (범위 확대)**

`useQuoteWorkbench.ts:460`의 "위 `:436`에서 대상 select DOM에 금융사를 이미 썼고"에서 실제 쓰기는 `:443`이다(`:436`은 `querySelector`). `#308`이 같은 커밋에서 `sameStringMap` 7줄을 상단에 넣으며 밀린 값 — 커밋 전 blob에서 `:436`이 정확히 그 대입이었음을 확인했다.
**범위 확대(V3)**: 레포의 같은 파일 bare `:NNN` 주석 참조 **5건 중 5건이 전부 스테일**이다(`CustomerManagementPage.tsx:205`→`:860`, `CustomerManagementPage.test.tsx:778`·`:810`, `OptionPanel.test.tsx:78`→`:265`(파일 총 80줄)). 이 관용구는 이 레포에서 100% 부패한다.
**이행**: `:436` → 숫자 교체가 아니라 **식별자 참조**로(`위 targetLender.value 대입에서`). 5건 전부 동일 처리 + `AGENTS.md` 코드 관례에 "같은 파일 참조는 라인 번호 대신 식별자로" 한 줄.

---

## 클러스터 K4: 넛지·팝오버 그물 (`#309`)

### K4-a "전수 열거형으로 잠갔다"는 그물이 손 목록이다 — **하 ADJUSTED (스팟 PROVEN)**

`QuoteList.opener-clear.test.tsx:41`의 `OPENERS`는 하드코딩 배열이고 팝오버 실제 버튼 집합과 대조하는 단언이 **0건**이다. 커버는 오프너×상태 **30쌍 중 10쌍**(넛지 6 + downgrade 4). 스팟이 두 시나리오를 모두 실증 — ①미등재 7번째 오프너 주입 → **163 tests green**(무검사), 손으로 등재만 하면 즉시 RED ②`QuoteList.tsx:454`의 `setConfirmingQuoteDeleteId(null)` 삭제 → **유닛 1062 전량 green**.
**정정 3건**: ①`#309`가 선언한 "전수"의 축은 **오프너**(6곳)이고 그 축은 실제로 충족된다 — 30쌍 매트릭스는 주장된 적이 없고, 커버된 2상태는 K2-a가 실제로 깨뜨린 바로 그 2개다 ②프로덕션은 **오늘 30/30 정확**하다(라이브 결함 0) ③오프너 삭제·라벨 변경은 `getByRole({name})`이 이미 RED로 잡는다 — 비어 있는 방향은 `DOM ⊆ OPENERS`(신규 버튼) 한쪽뿐이다.
**이행**: 표준 상태(draft·`quoteWritable`·`fileName` 없음)에서 팝오버 오픈 직후 `within(popover()).getAllByRole("button")`을 `OPENERS`와 **집합 비교**(그 상태의 버튼이 정확히 6개임을 확인 — "견적 원본 첨부"는 `<label>`+file input이라 button role 아님). **30쌍 it.each는 기각**(아래 박제).

### K4-b 워크벤치 오픈 경로 2곳에 반쪽 클리어 잔존 — **note ADJUSTED**

`openNewWorkbench:1545`·`openWorkbenchForQuoteRequest:173`이 팝오버-내부 6종 중 `setConfirmingQuoteDeleteId` 하나만 지운다 — `#309`가 `openEditQuote`에서 걷어낸 바로 그 패턴이고 배치 12 B#1이 세운 계약(`useQuoteList.ts:83-84` "클리어 지점을 산개 추가하는 대신 이 두 함수만 쓴다")과 어긋난다.
**정정(실패 시나리오 축소)**: `#309`가 닫은 상태는 **재현되지 않는다** — 제거된 꼬리 5줄에는 `setOpenQuoteActionId(null)`이 포함돼 있었고 그게 "보이지 않는 고아 + 리스너 미부착"의 원인이었다. 이 두 함수는 `openQuoteActionId`를 건드리지 않아 팝오버가 열린 채 남고 Escape/외부 클릭으로 즉시 회복된다. 열거된 트리거 3개 중 키보드 단축키는 **존재하지 않고**(grep 0건), URL `?quoteRequest=` 진입은 마운트 1회 가드라 상태가 전부 null이며, "프로그램 호출"의 실체도 니즈 카드 클릭이다. 실 잔존 경로는 focus trap 없는 팝오버에서 Shift+Tab → Enter로 `+` 버튼을 누르는 경우(잔상, 회복 가능)와 `openWorkbenchForQuoteRequest`의 `.then()` 비동기 창뿐.
**이행**: 2줄을 `closeQuoteActionPopover()`로 교체(둘 다 꼬리 지점이라 `#309`가 경고한 조기 반환 함정에 해당하지 않는다) + `:172` 주석("+ 버튼 onClick과 정렬")이 이미 스테일이므로 함께 정리. **분류는 버그 수정이 아니라 계약 정합 위생.** 🟡 미세 행위 변경.

---

## 클러스터 K5: 운영 설정 계약 (`#314`)

### K5-a 회귀 테스트가 6개 인자 중 `p_timezone` 하나만 잠근다 — **하 ADJUSTED (스팟 PROVEN)**

스팟이 인자 교환 변이(`p_force_message` ↔ `p_outside_hours_message`)를 주입해 **typecheck 0 · lint 0 · unit 1062 전량 green**을 확인했고, `auditSummary` 실행 결과 교환 사고와 평범한 문구 1건 수정의 요약 문자열이 **완전히 동일**("안내 문구 변경")함도 확인했다.
**정정(근거 교체)**: `#310` D#4/B#9의 "동형 인자 교환" 유추는 약하다 — 그쪽은 **위치 인자**라 호출부에 이름이 없어 원리적으로 불가시인 반면, 여기는 `p_force_message: draft.forceMessage`처럼 키와 값이 서로 메아리치는 명명 객체다. 게다가 `HandoffOperationPage.tsx:135-137`이 RPC 반환 행으로 두 textarea를 **재시드**하므로 저장 직후 입력 당사자 화면에서 뒤바뀜이 보인다(잠복형 아님). **유효한 근거는 "계약 프리즈 RPC의 페이로드 형상 잠금"**이다 — 현재 인자를 빼거나 개명해도 green이고, `AGENTS.md:90`이 "시그니처 전체 동결"을 선언하고 있다.
**이행**: `handoff-settings.test.ts:164` 케이스에서 `expect(args).toEqual({ p_mode, p_schedule, p_force_message, p_outside_hours_message, p_reason, p_timezone })` 전량 단언 + "프리즈 RPC이므로 인자가 늘거나 이름이 바뀌면 RED가 되는 것이 의도" 주석. 프로덕션 diff 0.

### K5-b 코드가 SSOT로 지목한 spec에 폐기된 동작이 남았다 — **하 ADJUSTED**

`handoff-settings.ts:2`가 계약 SSOT로 지목한 `ref/specs/2026-07-11-...md:140`이 여전히 "timezone 편집 UI(Asia/Seoul 고정 표시 — **RPC DEFAULT 사용**)"라 적는다. `#314`는 `AGENTS.md`와 회신 문서만 갱신했다(`git show 1de01a2 --stat`에 spec 없음).
**정정**: 배치 13 K1-e와 **같은 유형이 아니다** — K1-e는 이행하면 깨지는 **처방적 지시**였고 여기는 "범위 밖(의도)" 목록의 사후 근거 4단어다. 근접 방어도 4겹(삭제 라인 위 3줄 주석·`chat.ts:49-55`의 "DEFAULT는 편의 장치이지 계약이 아니다"·자동 로드되는 `AGENTS.md:90`·회귀 테스트). 그럼에도 3곳이 이 문서를 계약 SSOT로 지목하므로 1줄 정정 가치는 있다.
**이행**: `:140`에 `#308`이 쓴 것과 같은 형식의 폐기 콜아웃 — "**\"RPC DEFAULT 사용\"은 폐기됨(#314)** — 저장은 `p_timezone` 명시 전달, DEFAULT 의존 금지". `:72`(시그니처·DEFAULT 서술)는 원격 DB 실측과 일치하므로 **절대 건드리지 않는다.**

### K5-c `settings.timezone`을 읽어놓고 버린다 — **note 기록만**

스팟이 가장 강한 증명을 냈다 — `HandoffSettings`에서 `timezone` 필드를 **아예 삭제해도 `tsc --noEmit`이 EXIT=0**(컴파일타임 소비처 0의 소진적 증명), 값을 `"UTC"`로 오염시켜도 유닛 1062 green.
**그럼에도 기록만인 이유**(3렌즈 일치): ①스펙 `:140`이 "Asia/Seoul **고정 표시**"를 범위 밖으로 명시한 설계 결정이고 `#314`가 만든 게 아니다(`#218`부터, `#314`는 리터럴→상수 치환으로 렌더 결과 무변화) ②드리프트가 화면에서 완전 비가시가 아니다 — RPC 경유 변경은 `auditSummary`의 "타임존 변경"이 변경 이력 카드에 뜨고, 직접 UPDATE의 경우에도 `nextOpenAt`이 고정 KST로 렌더돼 09:00 스케줄 옆에 "오후 6시"가 뜬다 ③`#314` 이후 CRM 저장 1회로 자동 교정되고, 앱 DEFAULT 프리즈 + 시그니처 검증기(앱 #737)가 이중으로 걸려 있다 ④제안된 최소안 `기준 {settings.timezone} (KST)`는 접미사 하드코딩과 충돌해 **"기준 UTC (KST)"** 자기모순 문자열을 만든다.
**남기는 것**: 라벨을 저장값 기반으로 바꾸거나 불일치 시 경고 배지를 다는 것은 **행위 변경**이므로 pending 경유(아래). ⚠️ 손대더라도 `formatKst`의 `timeZone: HANDOFF_TIMEZONE`은 **건드리지 말 것** — 표시 축 고정(#204 교훈)이 드리프트를 이상한 KST 시각으로 드러내는 현존 단서다.

---

## 클러스터 K6: 위생·문서

| # | 판정 | 근거 요약 | 이행 |
|---|---|---|---|
| **D6** `#311` 폐기 잔재 | **하 CONFIRMED** | `#311`이 `tools/*.spec.ts` 3종을 지우고 글롭을 안 치워 `format:check`가 `[error] No files matching the pattern: "tools/**/*.ts"` + **exit 2**(격리 실행으로 tools 글롭 단독 기여 실증 — 뺀 대조군은 exit 1). 순증분의 정확한 서술은 "exit 1 → exit 2"보다 **"`bun run format`(--write)으로도 green이 될 수 없는 영구 실패 상태"**다. knip 쪽은 `Configuration hints (1)`이 생겼는데 `package.json:20`의 `--no-config-hints`가 정확히 그 리포트를 끈다 — `#311` PR 본문의 "knip 7/9 무드리프트" 진술이 이미 그 사각에 걸렸다(archive:262가 "hints 0"을 기준선으로 박제) | PR4 — `package.json:15`·`knip.json:6`에서 `"tools/**/*.ts"` 제거(2줄) |
| **D7** `gemini-embed.ts:6` 주석 0.75 | **note ADJUSTED** | `#312`가 쓰고 7분 뒤 `#313`이 값을 0.60으로 바꾸며 갱신 누락(`git show 4a98d57 --stat`에 이 파일 없음). 감사 중 **4개 앵글·스팟이 독립 재발견**. 단 이 블록이 떠받치는 논증(모델 교체 = 전량 재임베딩 원자성)은 0.60에서도 참이고 정본(`assistant.ts:45`)이 0.75를 "구 값"으로 정확히 라벨링하므로 판단 오도 위험은 좁다 | PR2 — 값 인용을 지우고 `SIMILARITY_THRESHOLD(assistant.ts)` 심볼 참조로(값을 두 곳에 적으면 또 갈린다) |
| **D8** 스펙이 001을 현행으로 서술 | **하 ADJUSTED** | 서술 7곳은 실재하나 **라인별 패치는 net-negative** — 그 문서는 커밋 1회(#132)짜리 B1 시점 기록이고 모델명 외에 "스트리밍 없음·멀티턴 없음"(결정 8)이 이미 코드와 정면 배치라, 부분 갱신은 문서를 "최신 관리됨"으로 오인시키며 더 큰 거짓을 남긴다. **진짜 결함은 다른 데 있다**: `AGENTS.md`에 임베딩 계약이 **0줄**이고(grep 무히트) "-2"를 담은 유일한 자동 로드 문서가 매 세션 **교체되는** brief다 → 다음 핸드오프 후 Codex 기준 항구 기록이 코드 주석뿐이 된다 | PR4 — **`AGENTS.md` ⚠️ 항목 신설**(현행 모델 SSOT = `EMBEDDING_MODEL` · 001↔2 코사인 0.03 · 해시에 모델명 포함 = 교체 시 전량 재임베딩 · 임계값은 모델별 재실측 · **앱 임베딩 참조 기능은 착수 전 양쪽 모델 일치 확인**) + 스펙 상단 배너 1줄 |
| **C7** `crm.embeddings` 스키마 주석 | **note ADJUSTED** | `:320`·`:323`이 초기 7종만 열거해 실 데이터 161행 중 124행(77%)을 서술하지 않는다(최대 항목 `quote_request` 99행 부재, 열거된 `consultation`은 0행). 오히려 **과소** — `:324`의 `(need_*는 customer_id)`도 실제 5종 중 3종만 적시. **귀속 정정**: `#312`가 만든 게 아니라 `#170`·`#173`·`#174`·`#178`이 `:49`만 고치며 누적시킨 선재 드리프트다(#162는 `quote` 추가 시 주석을 같이 갱신했다 = 전수 열거가 원저자 의도였음의 증거). 실패 경로는 `ORPHAN_PREDICATES: Record<CorpusSourceType, SQL>`가 타입으로 막아 도메인 이해 오류에 한정 | PR4 — 3줄을 `EMBEDDING_SOURCE_TYPES` **심볼 참조**로(라인 번호 금지) |
| **C8** `relay_test.ts`의 001 | **note 기록만** | 잔존은 사실이나 `relay.ts:11` allowlist가 모델명 와일드카드라 동작 잠금이 아니고, "저장소 유일 참조"가 거짓이다(프로덕션 소스 주석 2곳 + ref 문서 십수 건). Deno 테스트는 CI·package.json 어디에도 배선되지 않아 노출도 낮다 | — (D7과 함께 지나가는 김에 고치는 것은 무해) |
| **B6** 넛지 파생화 | **note 기록만 + 제안 기각** | "K1 원칙과 반대"라는 프레이밍은 성립하지 않는다 — K1의 파생 명령은 "**이벤트 없이 DOM이 바뀌는**" 거울에 한정된 것이고, 넛지 id는 spec §8-1 발동조건(신규 마킹 ∧ 진행상태 ≠ 계약완료)을 담은 UI 의도 상태라 `decisionStatus`로 환원 불가하다. 클리어 지점도 12곳이 아니라 6곳(오프너 6은 `dismissContractStageNudge()` **호출부**)이고 그 구조는 배치 12 B#1이 명시적으로 세운 설계다. **제안 수정은 회귀**(아래 박제) | — |
| **D9** `ragFakes` 기본 `similarity: 0.9` | **note 미검증 기록** | `#313`의 "테스트 하드코딩 전부 상대값화"에 예외 1건. 임계값을 0.9↑로 올리는 튜닝 시 무관해 보이는 7개 테스트가 깨져 원인 추적이 어렵다. **적대 검증을 거치지 않았다**(앵글 D의 note 판정만) | PR2에 곁들여 `similarity: 1`로(선택) |
| **D10** `@playwright/test` 잔존 | **note 미검증 기록** | spec 0개인데 유일 소비처 `playwright.config.ts`가 knip entry라 구조적으로 "미사용 의존성"으로 보고되지 않는다. 재도입 의사가 `#311`에서 명시됐으므로 제거 판단은 별건 | — |

---

## 이사님 판단 대기 (코드 변경 0 또는 소형 — pending 등재)

- **🔴 항목 20 정정(최우선)**: `#315`가 등재한 완화안 **"그 고객의 근거가 있을 때만 폴백"은 무효**다 — 폴백을 유발한 청크가 바로 그 고객 것이다(박서연 프로필 0.6582·할일 0.6445 실측). 이대로 회신을 받으면 **존재하지 않는 선택지를 고르게 된다.** 함께 병기할 것: ①`customer_consultations`는 **20/22 고객에게 상시 0행**(앱 연결 2명) — "예외적 엣지"가 아니다 ②true-zero 질문 7건 중 5건이 폴백을 탄다(집계형 어투는 안 걸림) ③유효 선택지 = ⓐ현행 유지 ⓑ**도구 결과 + RAG 근거를 둘 다 프롬프트에 적재**("상담신청은 없고, 대신 …") — ⓒ"true-zero면 도구 유지"는 `#315`가 고친 바로 그 케이스(박서연 "원하는 조건")를 되살리므로 **기각**.
- **sources 표시 노이즈(신규)**: 단일 고객 질문의 근거 목록에 타 고객 6건이 이름과 함께 표시된다(권한 위반 아님 — scope 필터 무손상, 관리자는 원래 열람 가능). 컷이 필요하다면 상수가 아니라 **표시 계층**에서(sources 전용 상대 마진 또는 top-N). `SIMILARITY_THRESHOLD` 되돌리기는 금물.
- **`none + hits=0` 문구 정책(신규)**: 지원 필터 밖의 정당한 CRM 질문에 "범위 밖" 안내가 나가는 것을 허용할지, `NO_HITS_ANSWER`로 되돌릴지. 실측 마진 0.0146·붕괴점 0.62.
- **타임존 라벨(신규·소형)**: 운영시간 카드 라벨을 저장값 기반으로 바꿀지(정상 상태 표시 불변, 불일치 시에만 경고 배지).
- 기존 항목 14(재판단)·16·17(사후 공유)·18·19 계속 대기.

## 🟡 행위 변경 (사후 공유 대상)

- **PR1 — AI 힌트 캐시 키 복원**: 지금 넣으면 22명 전원의 힌트 재생성(Gemini 22콜 + 문구 22건 교체)이 **발생하지 않는다**. 미루면 고객이 편집될 때마다 1건씩 실현되고, 나중에 고칠 때 반대 방향으로 또 1건씩 든다.
- **PR3 — `openNewWorkbench`/`openWorkbenchForQuoteRequest`가 팝오버 상태 6종을 전부 클리어**(현재는 삭제 확인 1종만). 잔상 제거이므로 사용자 체감은 "닫혔어야 할 확인창이 안 남는다".
- 나머지 PR은 주석·테스트·문서·설정만으로 프로덕션 행위 변경 0.

---

## 기각 박제 (재제안 금지)

**결함 기각 3건**
- **D5 `#313` 하향이 폴백을 증폭한다** — 기각. ①구 001 공간 실측상 관련 질문 최고가 0.765~0.835로 임계 0.75를 넘어 **hits>0은 `#313` 이전에도 기본값**이었다(두 값은 호환 불가 공간의 눈금이라 대소 비교 자체가 범주 오류 — `assistant.ts:44-48`이 경고하는 함정) ②`#315`는 0.60 체제의 실기 중 발견·검증됐다(PR 본문 첫 줄) ③헤드라인 시나리오 "박서연 견적 몇 개야?"는 실측 top1 **0.586**으로 hits=0 → 폴백 미발동. 살아남는 축은 K2-b가 흡수.
- **B8 랭킹 모달 경로에 `applyGateFallback` 누락** — 기각. ①스펙 §1 표가 "랭킹 모달 = 변경 없음"으로 명시 ②모달 행은 **파트너가 성공 응답을 낸 조합에만** 생긴다(`SolutionLenderRankingModal.tsx:88-114`) — 게이트 ON 5사는 미취급 시 throw로 행이 사라지고, clamp 3사는 매트릭스 `null`이라 게이트 자체가 안 켜진다(두 집합 배타) ③제안 수정은 파트너가 방금 채운 월납입(구 기간 기준) 위에 기간만 옮겨 **무음 오견적**을 저장·발송한다.
- **A2 후반부 "가드 테스트가 발생 불가한 전제를 잠근다"** — **스팟 DISPROVEN으로 기각.** `(call + 도구 0건 + hits 0)`은 실 코퍼스·실 라우터로 재현된다(박서연 견적·장우진 견적·홍길동 상담신청). 남는 건 테스트가 고른 질문 문자열이 실 코퍼스와 어긋난다는 라벨 문제뿐.

**제안(수정 방법) 기각 9종**
- **A5① 백필 배치 단위 upsert** — 시끄러운 전면 실패를 **조용한 부분 혼재**로 바꾼다. `gemini-embed.ts:5-8`이 재앙 모드로 지목한 상태를 스스로 제조하고, 재실행 시엔 hash-skip이 어느 쪽이든 이어받아 이득 0.
- **K2-c 라우터 `reason` 필드** — `#315`가 "시스템 프롬프트 한 줄은 실기에서 안 먹혔다"를 실측 기록한 지렛대에 다시 의존. 라우트 쪽에서 닫는다.
- **K3-a ref 선비교 조기 반환** — 거울이 DOM 파생이라는 `#308` 불변식을 판다(외부가 상태만 비우면 ref판 `{}` 고착, 현행은 자가치유 — V2 프로브 실측). 채택안은 클로저 state 선비교.
- **K4-a 30쌍 `it.each` 전개** — `STATES` 축도 수기라 실제 발화 이력(새 상태 추가 후 일부 미전파 = 배치 12 넛지)을 못 막고 스위트만 부풀린다.
- **B6 넛지 렌더 조건에 `decisionStatus === "contracting"` AND** — `editorOpen`(`useQuoteList.ts:344`)이 넛지 id를 렌더 트리 밖에서 읽어 드로어 Escape 게이트(`App.tsx:303`)에 먹이므로, 렌더만 가리면 배치 12 B#1의 "보이지 않는 고아 + Escape 무음 차단"이 재발하고 기존 테스트 3건이 RED가 된다.
- **K2-a 임계값 상향으로 sources 오염 해결** — 0.65면 관련 질문이 4/8로 떨어지고(주석 자체 실측) K2-c 마이바흐가 OUT_OF_SCOPE로 전락(붕괴점 0.62 실측).
- **K2-a 라우터 파생 `customerId` 하드 필터** — `#315`가 방금 닫은 파라미터 날조 위험을 되살리고, 이름 추출이 틀리면 조용한 NO_HITS가 된다.
- **D8 스펙 라인별 패치** — 부분 갱신이 문서를 "최신 관리됨"으로 오인시키며 결정 8(스트리밍·멀티턴 없음)의 더 큰 거짓을 남긴다. 배너 1줄 + `AGENTS.md` 계약이 정답.
- **K5-c notice-box 경고** — 코드 경로로 도달 불가한 조건을 위한 과설계. 최소안(라벨 노출)만.

배치 7~13 기각 박제 계속 유효.

---

## 별건 (배치 14 범위 밖)

- **✅ 브리프 "다음 작업 1: 업무 AI 실기 확인"은 이 감사가 사실상 수행했다.** 확인 3가지 답: ①**관련 질문 근거 회수 정상**(박서연 본인 2청크가 1·2위 = 100% 회수) ②**타 고객 청크는 실제로 섞인다**(단일 고객 질문 top-8 중 최대 6건 — 다만 원인은 TOP_K이고 권한 위반은 아니다) ③**답변 본문 오염·톤 변화는 관찰되지 않았다**(생성 5회 이상, 오귀속 유도 질문에도 거부). 남는 판단은 sources 표시 UX뿐이므로 별도 실기 세션은 불필요.
- **🆕 NO_HITS 문구와 sources가 동시에 렌더된다**(스팟 A1 부수 발견): "관련 CRM 데이터를 찾지 못했습니다"라고 답하면서 그 아래 타 고객 근거 8줄이 그대로 뜬다 — `assistant.ts:196-201`이 sources를 답변과 무관하게 `hits`에서 조립하기 때문. 표시 계층 결정과 함께 다룰 것.
- **`format:check` 스타일 warn 20건**은 `#206` 이전부터 red였고 CI·hook 어디에도 배선돼 있지 않다(레포에 `.github` 자체가 없다). D6는 그 위에 얹힌 **경로 에러**만 제거한다.
- **`knip --no-config-hints`** 플래그는 `#311` 소행이 아니라 최초 툴링 커밋(`88cb460`, 2026-05-17)부터 존재한다. 제거 여부는 툴체인 정책이므로 별건.
- 배치 13 별건(`K1 안 ②c` controlled 전환 · `resetQuoteWorkbench` DOM 미청소 · `dealerOptionsByCard` 동일 결함 클래스) 계속 유효.

---

## PR 분할

파일 겹침 기준 — **PR1·PR2·PR3·PR4는 서로 파일이 겹치지 않아 전부 병렬 가능.** 단 `AGENTS.md`·`ref/director-pending-confirmations.md`·본 plan 문서는 **PR4가 독점**한다(배치 13 스택 PR 함정 회피). 순서 권고는 **PR1 우선**(타이밍 민감). 4건 모두 squash 대상이므로 커밋 메시지에 스킵 마커 금지.

**PR1 — 임베딩 해시 축(서버)** · `assistant-corpus.ts` · `ai-hint-on-write.ts` · `embed-on-write.ts` · `backfill-embeddings.ts` · `assistant-corpus.test.ts` · 신규 가드
- K1-a 해시 네임스페이스 분리(ai-hint는 **순수 `sha256(material)`** — 22행 즉시 재매치) + 소비자 목록 주석 정정
- K1-b 스킴 잠금 테스트 2단언 · K1-c 스테일 검사 가드(`check:residue` 또는 소형 테스트)
- **회귀 그물**: M1 모델명 접두 제거 → RED · M2 ai-hint를 `embeddingContentHash`로 되돌림 → RED · 가드는 함수 단위 판정식 테스트(실 DB 쓰기 금지)
- 🟡 행위 변경 · ⏱ 타이밍 민감

**PR2 — 업무 AI 검색·라우팅 문서·그물(서버)** · `assistant.ts` · `assistant-prompt.ts` · `gemini-embed.ts` · `assistant.test.ts`
- K2-a 근거표 카테고리 복원(원인을 TOP_K로 정확히 서술) · K2-b 가드 테스트 질문 라벨 교체 + 폴백 주석 · K2-c `:50` 의존 주석 + SSOT 주석 정정 + **회귀 테스트 1건**(정당 CRM 질문 + none + hits 0) · K2-d `:157-159` 정정 + **프롬프트 하드닝 문자열 단언 1건** · D7 `gemini-embed.ts:6` 심볼 참조화 · (선택) D9 `similarity: 1`
- **회귀 그물**: 프롬프트 문구 삭제 → RED · none+hits=0 케이스가 임계값 상향 변이에서 RED
- 프로덕션 행위 변경 0

**PR3 — 워크벤치·팝오버(클라)** · `useQuoteWorkbench.ts` · `QuoteList.opener-clear.test.tsx` · `QuoteWorkbench.gate.test.tsx` · `ref/plans/2026-07-22-crm-refactor-batch-13.md`
- K3-a 클로저 state 선비교(+ `:1274` 주석 정정, disable 주석 유지) · K3-b 헤더·plan:88 정정 · K3-c 라인 참조 5건 식별자화 · K4-a 팝오버 버튼 집합 잠금 · K4-b 오픈 경로 2줄 + 스테일 주석 정리
- **회귀 그물**: gate 7종 전량 green 유지(변이 무력화 시 3·4·6·7 RED 재확인) · 7번째 오프너 주입 변이 → 집합 비교가 RED · `bump 1회 = host 렌더 1` 회복은 프로파일로 확인하되 테스트에 렌더 횟수를 단언하지 말 것(React 내부 bail-out 구현 의존)
- 🟡 K4-b 미세 행위 변경

**PR4 — 계약·위생·문서** · `handoff-settings.test.ts` · handoff spec · `package.json` · `knip.json` · `schema.ts` · work-ai spec · `AGENTS.md` · `ref/director-pending-confirmations.md` · 본 plan
- K5-a `toEqual` 전량 단언 · K5-b spec `:140` 폐기 콜아웃 · D6 글롭 2줄 제거 · C7 스키마 주석 심볼 참조화 · D8 `AGENTS.md` 임베딩 계약 신설 + 스펙 배너 · K3-c의 `AGENTS.md` 라인참조 관례 1줄 · **pending 항목 20 정정 + 신규 3건 등재** · 본 plan 박제
- **회귀 그물**: 인자 교환 변이 → RED · `bun run format:check`가 경로 에러 없이 exit 1(스타일 warn만) · `npx knip`(플래그 없이) hints 0

---

## 감사 자체의 한계

**실행하지 않은 검증**
- **`bun run test:server` 641 전량 미실행.** 앵글별 개별 파일만 돌렸다(A: assistant/gemini-embed/embed-on-write/ai-hint/corpus 95 pass · C: +fixture·roles-parity 16 pass · D: assistant/gemini-embed 45 pass). `#310`이 손댄 `src/routes/consultations.test.ts`는 **실 DB 쓰기를 유발하므로 코드 리뷰만** 했다.
- `bun run build`·prod 배포 번들 미확인(로컬 `client/dist`에 `p_timezone` 포함은 확인).
- **실 브라우저·실 Safari 확인 0.** B3의 렌더 비용은 jsdom 기준이고 실기기 체감(ms)은 측정하지 않았다(벽시계는 jsdom에서 ~9% 개선).
- **`#315` 라우터 프롬프트 품질**(오라우팅 재발률)은 표본 소수 실행에 그쳤다 — LLM 판단이라 자동 검증 수단이 없다.
- 앱 팀의 임베딩 모델 일치는 **문서 기반**이며 앱 코퍼스를 재측정하지 않았다.

**앵글 커버리지 편차**
- 앵글 A는 클라(#308·#309·#311·#314 클라측)를 보지 않았고, 앵글 B는 서버(#311~#313·#315)를 보지 않았다. C는 `#308`·`#309`를 diff만 확인했다. **교차 검증이 실제로 작동한 축은 임베딩 해시(A·C·D 3중)뿐**이고, `#310`(위생·문서)은 어느 앵글도 실행 검증하지 않았다.
- B8 도달성(파트너가 미취급 조합 항목을 반환하는지)은 파트너 API 실호출 없이 코드·문서 추론으로 판정했다.
- `#311` 폐기 잔재는 `tools/`·`knip.json`·`package.json`·`playwright.config.ts`까지만 봤다.

**⚠️ 방법론 사고 — 병렬 에이전트의 공유 워킹트리 동시 변이 (최소 5건)**
같은 레포에서 여러 검증자·스팟이 **동시에 변이를 주입**해 측정이 오염된 사례가 반복됐다.
- **A3 첫 실행이 정반대 결과**(22 MATCH-현재)를 냈다 — 다른 에이전트가 `assistant-corpus.ts`를 변이 중이었기 때문. 프로브에 **자가검증**(로드된 함수가 실제로 모델명을 섞는지 확인 후 아니면 abort)을 넣고서야 정정됐다.
- A4 스팟은 자기 변이(0.75)를 넣었는데 grep 시점 값이 남의 변이(0.65)였고, C2 스팟도 같은 이유로 변이 실험 결과를 폐기했다.
- B3·D3 스팟은 워킹트리 오염을 발견하고 **APFS clone / `git archive` 격리 사본**으로 옮겨 재측정했다.
- B1·B4 스팟은 남의 변이(`handoff-settings.ts` 인자 스왑, `assistant.ts` M1/M2)가 걸린 상태에서 full suite를 돌렸다(결론에는 영향 없음을 별도 확인).
- 앵글 A가 미추적 `test/zz-audit-probe*.test.tsx` 2건을 목격했다(현재 `git status` clean — 최종 정리 완료).
**교훈**: 다음 배치부터 변이 실험은 **격리 사본에서만** 하고, 프로브에는 "지금 로드된 코드가 내가 의도한 상태인가"를 확인하는 자가검증을 반드시 넣는다. 이 규칙이 없었으면 K1-a 판정이 정반대로 났다.

**감사 중 실제 부작용**: DB는 **SELECT만**(알림 트리거 4테이블은 조회조차 회피), `crm.assistant_messages` 신규 0행 확인, 임시 프로브 전량 삭제, 모든 변이 원복 확인(`git status` clean). 실 Gemini 임베딩·생성 호출은 다수 발생(비용만).

---

## 진행 상태

- [x] 기준선 부분 재실측(typecheck 0 · lint 0 · unit 1062 · knip 7/9 · 잔재 0 · 코퍼스 161/161)
- [x] 4앵글 병렬 감사(A 서버 · B 클라 · C 외부 계약 · D 크로스커팅) — 후보 31건
- [x] 적대 검증 3렌즈 × 29건(설계대조 · 반박 · 하급 전수 심사) — 87표
- [x] 오케스트레이터 ground-truth 스팟 11건(실 Gemini + 실 코퍼스 + 변이 주입) — **PROVEN 10 · PARTIAL 1**
- [x] 판정 확정 — CONFIRMED 3 · ADJUSTED 15 · 기록만 5 · 기각 3 + 제안 기각 9종 박제
- [ ] **PR1 이행**(임베딩 해시 축 — ⏱ 타이밍 민감, 먼저)
- [ ] **PR2 이행**(업무 AI 검색·라우팅 문서·그물)
- [ ] **PR3 이행**(워크벤치·팝오버)
- [ ] **PR4 이행**(계약·위생·문서 + pending 20 정정 + 신규 등재 3건)
- [ ] 통합 검증(typecheck · lint · unit · **server 641 전량** · build · knip hints 0 · 잔재 0) → 순차 squash 머지 → CF prod 배포 확인
