# 리팩토링 배치 13 — 감사·판정·이행 SSOT (2026-07-22)

**범위**: 배치 12 이후 미감사 = `#303`(배치 12 서버 이행)·`#304`(배치 12 클라 이행)·`#305`(의존성 bump)·`#306`(지원집합 게이트)·`#307`(staff 재배정 차단) — 코드 41파일, +1,483/−249. 특징 = **직전 배치의 이행 PR 2건이 감사 대상**(자기 이행분 검증) + 외부 계약 1건.
**방법**: 관례 — 기준선 재실측 → **4앵글 병렬 감사**(A 서버 게이트·B 클라 UI·C 외부 계약·D 크로스커팅) → **적대 검증 3명**(V1 K1 설계 비교·V2 K2 반박·V3 하급 전수 심사) → 오케스트레이터 ground-truth 스팟(A#1 규모·A#3·C#1 도달성·A#4 고아·롤 분포 — 전부 실측) → 판정 → PR 이행.
**기준선 재실측(전량 green·드리프트 0)**: typecheck 0 · lint 0 · unit **1038** · server **640** · build · 픽스처 잔재 0.
**신성 규칙 6종**: import 경계 **위반 1**(D#1 — 문서 미갱신) · Safari select clean(신규 select 0·기존 `bindSelect` 유지) · fixture registry clean(`CU-QRPF-` 등록·10 pass) · knip **7/9 무드리프트** · `[skip ci]` clean(머지 대상 squash 3건 토큰 0) · Edge 패리티 clean(`supabase/` 무접촉).

---

## 판정 (후보 병합 — CONFIRMED 8 · ADJUSTED 2 · 기록만 6 · 기각 3)

### 클러스터 K1: 지원집합 게이트 거울 `lenderByCard`가 화면 금융사와 어긋난다 — **중 CONFIRMED**

앵글 B·C·D가 **독립적으로** 같은 결함군을 발견했고(3중 교차), 오케스트레이터 스팟이 도달 경로를 확정했으며, V1이 **결함 2개를 추가 발견**했다.

- **K1-a** `openEditQuote`(`useQuoteWorkbench.ts:1530`)가 거울을 시드도 클리어도 안 한다. 형제 거울 `dealerOptionsByCard`는 바로 옆줄에서 `{}`로 청소된다(비대칭). `clearCardUiState`를 부르는 건 `resetQuoteWorkbench`·`openNewWorkbench`뿐이라 **"기존 견적 수정" 진입만 이전 세션 잔상을 물려받는다**. 카드 id가 `manual-condition-${scenarioNo}`로 결정적이라 견적이 달라도 잔상이 매칭된다.
- **K1-b** `applySolutionResult`(`:989-1000`, 랭킹 모달 선택)가 `lenderSelect.value`를 프로그램적으로 쓰면서 딜러 축만 재동기화하고 거울은 빠뜨린다.
- **K1-c** 구매방식 전환 시 option 목록이 바뀌면 `select.value`가 **change/input 발화 0으로** "미선택"이 되는데 거울은 구값을 유지한다(V1 jsdom 실측).
- **K1-d (V1 신규)** `clearCardUiState:627`의 `setLenderByCard({})`가 **DOM은 안 지우면서 거울만 지운다** — 섹션 key가 신규→신규에서 불변이라 리마운트가 없고, React는 non-multiple select의 `defaultValue` 갱신을 무시한다. 즉 **그 줄 자체가 divergence 생성기**다(초기화 후 게이트 전면 해제 실측).
- **K1-e (V1 신규)** 그래서 **스펙 §4.4의 "동기화 지점 4개" 목록 자체가 틀렸다** — ②·④가 빠졌고 ③(clear)은 반대로 어긋남을 만든다.
- **피해 방향 = fail-closed**(스펙이 "안 켜는 것보다 나쁘다"고 규정한 실패 모드). 실측: 실제 BNK 카드인데 거울이 MG면 **12·24개월 비활성 + 약정거리 7종→3종**(BNK가 취급하는 15,000·40,000km 소실), 무안내·무토스트, 금융사를 다시 건드리기 전엔 복구 불가.
- **뿌리**: 순수 함수층은 과할 만큼 촘촘한데 **훅↔컴포넌트 배선층이 통째로 무테스트**(스펙 §5가 약속한 "워크벤치 유닛" 2행 미이행). K1 전량이 그 빈칸을 통과했다.

**판정·이행 = V1 안 ④ 채택**(`DOM=진실 · 거울=파생 · 매 커밋 재동기화`): `useLayoutEffect`(dep 열거 없음) 1개가 카드별 select를 읽어 거울을 재동기화 + 얕은 비교 bail-out, `clearCardUiState:627`·`copyManualQuoteCondition:455`의 거울 쓰기 **삭제**, `syncDealerOnLenderChange:798`은 **유지**(카드 미filled 시 커밋을 보장하는 load-bearing). **+14/−2줄로 K1-a~d 전부 닫힘**(V1이 실측 A·B·C 전부 GREEN 전환 + typecheck 0·lint 0·96 tests green 확인).
근거: "동기화 지점 N개를 사람이 기억한다"가 이 결함군의 원인이므로, 지점을 더 채우는 안 ①은 원인 구조를 남긴다. K1-c·K1-d는 **이벤트 없이 DOM이 바뀌는** 결함이라 핸들러에 코드를 더하는 방식으로는 원리적으로 도달 불가.

### 클러스터 K2: 계약 넛지 상태 잔존 (배치 12 B#1의 미완분) — **CONFIRMED**

- **K2-a = B#5 [하→중 상향]** 오프너 6개 중 **2개**가 클리어를 빠뜨린다: `견적 수정`(`QuoteList.tsx:341`)이 넛지 미러 누락(#304가 손댄 5곳의 6번째 = **배치 12 미완**), `앱 발송`(`:312`)이 `ContractDowngrade` 미클리어(선재·동종). V2 실측 = **정상 조작만으로 다이얼로그 3중 동시 렌더**(`견적 작업 | 계약 진행 견적 수정 안내 | 진행 상태 계약완료 변경 확인`). K2-b와 결합하면 **보이지 않는 넛지 고아 + `editorOpen` true + 리스너 미부착** = 배치 12 B#1(a)가 잡으려던 상태가 되살아난다.
- **K2-b = B#3 [중→하 하향]** 마킹 PATCH 롤백 콜백(`useQuoteList.ts:272`)이 `quotes`만 되돌리고 넛지를 안 지운다 → "저장에 실패했습니다" 토스트가 뜬 팝오버에서 발주 경로 클릭 시 **contracting 견적 0인데 계약완료 전이**. V2 반박 3종(렌더 조건·타이밍·서버 거부) 전부 실패해 사실은 확정. 하향 사유 = 되돌릴 수 있고(진행 상태 수동 편집) 피해 상한이 출고 프리필 공백이며 에러 경로 한정. **⚠️ 어떤 클라 픽스도 "롤백 전 클릭" 레이스는 못 닫는다 — 창을 좁힐 뿐임을 주석에 박제할 것.**
- **K2-c = B#4 [중→하 ADJUSTED]** 니즈 카드 "견적 보기" → contracting 견적이면 `openEditQuote`가 상태만 세팅하고 조기 반환하는데 안내 다이얼로그의 유일한 렌더 지점이 팝오버 내부(`QuoteList.tsx:347`)라 **아무 반응이 없다**. V2가 "스스로 못 푼다"를 **반박 성공**(재클릭 토글 자가치유·팝오버 오픈 시 클리어·백드롭 닫기 정상) → 결함 본체는 Escape 차단이 아니라 **죽은 버튼**. blame = 선재(#156·#158, 2026-07-04)이나 배치 12가 "선재 여부 확인 후 차기 배치"로 명시 이월한 정당 이월분.

**이행**: K2-a = `QuoteList.tsx:341` 오프너를 다른 5곳과 동일 관례로 + `openEditQuote:1531-1534`의 중복 3 setter 제거(안 그러면 클리어가 caller/callee로 쪼개져 드리프트 잔존) + `앱 발송` 오프너에 downgrade 1줄. K2-b = 롤백 콜백에 `deleteQuote:225` 선례 미러 1줄 + 테스트. K2-c = `viewPromotedQuote`가 contracting을 **먼저 검사해 토스트**(상태 미세팅이라 누수 자체가 소멸, 3줄). 추가로 `openEditQuote:1616-1620`(비-contracting **닫힘** 지점)의 5줄을 `closeQuoteActionPopover()` 한 줄로.
**⚠️ V2가 잡은 이행 함정**: 그 한 줄 교체를 **조기 반환 분기(`:1531`)에 적용하면 회귀** — 팝오버를 닫으면 안내가 아예 안 뜨고 K2-c가 전 경로로 확대된다. 두 안은 양자택일이 아니라 **서로 다른 지점의 정답**이다.

### 하급 (V3 전수 심사 — "지금 안 고치면 무엇이 나빠지나"로 판정)

| # | 판정 | 근거 요약 | 이행 |
|---|---|---|---|
| A#4 | **이행(축소)** | 고아 dismissal 1건의 **생성 경위 특정** — `consultations.test.ts:212`(소유권 불일치, 403/404 전제라 평소 0행)만 `finally`에서 dismissal을 안 지워, 배치 12 게이트 변이 검증 때 실 INSERT가 남았다(07-21 16:19 KST 일치). `fixture-codes.ts:40-41`에 박제된 교훈의 재발 | PR3 — 그 테스트 `finally`에 삭제 1줄 + 고아 1건 psql 정리. **스캐너 확장은 기각**(고아 dismissal은 상관 서브쿼리라 질의 결과 불변 = 무해 + 판별 술어가 "고아"뿐이라 앱의 정당한 상담 삭제에 오탐 → `test:server`가 우리 잘못 아닌 이유로 RED) |
| B#9 | **이행** | 배치 12 K1이 고친 결함이 **잠복형**(staff 실계정 부재로 아무도 몰랐다). 쌍둥이 `fetchQuoteRequestDetail`에만 URL 잠금이 있는 비대칭 — 되돌아가면 같은 방식으로 또 조용히 죽는다. 두 인자 모두 string이라 순서 교환도 typecheck 통과 | PR3 — `consultations.test.ts`에 URL 단언 1케이스(프로덕션 diff 0) |
| A#3=D#1 | **이행** | `AGENTS.md:92` 허용목록 4개인데 실제 서버 import는 5개(`advisor-assign-access.ts` 누락). 모듈 헤더가 "등재됨"이라 **자기증명**해 조사가 거기서 끝난다. 이 경계는 **eslint 룰이 없어 문서가 유일한 집행 수단** | PR3 — `AGENTS.md:92`에 1항목 추가 |
| D#4 | **이행** | `git log -L`로 스테일 확정(#265 도입 → #306이 기간 축을 이관하며 첫 줄만 축약) — 남은 축은 취득세뿐이고 양쪽 다 string인데 주석은 "워크벤치 `number`"라 오독시킨다 | PR3 — 주석 2줄 |
| A#2=D#3 | **기록만** | `getQuoteRequestDetail`의 `ownerUserId?` optional은 도달 경로가 "미래에 누가 잘못 부르면"뿐이고, 그 라우터는 `customerScopeGate`가 이미 덮는 이중 겹이다. 필수화하면 쿼리 레벨 테스트 3곳이 무관한 인자를 들고 다니며 **미존재와 소유권 불일치가 한 단언에 뭉개진다**(현재는 축이 깨끗이 분리). 주석 경고도 이미 `:280-281`에 존재 | — |
| C#3 | **기록만** | 값 단위 드리프트 시 fail-closed는 사실이나 ①그 UI 상태는 `[]`(전부 미지원)와 **설계상 동일한 정당 상태** ②전제가 파트너 계약 위반인데 파트너가 문서로 못박음 ③`parseSupportList`는 축을 모르는 공용 파서라 어휘 검사를 넣으면 순수 파서가 도메인 어휘에 결합. **"역효과 우려는 미성립" 병기**(강등은 완전 공집합일 때만) | — |
| B#7 | **기록만** | **감사 전제 정정** — "오늘 도달 불가"는 오판이고 **MG로 오늘 도달한다**(운용리스 `[36,48,60]` vs 장기렌트 `null`). 그럼에도 ①활성 버튼 클릭 1회로 벗어나는 자가 치유 ②고치면 "시스템이 상담사 입력을 바꾸는" 표면이 넓어지는데 **그 표면이 pending 17로 회신 대기 중**이라 순서가 틀림 ③구현도 ~15줄(클로저 구값 문제) | Phase B 실기 체크리스트 1줄 + pending 17 회신 후 재론 |
| B#8 | **기록만 + 기각** | 컴포넌트 테스트 = `CLAUDE.md` "거대 컴포넌트는 수동/스크린샷 허용" 관례와 충돌(316줄·props 7·`navigate` → 스캐폴드 도입), 순수 판정은 6케이스로 잠김, 서버 403이 진짜 방어선, 07-21 실기로 사람이 확인. **판정식 통일은 기각** — 목록 `shouldShowAdvisorColumn`은 **정보 노출 축**, 상세 `canAssignAdvisor`는 **쓰기 권한 축**으로 계층이 다르다(테스트 주석이 그 구분을 명시). 묶으면 "컬럼은 보이되 배정 불가" 분기가 구조적으로 불가능 | — |
| D#5 | **이행 불필요 + 부수 이행** | 커밋 본문 자기모순은 사실이나 brief·`package.json` 실측이 전부 정확해 **전파 0**. 단 **부수 발견**: `grep -rn "0912afc" ref/` → 0건. 배치 12 세션 블록이 아카이브 강등 없이 통째로 교체돼 **배치 13이 감사하는 #305의 근거가 repo에 없다**(압축 커밋의 "정보 손실 0" 주장 미성립) | PR3 — 배치 12 블록을 `ref/session-archive.md`에 복원 |

### 이사님 판단 대기 (코드 변경 0 — pending 등재)

- **A#1 dismiss 부수 범위**: `dismissal`의 원 정의가 "**CRM 뷰에서만** 숨김"(`schema.ts:352`)이라 인박스·AI 힌트·업무 AI에서 함께 사라지는 것은 **설계 의도대로**다. 남는 쟁점은 정책 하나 — staff에게 그 전역 숨김 권한 + **되돌리기 UI 부재**(un-dismiss 경로 0), 그리고 같은 배치 #307이 세운 원칙("되돌릴 수 없는 조작은 admin·manager만")과의 정합성. **현재 실도달 0**(오케스트레이터 실측: staff 계정 1 = `상담사테스트`·담당 고객 0), 잠재 규모 43건(pending 상담 81 중 CRM 연결·담당 배정).
- **폴백 일방향성(V1 권고)**: 금융사를 원복해도 옮겨진 값(60개월·20,000km)은 돌아오지 않고 **되돌리는 쪽엔 토스트도 없다**(조용한 손실). 스펙에 비가역 언급이 없어 "의도된 동작"이라 부를 근거가 약하다. 기간 축은 MG뿐이라 체감이 작지만 **약정거리 축은 5사가 전부 달라 "금융사를 갈아끼우며 비교"하는 주 흐름에서 왕복 2~3회면 초기 세팅이 조용히 굳는다.** K1(결함 수정)과 성격이 달라(행위 변경) **같은 PR에 섞지 않는다**. 저비용 완화안 = 폴백 직전 값 기록 후 재지원 시 같은 톤으로 복원 고지.
- **문서 정정(오케스트레이터 발견)**: pending 항목 16의 "staff 실계정 0이라 현재 실영향 0"이 부정확(계정 1·담당 0) — 배치 12 C#3과 같은 하드 수치 드리프트 유형.

---

## 기각 박제 (재제안 금지)

- **K1 안 ①**(스펙 §4.4 4지점 채우기) — 목록 자체가 틀렸고(K1-e) 원인 구조("지점을 사람이 기억")를 남긴다.
- **K1 안 ②a**(렌더 시 DOM 읽기) — 렌더 순수성 위배 + 선택이 리렌더를 유발하지 않아 원리적 불가.
- **K1 안 ②b**(`condition.lender` 라이브화) — 그 값이 "구 어휘 표시 유지" option의 원천이라 다른 금융사 선택 시 레거시 값이 목록에서 사라져 되돌아갈 수 없다.
- **K1 안 ③**(`cardUi` 승격) — 이득은 안 ①과 같은데 **저장 payload 계약**(`DEFAULT_CARD_UI`)까지 건드려 blast radius만 크다.
- **K2 안 2를 조기 반환 분기에 적용** — 안내 다이얼로그의 유일한 렌더 지점이 팝오버 내부라 K2-c가 전 경로로 확대되는 회귀.
- **A#4 잔재 스캐너 확장** — 무해한 고아를 잡느라 앱의 정당한 상담 삭제에 오탐 → 우리 잘못 아닌 RED. 게다가 잔재 정리 절차 자체가 고아를 만든다.
- **B#8 판정식 통일** — 정보 노출 축 ≠ 쓰기 권한 축.
- **C#3 어휘 교집합 강등** · **A#2 필수화** — 위 표 근거.
- 배치 7~12 기각 박제 계속 유효.

## 별건 (배치 13 범위 밖 — 결정 필요)

- ✅ **종결(2026-07-22, 유슨생 결정 = 폐기)** — spec **3종 전부** 죽어 있음을 실측 확인하고(`screenshot:crm`도 직접 실행해 동일 실패 재현, `customer-detail-screenshot`도 같은 진입 패턴) 스크립트 3개·spec 3개·스냅샷 2장 제거. `AGENTS.md` 검증 예산 문구를 "실화면 눈으로 1회 + magiclink 절차"로 정정하고, `playwright.config.ts`는 재도입 비용 절감용으로 존치하되 **"재도입 시 로그인 처리(storageState)부터"** 주석 박제. knip 7/9 무드리프트 확인. 아래는 발견 당시 기록:
- **🔴 `visual:crm` 하네스 사망(V3 실증)**: 스냅샷 베이스라인 `88cb460`(2026-05-17) < 카카오 로그인 게이트 `b3df832`(2026-06-18) → **약 2개월간 실행 원천 불가**(로그인 화면에서 멈춤, 비교 도달 0). 베이스라인도 pre-auth UI라 로그인을 붙여도 전량 불일치. **`AGENTS.md` 검증 예산의 "Large visual layout changes: run Playwright screenshot once"가 죽은 도구를 가리킨다.** (a) 되살리기(magiclink storageState + 베이스라인 재촬영) vs (b) 폐기(스크립트·스냅샷 제거) 결정 필요.
- **K1 안 ②c**(워크벤치 select controlled 전환) — 구조적 정답이나 DOM 쓰기 6경로 전환 + 저장 payload 회귀 재검증이라 별건. Safari 제약상 `bindSelect` 필수.
- **`resetQuoteWorkbench`가 금융사 DOM을 안 지운다**(V1 인접) — 섹션 key가 신규→신규 불변이라 uncontrolled 값이 살아남는다. "초기화" 의미론과 어긋나나 **행위 변경**이라 별건. 안 ④는 이 정책과 독립적으로 성립.
- ✅ **종결(2026-07-22)** — `dealerOptionsByCard`가 K1과 동일 결함 클래스(V1 인접). 제안대로 **안 ④ effect를 딜러까지 확장**해 닫았다(재동기화 지점에서 금융사가 없거나 "미선택"인 카드의 딜러 엔트리 제거). 재현 테스트로 증상을 먼저 확정했다 — ⚠️ **당시 서술 정정**: placeholder가 "금융사 먼저 선택"으로 거짓 표시되는 게 아니라 **반대**였다. stale 목록이 남아 `hasChoices`가 참이 되므로 placeholder는 "선택"인 채 **이전 금융사의 딜러가 계속 선택지로 제시**된다(무음 오계산 입구). 행위 변경이라 pending 항목 25 등재.

## PR 분할 (PR1·PR2는 `useQuoteWorkbench.ts` 겹침 → 순차. PR3는 독립·병렬 가능)

- **PR1 (K1 게이트 거울)**: 안 ④ 재동기화 effect + `:455`·`:627` 거울 쓰기 삭제 + 회귀 그물(신규 `QuoteWorkbench.gate.test.tsx` — 실 컴포넌트 렌더, **arrange로 생명주기 전이 재현 필수**) + 스펙 §4.4·§5 정정 + 이 plan 박제.
- **PR2 (K2 넛지)**: K2-a(오프너 2곳 + `openEditQuote` 중복 3 setter 제거) + K2-b(롤백 1줄 + 테스트 + 레이스 한계 주석) + K2-c(`viewPromotedQuote` 3줄) + `openEditQuote:1616-1620` → `closeQuoteActionPopover()` + 오프너 **전수 열거형** 테스트(배치 12 B#6이 "5곳"만 커버해 6번째를 놓쳤다).
- **PR3 (위생·문서)**: A#4 원천 1줄(+고아 정리) · B#9 URL 테스트 · `AGENTS.md:92` · D#4 주석 · pending 16 수치 정정 + A#1·폴백 일방향 등재 · 배치 12 아카이브 복원.

## 진행 상태

- [x] 기준선 재실측(typecheck 0·lint 0·unit 1038·server 640·build·잔재 0)
- [x] 4앵글 병렬 감사 + 적대 검증 3명(V1 설계 비교·실측 3종 GREEN 전환 / V2 반박 5프로브 / V3 전수 심사·`visual:crm` 실행) + 오케스트레이터 ground-truth 스팟 — 판정 확정
- [x] **PR1 이행** — `refactor/crm-batch-13-gate-mirror`: 안 ④(재동기화 effect + bail-out) + 거울 쓰기 2곳 제거 + 회귀 그물 **7종**(요청 6 + V1이 사각 발견해 추가한 "조건 복사" 1) + 스펙 §4.4 폐기·§5 갱신. **변이 3종 실관찰**: M1(픽스 전 완전 원복) 4🔴 · M2(effect만 비활성) 4🔴 · M3(bail-out 무력화) **7🔴 = `Maximum update depth exceeded`**(bail-out이 load-bearing 실증). ⚠️ 케이스 5는 M2를 못 잡고 M1만 잡는다(D4 원인이 effect 부재가 아니라 clear 줄이었으므로) — 그래서 케이스 7이 M2를 잡는다. ⚠️ **정정(배치 14 K3-b)**: 여기 있던 "제거한 두 setter 각각에 전용 그물 1개씩 — 한 줄만 되살아나도 잡힌다"는 **거짓이었다** — clear 복원·copy 복원을 각각 넣고 유닛 1062 전량 green을 실측했다. dep 없는 effect가 paint 전에 DOM에서 재파생하므로 두 setter의 부활은 원리적으로 관측 불가능하다(그래서 되살리면 안 된다는 근거는 테스트가 아니라 주석이다). 위 M2 RED 집합도 실측은 **3·4·6·7**이다(당시 기록 "3·4·5·6"은 오기). 검증 typecheck 0·lint 0·unit **1045**(+7)
- [x] **PR2 이행** — `refactor/crm-batch-13-nudge`: K2-a(견적 수정 오프너 클리어 5종 + 앱 발송 downgrade 1줄 + `openEditQuote` 조기 반환 분기의 중복 3 setter 제거 — 안 지우면 caller/callee 드리프트가 이름만 바뀐 채 존속) + 비-contracting 꼬리 5줄 → `closeQuoteActionPopover()` + K2-b(롤백 클리어 + **"클릭-선행 레이스 잔존(수용)" 주석 박제**) + K2-c(니즈 카드 선검사 3줄). 테스트 **+15**(`QuoteList.opener-clear.test.tsx` 전수 열거형 11 · 롤백 1 · 워크벤치 두 분기 정반대 팝오버 계약 2 · `CustomerDetailPage.promoted-quote.test.tsx` 1). **변이 6종 실관찰**: 6번째 오프너 되돌림·downgrade 1줄·롤백 클리어·선검사·꼬리 산개 복원 각각 해당 케이스만 RED + **M6 = 함정 변이(조기 반환 분기에 `closeQuoteActionPopover` 적용)도 RED**로 회귀 자체가 그물에 걸림. 검증 typecheck 0·lint 0·unit **1060**(+15)
- [x] **PR3 이행** — `refactor/crm-batch-13-hygiene`: A#4 원천 1줄(+**고아 dismissal 1건 실 DB 정리 완료** — `not exists` 술어 동반 삭제, 실 사용자 조작 1건은 보존) · B#9 URL 회귀 테스트 · `AGENTS.md:92` 허용목록 등재 · D#4 주석 정정 · pending 항목 16 수치 실측 정정 + **항목 18·19 신설**(dismiss 권한 · 폴백 비가역성) · 배치 12 세션 블록 아카이브 복원. 검증 typecheck 0·lint 0·unit **1061**(+1)·server(consultations) 10 pass
- [x] **통합 검증·머지** — #308 `61ec742` · #309 `8414b6f` · #310 `92b178b` 순차 squash. **머지 후 main 통합 전량 green**: typecheck 0 · lint 0 · unit **1061**(기준선 1038 → +23) · server **640**(실 master·0 fail) · build · knip 7/9 무드리프트 · 픽스처 잔재 0 · **고아 dismissal 0**(전체 1 = 실 사용자 조작만 잔존). CF prod 배포 head `92b178b` 트리거(머지 커밋 3건 모두 스킵 마커 없음)
