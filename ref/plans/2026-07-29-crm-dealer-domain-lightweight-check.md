# 딜러 도메인 경량 체크 (2026-07-29 · 유슨생 승인)

> **판정 SSOT.** 관례 = 배치 15 이후 트리거 기반·경량(AGENTS.md "리팩토링 배치 감사" 항목).
> 대상 = 07-27~28 딜러 도메인(`#372`~`#386` + main 직접 3건). 트리거 = ⓐ실 데이터 변형
> (채택→catalog.trims 실반영·삭제 2종·마이그 3건). ⓒ무검증 머지는 0건(전량 TDD·4종·실 DB)이라
> **풀 감사가 아닌 기본형**(2앵글 + 실측 렌즈 1)로 수행. 적대 검증은 상/중 발견에만 — 이번엔 해당 없음.

## 결과 요약 — **상 0 · 중 0 · 하 5** (전건 수정 완료, 이 PR)

| # | 등급 | 앵글 | 발견 | 조치 |
|---|---|---|---|---|
| 1 | 하 | 정합성 | `routes/dealer.ts` 헤더가 "지금은 관리자용 브랜드 매칭만(슬라이스 A), B는 나중에 붙는다"는 **미래형 스테일** — B는 같은 파일에 이미 구현 | 현재형(두 축 구조)으로 개정 |
| 2 | 하 | 정합성 | spec §6.3 라우트 표 3중 스테일: 제안 조회 트림→모델 단위 반영 안 됨 · `GET/PUT /api/dealer-profiles`(실경로와 다름) · roster/삭제 2종 부재 | 실경로로 전면 개정 + 정정 각주 |
| 3 | 하 | 정합성 | `client/src/lib/dealer-profiles.ts` EOF 이중 개행(#384 블록 제거 잔재) · `DealerBrandCell`의 `entry?` 죽은 가드(명부 행은 entry 항상 defined) | EOF 정리 · prop `DealerRosterEntry`로 조임 + 가드 8곳 제거 |
| 4 | 하 | 회귀그물 | `GET /api/dealer/roster` 응답 계약(클라 `DealerRosterEntry` 동형) 잠금 없음 — 서버 snake_case 매핑이 빠지면 화면이 빈 값으로 조용히 깨짐 | role-gate 테스트에 필드 계약 단언 추가 |
| 5 | 하 | 회귀그물 | `proposalCount`(삭제 confirm "지울 N건"의 근거) 미커버 | 명부 롤백 테스트에 제안 1건 심고 단언 |

## 이상 없음 판정 (근거 실측)

- **#382 채택 자격 차단**: `discount-adoptions.test.ts` 18케이스가 자격 상실·이직(brandMatches) 모두 커버 — 공백 아님.
- **실 DB 정합**(읽기 전용 psql): 고아 0(매칭↛profiles·제안↛profiles·제안↛trims·감사↛trims 4축) ·
  감사 사슬 정합 — (trim, field)별 최신 감사 amount = 현재 확정값(708/financial 5,300,000 관리자 직접
  · 708/partner 6,000,000 딜러 채택 — 브리프의 BMW 523d 서술과 일치) · 행수 매칭 1·제안 1·감사 3.
- **CSS**: `.org-dealer-*` 5클래스 전부 JSX 사용처 존재 — 죽은 룰 0.
- **import 경계·knip·format**: 기준선 0 유지(CI green).

## 알려진 공백 — 수정하지 않음 (근거)

- **PUT 대상 가드 positive 경로**(admin이 실 딜러에 PUT → 200) 라우트 테스트 없음: 라우트 테스트는
  공유 master에 커밋되므로 실 딜러 행을 변형한다(#381 "실 트림 수정 = 스탬프 오염"과 같은 축).
  가드 판정(`isDealerRole` 3케이스)·부정 경로(409 + 행 미생성)·쿼리 upsert는 각각 잠겨 있어
  배선 1칸만 실기 검증 영역으로 남긴다.
- **confirm 문구 로직 추출 없음**: 페이지 컴포넌트는 수동 검증 허용 관례(2026-06-15) — 추출은
  그 자체가 리팩토링 명분이 없다.
