# 리팩토링 배치 15 — 감사·판정 SSOT (2026-07-22)

**범위**: 배치 14 감사 SSOT 박제(`67d147b`) 이후 main 전부 = `a65c327`(#316 임베딩 해시 네임스페이스 분리)·`ef45ebb`(업무 AI 문서 정정+회귀 그물)·`e70aef2`(#317 워크벤치 재동기화·팝오버)·`881ff6e`(#318 계약 프리즈·위생)·`6e3e82d`(#319 Gemini 생성 모델 상향) + docs 4건. 총 414+/93-.
**성격**: 이번 5건은 **배치 14 감사의 이행물**이다. 따라서 축은 "판정대로 이행됐는가 / 이행하며 새 결함을 만들지 않았는가".
**방법**: 기준선 재실측 → **4앵글 병렬 감사**(A1 정합성·계약 / A2 회귀그물 변이주입 / A3 위생·사문화 / A4 행위변경·실데이터) → 트리아지 → **적대 검증 3렌즈**(V1 설계대조 · V2 반박 · V3 실측반증) **42표** → ground-truth 스팟 → 판정.
**규모**: 48 에이전트 · 1,632 tool call · 5.39M 토큰 · 68분 · 에러 0.

**기준선(전량 재실측)**: typecheck 0 · lint 0 · unit **1063 pass**(110 files) · server **650 pass 0 fail** · build ✓ · knip 7 unused exports / 9 unused types(무드리프트) · 픽스처 잔재 0.

**신성 규칙 점검**: `db:push`/마이그레이션 0 · `public.profiles` 쓰기 0 · 알림 트리거 4테이블 무접촉(조회까지 회피 — 힌트 대조도 `app_user_id IS NULL` 20명으로 한정) · 픽스처 잔재 0 · handoff RPC 프리즈 유지(원격 `pg_get_function_arguments()` 실측 일치) · Edge 패리티 clean(doc-types 2 pass·roles 1 pass) · 서버→클라 import 경계 신규 위반 0 · 메인 워킹트리 무손상(전 에이전트 worktree 격리).

**판정 총계 — 트리아지 14건 전건 생존(기각 0) · 심각도 상 0 · 중 5 · 하 9 · 행위 변경 0건.** 순수 CONFIRMED는 M6·M13뿐이고 나머지는 ADJUSTED — 즉 **앵글의 과장을 검증이 되돌린 비중이 압도적**이다(이 사실 자체가 아래 "메타 결정"의 근거).

---

## 🔴 스팟이 잡은 결정적 사실 (finding과 무관하게 박제)

### GOLDEN-319 — 실기 골든 4종은 모델 상향을 판별하지 못한다 (PROVEN)

`GEN_MODEL = gemini-3.5-flash-lite`로 골든 4종 **4/4 통과·인자 날조 0**(①마이바흐→`none` ②김지안 견적→`customer_quotes{name}` ③앱 유입→`search_customers{source}` ④잡담→`none`). 실 `routeAssistantTool` 직접 호출도 4/4 동일.
**그러나 구 `gemini-3.1-flash-lite`도 4/4 동일했다.** `AGENTS.md`는 이 4종을 "`GEN_MODEL` 변경 = 라우팅 판단 변경이므로 실기 골든 4종 필수"의 검증 수단으로 지정했는데, **두 모델을 구분하지 못한다.** 회귀 감시로는 유효하나 "상향이 라우팅을 바꿨다"의 증거로는 쓸 수 없다.
⚠️ 이것이 "`GEN_MODEL` 변경 = 라우팅 변경" 경고 자체를 반증하지는 않는다(다른 질문에서는 갈릴 수 있다). 확정된 것은 **현재 지정된 검증 수단이 그 변화를 잡지 못한다**는 것뿐. 관측은 각 1회(temperature 0이라 결정론에 가깝지만 표본 1).

### 하드닝 2축은 중복 방어다 (PROVEN · 실 Gemini 2회 반복 동일)

라우터 프롬프트 하드닝과 도구 description 하드닝은 defense-in-depth다 — **어느 한쪽만 제거하면 3/3 안전**, **둘 다 제거해야** "마이바흐 관심 고객이 누구야?" → `search_customers{name:"마이바흐"}` 인자 날조가 재현된다. (배치 14의 "description을 조여야 멈춘다" 교훈은 유효하되, 프롬프트 축도 실제 하중을 진다.)

### M7 — `test:server`는 헤르메틱하지 않다 (PARTIAL)

`bun test src/routes/assistant.test.ts -t "POST /ask"` 1회 = `/v1beta/models/gemini-3.5-flash-lite:generateContent` **실 요청 9건**(계측 서버로 확인). `.env.local`에 `GEMINI_PROXY_URL`이 없어 평시 Google 직결.
→ `#319`가 `AGENTS.md`·`gemini-generate.ts`에 옮겨 적은 "자동 테스트는 라우터를 **페이크 주입**해"는 사실과 다르다. 원 출처 `ef45ebb`는 "**골든 테스트는**"으로 정확히 한정했는데 옮기며 범위가 넓어졌다.
**단, 피해 축은 반증됐다** — 업스트림 불통(죽은 포트)에서도 **29/29 통과**한다(`routeAssistantTool` catch→null→NO_HITS 폴백). 실해는 '불안정'이 아니라 **무기록 외부 의존 + 매 실행 9콜 비용/지연**.

### 실 데이터 기준선 (읽기 전용 SELECT)

- `crm.embeddings` **161행 / 현행 스킴 일치 161 / 불일치 0**. 구 스킴(순수 sha256) 0, `gemini-embedding-001` salt 스킴 0 → 코퍼스 순수. 불변식 테스트가 실 DB에서 GREEN.
- `crm.customers` 22행 전원 `ai_summary`·hash non-null, 힌트 평균 **52자**. **앱 미연결 20/20이 신 스킴 `aiHintSourceHash`(순수 sha256)** → `#316` 네임스페이스 분리가 DB 상태로 확인.
  ⚠️ 동시에 **다음 `GEN_MODEL` 교체 시 전원 `unchanged`로 조기 반환돼 자동 재생성 0건**임도 확정(= M4의 기전).
- 생성 산출물 영속 저장처 **2종 122행**: `customers.ai_summary` 22 + `crm.assistant_messages(role='assistant')` 100.
- Edge 배포 실측: `crm-analyst` v20 `updated_at` 2026-07-22 18:50 KST(커밋 이후) · `crm-gemini-proxy` 07-14 그대로 → **슬러그 명시가 실효**(대상 함수만 나갔다).

---

## 판정 14건

표기: **정정된 주장**만 싣는다(원 주장 그대로인 것은 CONFIRMED 표기).

### 중 (5건)

| id | 대상 | 정정된 주장 | 이행 |
|---|---|---|---|
| **M1** | `#316` | `assistant-corpus.ts`의 "실 DB 불변식은 `check:residue`가 본다" 반문장이 **오지목** — 그 스크립트 클로저 3파일에 해시/모델 토큰 0줄. 같은 축을 `schema.ts`·`AGENTS.md`는 정확히 지목하므로 **코드 주석 1곳만 틀렸다**(동작 영향 0) | 반문장을 `test-utils/embedding-model-consistency.test.ts`로 교체 (1줄) |
| **M2** | `#318`·docs | pending 롤업 2문장이 열린 항목 집합과 어긋난다(:3=2건 / :19=5건 / 본문 실제 **10건**). 누락분에 **판단 요청 2건(21·22)** 포함 = 실해. ⚠️**원인 귀속 정정**: 이 배치 소행이 아니라 `67d147b` 시점 **선재 드리프트가 확대**된 것 | 롤업 2줄 갱신 + 항목 24를 20↔21 사이에서 말미로 이동 |
| **M3** | docs·`#319` | 이사님께 약속한 "구 힌트 22건 백업 → 원복 가능"의 유일 사본이 **세션 스크래치패드(휘발성)** 에만 있고 git 추적 포인터 0. 구 모델 출력은 재생성으로 복원 불가 | **✅ 조치 완료** — `~/Documents/TypeScript/mrcha-crm-local-backups/2026-07-22-ai-hint-319/`로 이관(before/after 22행 + 원복 README). pending에 경로 1줄 등재 남음 |
| **M4** | `#319` | `AGENTS.md` 생성 모델 계약의 근거절 **"생성은 저장물이 없어"가 문자 그대로 거짓**(저장처 2종 122행). 그 결과 이번에 실제로 수행한 **'해시 클리어 + 백필' 절차가 계약에 0줄**. ⚠️단 **규범적 결론("앱과 달라도 무방")은 참** — 두 저장처 모두 crm 스키마 CRM 소유 | 근거절을 "저장물이 있으나 **앱과 공유되지 않는다**"로 정정 + 힌트 재생성 절차 크로스링크 |
| **M5** | `ef45ebb` | 프롬프트 하드닝 잠금이 **절반** — `ROUTER_SYSTEM_PROMPT`의 같은 두 축은 통째로 지워도 전 스위트 GREEN. ⚠️**과장 제거**: 두 축이 중복 방어라 "지금 위험하다"는 거짓. 실질 위험은 미잠금 축 삭제가 무음 통과한 **뒤** 잠긴 축까지 건드리는 2단 시나리오 | 라우터 프롬프트 문자열 포함 단언 1건 |

### 하 (9건)

| id | 대상 | 판정 | 이행 |
|---|---|---|---|
| **M6** | `#318` | **CONFIRMED(2/3)** — PR4가 `[x]` 처리한 D8 중 **spec 배너만 누락**. work-ai spec은 아직 `gemini-embedding-001`·`3.1-flash-lite`를 현행 SSOT로 서술. 배너는 기각 박제(라인별 패치)와 **별개인 명시적 채택안** | spec 상단 배너 1줄 |
| **M7** | `#319` | 위 "결정적 사실" 참조 — 문서 축만 이행 | `AGENTS.md`·`gemini-generate.ts` 문구를 "**골든 테스트는**"으로 한정 복원 |
| **M8** | `#316` | **주어 모호**로 격하(V1=REJECTED). 주석은 단언 ② 바로 위에 있고 ② 기준 "구분자·순서 면역"은 **참**. 동시에 케이스 전체로는 구분자 변이가 RED이고 실패 표면 이름이 오진을 유인 | 주석에 한정어("아래 단언 ②는 …") |
| **M9** | `#317` | K4-a 팝오버 잠금이 **표준 상태 1프레임만** 관측 — 조건부 오프너는 등재 없이 통과하는데 테스트 타이틀·커밋 문구는 무조건으로 읽힌다 | 조건부 오프너 케이스 추가 또는 타이틀 한정 |
| **M10** | `#317` | **DISPROVEN(조치 불요·기록만)** — "표식이 없다"는 거짓. 표식이 이미 있고 지목된 대조군보다 상세하다. "그물 0"은 참이나 배치 14 plan이 "렌더 횟수를 단언하지 말 것"으로 **사전 결정**한 사항 | 없음 |
| **M11** | `#319` | **DISPROVEN(제목)** — "어떤 러너로도 실행되지 않는다"는 거짓. Deno 2.9.3으로 **26/26 green**. 살아남는 사실은 "**npm 스크립트·CI 배선 0**"(수동 워크플로우) | 선택 — `package.json`에 deno 스크립트 1줄 |
| **M12** | `ef45ebb`·`#318` | bare 라인 참조 스테일 5건. ⚠️**정정**: 코드 3건은 **선재**이고 K3-c 스윕이 same-file bare `:NNN`만 대상으로 명시했으므로 '누락'이 아니라 '스코프 밖'. 실질 소비자가 있는 것은 **plan:197 한 건** | plan:197을 식별자 참조로 정정 |
| **M13** | `#318` | **CONFIRMED(V3)** — `bunfig.toml`이 삭제된 `tools/*.spec.ts`를 현재시제로 지목. ⚠️**원인 정정**: `#318` D6가 아니라 `#311`(spec 삭제 시 미정정) = **선재 잔재**. 부수: 오늘 유효한 제외 대상은 supabase Deno `*_test.ts`인데 주석에 없다 | 주석 정정 |
| **M14** | `#318`·`#319` | plan 종결 기록 어긋남 — 마지막 체크박스 미완결·수치 스테일(641 vs 실측 **650**). ⚠️**성격 규정 정정**: "churn 0 회피가 `#319`로 무효화"는 과하다 — `#316`이 추가한 주석이 "프롬프트·생성 모델을 바꿔도 이 해시는 불변 … 그때는 hash 클리어→백필 수동 절차"로 **`#319`의 행위를 미리 설계로 규정**했다. 무효화된 것은 '이번에 얻은 이득'이라는 서술뿐 | plan 마지막 줄을 머지 SHA·650으로 채워 `[x]` 종결 |

### 트리아지 드롭 1건

- **A2-6** "`#318` D6가 고쳤다는 `format:check`가 지금도 실패한다(20파일)" → 배치 14 plan이 **기대 결과를 미리 못박아 둔 항목**이라 서술 과장이 아니다(별건 절에 "스타일 warn 20건은 `#206` 이전부터 red이고 CI·hook 배선 0"을 이미 기록).

---

## 감사 한계 (정직 기록)

- **지시 위반 1건**: A2가 실 Gemini를 호출했다. 금지 지시를 받았으나 레포 자신의 `src/routes/assistant.test.ts`가 override 없는 경로에서 실 라우터를 부르는 구조라(= M7) baseline·변이 반복 실행 중 라우팅 호출이 나갔다. 인지 시점에 **그 사실 자체를 finding으로 등재**했다. 생성(답변)·임베딩 API는 부르지 않았다.
- 앱 연결 고객 **2명의 힌트 해시 미검증** — `loadAiHintSource`가 `app_user_id` 보유 시 `public.consultations`(알림 트리거 테이블)를 조회하므로 지침대로 제외. 커버리지 20/22.
- 실 Gemini 골든은 **각 질문 1회**(절제 실험만 2회). 신/구 모델 대조도 각 1회 → "구 모델도 4/4 동일"은 표본 1.
- **답변 생성(SSE)·힌트 문구 품질은 모델별 대조하지 않았다** — 라우팅 판단만. 힌트 톤/길이는 DB 잔존 결과 관측(평균 52자·볼드 1줄 유지)이지 재생성 비교가 아니다(쓰기 금지).
- `crm.embeddings` 불변식은 **저장된 `content` 스냅샷 기준** — 원본 행이 그 사이 편집됐는지는 미검사(프로덕션 가드와 동일 범위).
- M9·M10은 스팟이 독립 재실행하지 않았다(3렌즈 결과 + 원문 직독으로 판정).

---

## PR 분할 / 이행

- [x] **PR1 문서·계약 정정** — 브랜치 `docs/batch-15-pr1-contract-fixes`. 9건 이행:
  - M1 `assistant-corpus.ts` 반문장을 `embedding-model-consistency.test.ts`로 교체(+ `check:residue`가 아닌 이유 명시)
  - M2 pending 롤업 2줄을 **열린 10건**으로 갱신(+ 재발 방지 지시 한 줄) · 항목 24를 말미로 이동
  - M3 백업 **실경로 등재**(`~/Documents/TypeScript/mrcha-crm-local-backups/2026-07-22-ai-hint-319/`) + 레포 미커밋 사유(고객 요약문 전량)
  - M4 `AGENTS.md`·`gemini-generate.ts` 근거절 정정 — "저장물이 없어서"(거짓) → "**crm 소유라 앱과 공유되지 않아서**" + 모델 교체 시 힌트가 자동 갱신되지 않는다는 사실과 클리어→백필 절차
  - M6 work-ai spec 상단 배너(구 모델값 무효 · 현행 SSOT는 `AGENTS.md`)
  - M7 "자동 테스트는 페이크 주입" → "**골든은 페이크, 라우트 테스트는 실 라우터지만 결과 비의존**"으로 한정 복원(+ `test:server` 9콜 실측)
  - M12 batch-14 plan의 `assistant.ts:196-201` → 식별자 참조
  - M13 `bunfig.toml` 주석에서 삭제된 Playwright 경로 제거 → Deno `*_test.ts`로 교체
  - M14 batch-14 plan 마지막 체크박스 종결(머지 SHA 4개 + 641→**650** 정정)
  - **+ GOLDEN-319 한계 박제**(`AGENTS.md`·`gemini-generate.ts`·pending 24 — 골든 4종은 구 모델도 4/4라 두 모델을 구분 못 한다)
  - **+ 감사 관례 축소 박제**(`AGENTS.md` Verification Budget에 신설 — 아래 메타 결정)
  - 검증: typecheck 0 · lint 0 · `assistant-corpus`+`ai-hint` 38 pass(bunfig 변경 후 러너 디스커버리 확인)
- [x] **PR2 회귀 그물 보강** — 브랜치 `test/batch-15-pr2-regression-nets`. 3건 이행, **각각 변이로 RED 실증**(5단계 자가검증):
  - **M5** `ROUTER_SYSTEM_PROMPT`를 export하고 하드닝 2축(날조 금지·필터 밖 호출 금지)을 `assistant-tools.test.ts`에서 잠금. 변이(축 1줄 제거) → **1 fail** 확인. description 축과 **중복 방어**라는 실측을 양쪽 주석에 명시.
  - **M8** 스킴 잠금 주석을 단언 ①/②로 분리 — ①은 구분자·순서까지 잠그고(의도적), ②만 salt 제거에 면역. 변이(`\n`→`|`) → **1 fail**이 ①에서 발생함을 확인(구 주석이 이 한정 없이 "면역"이라 적어 테스트 전체 성질로 읽혔다).
  - **M9** 버튼 집합 잠금을 **상태 축 4칸**(쓰기 권한 × 발송 여부)으로 확장. 기대값은 추측이 아니라 **프로브로 실제 렌더 결과를 확정**했고, 그 과정에서 `appStatus:"sent"`에만 나타나는 **"발송 견적 보기"**(OPENERS 미등재)를 발견 — 기존 테스트가 draft 1프레임만 봐서 영원히 못 보던 버튼이다. 클릭 시 팝오버가 닫히므로(실측) 오프너가 아니라 원본 보기 계열임도 확인해 주석에 기록. 변이(그 라벨 변경) → **sent 2건 RED · 표준 상태는 GREEN**(= 기존 그물로는 못 잡던 변경임이 증명됨).
  - 검증: typecheck 0 · lint 0 · **unit 1063 → 1067** · 서버 관련 33 pass · knip 7/9 무드리프트(신규 export는 테스트가 소비).
  - ⚠️ **이행 중 자책 1건**: 변이 원복에 `git checkout -- <file>`을 썼는데 그 파일에 **본작업(M5 export)도 함께 있어 같이 날아갔다**(테스트가 0 pass/1 error로 즉시 드러나 복구). 감사 단계에서는 에이전트가 변이만 했기에 안전했던 절차가 **이행 단계에선 위험하다** — 본작업과 변이가 같은 파일에 있으면 `git stash`/patch 단위 원복을 쓸 것.
- [ ] **별건 등재(미착수)**: `test:server` 실 Gemini 9콜 위생 · Deno 테스트 배선(M11) · **Topbar 팝오버 우측 잘림**(별도 제보)

### ✅ 배치 15 종결 (2026-07-22)

**머지**: `2460d55`(PR1 `#320`) · `b8a12a8`(PR2 `#321`). 판정 14건 중 **이행 12건 · 조치 불요 2건**(M10 표식 이미 존재 · M11 제목 DISPROVEN → 잔여는 별건 배선).

**main 통합 검증(실측)**: typecheck **0** · lint **0** · unit **1067 pass**(110 files) · server **651 pass 0 fail**(배치 14 종결 시 650 → M5 케이스 1건 증가) · build ✓ 362ms · knip **7 unused exports / 9 unused types 무드리프트** · 픽스처 잔재 **0**.

⚠️ `test:server` 전량은 이 배치가 문서화한 대로 **실 Gemini 라우팅 9콜을 발사한다**(M7). 종결 검증이라 한 번 감수했다 — 상시 반복은 피하고, 위생 조치는 별건으로 남긴다.

**이 배치가 남긴 것 중 다음 사람이 알아야 할 3가지**:
1. **골든 4종은 모델 교체 검증에 불충분하다**(구 모델도 4/4). 모델을 바꿀 땐 그 변경이 노리는 질문 유형을 골든에 추가할 것.
2. **하드닝은 2겹이고 한 겹이 빠져도 증상이 없다**. 그래서 각각 잠갔다 — 어느 한쪽 테스트가 RED면 "다른 겹이 막아주니 괜찮다"고 넘기지 말 것.
3. **이행 단계의 변이 원복은 `git checkout -- <file>`을 쓰지 말 것**(본작업이 같은 파일에 있으면 함께 날아간다 — PR2에서 실제로 겪었다).

---

## 🔴 메타 결정 — 배치 15를 마지막 풀 감사로 (2026-07-22 유슨생 승인)

**근거(실측)**: 배치 14는 반나절+87표를 써서 **상 0 · 중 0 · 하 14**를 냈고, 배치 15도 48 에이전트·5.39M 토큰으로 **상 0 · 중 5 · 하 9 · 행위 변경 0**을 냈다. 두 배치 연속 **사용자 가시 오작동 0건**이다. `ADJUSTED` 비중이 압도적인 것은 결함을 그만큼 찾았다는 뜻이 아니라 **앵글의 과장을 검증이 되돌린 횟수**다 — 파이프라인의 상당분이 자기 노이즈 청소에 쓰인다.
반면 레포엔 `.github`·`.husky`가 없어 **자동 그물이 0**이고, 감사 자체가 사고를 냈다(배치 14 워킹트리 오염 5건 · 실 DB 픽스처 잔재 · main 직접 푸시).

**다음 사이클부터**:
1. **트리거 기반** — 5~6건 누적으로 자동 착수하지 않는다. ①실 데이터를 변형하는 변경 ②외부 계약(앱팀·DB·모델)을 건드리는 변경 ③검증 없이 급히 나간 변경 — 이 중 하나가 포함될 때만 풀 감사.
2. **기본형은 경량** — 2앵글(정합성·회귀그물) + **실측 렌즈 1개**. 적대 검증은 **심각도 상/중에만**.
3. **CI 도입이 감사보다 값어치 있다** — Actions에 typecheck·lint·test:unit·build. (`test:server`는 공유 master DB라 CI 불가 = 로컬 유지.)
   → ✅ **이행 완료(2026-07-22, `.github/workflows/ci.yml`)**. 레포 최초의 자동 검증 그물이다. knip·format:check는 도입 시점부터 red인 선재 상태라 제외했다 — **기준선을 0으로 만든 뒤에** 넣는다. 상세·금지사항은 `AGENTS.md` Verification Budget 절.

**풀 감사를 돌릴 때 유지할 것**: worktree 격리 + 변이 5단계 자가검증(전 GREEN 확인 → 주입 → 재실행 → 원복 → `git status` clean). 배치 15에서 실제로 작동해 **메인 워킹트리 무손상**을 확인했다(배치 14 오염 5건의 재발 0).
