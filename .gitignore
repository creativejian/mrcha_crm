node_modules/
client/dist/
build/
screenshots/
test-results/
playwright-report/
.wrangler/
.dev.vars
.env
.env.*
!.env.example
# .env.production: 클라이언트 번들에 박히는 공개값(VITE_SUPABASE_URL/PUBLISHABLE_KEY)만.
# CF Pages 빌드에서 vite가 읽어 번들 주입. 비밀(DATABASE_URL 등)은 절대 넣지 말 것(대시보드 secret).
!.env.production
.DS_Store
*.log
supabase/.temp/
# Deno를 repo root에서 실행할 때 생기는 부산물. 정본 lock은 supabase/functions/deno.lock(tracked).
/deno.lock
docs/

# 차량 거울 DB import 자산 (대용량 dump + CRM 전용 publishable 키 포함, 공유 금지)
ref/db_import/

# catalog 재introspect baseline (db:pull:catalog 산출물 — 재생성 가능, src/db/catalog.ts가 정본)
drizzle/_catalog_introspect/
